import {
  Comment,
  Fragment,
  Teleport,
  Transition,
  computed,
  createBaseVNode,
  createBlock,
  createCommentVNode,
  createElementBlock,
  createSlots,
  createTextVNode,
  createVNode,
  defineComponent,
  getCurrentInstance,
  guardReactiveProps,
  h,
  inject,
  isReactive,
  isRef,
  mergeProps,
  nextTick,
  normalizeClass,
  normalizeProps,
  normalizeStyle,
  onActivated,
  onBeforeUnmount,
  onMounted,
  onUnmounted,
  openBlock,
  provide,
  reactive,
  readonly,
  ref,
  renderList,
  renderSlot,
  resolveComponent,
  resolveDynamicComponent,
  shallowRef,
  toDisplayString,
  toRef,
  unref,
  useAttrs,
  useSlots,
  vModelCheckbox,
  vModelRadio,
  vModelSelect,
  vShow,
  watch,
  watchEffect,
  withCtx,
  withDirectives,
  withKeys,
  withModifiers
} from "./chunk-JUNVK2NX.js";
import {
  __export
} from "./chunk-HUBM7RA2.js";

// node_modules/@popperjs/core/lib/index.js
var lib_exports = {};
__export(lib_exports, {
  afterMain: () => afterMain,
  afterRead: () => afterRead,
  afterWrite: () => afterWrite,
  applyStyles: () => applyStyles_default,
  arrow: () => arrow_default,
  auto: () => auto,
  basePlacements: () => basePlacements,
  beforeMain: () => beforeMain,
  beforeRead: () => beforeRead,
  beforeWrite: () => beforeWrite,
  bottom: () => bottom,
  clippingParents: () => clippingParents,
  computeStyles: () => computeStyles_default,
  createPopper: () => createPopper3,
  createPopperBase: () => createPopper,
  createPopperLite: () => createPopper2,
  detectOverflow: () => detectOverflow,
  end: () => end,
  eventListeners: () => eventListeners_default,
  flip: () => flip_default,
  hide: () => hide_default,
  left: () => left,
  main: () => main,
  modifierPhases: () => modifierPhases,
  offset: () => offset_default,
  placements: () => placements,
  popper: () => popper,
  popperGenerator: () => popperGenerator,
  popperOffsets: () => popperOffsets_default,
  preventOverflow: () => preventOverflow_default,
  read: () => read,
  reference: () => reference,
  right: () => right,
  start: () => start,
  top: () => top,
  variationPlacements: () => variationPlacements,
  viewport: () => viewport,
  write: () => write
});

// node_modules/@popperjs/core/lib/enums.js
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = basePlacements.reduce(function(acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements = [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];

// node_modules/@popperjs/core/lib/dom-utils/getNodeName.js
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}

// node_modules/@popperjs/core/lib/dom-utils/getWindow.js
function getWindow(node) {
  if (node == null) {
    return window;
  }
  if (node.toString() !== "[object Window]") {
    var ownerDocument = node.ownerDocument;
    return ownerDocument ? ownerDocument.defaultView || window : window;
  }
  return node;
}

// node_modules/@popperjs/core/lib/dom-utils/instanceOf.js
function isElement(node) {
  var OwnElement = getWindow(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}
function isHTMLElement(node) {
  var OwnElement = getWindow(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}
function isShadowRoot(node) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}

// node_modules/@popperjs/core/lib/modifiers/applyStyles.js
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function(name) {
    var style = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name];
    if (!isHTMLElement(element) || !getNodeName(element)) {
      return;
    }
    Object.assign(element.style, style);
    Object.keys(attributes).forEach(function(name2) {
      var value = attributes[name2];
      if (value === false) {
        element.removeAttribute(name2);
      } else {
        element.setAttribute(name2, value === true ? "" : value);
      }
    });
  });
}
function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function() {
    Object.keys(state.elements).forEach(function(name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]);
      var style = styleProperties.reduce(function(style2, property) {
        style2[property] = "";
        return style2;
      }, {});
      if (!isHTMLElement(element) || !getNodeName(element)) {
        return;
      }
      Object.assign(element.style, style);
      Object.keys(attributes).forEach(function(attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles_default = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles,
  effect,
  requires: ["computeStyles"]
};

// node_modules/@popperjs/core/lib/utils/getBasePlacement.js
function getBasePlacement(placement) {
  return placement.split("-")[0];
}

// node_modules/@popperjs/core/lib/utils/math.js
var max = Math.max;
var min = Math.min;
var round = Math.round;

// node_modules/@popperjs/core/lib/utils/userAgent.js
function getUAString() {
  var uaData = navigator.userAgentData;
  if (uaData != null && uaData.brands) {
    return uaData.brands.map(function(item) {
      return item.brand + "/" + item.version;
    }).join(" ");
  }
  return navigator.userAgent;
}

// node_modules/@popperjs/core/lib/dom-utils/isLayoutViewport.js
function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}

// node_modules/@popperjs/core/lib/dom-utils/getBoundingClientRect.js
function getBoundingClientRect(element, includeScale, isFixedStrategy) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  var clientRect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (includeScale && isHTMLElement(element)) {
    scaleX = element.offsetWidth > 0 ? round(clientRect.width) / element.offsetWidth || 1 : 1;
    scaleY = element.offsetHeight > 0 ? round(clientRect.height) / element.offsetHeight || 1 : 1;
  }
  var _ref = isElement(element) ? getWindow(element) : window, visualViewport = _ref.visualViewport;
  var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
  var x = (clientRect.left + (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) / scaleX;
  var y = (clientRect.top + (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) / scaleY;
  var width = clientRect.width / scaleX;
  var height = clientRect.height / scaleY;
  return {
    width,
    height,
    top: y,
    right: x + width,
    bottom: y + height,
    left: x,
    x,
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getLayoutRect.js
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width = element.offsetWidth;
  var height = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height
  };
}

// node_modules/@popperjs/core/lib/dom-utils/contains.js
function contains(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot(rootNode)) {
    var next = child;
    do {
      if (next && parent.isSameNode(next)) {
        return true;
      }
      next = next.parentNode || next.host;
    } while (next);
  }
  return false;
}

// node_modules/@popperjs/core/lib/dom-utils/getComputedStyle.js
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}

// node_modules/@popperjs/core/lib/dom-utils/isTableElement.js
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}

// node_modules/@popperjs/core/lib/dom-utils/getDocumentElement.js
function getDocumentElement(element) {
  return ((isElement(element) ? element.ownerDocument : element.document) || window.document).documentElement;
}

// node_modules/@popperjs/core/lib/dom-utils/getParentNode.js
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return element.assignedSlot || element.parentNode || (isShadowRoot(element) ? element.host : null) || getDocumentElement(element);
}

// node_modules/@popperjs/core/lib/dom-utils/getOffsetParent.js
function getTrueOffsetParent(element) {
  if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox = /firefox/i.test(getUAString());
  var isIE = /Trident/i.test(getUAString());
  if (isIE && isHTMLElement(element)) {
    var elementCss = getComputedStyle2(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  if (isShadowRoot(currentNode)) {
    currentNode = currentNode.host;
  }
  while (isHTMLElement(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
    var css = getComputedStyle2(currentNode);
    if (css.transform !== "none" || css.perspective !== "none" || css.contain === "paint" || ["transform", "perspective"].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === "filter" || isFirefox && css.filter && css.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow(element);
  var offsetParent = getTrueOffsetParent(element);
  while (offsetParent && isTableElement(offsetParent) && getComputedStyle2(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent(offsetParent);
  }
  if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle2(offsetParent).position === "static")) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}

// node_modules/@popperjs/core/lib/utils/getMainAxisFromPlacement.js
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}

// node_modules/@popperjs/core/lib/utils/within.js
function within(min2, value, max2) {
  return max(min2, min(value, max2));
}
function withinMaxClamp(min2, value, max2) {
  var v = within(min2, value, max2);
  return v > max2 ? max2 : v;
}

// node_modules/@popperjs/core/lib/utils/getFreshSideObject.js
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}

// node_modules/@popperjs/core/lib/utils/mergePaddingObject.js
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}

// node_modules/@popperjs/core/lib/utils/expandToHashMap.js
function expandToHashMap(value, keys) {
  return keys.reduce(function(hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

// node_modules/@popperjs/core/lib/modifiers/arrow.js
var toPaddingObject = function toPaddingObject2(padding, state) {
  padding = typeof padding === "function" ? padding(Object.assign({}, state.rects, {
    placement: state.placement
  })) : padding;
  return mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
};
function arrow(_ref) {
  var _state$modifiersData$;
  var state = _ref.state, name = _ref.name, options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets2[axis] - state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent(arrowElement);
  var clientSize = arrowOffsetParent ? axis === "y" ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min2 = paddingObject[minProp];
  var max2 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset2 = within(min2, center, max2);
  var axisProp = axis;
  state.modifiersData[name] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset2, _state$modifiersData$.centerOffset = offset2 - center, _state$modifiersData$);
}
function effect2(_ref2) {
  var state = _ref2.state, options = _ref2.options;
  var _options$element = options.element, arrowElement = _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (true) {
    if (!isHTMLElement(arrowElement)) {
      console.error(['Popper: "arrow" element must be an HTMLElement (not an SVGElement).', "To use an SVG arrow, wrap it in an HTMLElement that will be used as", "the arrow."].join(" "));
    }
  }
  if (!contains(state.elements.popper, arrowElement)) {
    if (true) {
      console.error(['Popper: "arrow" modifier\'s `element` must be a child of the popper', "element."].join(" "));
    }
    return;
  }
  state.elements.arrow = arrowElement;
}
var arrow_default = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow,
  effect: effect2,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};

// node_modules/@popperjs/core/lib/utils/getVariation.js
function getVariation(placement) {
  return placement.split("-")[1];
}

// node_modules/@popperjs/core/lib/modifiers/computeStyles.js
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref) {
  var x = _ref.x, y = _ref.y;
  var win = window;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round(x * dpr) / dpr || 0,
    y: round(y * dpr) / dpr || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed = _ref2.isFixed;
  var _offsets$x = offsets.x, x = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
    x,
    y
  }) : {
    x,
    y
  };
  x = _ref3.x;
  y = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow(popper2)) {
      offsetParent = getDocumentElement(popper2);
      if (getComputedStyle2(offsetParent).position !== "static" && position === "absolute") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (placement === top || (placement === left || placement === right) && variation === end) {
      sideY = bottom;
      var offsetY = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.height : offsetParent[heightProp];
      y -= offsetY - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left || (placement === top || placement === bottom) && variation === end) {
      sideX = right;
      var offsetX = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.width : offsetParent[widthProp];
      x -= offsetX - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position
  }, adaptive && unsetSides);
  var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
    x,
    y
  }) : {
    x,
    y
  };
  x = _ref4.x;
  y = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref5) {
  var state = _ref5.state, options = _ref5.options;
  var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  if (true) {
    var transitionProperty = getComputedStyle2(state.elements.popper).transitionProperty || "";
    if (adaptive && ["transform", "top", "right", "bottom", "left"].some(function(property) {
      return transitionProperty.indexOf(property) >= 0;
    })) {
      console.warn(["Popper: Detected CSS transitions on at least one of the following", 'CSS properties: "transform", "top", "right", "bottom", "left".', "\n\n", 'Disable the "computeStyles" modifier\'s `adaptive` option to allow', "for smooth transitions, or remove these properties from the CSS", "transition declaration on the popper element if only transitioning", "opacity or background-color for example.", "\n\n", "We recommend using the popper element as a wrapper around an inner", "element that can have any CSS property transitioned for animations."].join(" "));
    }
  }
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed"
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
var computeStyles_default = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};

// node_modules/@popperjs/core/lib/modifiers/eventListeners.js
var passive = {
  passive: true
};
function effect3(_ref) {
  var state = _ref.state, instance = _ref.instance, options = _ref.options;
  var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent) {
      scrollParent.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent) {
        scrollParent.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
var eventListeners_default = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect: effect3,
  data: {}
};

// node_modules/@popperjs/core/lib/utils/getOppositePlacement.js
var hash = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function(matched) {
    return hash[matched];
  });
}

// node_modules/@popperjs/core/lib/utils/getOppositeVariationPlacement.js
var hash2 = {
  start: "end",
  end: "start"
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function(matched) {
    return hash2[matched];
  });
}

// node_modules/@popperjs/core/lib/dom-utils/getWindowScroll.js
function getWindowScroll(node) {
  var win = getWindow(node);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getWindowScrollBarX.js
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
}

// node_modules/@popperjs/core/lib/dom-utils/getViewportRect.js
function getViewportRect(element, strategy) {
  var win = getWindow(element);
  var html = getDocumentElement(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height = html.clientHeight;
  var x = 0;
  var y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    var layoutViewport = isLayoutViewport();
    if (layoutViewport || !layoutViewport && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x: x + getWindowScrollBarX(element),
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getDocumentRect.js
function getDocumentRect(element) {
  var _element$ownerDocumen;
  var html = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = max(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y = -winScroll.scrollTop;
  if (getComputedStyle2(body || html).direction === "rtl") {
    x += max(html.clientWidth, body ? body.clientWidth : 0) - width;
  }
  return {
    width,
    height,
    x,
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/isScrollParent.js
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle2(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

// node_modules/@popperjs/core/lib/dom-utils/getScrollParent.js
function getScrollParent(node) {
  if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
    return node.ownerDocument.body;
  }
  if (isHTMLElement(node) && isScrollParent(node)) {
    return node;
  }
  return getScrollParent(getParentNode(node));
}

// node_modules/@popperjs/core/lib/dom-utils/listScrollParents.js
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent = getScrollParent(element);
  var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow(scrollParent);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent) ? scrollParent : []) : scrollParent;
  var updatedList = list.concat(target);
  return isBody ? updatedList : updatedList.concat(listScrollParents(getParentNode(target)));
}

// node_modules/@popperjs/core/lib/utils/rectToClientRect.js
function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}

// node_modules/@popperjs/core/lib/dom-utils/getClippingRect.js
function getInnerBoundingClientRect(element, strategy) {
  var rect = getBoundingClientRect(element, false, strategy === "fixed");
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent, strategy) {
  return clippingParent === viewport ? rectToClientRect(getViewportRect(element, strategy)) : isElement(clippingParent) ? getInnerBoundingClientRect(clippingParent, strategy) : rectToClientRect(getDocumentRect(getDocumentElement(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode(element));
  var canEscapeClipping = ["absolute", "fixed"].indexOf(getComputedStyle2(element).position) >= 0;
  var clipperElement = canEscapeClipping && isHTMLElement(element) ? getOffsetParent(element) : element;
  if (!isElement(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function(clippingParent) {
    return isElement(clippingParent) && contains(clippingParent, clipperElement) && getNodeName(clippingParent) !== "body";
  });
}
function getClippingRect(element, boundary, rootBoundary, strategy) {
  var mainClippingParents = boundary === "clippingParents" ? getClippingParents(element) : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(function(accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent, strategy);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent, strategy));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

// node_modules/@popperjs/core/lib/utils/computeOffsets.js
function computeOffsets(_ref) {
  var reference2 = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
      default:
    }
  }
  return offsets;
}

// node_modules/@popperjs/core/lib/utils/detectOverflow.js
function detectOverflow(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, _options$placement = _options.placement, placement = _options$placement === void 0 ? state.placement : _options$placement, _options$strategy = _options.strategy, strategy = _options$strategy === void 0 ? state.strategy : _options$strategy, _options$boundary = _options.boundary, boundary = _options$boundary === void 0 ? clippingParents : _options$boundary, _options$rootBoundary = _options.rootBoundary, rootBoundary = _options$rootBoundary === void 0 ? viewport : _options$rootBoundary, _options$elementConte = _options.elementContext, elementContext = _options$elementConte === void 0 ? popper : _options$elementConte, _options$altBoundary = _options.altBoundary, altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary, _options$padding = _options.padding, padding = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect(isElement(element) ? element : element.contextElement || getDocumentElement(state.elements.popper), boundary, rootBoundary, strategy);
  var referenceClientRect = getBoundingClientRect(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    strategy: "absolute",
    placement
  });
  var popperClientRect = rectToClientRect(Object.assign({}, popperRect, popperOffsets2));
  var elementClientRect = elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset2 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function(key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset2[axis] * multiply;
    });
  }
  return overflowOffsets;
}

// node_modules/@popperjs/core/lib/utils/computeAutoPlacement.js
function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, placement = _options.placement, boundary = _options.boundary, rootBoundary = _options.rootBoundary, padding = _options.padding, flipVariations = _options.flipVariations, _options$allowedAutoP = _options.allowedAutoPlacements, allowedAutoPlacements = _options$allowedAutoP === void 0 ? placements : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements2 = variation ? flipVariations ? variationPlacements : variationPlacements.filter(function(placement2) {
    return getVariation(placement2) === variation;
  }) : basePlacements;
  var allowedPlacements = placements2.filter(function(placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements2;
    if (true) {
      console.error(["Popper: The `allowedAutoPlacements` option did not allow any", "placements. Ensure the `placement` option matches the variation", "of the allowed placements.", 'For example, "auto" cannot be used to allow "bottom-start".', 'Use "auto-start" instead.'].join(" "));
    }
  }
  var overflows = allowedPlacements.reduce(function(acc, placement2) {
    acc[placement2] = detectOverflow(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function(a, b) {
    return overflows[a] - overflows[b];
  });
}

// node_modules/@popperjs/core/lib/modifiers/flip.js
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement(placement);
  return [getOppositeVariationPlacement(placement), oppositePlacement, getOppositeVariationPlacement(oppositePlacement)];
}
function flip(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  if (state.modifiersData[name]._skip) {
    return;
  }
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis, specifiedFallbackPlacements = options.fallbackPlacements, padding = options.padding, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, _options$flipVariatio = options.flipVariations, flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio, allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [getOppositePlacement(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements2 = [preferredPlacement].concat(fallbackPlacements).reduce(function(acc, placement2) {
    return acc.concat(getBasePlacement(placement2) === auto ? computeAutoPlacement(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding,
      flipVariations,
      allowedAutoPlacements
    }) : placement2);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements2[0];
  for (var i = 0; i < placements2.length; i++) {
    var placement = placements2[i];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding
    });
    var mainVariationSide = isVertical ? isStartVariation ? right : left : isStartVariation ? bottom : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }
    if (checks.every(function(check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i3) {
      var fittingPlacement = placements2.find(function(placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i3).every(function(check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i2 = numberOfChecks; _i2 > 0; _i2--) {
      var _ret = _loop(_i2);
      if (_ret === "break")
        break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
var flip_default = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip,
  requiresIfExists: ["offset"],
  data: {
    _skip: false
  }
};

// node_modules/@popperjs/core/lib/modifiers/hide.js
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function(side) {
    return overflow[side] >= 0;
  });
}
function hide(_ref) {
  var state = _ref.state, name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow(state, {
    elementContext: "reference"
  });
  var popperAltOverflow = detectOverflow(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped
  });
}
var hide_default = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide
};

// node_modules/@popperjs/core/lib/modifiers/offset.js
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
    placement
  })) : offset2, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset(_ref2) {
  var state = _ref2.state, options = _ref2.options, name = _ref2.name;
  var _options$offset = options.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x = _data$state$placement.x, y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name] = data;
}
var offset_default = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset
};

// node_modules/@popperjs/core/lib/modifiers/popperOffsets.js
function popperOffsets(_ref) {
  var state = _ref.state, name = _ref.name;
  state.modifiersData[name] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement
  });
}
var popperOffsets_default = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};

// node_modules/@popperjs/core/lib/utils/getAltAxis.js
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}

// node_modules/@popperjs/core/lib/modifiers/preventOverflow.js
function preventOverflow(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, padding = options.padding, _options$tether = options.tether, tether = _options$tether === void 0 ? true : _options$tether, _options$tetherOffset = options.tetherOffset, tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow(state, {
    boundary,
    rootBoundary,
    padding,
    altBoundary
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === "function" ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var normalizedTetherOffsetValue = typeof tetherOffsetValue === "number" ? {
    mainAxis: tetherOffsetValue,
    altAxis: tetherOffsetValue
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, tetherOffsetValue);
  var offsetModifierState = state.modifiersData.offset ? state.modifiersData.offset[state.placement] : null;
  var data = {
    x: 0,
    y: 0
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset2 = popperOffsets2[mainAxis];
    var min2 = offset2 + overflow[mainSide];
    var max2 = offset2 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? getLayoutRect(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"] ? state.modifiersData["arrow#persistent"].padding : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis : minLen - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis : maxLen + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent = state.elements.arrow && getOffsetParent(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === "y" ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = (_offsetModifierState$ = offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) != null ? _offsetModifierState$ : 0;
    var tetherMin = offset2 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset2 + maxOffset - offsetModifierValue;
    var preventedOffset = within(tether ? min(min2, tetherMin) : min2, offset2, tether ? max(max2, tetherMax) : max2);
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset2;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue = (_offsetModifierState$2 = offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) != null ? _offsetModifierState$2 : 0;
    var _tetherMin = isOriginSide ? _min : _offset - referenceRect[_len] - popperRect[_len] - _offsetModifierValue + normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide ? _offset + referenceRect[_len] + popperRect[_len] - _offsetModifierValue - normalizedTetherOffsetValue.altAxis : _max;
    var _preventedOffset = tether && isOriginSide ? withinMaxClamp(_tetherMin, _offset, _tetherMax) : within(tether ? _tetherMin : _min, _offset, tether ? _tetherMax : _max);
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name] = data;
}
var preventOverflow_default = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"]
};

// node_modules/@popperjs/core/lib/dom-utils/getHTMLElementScroll.js
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getNodeScroll.js
function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}

// node_modules/@popperjs/core/lib/dom-utils/getCompositeRect.js
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round(rect.width) / element.offsetWidth || 1;
  var scaleY = round(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var isOffsetParentAnElement = isHTMLElement(offsetParent);
  var offsetParentIsScaled = isHTMLElement(offsetParent) && isElementScaled(offsetParent);
  var documentElement = getDocumentElement(offsetParent);
  var rect = getBoundingClientRect(elementOrVirtualElement, offsetParentIsScaled, isFixed);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isScrollParent(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      offsets = getBoundingClientRect(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}

// node_modules/@popperjs/core/lib/utils/orderModifiers.js
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}

// node_modules/@popperjs/core/lib/utils/debounce.js
function debounce(fn3) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn3());
        });
      });
    }
    return pending;
  };
}

// node_modules/@popperjs/core/lib/utils/format.js
function format(str) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  return [].concat(args).reduce(function(p, c) {
    return p.replace(/%s/, c);
  }, str);
}

// node_modules/@popperjs/core/lib/utils/validateModifiers.js
var INVALID_MODIFIER_ERROR = 'Popper: modifier "%s" provided an invalid %s property, expected %s but got %s';
var MISSING_DEPENDENCY_ERROR = 'Popper: modifier "%s" requires "%s", but "%s" modifier is not available';
var VALID_PROPERTIES = ["name", "enabled", "phase", "fn", "effect", "requires", "options"];
function validateModifiers(modifiers) {
  modifiers.forEach(function(modifier) {
    [].concat(Object.keys(modifier), VALID_PROPERTIES).filter(function(value, index, self) {
      return self.indexOf(value) === index;
    }).forEach(function(key) {
      switch (key) {
        case "name":
          if (typeof modifier.name !== "string") {
            console.error(format(INVALID_MODIFIER_ERROR, String(modifier.name), '"name"', '"string"', '"' + String(modifier.name) + '"'));
          }
          break;
        case "enabled":
          if (typeof modifier.enabled !== "boolean") {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"enabled"', '"boolean"', '"' + String(modifier.enabled) + '"'));
          }
          break;
        case "phase":
          if (modifierPhases.indexOf(modifier.phase) < 0) {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"phase"', "either " + modifierPhases.join(", "), '"' + String(modifier.phase) + '"'));
          }
          break;
        case "fn":
          if (typeof modifier.fn !== "function") {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"fn"', '"function"', '"' + String(modifier.fn) + '"'));
          }
          break;
        case "effect":
          if (modifier.effect != null && typeof modifier.effect !== "function") {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"effect"', '"function"', '"' + String(modifier.fn) + '"'));
          }
          break;
        case "requires":
          if (modifier.requires != null && !Array.isArray(modifier.requires)) {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"requires"', '"array"', '"' + String(modifier.requires) + '"'));
          }
          break;
        case "requiresIfExists":
          if (!Array.isArray(modifier.requiresIfExists)) {
            console.error(format(INVALID_MODIFIER_ERROR, modifier.name, '"requiresIfExists"', '"array"', '"' + String(modifier.requiresIfExists) + '"'));
          }
          break;
        case "options":
        case "data":
          break;
        default:
          console.error('PopperJS: an invalid property has been provided to the "' + modifier.name + '" modifier, valid properties are ' + VALID_PROPERTIES.map(function(s) {
            return '"' + s + '"';
          }).join(", ") + '; but "' + key + '" was provided.');
      }
      modifier.requires && modifier.requires.forEach(function(requirement) {
        if (modifiers.find(function(mod) {
          return mod.name === requirement;
        }) == null) {
          console.error(format(MISSING_DEPENDENCY_ERROR, String(modifier.name), requirement, requirement));
        }
      });
    });
  });
}

// node_modules/@popperjs/core/lib/utils/uniqueBy.js
function uniqueBy(arr, fn3) {
  var identifiers = /* @__PURE__ */ new Set();
  return arr.filter(function(item) {
    var identifier = fn3(item);
    if (!identifiers.has(identifier)) {
      identifiers.add(identifier);
      return true;
    }
  });
}

// node_modules/@popperjs/core/lib/utils/mergeByName.js
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}

// node_modules/@popperjs/core/lib/createPopper.js
var INVALID_ELEMENT_ERROR = "Popper: Invalid reference or popper argument provided. They must be either a DOM element or virtual element.";
var INFINITE_LOOP_ERROR = "Popper: An infinite loop in the modifiers cycle has been detected! The cycle has been interrupted to prevent a browser crash.";
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers3 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper4(reference2, popper2, options) {
    if (options === void 0) {
      options = defaultOptions;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options2 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions, state.options, options2);
        state.scrollParents = {
          reference: isElement(reference2) ? listScrollParents(reference2) : reference2.contextElement ? listScrollParents(reference2.contextElement) : [],
          popper: listScrollParents(popper2)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers3, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m) {
          return m.enabled;
        });
        if (true) {
          var modifiers = uniqueBy([].concat(orderedModifiers, state.options.modifiers), function(_ref) {
            var name = _ref.name;
            return name;
          });
          validateModifiers(modifiers);
          if (getBasePlacement(state.options.placement) === auto) {
            var flipModifier = state.orderedModifiers.find(function(_ref2) {
              var name = _ref2.name;
              return name === "flip";
            });
            if (!flipModifier) {
              console.error(['Popper: "auto" placements require the "flip" modifier be', "present and enabled to work."].join(" "));
            }
          }
          var _getComputedStyle = getComputedStyle2(popper2), marginTop = _getComputedStyle.marginTop, marginRight = _getComputedStyle.marginRight, marginBottom = _getComputedStyle.marginBottom, marginLeft = _getComputedStyle.marginLeft;
          if ([marginTop, marginRight, marginBottom, marginLeft].some(function(margin) {
            return parseFloat(margin);
          })) {
            console.warn(['Popper: CSS "margin" styles cannot be used to apply padding', "between the popper and its reference element or boundary.", "To replicate margin, use the `offset` modifier, as well as", "the `padding` option in the `preventOverflow` and `flip`", "modifiers."].join(" "));
          }
        }
        runModifierEffects();
        return instance.update();
      },
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference3 = _state$elements.reference, popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          if (true) {
            console.error(INVALID_ELEMENT_ERROR);
          }
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference3, getOffsetParent(popper3), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper3)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        var __debug_loops__ = 0;
        for (var index = 0; index < state.orderedModifiers.length; index++) {
          if (true) {
            __debug_loops__ += 1;
            if (__debug_loops__ > 100) {
              console.error(INFINITE_LOOP_ERROR);
              break;
            }
          }
          if (state.reset === true) {
            state.reset = false;
            index = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index], fn3 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name = _state$orderedModifie.name;
          if (typeof fn3 === "function") {
            state = fn3({
              state,
              options: _options,
              name,
              instance
            }) || state;
          }
        }
      },
      update: debounce(function() {
        return new Promise(function(resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference2, popper2)) {
      if (true) {
        console.error(INVALID_ELEMENT_ERROR);
      }
      return instance;
    }
    instance.setOptions(options).then(function(state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref3) {
        var name = _ref3.name, _ref3$options = _ref3.options, options2 = _ref3$options === void 0 ? {} : _ref3$options, effect4 = _ref3.effect;
        if (typeof effect4 === "function") {
          var cleanupFn = effect4({
            state,
            name,
            instance,
            options: options2
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn3) {
        return fn3();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}
var createPopper = popperGenerator();

// node_modules/@popperjs/core/lib/popper-lite.js
var defaultModifiers = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default];
var createPopper2 = popperGenerator({
  defaultModifiers
});

// node_modules/@popperjs/core/lib/popper.js
var defaultModifiers2 = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default, offset_default, flip_default, preventOverflow_default, arrow_default, hide_default];
var createPopper3 = popperGenerator({
  defaultModifiers: defaultModifiers2
});

// node_modules/bootstrap/dist/js/bootstrap.esm.js
var MAX_UID = 1e6;
var MILLISECONDS_MULTIPLIER = 1e3;
var TRANSITION_END = "transitionend";
var toType = (object) => {
  if (object === null || object === void 0) {
    return `${object}`;
  }
  return Object.prototype.toString.call(object).match(/\s([a-z]+)/i)[1].toLowerCase();
};
var getUID = (prefix) => {
  do {
    prefix += Math.floor(Math.random() * MAX_UID);
  } while (document.getElementById(prefix));
  return prefix;
};
var getSelector = (element) => {
  let selector = element.getAttribute("data-bs-target");
  if (!selector || selector === "#") {
    let hrefAttribute = element.getAttribute("href");
    if (!hrefAttribute || !hrefAttribute.includes("#") && !hrefAttribute.startsWith(".")) {
      return null;
    }
    if (hrefAttribute.includes("#") && !hrefAttribute.startsWith("#")) {
      hrefAttribute = `#${hrefAttribute.split("#")[1]}`;
    }
    selector = hrefAttribute && hrefAttribute !== "#" ? hrefAttribute.trim() : null;
  }
  return selector;
};
var getSelectorFromElement = (element) => {
  const selector = getSelector(element);
  if (selector) {
    return document.querySelector(selector) ? selector : null;
  }
  return null;
};
var getElementFromSelector = (element) => {
  const selector = getSelector(element);
  return selector ? document.querySelector(selector) : null;
};
var getTransitionDurationFromElement = (element) => {
  if (!element) {
    return 0;
  }
  let {
    transitionDuration,
    transitionDelay
  } = window.getComputedStyle(element);
  const floatTransitionDuration = Number.parseFloat(transitionDuration);
  const floatTransitionDelay = Number.parseFloat(transitionDelay);
  if (!floatTransitionDuration && !floatTransitionDelay) {
    return 0;
  }
  transitionDuration = transitionDuration.split(",")[0];
  transitionDelay = transitionDelay.split(",")[0];
  return (Number.parseFloat(transitionDuration) + Number.parseFloat(transitionDelay)) * MILLISECONDS_MULTIPLIER;
};
var triggerTransitionEnd = (element) => {
  element.dispatchEvent(new Event(TRANSITION_END));
};
var isElement2 = (object) => {
  if (!object || typeof object !== "object") {
    return false;
  }
  if (typeof object.jquery !== "undefined") {
    object = object[0];
  }
  return typeof object.nodeType !== "undefined";
};
var getElement = (object) => {
  if (isElement2(object)) {
    return object.jquery ? object[0] : object;
  }
  if (typeof object === "string" && object.length > 0) {
    return document.querySelector(object);
  }
  return null;
};
var isVisible = (element) => {
  if (!isElement2(element) || element.getClientRects().length === 0) {
    return false;
  }
  const elementIsVisible = getComputedStyle(element).getPropertyValue("visibility") === "visible";
  const closedDetails = element.closest("details:not([open])");
  if (!closedDetails) {
    return elementIsVisible;
  }
  if (closedDetails !== element) {
    const summary = element.closest("summary");
    if (summary && summary.parentNode !== closedDetails) {
      return false;
    }
    if (summary === null) {
      return false;
    }
  }
  return elementIsVisible;
};
var isDisabled = (element) => {
  if (!element || element.nodeType !== Node.ELEMENT_NODE) {
    return true;
  }
  if (element.classList.contains("disabled")) {
    return true;
  }
  if (typeof element.disabled !== "undefined") {
    return element.disabled;
  }
  return element.hasAttribute("disabled") && element.getAttribute("disabled") !== "false";
};
var findShadowRoot = (element) => {
  if (!document.documentElement.attachShadow) {
    return null;
  }
  if (typeof element.getRootNode === "function") {
    const root = element.getRootNode();
    return root instanceof ShadowRoot ? root : null;
  }
  if (element instanceof ShadowRoot) {
    return element;
  }
  if (!element.parentNode) {
    return null;
  }
  return findShadowRoot(element.parentNode);
};
var noop = () => {
};
var reflow = (element) => {
  element.offsetHeight;
};
var getjQuery = () => {
  if (window.jQuery && !document.body.hasAttribute("data-bs-no-jquery")) {
    return window.jQuery;
  }
  return null;
};
var DOMContentLoadedCallbacks = [];
var onDOMContentLoaded = (callback) => {
  if (document.readyState === "loading") {
    if (!DOMContentLoadedCallbacks.length) {
      document.addEventListener("DOMContentLoaded", () => {
        for (const callback2 of DOMContentLoadedCallbacks) {
          callback2();
        }
      });
    }
    DOMContentLoadedCallbacks.push(callback);
  } else {
    callback();
  }
};
var isRTL = () => document.documentElement.dir === "rtl";
var defineJQueryPlugin = (plugin) => {
  onDOMContentLoaded(() => {
    const $ = getjQuery();
    if ($) {
      const name = plugin.NAME;
      const JQUERY_NO_CONFLICT = $.fn[name];
      $.fn[name] = plugin.jQueryInterface;
      $.fn[name].Constructor = plugin;
      $.fn[name].noConflict = () => {
        $.fn[name] = JQUERY_NO_CONFLICT;
        return plugin.jQueryInterface;
      };
    }
  });
};
var execute = (callback) => {
  if (typeof callback === "function") {
    callback();
  }
};
var executeAfterTransition = (callback, transitionElement, waitForTransition = true) => {
  if (!waitForTransition) {
    execute(callback);
    return;
  }
  const durationPadding = 5;
  const emulatedDuration = getTransitionDurationFromElement(transitionElement) + durationPadding;
  let called = false;
  const handler = ({
    target
  }) => {
    if (target !== transitionElement) {
      return;
    }
    called = true;
    transitionElement.removeEventListener(TRANSITION_END, handler);
    execute(callback);
  };
  transitionElement.addEventListener(TRANSITION_END, handler);
  setTimeout(() => {
    if (!called) {
      triggerTransitionEnd(transitionElement);
    }
  }, emulatedDuration);
};
var getNextActiveElement = (list, activeElement, shouldGetNext, isCycleAllowed) => {
  const listLength = list.length;
  let index = list.indexOf(activeElement);
  if (index === -1) {
    return !shouldGetNext && isCycleAllowed ? list[listLength - 1] : list[0];
  }
  index += shouldGetNext ? 1 : -1;
  if (isCycleAllowed) {
    index = (index + listLength) % listLength;
  }
  return list[Math.max(0, Math.min(index, listLength - 1))];
};
var namespaceRegex = /[^.]*(?=\..*)\.|.*/;
var stripNameRegex = /\..*/;
var stripUidRegex = /::\d+$/;
var eventRegistry = {};
var uidEvent = 1;
var customEvents = {
  mouseenter: "mouseover",
  mouseleave: "mouseout"
};
var nativeEvents = /* @__PURE__ */ new Set(["click", "dblclick", "mouseup", "mousedown", "contextmenu", "mousewheel", "DOMMouseScroll", "mouseover", "mouseout", "mousemove", "selectstart", "selectend", "keydown", "keypress", "keyup", "orientationchange", "touchstart", "touchmove", "touchend", "touchcancel", "pointerdown", "pointermove", "pointerup", "pointerleave", "pointercancel", "gesturestart", "gesturechange", "gestureend", "focus", "blur", "change", "reset", "select", "submit", "focusin", "focusout", "load", "unload", "beforeunload", "resize", "move", "DOMContentLoaded", "readystatechange", "error", "abort", "scroll"]);
function makeEventUid(element, uid) {
  return uid && `${uid}::${uidEvent++}` || element.uidEvent || uidEvent++;
}
function getElementEvents(element) {
  const uid = makeEventUid(element);
  element.uidEvent = uid;
  eventRegistry[uid] = eventRegistry[uid] || {};
  return eventRegistry[uid];
}
function bootstrapHandler(element, fn3) {
  return function handler(event) {
    hydrateObj(event, {
      delegateTarget: element
    });
    if (handler.oneOff) {
      EventHandler.off(element, event.type, fn3);
    }
    return fn3.apply(element, [event]);
  };
}
function bootstrapDelegationHandler(element, selector, fn3) {
  return function handler(event) {
    const domElements = element.querySelectorAll(selector);
    for (let {
      target
    } = event; target && target !== this; target = target.parentNode) {
      for (const domElement of domElements) {
        if (domElement !== target) {
          continue;
        }
        hydrateObj(event, {
          delegateTarget: target
        });
        if (handler.oneOff) {
          EventHandler.off(element, event.type, selector, fn3);
        }
        return fn3.apply(target, [event]);
      }
    }
  };
}
function findHandler(events, callable, delegationSelector = null) {
  return Object.values(events).find((event) => event.callable === callable && event.delegationSelector === delegationSelector);
}
function normalizeParameters(originalTypeEvent, handler, delegationFunction) {
  const isDelegated = typeof handler === "string";
  const callable = isDelegated ? delegationFunction : handler || delegationFunction;
  let typeEvent = getTypeEvent(originalTypeEvent);
  if (!nativeEvents.has(typeEvent)) {
    typeEvent = originalTypeEvent;
  }
  return [isDelegated, callable, typeEvent];
}
function addHandler(element, originalTypeEvent, handler, delegationFunction, oneOff) {
  if (typeof originalTypeEvent !== "string" || !element) {
    return;
  }
  let [isDelegated, callable, typeEvent] = normalizeParameters(originalTypeEvent, handler, delegationFunction);
  if (originalTypeEvent in customEvents) {
    const wrapFunction = (fn4) => {
      return function(event) {
        if (!event.relatedTarget || event.relatedTarget !== event.delegateTarget && !event.delegateTarget.contains(event.relatedTarget)) {
          return fn4.call(this, event);
        }
      };
    };
    callable = wrapFunction(callable);
  }
  const events = getElementEvents(element);
  const handlers = events[typeEvent] || (events[typeEvent] = {});
  const previousFunction = findHandler(handlers, callable, isDelegated ? handler : null);
  if (previousFunction) {
    previousFunction.oneOff = previousFunction.oneOff && oneOff;
    return;
  }
  const uid = makeEventUid(callable, originalTypeEvent.replace(namespaceRegex, ""));
  const fn3 = isDelegated ? bootstrapDelegationHandler(element, handler, callable) : bootstrapHandler(element, callable);
  fn3.delegationSelector = isDelegated ? handler : null;
  fn3.callable = callable;
  fn3.oneOff = oneOff;
  fn3.uidEvent = uid;
  handlers[uid] = fn3;
  element.addEventListener(typeEvent, fn3, isDelegated);
}
function removeHandler(element, events, typeEvent, handler, delegationSelector) {
  const fn3 = findHandler(events[typeEvent], handler, delegationSelector);
  if (!fn3) {
    return;
  }
  element.removeEventListener(typeEvent, fn3, Boolean(delegationSelector));
  delete events[typeEvent][fn3.uidEvent];
}
function removeNamespacedHandlers(element, events, typeEvent, namespace) {
  const storeElementEvent = events[typeEvent] || {};
  for (const handlerKey of Object.keys(storeElementEvent)) {
    if (handlerKey.includes(namespace)) {
      const event = storeElementEvent[handlerKey];
      removeHandler(element, events, typeEvent, event.callable, event.delegationSelector);
    }
  }
}
function getTypeEvent(event) {
  event = event.replace(stripNameRegex, "");
  return customEvents[event] || event;
}
var EventHandler = {
  on(element, event, handler, delegationFunction) {
    addHandler(element, event, handler, delegationFunction, false);
  },
  one(element, event, handler, delegationFunction) {
    addHandler(element, event, handler, delegationFunction, true);
  },
  off(element, originalTypeEvent, handler, delegationFunction) {
    if (typeof originalTypeEvent !== "string" || !element) {
      return;
    }
    const [isDelegated, callable, typeEvent] = normalizeParameters(originalTypeEvent, handler, delegationFunction);
    const inNamespace = typeEvent !== originalTypeEvent;
    const events = getElementEvents(element);
    const storeElementEvent = events[typeEvent] || {};
    const isNamespace = originalTypeEvent.startsWith(".");
    if (typeof callable !== "undefined") {
      if (!Object.keys(storeElementEvent).length) {
        return;
      }
      removeHandler(element, events, typeEvent, callable, isDelegated ? handler : null);
      return;
    }
    if (isNamespace) {
      for (const elementEvent of Object.keys(events)) {
        removeNamespacedHandlers(element, events, elementEvent, originalTypeEvent.slice(1));
      }
    }
    for (const keyHandlers of Object.keys(storeElementEvent)) {
      const handlerKey = keyHandlers.replace(stripUidRegex, "");
      if (!inNamespace || originalTypeEvent.includes(handlerKey)) {
        const event = storeElementEvent[keyHandlers];
        removeHandler(element, events, typeEvent, event.callable, event.delegationSelector);
      }
    }
  },
  trigger(element, event, args) {
    if (typeof event !== "string" || !element) {
      return null;
    }
    const $ = getjQuery();
    const typeEvent = getTypeEvent(event);
    const inNamespace = event !== typeEvent;
    let jQueryEvent = null;
    let bubbles = true;
    let nativeDispatch = true;
    let defaultPrevented = false;
    if (inNamespace && $) {
      jQueryEvent = $.Event(event, args);
      $(element).trigger(jQueryEvent);
      bubbles = !jQueryEvent.isPropagationStopped();
      nativeDispatch = !jQueryEvent.isImmediatePropagationStopped();
      defaultPrevented = jQueryEvent.isDefaultPrevented();
    }
    let evt = new Event(event, {
      bubbles,
      cancelable: true
    });
    evt = hydrateObj(evt, args);
    if (defaultPrevented) {
      evt.preventDefault();
    }
    if (nativeDispatch) {
      element.dispatchEvent(evt);
    }
    if (evt.defaultPrevented && jQueryEvent) {
      jQueryEvent.preventDefault();
    }
    return evt;
  }
};
function hydrateObj(obj, meta) {
  for (const [key, value] of Object.entries(meta || {})) {
    try {
      obj[key] = value;
    } catch (_unused) {
      Object.defineProperty(obj, key, {
        configurable: true,
        get() {
          return value;
        }
      });
    }
  }
  return obj;
}
var elementMap = /* @__PURE__ */ new Map();
var Data = {
  set(element, key, instance) {
    if (!elementMap.has(element)) {
      elementMap.set(element, /* @__PURE__ */ new Map());
    }
    const instanceMap = elementMap.get(element);
    if (!instanceMap.has(key) && instanceMap.size !== 0) {
      console.error(`Bootstrap doesn't allow more than one instance per element. Bound instance: ${Array.from(instanceMap.keys())[0]}.`);
      return;
    }
    instanceMap.set(key, instance);
  },
  get(element, key) {
    if (elementMap.has(element)) {
      return elementMap.get(element).get(key) || null;
    }
    return null;
  },
  remove(element, key) {
    if (!elementMap.has(element)) {
      return;
    }
    const instanceMap = elementMap.get(element);
    instanceMap.delete(key);
    if (instanceMap.size === 0) {
      elementMap.delete(element);
    }
  }
};
function normalizeData(value) {
  if (value === "true") {
    return true;
  }
  if (value === "false") {
    return false;
  }
  if (value === Number(value).toString()) {
    return Number(value);
  }
  if (value === "" || value === "null") {
    return null;
  }
  if (typeof value !== "string") {
    return value;
  }
  try {
    return JSON.parse(decodeURIComponent(value));
  } catch (_unused) {
    return value;
  }
}
function normalizeDataKey(key) {
  return key.replace(/[A-Z]/g, (chr) => `-${chr.toLowerCase()}`);
}
var Manipulator = {
  setDataAttribute(element, key, value) {
    element.setAttribute(`data-bs-${normalizeDataKey(key)}`, value);
  },
  removeDataAttribute(element, key) {
    element.removeAttribute(`data-bs-${normalizeDataKey(key)}`);
  },
  getDataAttributes(element) {
    if (!element) {
      return {};
    }
    const attributes = {};
    const bsKeys = Object.keys(element.dataset).filter((key) => key.startsWith("bs") && !key.startsWith("bsConfig"));
    for (const key of bsKeys) {
      let pureKey = key.replace(/^bs/, "");
      pureKey = pureKey.charAt(0).toLowerCase() + pureKey.slice(1, pureKey.length);
      attributes[pureKey] = normalizeData(element.dataset[key]);
    }
    return attributes;
  },
  getDataAttribute(element, key) {
    return normalizeData(element.getAttribute(`data-bs-${normalizeDataKey(key)}`));
  }
};
var Config = class {
  static get Default() {
    return {};
  }
  static get DefaultType() {
    return {};
  }
  static get NAME() {
    throw new Error('You have to implement the static method "NAME", for each component!');
  }
  _getConfig(config) {
    config = this._mergeConfigObj(config);
    config = this._configAfterMerge(config);
    this._typeCheckConfig(config);
    return config;
  }
  _configAfterMerge(config) {
    return config;
  }
  _mergeConfigObj(config, element) {
    const jsonConfig = isElement2(element) ? Manipulator.getDataAttribute(element, "config") : {};
    return {
      ...this.constructor.Default,
      ...typeof jsonConfig === "object" ? jsonConfig : {},
      ...isElement2(element) ? Manipulator.getDataAttributes(element) : {},
      ...typeof config === "object" ? config : {}
    };
  }
  _typeCheckConfig(config, configTypes = this.constructor.DefaultType) {
    for (const property of Object.keys(configTypes)) {
      const expectedTypes = configTypes[property];
      const value = config[property];
      const valueType = isElement2(value) ? "element" : toType(value);
      if (!new RegExp(expectedTypes).test(valueType)) {
        throw new TypeError(`${this.constructor.NAME.toUpperCase()}: Option "${property}" provided type "${valueType}" but expected type "${expectedTypes}".`);
      }
    }
  }
};
var VERSION = "5.2.3";
var BaseComponent = class extends Config {
  constructor(element, config) {
    super();
    element = getElement(element);
    if (!element) {
      return;
    }
    this._element = element;
    this._config = this._getConfig(config);
    Data.set(this._element, this.constructor.DATA_KEY, this);
  }
  dispose() {
    Data.remove(this._element, this.constructor.DATA_KEY);
    EventHandler.off(this._element, this.constructor.EVENT_KEY);
    for (const propertyName of Object.getOwnPropertyNames(this)) {
      this[propertyName] = null;
    }
  }
  _queueCallback(callback, element, isAnimated = true) {
    executeAfterTransition(callback, element, isAnimated);
  }
  _getConfig(config) {
    config = this._mergeConfigObj(config, this._element);
    config = this._configAfterMerge(config);
    this._typeCheckConfig(config);
    return config;
  }
  static getInstance(element) {
    return Data.get(getElement(element), this.DATA_KEY);
  }
  static getOrCreateInstance(element, config = {}) {
    return this.getInstance(element) || new this(element, typeof config === "object" ? config : null);
  }
  static get VERSION() {
    return VERSION;
  }
  static get DATA_KEY() {
    return `bs.${this.NAME}`;
  }
  static get EVENT_KEY() {
    return `.${this.DATA_KEY}`;
  }
  static eventName(name) {
    return `${name}${this.EVENT_KEY}`;
  }
};
var enableDismissTrigger = (component, method = "hide") => {
  const clickEvent = `click.dismiss${component.EVENT_KEY}`;
  const name = component.NAME;
  EventHandler.on(document, clickEvent, `[data-bs-dismiss="${name}"]`, function(event) {
    if (["A", "AREA"].includes(this.tagName)) {
      event.preventDefault();
    }
    if (isDisabled(this)) {
      return;
    }
    const target = getElementFromSelector(this) || this.closest(`.${name}`);
    const instance = component.getOrCreateInstance(target);
    instance[method]();
  });
};
var NAME$f = "alert";
var DATA_KEY$a = "bs.alert";
var EVENT_KEY$b = `.${DATA_KEY$a}`;
var EVENT_CLOSE = `close${EVENT_KEY$b}`;
var EVENT_CLOSED = `closed${EVENT_KEY$b}`;
var CLASS_NAME_FADE$5 = "fade";
var CLASS_NAME_SHOW$8 = "show";
var Alert = class extends BaseComponent {
  static get NAME() {
    return NAME$f;
  }
  close() {
    const closeEvent = EventHandler.trigger(this._element, EVENT_CLOSE);
    if (closeEvent.defaultPrevented) {
      return;
    }
    this._element.classList.remove(CLASS_NAME_SHOW$8);
    const isAnimated = this._element.classList.contains(CLASS_NAME_FADE$5);
    this._queueCallback(() => this._destroyElement(), this._element, isAnimated);
  }
  _destroyElement() {
    this._element.remove();
    EventHandler.trigger(this._element, EVENT_CLOSED);
    this.dispose();
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Alert.getOrCreateInstance(this);
      if (typeof config !== "string") {
        return;
      }
      if (data[config] === void 0 || config.startsWith("_") || config === "constructor") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config](this);
    });
  }
};
enableDismissTrigger(Alert, "close");
defineJQueryPlugin(Alert);
var NAME$e = "button";
var DATA_KEY$9 = "bs.button";
var EVENT_KEY$a = `.${DATA_KEY$9}`;
var DATA_API_KEY$6 = ".data-api";
var CLASS_NAME_ACTIVE$3 = "active";
var SELECTOR_DATA_TOGGLE$5 = '[data-bs-toggle="button"]';
var EVENT_CLICK_DATA_API$6 = `click${EVENT_KEY$a}${DATA_API_KEY$6}`;
var Button = class extends BaseComponent {
  static get NAME() {
    return NAME$e;
  }
  toggle() {
    this._element.setAttribute("aria-pressed", this._element.classList.toggle(CLASS_NAME_ACTIVE$3));
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Button.getOrCreateInstance(this);
      if (config === "toggle") {
        data[config]();
      }
    });
  }
};
EventHandler.on(document, EVENT_CLICK_DATA_API$6, SELECTOR_DATA_TOGGLE$5, (event) => {
  event.preventDefault();
  const button = event.target.closest(SELECTOR_DATA_TOGGLE$5);
  const data = Button.getOrCreateInstance(button);
  data.toggle();
});
defineJQueryPlugin(Button);
var SelectorEngine = {
  find(selector, element = document.documentElement) {
    return [].concat(...Element.prototype.querySelectorAll.call(element, selector));
  },
  findOne(selector, element = document.documentElement) {
    return Element.prototype.querySelector.call(element, selector);
  },
  children(element, selector) {
    return [].concat(...element.children).filter((child) => child.matches(selector));
  },
  parents(element, selector) {
    const parents = [];
    let ancestor = element.parentNode.closest(selector);
    while (ancestor) {
      parents.push(ancestor);
      ancestor = ancestor.parentNode.closest(selector);
    }
    return parents;
  },
  prev(element, selector) {
    let previous = element.previousElementSibling;
    while (previous) {
      if (previous.matches(selector)) {
        return [previous];
      }
      previous = previous.previousElementSibling;
    }
    return [];
  },
  next(element, selector) {
    let next = element.nextElementSibling;
    while (next) {
      if (next.matches(selector)) {
        return [next];
      }
      next = next.nextElementSibling;
    }
    return [];
  },
  focusableChildren(element) {
    const focusables = ["a", "button", "input", "textarea", "select", "details", "[tabindex]", '[contenteditable="true"]'].map((selector) => `${selector}:not([tabindex^="-"])`).join(",");
    return this.find(focusables, element).filter((el2) => !isDisabled(el2) && isVisible(el2));
  }
};
var NAME$d = "swipe";
var EVENT_KEY$9 = ".bs.swipe";
var EVENT_TOUCHSTART = `touchstart${EVENT_KEY$9}`;
var EVENT_TOUCHMOVE = `touchmove${EVENT_KEY$9}`;
var EVENT_TOUCHEND = `touchend${EVENT_KEY$9}`;
var EVENT_POINTERDOWN = `pointerdown${EVENT_KEY$9}`;
var EVENT_POINTERUP = `pointerup${EVENT_KEY$9}`;
var POINTER_TYPE_TOUCH = "touch";
var POINTER_TYPE_PEN = "pen";
var CLASS_NAME_POINTER_EVENT = "pointer-event";
var SWIPE_THRESHOLD = 40;
var Default$c = {
  endCallback: null,
  leftCallback: null,
  rightCallback: null
};
var DefaultType$c = {
  endCallback: "(function|null)",
  leftCallback: "(function|null)",
  rightCallback: "(function|null)"
};
var Swipe = class extends Config {
  constructor(element, config) {
    super();
    this._element = element;
    if (!element || !Swipe.isSupported()) {
      return;
    }
    this._config = this._getConfig(config);
    this._deltaX = 0;
    this._supportPointerEvents = Boolean(window.PointerEvent);
    this._initEvents();
  }
  static get Default() {
    return Default$c;
  }
  static get DefaultType() {
    return DefaultType$c;
  }
  static get NAME() {
    return NAME$d;
  }
  dispose() {
    EventHandler.off(this._element, EVENT_KEY$9);
  }
  _start(event) {
    if (!this._supportPointerEvents) {
      this._deltaX = event.touches[0].clientX;
      return;
    }
    if (this._eventIsPointerPenTouch(event)) {
      this._deltaX = event.clientX;
    }
  }
  _end(event) {
    if (this._eventIsPointerPenTouch(event)) {
      this._deltaX = event.clientX - this._deltaX;
    }
    this._handleSwipe();
    execute(this._config.endCallback);
  }
  _move(event) {
    this._deltaX = event.touches && event.touches.length > 1 ? 0 : event.touches[0].clientX - this._deltaX;
  }
  _handleSwipe() {
    const absDeltaX = Math.abs(this._deltaX);
    if (absDeltaX <= SWIPE_THRESHOLD) {
      return;
    }
    const direction = absDeltaX / this._deltaX;
    this._deltaX = 0;
    if (!direction) {
      return;
    }
    execute(direction > 0 ? this._config.rightCallback : this._config.leftCallback);
  }
  _initEvents() {
    if (this._supportPointerEvents) {
      EventHandler.on(this._element, EVENT_POINTERDOWN, (event) => this._start(event));
      EventHandler.on(this._element, EVENT_POINTERUP, (event) => this._end(event));
      this._element.classList.add(CLASS_NAME_POINTER_EVENT);
    } else {
      EventHandler.on(this._element, EVENT_TOUCHSTART, (event) => this._start(event));
      EventHandler.on(this._element, EVENT_TOUCHMOVE, (event) => this._move(event));
      EventHandler.on(this._element, EVENT_TOUCHEND, (event) => this._end(event));
    }
  }
  _eventIsPointerPenTouch(event) {
    return this._supportPointerEvents && (event.pointerType === POINTER_TYPE_PEN || event.pointerType === POINTER_TYPE_TOUCH);
  }
  static isSupported() {
    return "ontouchstart" in document.documentElement || navigator.maxTouchPoints > 0;
  }
};
var NAME$c = "carousel";
var DATA_KEY$8 = "bs.carousel";
var EVENT_KEY$8 = `.${DATA_KEY$8}`;
var DATA_API_KEY$5 = ".data-api";
var ARROW_LEFT_KEY$1 = "ArrowLeft";
var ARROW_RIGHT_KEY$1 = "ArrowRight";
var TOUCHEVENT_COMPAT_WAIT = 500;
var ORDER_NEXT = "next";
var ORDER_PREV = "prev";
var DIRECTION_LEFT = "left";
var DIRECTION_RIGHT = "right";
var EVENT_SLIDE = `slide${EVENT_KEY$8}`;
var EVENT_SLID = `slid${EVENT_KEY$8}`;
var EVENT_KEYDOWN$1 = `keydown${EVENT_KEY$8}`;
var EVENT_MOUSEENTER$1 = `mouseenter${EVENT_KEY$8}`;
var EVENT_MOUSELEAVE$1 = `mouseleave${EVENT_KEY$8}`;
var EVENT_DRAG_START = `dragstart${EVENT_KEY$8}`;
var EVENT_LOAD_DATA_API$3 = `load${EVENT_KEY$8}${DATA_API_KEY$5}`;
var EVENT_CLICK_DATA_API$5 = `click${EVENT_KEY$8}${DATA_API_KEY$5}`;
var CLASS_NAME_CAROUSEL = "carousel";
var CLASS_NAME_ACTIVE$2 = "active";
var CLASS_NAME_SLIDE = "slide";
var CLASS_NAME_END = "carousel-item-end";
var CLASS_NAME_START = "carousel-item-start";
var CLASS_NAME_NEXT = "carousel-item-next";
var CLASS_NAME_PREV = "carousel-item-prev";
var SELECTOR_ACTIVE = ".active";
var SELECTOR_ITEM = ".carousel-item";
var SELECTOR_ACTIVE_ITEM = SELECTOR_ACTIVE + SELECTOR_ITEM;
var SELECTOR_ITEM_IMG = ".carousel-item img";
var SELECTOR_INDICATORS = ".carousel-indicators";
var SELECTOR_DATA_SLIDE = "[data-bs-slide], [data-bs-slide-to]";
var SELECTOR_DATA_RIDE = '[data-bs-ride="carousel"]';
var KEY_TO_DIRECTION = {
  [ARROW_LEFT_KEY$1]: DIRECTION_RIGHT,
  [ARROW_RIGHT_KEY$1]: DIRECTION_LEFT
};
var Default$b = {
  interval: 5e3,
  keyboard: true,
  pause: "hover",
  ride: false,
  touch: true,
  wrap: true
};
var DefaultType$b = {
  interval: "(number|boolean)",
  keyboard: "boolean",
  pause: "(string|boolean)",
  ride: "(boolean|string)",
  touch: "boolean",
  wrap: "boolean"
};
var Carousel = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._interval = null;
    this._activeElement = null;
    this._isSliding = false;
    this.touchTimeout = null;
    this._swipeHelper = null;
    this._indicatorsElement = SelectorEngine.findOne(SELECTOR_INDICATORS, this._element);
    this._addEventListeners();
    if (this._config.ride === CLASS_NAME_CAROUSEL) {
      this.cycle();
    }
  }
  static get Default() {
    return Default$b;
  }
  static get DefaultType() {
    return DefaultType$b;
  }
  static get NAME() {
    return NAME$c;
  }
  next() {
    this._slide(ORDER_NEXT);
  }
  nextWhenVisible() {
    if (!document.hidden && isVisible(this._element)) {
      this.next();
    }
  }
  prev() {
    this._slide(ORDER_PREV);
  }
  pause() {
    if (this._isSliding) {
      triggerTransitionEnd(this._element);
    }
    this._clearInterval();
  }
  cycle() {
    this._clearInterval();
    this._updateInterval();
    this._interval = setInterval(() => this.nextWhenVisible(), this._config.interval);
  }
  _maybeEnableCycle() {
    if (!this._config.ride) {
      return;
    }
    if (this._isSliding) {
      EventHandler.one(this._element, EVENT_SLID, () => this.cycle());
      return;
    }
    this.cycle();
  }
  to(index) {
    const items = this._getItems();
    if (index > items.length - 1 || index < 0) {
      return;
    }
    if (this._isSliding) {
      EventHandler.one(this._element, EVENT_SLID, () => this.to(index));
      return;
    }
    const activeIndex = this._getItemIndex(this._getActive());
    if (activeIndex === index) {
      return;
    }
    const order2 = index > activeIndex ? ORDER_NEXT : ORDER_PREV;
    this._slide(order2, items[index]);
  }
  dispose() {
    if (this._swipeHelper) {
      this._swipeHelper.dispose();
    }
    super.dispose();
  }
  _configAfterMerge(config) {
    config.defaultInterval = config.interval;
    return config;
  }
  _addEventListeners() {
    if (this._config.keyboard) {
      EventHandler.on(this._element, EVENT_KEYDOWN$1, (event) => this._keydown(event));
    }
    if (this._config.pause === "hover") {
      EventHandler.on(this._element, EVENT_MOUSEENTER$1, () => this.pause());
      EventHandler.on(this._element, EVENT_MOUSELEAVE$1, () => this._maybeEnableCycle());
    }
    if (this._config.touch && Swipe.isSupported()) {
      this._addTouchEventListeners();
    }
  }
  _addTouchEventListeners() {
    for (const img of SelectorEngine.find(SELECTOR_ITEM_IMG, this._element)) {
      EventHandler.on(img, EVENT_DRAG_START, (event) => event.preventDefault());
    }
    const endCallBack = () => {
      if (this._config.pause !== "hover") {
        return;
      }
      this.pause();
      if (this.touchTimeout) {
        clearTimeout(this.touchTimeout);
      }
      this.touchTimeout = setTimeout(() => this._maybeEnableCycle(), TOUCHEVENT_COMPAT_WAIT + this._config.interval);
    };
    const swipeConfig = {
      leftCallback: () => this._slide(this._directionToOrder(DIRECTION_LEFT)),
      rightCallback: () => this._slide(this._directionToOrder(DIRECTION_RIGHT)),
      endCallback: endCallBack
    };
    this._swipeHelper = new Swipe(this._element, swipeConfig);
  }
  _keydown(event) {
    if (/input|textarea/i.test(event.target.tagName)) {
      return;
    }
    const direction = KEY_TO_DIRECTION[event.key];
    if (direction) {
      event.preventDefault();
      this._slide(this._directionToOrder(direction));
    }
  }
  _getItemIndex(element) {
    return this._getItems().indexOf(element);
  }
  _setActiveIndicatorElement(index) {
    if (!this._indicatorsElement) {
      return;
    }
    const activeIndicator = SelectorEngine.findOne(SELECTOR_ACTIVE, this._indicatorsElement);
    activeIndicator.classList.remove(CLASS_NAME_ACTIVE$2);
    activeIndicator.removeAttribute("aria-current");
    const newActiveIndicator = SelectorEngine.findOne(`[data-bs-slide-to="${index}"]`, this._indicatorsElement);
    if (newActiveIndicator) {
      newActiveIndicator.classList.add(CLASS_NAME_ACTIVE$2);
      newActiveIndicator.setAttribute("aria-current", "true");
    }
  }
  _updateInterval() {
    const element = this._activeElement || this._getActive();
    if (!element) {
      return;
    }
    const elementInterval = Number.parseInt(element.getAttribute("data-bs-interval"), 10);
    this._config.interval = elementInterval || this._config.defaultInterval;
  }
  _slide(order2, element = null) {
    if (this._isSliding) {
      return;
    }
    const activeElement = this._getActive();
    const isNext = order2 === ORDER_NEXT;
    const nextElement = element || getNextActiveElement(this._getItems(), activeElement, isNext, this._config.wrap);
    if (nextElement === activeElement) {
      return;
    }
    const nextElementIndex = this._getItemIndex(nextElement);
    const triggerEvent = (eventName) => {
      return EventHandler.trigger(this._element, eventName, {
        relatedTarget: nextElement,
        direction: this._orderToDirection(order2),
        from: this._getItemIndex(activeElement),
        to: nextElementIndex
      });
    };
    const slideEvent = triggerEvent(EVENT_SLIDE);
    if (slideEvent.defaultPrevented) {
      return;
    }
    if (!activeElement || !nextElement) {
      return;
    }
    const isCycling = Boolean(this._interval);
    this.pause();
    this._isSliding = true;
    this._setActiveIndicatorElement(nextElementIndex);
    this._activeElement = nextElement;
    const directionalClassName = isNext ? CLASS_NAME_START : CLASS_NAME_END;
    const orderClassName = isNext ? CLASS_NAME_NEXT : CLASS_NAME_PREV;
    nextElement.classList.add(orderClassName);
    reflow(nextElement);
    activeElement.classList.add(directionalClassName);
    nextElement.classList.add(directionalClassName);
    const completeCallBack = () => {
      nextElement.classList.remove(directionalClassName, orderClassName);
      nextElement.classList.add(CLASS_NAME_ACTIVE$2);
      activeElement.classList.remove(CLASS_NAME_ACTIVE$2, orderClassName, directionalClassName);
      this._isSliding = false;
      triggerEvent(EVENT_SLID);
    };
    this._queueCallback(completeCallBack, activeElement, this._isAnimated());
    if (isCycling) {
      this.cycle();
    }
  }
  _isAnimated() {
    return this._element.classList.contains(CLASS_NAME_SLIDE);
  }
  _getActive() {
    return SelectorEngine.findOne(SELECTOR_ACTIVE_ITEM, this._element);
  }
  _getItems() {
    return SelectorEngine.find(SELECTOR_ITEM, this._element);
  }
  _clearInterval() {
    if (this._interval) {
      clearInterval(this._interval);
      this._interval = null;
    }
  }
  _directionToOrder(direction) {
    if (isRTL()) {
      return direction === DIRECTION_LEFT ? ORDER_PREV : ORDER_NEXT;
    }
    return direction === DIRECTION_LEFT ? ORDER_NEXT : ORDER_PREV;
  }
  _orderToDirection(order2) {
    if (isRTL()) {
      return order2 === ORDER_PREV ? DIRECTION_LEFT : DIRECTION_RIGHT;
    }
    return order2 === ORDER_PREV ? DIRECTION_RIGHT : DIRECTION_LEFT;
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Carousel.getOrCreateInstance(this, config);
      if (typeof config === "number") {
        data.to(config);
        return;
      }
      if (typeof config === "string") {
        if (data[config] === void 0 || config.startsWith("_") || config === "constructor") {
          throw new TypeError(`No method named "${config}"`);
        }
        data[config]();
      }
    });
  }
};
EventHandler.on(document, EVENT_CLICK_DATA_API$5, SELECTOR_DATA_SLIDE, function(event) {
  const target = getElementFromSelector(this);
  if (!target || !target.classList.contains(CLASS_NAME_CAROUSEL)) {
    return;
  }
  event.preventDefault();
  const carousel = Carousel.getOrCreateInstance(target);
  const slideIndex = this.getAttribute("data-bs-slide-to");
  if (slideIndex) {
    carousel.to(slideIndex);
    carousel._maybeEnableCycle();
    return;
  }
  if (Manipulator.getDataAttribute(this, "slide") === "next") {
    carousel.next();
    carousel._maybeEnableCycle();
    return;
  }
  carousel.prev();
  carousel._maybeEnableCycle();
});
EventHandler.on(window, EVENT_LOAD_DATA_API$3, () => {
  const carousels = SelectorEngine.find(SELECTOR_DATA_RIDE);
  for (const carousel of carousels) {
    Carousel.getOrCreateInstance(carousel);
  }
});
defineJQueryPlugin(Carousel);
var NAME$b = "collapse";
var DATA_KEY$7 = "bs.collapse";
var EVENT_KEY$7 = `.${DATA_KEY$7}`;
var DATA_API_KEY$4 = ".data-api";
var EVENT_SHOW$6 = `show${EVENT_KEY$7}`;
var EVENT_SHOWN$6 = `shown${EVENT_KEY$7}`;
var EVENT_HIDE$6 = `hide${EVENT_KEY$7}`;
var EVENT_HIDDEN$6 = `hidden${EVENT_KEY$7}`;
var EVENT_CLICK_DATA_API$4 = `click${EVENT_KEY$7}${DATA_API_KEY$4}`;
var CLASS_NAME_SHOW$7 = "show";
var CLASS_NAME_COLLAPSE = "collapse";
var CLASS_NAME_COLLAPSING = "collapsing";
var CLASS_NAME_COLLAPSED = "collapsed";
var CLASS_NAME_DEEPER_CHILDREN = `:scope .${CLASS_NAME_COLLAPSE} .${CLASS_NAME_COLLAPSE}`;
var CLASS_NAME_HORIZONTAL = "collapse-horizontal";
var WIDTH = "width";
var HEIGHT = "height";
var SELECTOR_ACTIVES = ".collapse.show, .collapse.collapsing";
var SELECTOR_DATA_TOGGLE$4 = '[data-bs-toggle="collapse"]';
var Default$a = {
  parent: null,
  toggle: true
};
var DefaultType$a = {
  parent: "(null|element)",
  toggle: "boolean"
};
var Collapse = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._isTransitioning = false;
    this._triggerArray = [];
    const toggleList = SelectorEngine.find(SELECTOR_DATA_TOGGLE$4);
    for (const elem of toggleList) {
      const selector = getSelectorFromElement(elem);
      const filterElement = SelectorEngine.find(selector).filter((foundElement) => foundElement === this._element);
      if (selector !== null && filterElement.length) {
        this._triggerArray.push(elem);
      }
    }
    this._initializeChildren();
    if (!this._config.parent) {
      this._addAriaAndCollapsedClass(this._triggerArray, this._isShown());
    }
    if (this._config.toggle) {
      this.toggle();
    }
  }
  static get Default() {
    return Default$a;
  }
  static get DefaultType() {
    return DefaultType$a;
  }
  static get NAME() {
    return NAME$b;
  }
  toggle() {
    if (this._isShown()) {
      this.hide();
    } else {
      this.show();
    }
  }
  show() {
    if (this._isTransitioning || this._isShown()) {
      return;
    }
    let activeChildren = [];
    if (this._config.parent) {
      activeChildren = this._getFirstLevelChildren(SELECTOR_ACTIVES).filter((element) => element !== this._element).map((element) => Collapse.getOrCreateInstance(element, {
        toggle: false
      }));
    }
    if (activeChildren.length && activeChildren[0]._isTransitioning) {
      return;
    }
    const startEvent = EventHandler.trigger(this._element, EVENT_SHOW$6);
    if (startEvent.defaultPrevented) {
      return;
    }
    for (const activeInstance of activeChildren) {
      activeInstance.hide();
    }
    const dimension = this._getDimension();
    this._element.classList.remove(CLASS_NAME_COLLAPSE);
    this._element.classList.add(CLASS_NAME_COLLAPSING);
    this._element.style[dimension] = 0;
    this._addAriaAndCollapsedClass(this._triggerArray, true);
    this._isTransitioning = true;
    const complete = () => {
      this._isTransitioning = false;
      this._element.classList.remove(CLASS_NAME_COLLAPSING);
      this._element.classList.add(CLASS_NAME_COLLAPSE, CLASS_NAME_SHOW$7);
      this._element.style[dimension] = "";
      EventHandler.trigger(this._element, EVENT_SHOWN$6);
    };
    const capitalizedDimension = dimension[0].toUpperCase() + dimension.slice(1);
    const scrollSize = `scroll${capitalizedDimension}`;
    this._queueCallback(complete, this._element, true);
    this._element.style[dimension] = `${this._element[scrollSize]}px`;
  }
  hide() {
    if (this._isTransitioning || !this._isShown()) {
      return;
    }
    const startEvent = EventHandler.trigger(this._element, EVENT_HIDE$6);
    if (startEvent.defaultPrevented) {
      return;
    }
    const dimension = this._getDimension();
    this._element.style[dimension] = `${this._element.getBoundingClientRect()[dimension]}px`;
    reflow(this._element);
    this._element.classList.add(CLASS_NAME_COLLAPSING);
    this._element.classList.remove(CLASS_NAME_COLLAPSE, CLASS_NAME_SHOW$7);
    for (const trigger of this._triggerArray) {
      const element = getElementFromSelector(trigger);
      if (element && !this._isShown(element)) {
        this._addAriaAndCollapsedClass([trigger], false);
      }
    }
    this._isTransitioning = true;
    const complete = () => {
      this._isTransitioning = false;
      this._element.classList.remove(CLASS_NAME_COLLAPSING);
      this._element.classList.add(CLASS_NAME_COLLAPSE);
      EventHandler.trigger(this._element, EVENT_HIDDEN$6);
    };
    this._element.style[dimension] = "";
    this._queueCallback(complete, this._element, true);
  }
  _isShown(element = this._element) {
    return element.classList.contains(CLASS_NAME_SHOW$7);
  }
  _configAfterMerge(config) {
    config.toggle = Boolean(config.toggle);
    config.parent = getElement(config.parent);
    return config;
  }
  _getDimension() {
    return this._element.classList.contains(CLASS_NAME_HORIZONTAL) ? WIDTH : HEIGHT;
  }
  _initializeChildren() {
    if (!this._config.parent) {
      return;
    }
    const children = this._getFirstLevelChildren(SELECTOR_DATA_TOGGLE$4);
    for (const element of children) {
      const selected = getElementFromSelector(element);
      if (selected) {
        this._addAriaAndCollapsedClass([element], this._isShown(selected));
      }
    }
  }
  _getFirstLevelChildren(selector) {
    const children = SelectorEngine.find(CLASS_NAME_DEEPER_CHILDREN, this._config.parent);
    return SelectorEngine.find(selector, this._config.parent).filter((element) => !children.includes(element));
  }
  _addAriaAndCollapsedClass(triggerArray, isOpen) {
    if (!triggerArray.length) {
      return;
    }
    for (const element of triggerArray) {
      element.classList.toggle(CLASS_NAME_COLLAPSED, !isOpen);
      element.setAttribute("aria-expanded", isOpen);
    }
  }
  static jQueryInterface(config) {
    const _config = {};
    if (typeof config === "string" && /show|hide/.test(config)) {
      _config.toggle = false;
    }
    return this.each(function() {
      const data = Collapse.getOrCreateInstance(this, _config);
      if (typeof config === "string") {
        if (typeof data[config] === "undefined") {
          throw new TypeError(`No method named "${config}"`);
        }
        data[config]();
      }
    });
  }
};
EventHandler.on(document, EVENT_CLICK_DATA_API$4, SELECTOR_DATA_TOGGLE$4, function(event) {
  if (event.target.tagName === "A" || event.delegateTarget && event.delegateTarget.tagName === "A") {
    event.preventDefault();
  }
  const selector = getSelectorFromElement(this);
  const selectorElements = SelectorEngine.find(selector);
  for (const element of selectorElements) {
    Collapse.getOrCreateInstance(element, {
      toggle: false
    }).toggle();
  }
});
defineJQueryPlugin(Collapse);
var NAME$a = "dropdown";
var DATA_KEY$6 = "bs.dropdown";
var EVENT_KEY$6 = `.${DATA_KEY$6}`;
var DATA_API_KEY$3 = ".data-api";
var ESCAPE_KEY$2 = "Escape";
var TAB_KEY$1 = "Tab";
var ARROW_UP_KEY$1 = "ArrowUp";
var ARROW_DOWN_KEY$1 = "ArrowDown";
var RIGHT_MOUSE_BUTTON = 2;
var EVENT_HIDE$5 = `hide${EVENT_KEY$6}`;
var EVENT_HIDDEN$5 = `hidden${EVENT_KEY$6}`;
var EVENT_SHOW$5 = `show${EVENT_KEY$6}`;
var EVENT_SHOWN$5 = `shown${EVENT_KEY$6}`;
var EVENT_CLICK_DATA_API$3 = `click${EVENT_KEY$6}${DATA_API_KEY$3}`;
var EVENT_KEYDOWN_DATA_API = `keydown${EVENT_KEY$6}${DATA_API_KEY$3}`;
var EVENT_KEYUP_DATA_API = `keyup${EVENT_KEY$6}${DATA_API_KEY$3}`;
var CLASS_NAME_SHOW$6 = "show";
var CLASS_NAME_DROPUP = "dropup";
var CLASS_NAME_DROPEND = "dropend";
var CLASS_NAME_DROPSTART = "dropstart";
var CLASS_NAME_DROPUP_CENTER = "dropup-center";
var CLASS_NAME_DROPDOWN_CENTER = "dropdown-center";
var SELECTOR_DATA_TOGGLE$3 = '[data-bs-toggle="dropdown"]:not(.disabled):not(:disabled)';
var SELECTOR_DATA_TOGGLE_SHOWN = `${SELECTOR_DATA_TOGGLE$3}.${CLASS_NAME_SHOW$6}`;
var SELECTOR_MENU = ".dropdown-menu";
var SELECTOR_NAVBAR = ".navbar";
var SELECTOR_NAVBAR_NAV = ".navbar-nav";
var SELECTOR_VISIBLE_ITEMS = ".dropdown-menu .dropdown-item:not(.disabled):not(:disabled)";
var PLACEMENT_TOP = isRTL() ? "top-end" : "top-start";
var PLACEMENT_TOPEND = isRTL() ? "top-start" : "top-end";
var PLACEMENT_BOTTOM = isRTL() ? "bottom-end" : "bottom-start";
var PLACEMENT_BOTTOMEND = isRTL() ? "bottom-start" : "bottom-end";
var PLACEMENT_RIGHT = isRTL() ? "left-start" : "right-start";
var PLACEMENT_LEFT = isRTL() ? "right-start" : "left-start";
var PLACEMENT_TOPCENTER = "top";
var PLACEMENT_BOTTOMCENTER = "bottom";
var Default$9 = {
  autoClose: true,
  boundary: "clippingParents",
  display: "dynamic",
  offset: [0, 2],
  popperConfig: null,
  reference: "toggle"
};
var DefaultType$9 = {
  autoClose: "(boolean|string)",
  boundary: "(string|element)",
  display: "string",
  offset: "(array|string|function)",
  popperConfig: "(null|object|function)",
  reference: "(string|element|object)"
};
var Dropdown = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._popper = null;
    this._parent = this._element.parentNode;
    this._menu = SelectorEngine.next(this._element, SELECTOR_MENU)[0] || SelectorEngine.prev(this._element, SELECTOR_MENU)[0] || SelectorEngine.findOne(SELECTOR_MENU, this._parent);
    this._inNavbar = this._detectNavbar();
  }
  static get Default() {
    return Default$9;
  }
  static get DefaultType() {
    return DefaultType$9;
  }
  static get NAME() {
    return NAME$a;
  }
  toggle() {
    return this._isShown() ? this.hide() : this.show();
  }
  show() {
    if (isDisabled(this._element) || this._isShown()) {
      return;
    }
    const relatedTarget = {
      relatedTarget: this._element
    };
    const showEvent = EventHandler.trigger(this._element, EVENT_SHOW$5, relatedTarget);
    if (showEvent.defaultPrevented) {
      return;
    }
    this._createPopper();
    if ("ontouchstart" in document.documentElement && !this._parent.closest(SELECTOR_NAVBAR_NAV)) {
      for (const element of [].concat(...document.body.children)) {
        EventHandler.on(element, "mouseover", noop);
      }
    }
    this._element.focus();
    this._element.setAttribute("aria-expanded", true);
    this._menu.classList.add(CLASS_NAME_SHOW$6);
    this._element.classList.add(CLASS_NAME_SHOW$6);
    EventHandler.trigger(this._element, EVENT_SHOWN$5, relatedTarget);
  }
  hide() {
    if (isDisabled(this._element) || !this._isShown()) {
      return;
    }
    const relatedTarget = {
      relatedTarget: this._element
    };
    this._completeHide(relatedTarget);
  }
  dispose() {
    if (this._popper) {
      this._popper.destroy();
    }
    super.dispose();
  }
  update() {
    this._inNavbar = this._detectNavbar();
    if (this._popper) {
      this._popper.update();
    }
  }
  _completeHide(relatedTarget) {
    const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE$5, relatedTarget);
    if (hideEvent.defaultPrevented) {
      return;
    }
    if ("ontouchstart" in document.documentElement) {
      for (const element of [].concat(...document.body.children)) {
        EventHandler.off(element, "mouseover", noop);
      }
    }
    if (this._popper) {
      this._popper.destroy();
    }
    this._menu.classList.remove(CLASS_NAME_SHOW$6);
    this._element.classList.remove(CLASS_NAME_SHOW$6);
    this._element.setAttribute("aria-expanded", "false");
    Manipulator.removeDataAttribute(this._menu, "popper");
    EventHandler.trigger(this._element, EVENT_HIDDEN$5, relatedTarget);
  }
  _getConfig(config) {
    config = super._getConfig(config);
    if (typeof config.reference === "object" && !isElement2(config.reference) && typeof config.reference.getBoundingClientRect !== "function") {
      throw new TypeError(`${NAME$a.toUpperCase()}: Option "reference" provided type "object" without a required "getBoundingClientRect" method.`);
    }
    return config;
  }
  _createPopper() {
    if (typeof lib_exports === "undefined") {
      throw new TypeError("Bootstrap's dropdowns require Popper (https://popper.js.org)");
    }
    let referenceElement = this._element;
    if (this._config.reference === "parent") {
      referenceElement = this._parent;
    } else if (isElement2(this._config.reference)) {
      referenceElement = getElement(this._config.reference);
    } else if (typeof this._config.reference === "object") {
      referenceElement = this._config.reference;
    }
    const popperConfig = this._getPopperConfig();
    this._popper = createPopper3(referenceElement, this._menu, popperConfig);
  }
  _isShown() {
    return this._menu.classList.contains(CLASS_NAME_SHOW$6);
  }
  _getPlacement() {
    const parentDropdown = this._parent;
    if (parentDropdown.classList.contains(CLASS_NAME_DROPEND)) {
      return PLACEMENT_RIGHT;
    }
    if (parentDropdown.classList.contains(CLASS_NAME_DROPSTART)) {
      return PLACEMENT_LEFT;
    }
    if (parentDropdown.classList.contains(CLASS_NAME_DROPUP_CENTER)) {
      return PLACEMENT_TOPCENTER;
    }
    if (parentDropdown.classList.contains(CLASS_NAME_DROPDOWN_CENTER)) {
      return PLACEMENT_BOTTOMCENTER;
    }
    const isEnd = getComputedStyle(this._menu).getPropertyValue("--bs-position").trim() === "end";
    if (parentDropdown.classList.contains(CLASS_NAME_DROPUP)) {
      return isEnd ? PLACEMENT_TOPEND : PLACEMENT_TOP;
    }
    return isEnd ? PLACEMENT_BOTTOMEND : PLACEMENT_BOTTOM;
  }
  _detectNavbar() {
    return this._element.closest(SELECTOR_NAVBAR) !== null;
  }
  _getOffset() {
    const {
      offset: offset2
    } = this._config;
    if (typeof offset2 === "string") {
      return offset2.split(",").map((value) => Number.parseInt(value, 10));
    }
    if (typeof offset2 === "function") {
      return (popperData) => offset2(popperData, this._element);
    }
    return offset2;
  }
  _getPopperConfig() {
    const defaultBsPopperConfig = {
      placement: this._getPlacement(),
      modifiers: [{
        name: "preventOverflow",
        options: {
          boundary: this._config.boundary
        }
      }, {
        name: "offset",
        options: {
          offset: this._getOffset()
        }
      }]
    };
    if (this._inNavbar || this._config.display === "static") {
      Manipulator.setDataAttribute(this._menu, "popper", "static");
      defaultBsPopperConfig.modifiers = [{
        name: "applyStyles",
        enabled: false
      }];
    }
    return {
      ...defaultBsPopperConfig,
      ...typeof this._config.popperConfig === "function" ? this._config.popperConfig(defaultBsPopperConfig) : this._config.popperConfig
    };
  }
  _selectMenuItem({
    key,
    target
  }) {
    const items = SelectorEngine.find(SELECTOR_VISIBLE_ITEMS, this._menu).filter((element) => isVisible(element));
    if (!items.length) {
      return;
    }
    getNextActiveElement(items, target, key === ARROW_DOWN_KEY$1, !items.includes(target)).focus();
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Dropdown.getOrCreateInstance(this, config);
      if (typeof config !== "string") {
        return;
      }
      if (typeof data[config] === "undefined") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config]();
    });
  }
  static clearMenus(event) {
    if (event.button === RIGHT_MOUSE_BUTTON || event.type === "keyup" && event.key !== TAB_KEY$1) {
      return;
    }
    const openToggles = SelectorEngine.find(SELECTOR_DATA_TOGGLE_SHOWN);
    for (const toggle of openToggles) {
      const context = Dropdown.getInstance(toggle);
      if (!context || context._config.autoClose === false) {
        continue;
      }
      const composedPath = event.composedPath();
      const isMenuTarget = composedPath.includes(context._menu);
      if (composedPath.includes(context._element) || context._config.autoClose === "inside" && !isMenuTarget || context._config.autoClose === "outside" && isMenuTarget) {
        continue;
      }
      if (context._menu.contains(event.target) && (event.type === "keyup" && event.key === TAB_KEY$1 || /input|select|option|textarea|form/i.test(event.target.tagName))) {
        continue;
      }
      const relatedTarget = {
        relatedTarget: context._element
      };
      if (event.type === "click") {
        relatedTarget.clickEvent = event;
      }
      context._completeHide(relatedTarget);
    }
  }
  static dataApiKeydownHandler(event) {
    const isInput = /input|textarea/i.test(event.target.tagName);
    const isEscapeEvent = event.key === ESCAPE_KEY$2;
    const isUpOrDownEvent = [ARROW_UP_KEY$1, ARROW_DOWN_KEY$1].includes(event.key);
    if (!isUpOrDownEvent && !isEscapeEvent) {
      return;
    }
    if (isInput && !isEscapeEvent) {
      return;
    }
    event.preventDefault();
    const getToggleButton = this.matches(SELECTOR_DATA_TOGGLE$3) ? this : SelectorEngine.prev(this, SELECTOR_DATA_TOGGLE$3)[0] || SelectorEngine.next(this, SELECTOR_DATA_TOGGLE$3)[0] || SelectorEngine.findOne(SELECTOR_DATA_TOGGLE$3, event.delegateTarget.parentNode);
    const instance = Dropdown.getOrCreateInstance(getToggleButton);
    if (isUpOrDownEvent) {
      event.stopPropagation();
      instance.show();
      instance._selectMenuItem(event);
      return;
    }
    if (instance._isShown()) {
      event.stopPropagation();
      instance.hide();
      getToggleButton.focus();
    }
  }
};
EventHandler.on(document, EVENT_KEYDOWN_DATA_API, SELECTOR_DATA_TOGGLE$3, Dropdown.dataApiKeydownHandler);
EventHandler.on(document, EVENT_KEYDOWN_DATA_API, SELECTOR_MENU, Dropdown.dataApiKeydownHandler);
EventHandler.on(document, EVENT_CLICK_DATA_API$3, Dropdown.clearMenus);
EventHandler.on(document, EVENT_KEYUP_DATA_API, Dropdown.clearMenus);
EventHandler.on(document, EVENT_CLICK_DATA_API$3, SELECTOR_DATA_TOGGLE$3, function(event) {
  event.preventDefault();
  Dropdown.getOrCreateInstance(this).toggle();
});
defineJQueryPlugin(Dropdown);
var SELECTOR_FIXED_CONTENT = ".fixed-top, .fixed-bottom, .is-fixed, .sticky-top";
var SELECTOR_STICKY_CONTENT = ".sticky-top";
var PROPERTY_PADDING = "padding-right";
var PROPERTY_MARGIN = "margin-right";
var ScrollBarHelper = class {
  constructor() {
    this._element = document.body;
  }
  getWidth() {
    const documentWidth = document.documentElement.clientWidth;
    return Math.abs(window.innerWidth - documentWidth);
  }
  hide() {
    const width = this.getWidth();
    this._disableOverFlow();
    this._setElementAttributes(this._element, PROPERTY_PADDING, (calculatedValue) => calculatedValue + width);
    this._setElementAttributes(SELECTOR_FIXED_CONTENT, PROPERTY_PADDING, (calculatedValue) => calculatedValue + width);
    this._setElementAttributes(SELECTOR_STICKY_CONTENT, PROPERTY_MARGIN, (calculatedValue) => calculatedValue - width);
  }
  reset() {
    this._resetElementAttributes(this._element, "overflow");
    this._resetElementAttributes(this._element, PROPERTY_PADDING);
    this._resetElementAttributes(SELECTOR_FIXED_CONTENT, PROPERTY_PADDING);
    this._resetElementAttributes(SELECTOR_STICKY_CONTENT, PROPERTY_MARGIN);
  }
  isOverflowing() {
    return this.getWidth() > 0;
  }
  _disableOverFlow() {
    this._saveInitialAttribute(this._element, "overflow");
    this._element.style.overflow = "hidden";
  }
  _setElementAttributes(selector, styleProperty, callback) {
    const scrollbarWidth = this.getWidth();
    const manipulationCallBack = (element) => {
      if (element !== this._element && window.innerWidth > element.clientWidth + scrollbarWidth) {
        return;
      }
      this._saveInitialAttribute(element, styleProperty);
      const calculatedValue = window.getComputedStyle(element).getPropertyValue(styleProperty);
      element.style.setProperty(styleProperty, `${callback(Number.parseFloat(calculatedValue))}px`);
    };
    this._applyManipulationCallback(selector, manipulationCallBack);
  }
  _saveInitialAttribute(element, styleProperty) {
    const actualValue = element.style.getPropertyValue(styleProperty);
    if (actualValue) {
      Manipulator.setDataAttribute(element, styleProperty, actualValue);
    }
  }
  _resetElementAttributes(selector, styleProperty) {
    const manipulationCallBack = (element) => {
      const value = Manipulator.getDataAttribute(element, styleProperty);
      if (value === null) {
        element.style.removeProperty(styleProperty);
        return;
      }
      Manipulator.removeDataAttribute(element, styleProperty);
      element.style.setProperty(styleProperty, value);
    };
    this._applyManipulationCallback(selector, manipulationCallBack);
  }
  _applyManipulationCallback(selector, callBack) {
    if (isElement2(selector)) {
      callBack(selector);
      return;
    }
    for (const sel of SelectorEngine.find(selector, this._element)) {
      callBack(sel);
    }
  }
};
var NAME$9 = "backdrop";
var CLASS_NAME_FADE$4 = "fade";
var CLASS_NAME_SHOW$5 = "show";
var EVENT_MOUSEDOWN = `mousedown.bs.${NAME$9}`;
var Default$8 = {
  className: "modal-backdrop",
  clickCallback: null,
  isAnimated: false,
  isVisible: true,
  rootElement: "body"
};
var DefaultType$8 = {
  className: "string",
  clickCallback: "(function|null)",
  isAnimated: "boolean",
  isVisible: "boolean",
  rootElement: "(element|string)"
};
var Backdrop = class extends Config {
  constructor(config) {
    super();
    this._config = this._getConfig(config);
    this._isAppended = false;
    this._element = null;
  }
  static get Default() {
    return Default$8;
  }
  static get DefaultType() {
    return DefaultType$8;
  }
  static get NAME() {
    return NAME$9;
  }
  show(callback) {
    if (!this._config.isVisible) {
      execute(callback);
      return;
    }
    this._append();
    const element = this._getElement();
    if (this._config.isAnimated) {
      reflow(element);
    }
    element.classList.add(CLASS_NAME_SHOW$5);
    this._emulateAnimation(() => {
      execute(callback);
    });
  }
  hide(callback) {
    if (!this._config.isVisible) {
      execute(callback);
      return;
    }
    this._getElement().classList.remove(CLASS_NAME_SHOW$5);
    this._emulateAnimation(() => {
      this.dispose();
      execute(callback);
    });
  }
  dispose() {
    if (!this._isAppended) {
      return;
    }
    EventHandler.off(this._element, EVENT_MOUSEDOWN);
    this._element.remove();
    this._isAppended = false;
  }
  _getElement() {
    if (!this._element) {
      const backdrop = document.createElement("div");
      backdrop.className = this._config.className;
      if (this._config.isAnimated) {
        backdrop.classList.add(CLASS_NAME_FADE$4);
      }
      this._element = backdrop;
    }
    return this._element;
  }
  _configAfterMerge(config) {
    config.rootElement = getElement(config.rootElement);
    return config;
  }
  _append() {
    if (this._isAppended) {
      return;
    }
    const element = this._getElement();
    this._config.rootElement.append(element);
    EventHandler.on(element, EVENT_MOUSEDOWN, () => {
      execute(this._config.clickCallback);
    });
    this._isAppended = true;
  }
  _emulateAnimation(callback) {
    executeAfterTransition(callback, this._getElement(), this._config.isAnimated);
  }
};
var NAME$8 = "focustrap";
var DATA_KEY$5 = "bs.focustrap";
var EVENT_KEY$5 = `.${DATA_KEY$5}`;
var EVENT_FOCUSIN$2 = `focusin${EVENT_KEY$5}`;
var EVENT_KEYDOWN_TAB = `keydown.tab${EVENT_KEY$5}`;
var TAB_KEY = "Tab";
var TAB_NAV_FORWARD = "forward";
var TAB_NAV_BACKWARD = "backward";
var Default$7 = {
  autofocus: true,
  trapElement: null
};
var DefaultType$7 = {
  autofocus: "boolean",
  trapElement: "element"
};
var FocusTrap = class extends Config {
  constructor(config) {
    super();
    this._config = this._getConfig(config);
    this._isActive = false;
    this._lastTabNavDirection = null;
  }
  static get Default() {
    return Default$7;
  }
  static get DefaultType() {
    return DefaultType$7;
  }
  static get NAME() {
    return NAME$8;
  }
  activate() {
    if (this._isActive) {
      return;
    }
    if (this._config.autofocus) {
      this._config.trapElement.focus();
    }
    EventHandler.off(document, EVENT_KEY$5);
    EventHandler.on(document, EVENT_FOCUSIN$2, (event) => this._handleFocusin(event));
    EventHandler.on(document, EVENT_KEYDOWN_TAB, (event) => this._handleKeydown(event));
    this._isActive = true;
  }
  deactivate() {
    if (!this._isActive) {
      return;
    }
    this._isActive = false;
    EventHandler.off(document, EVENT_KEY$5);
  }
  _handleFocusin(event) {
    const {
      trapElement
    } = this._config;
    if (event.target === document || event.target === trapElement || trapElement.contains(event.target)) {
      return;
    }
    const elements = SelectorEngine.focusableChildren(trapElement);
    if (elements.length === 0) {
      trapElement.focus();
    } else if (this._lastTabNavDirection === TAB_NAV_BACKWARD) {
      elements[elements.length - 1].focus();
    } else {
      elements[0].focus();
    }
  }
  _handleKeydown(event) {
    if (event.key !== TAB_KEY) {
      return;
    }
    this._lastTabNavDirection = event.shiftKey ? TAB_NAV_BACKWARD : TAB_NAV_FORWARD;
  }
};
var NAME$7 = "modal";
var DATA_KEY$4 = "bs.modal";
var EVENT_KEY$4 = `.${DATA_KEY$4}`;
var DATA_API_KEY$2 = ".data-api";
var ESCAPE_KEY$1 = "Escape";
var EVENT_HIDE$4 = `hide${EVENT_KEY$4}`;
var EVENT_HIDE_PREVENTED$1 = `hidePrevented${EVENT_KEY$4}`;
var EVENT_HIDDEN$4 = `hidden${EVENT_KEY$4}`;
var EVENT_SHOW$4 = `show${EVENT_KEY$4}`;
var EVENT_SHOWN$4 = `shown${EVENT_KEY$4}`;
var EVENT_RESIZE$1 = `resize${EVENT_KEY$4}`;
var EVENT_CLICK_DISMISS = `click.dismiss${EVENT_KEY$4}`;
var EVENT_MOUSEDOWN_DISMISS = `mousedown.dismiss${EVENT_KEY$4}`;
var EVENT_KEYDOWN_DISMISS$1 = `keydown.dismiss${EVENT_KEY$4}`;
var EVENT_CLICK_DATA_API$2 = `click${EVENT_KEY$4}${DATA_API_KEY$2}`;
var CLASS_NAME_OPEN = "modal-open";
var CLASS_NAME_FADE$3 = "fade";
var CLASS_NAME_SHOW$4 = "show";
var CLASS_NAME_STATIC = "modal-static";
var OPEN_SELECTOR$1 = ".modal.show";
var SELECTOR_DIALOG = ".modal-dialog";
var SELECTOR_MODAL_BODY = ".modal-body";
var SELECTOR_DATA_TOGGLE$2 = '[data-bs-toggle="modal"]';
var Default$6 = {
  backdrop: true,
  focus: true,
  keyboard: true
};
var DefaultType$6 = {
  backdrop: "(boolean|string)",
  focus: "boolean",
  keyboard: "boolean"
};
var Modal = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._dialog = SelectorEngine.findOne(SELECTOR_DIALOG, this._element);
    this._backdrop = this._initializeBackDrop();
    this._focustrap = this._initializeFocusTrap();
    this._isShown = false;
    this._isTransitioning = false;
    this._scrollBar = new ScrollBarHelper();
    this._addEventListeners();
  }
  static get Default() {
    return Default$6;
  }
  static get DefaultType() {
    return DefaultType$6;
  }
  static get NAME() {
    return NAME$7;
  }
  toggle(relatedTarget) {
    return this._isShown ? this.hide() : this.show(relatedTarget);
  }
  show(relatedTarget) {
    if (this._isShown || this._isTransitioning) {
      return;
    }
    const showEvent = EventHandler.trigger(this._element, EVENT_SHOW$4, {
      relatedTarget
    });
    if (showEvent.defaultPrevented) {
      return;
    }
    this._isShown = true;
    this._isTransitioning = true;
    this._scrollBar.hide();
    document.body.classList.add(CLASS_NAME_OPEN);
    this._adjustDialog();
    this._backdrop.show(() => this._showElement(relatedTarget));
  }
  hide() {
    if (!this._isShown || this._isTransitioning) {
      return;
    }
    const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE$4);
    if (hideEvent.defaultPrevented) {
      return;
    }
    this._isShown = false;
    this._isTransitioning = true;
    this._focustrap.deactivate();
    this._element.classList.remove(CLASS_NAME_SHOW$4);
    this._queueCallback(() => this._hideModal(), this._element, this._isAnimated());
  }
  dispose() {
    for (const htmlElement of [window, this._dialog]) {
      EventHandler.off(htmlElement, EVENT_KEY$4);
    }
    this._backdrop.dispose();
    this._focustrap.deactivate();
    super.dispose();
  }
  handleUpdate() {
    this._adjustDialog();
  }
  _initializeBackDrop() {
    return new Backdrop({
      isVisible: Boolean(this._config.backdrop),
      isAnimated: this._isAnimated()
    });
  }
  _initializeFocusTrap() {
    return new FocusTrap({
      trapElement: this._element
    });
  }
  _showElement(relatedTarget) {
    if (!document.body.contains(this._element)) {
      document.body.append(this._element);
    }
    this._element.style.display = "block";
    this._element.removeAttribute("aria-hidden");
    this._element.setAttribute("aria-modal", true);
    this._element.setAttribute("role", "dialog");
    this._element.scrollTop = 0;
    const modalBody = SelectorEngine.findOne(SELECTOR_MODAL_BODY, this._dialog);
    if (modalBody) {
      modalBody.scrollTop = 0;
    }
    reflow(this._element);
    this._element.classList.add(CLASS_NAME_SHOW$4);
    const transitionComplete = () => {
      if (this._config.focus) {
        this._focustrap.activate();
      }
      this._isTransitioning = false;
      EventHandler.trigger(this._element, EVENT_SHOWN$4, {
        relatedTarget
      });
    };
    this._queueCallback(transitionComplete, this._dialog, this._isAnimated());
  }
  _addEventListeners() {
    EventHandler.on(this._element, EVENT_KEYDOWN_DISMISS$1, (event) => {
      if (event.key !== ESCAPE_KEY$1) {
        return;
      }
      if (this._config.keyboard) {
        event.preventDefault();
        this.hide();
        return;
      }
      this._triggerBackdropTransition();
    });
    EventHandler.on(window, EVENT_RESIZE$1, () => {
      if (this._isShown && !this._isTransitioning) {
        this._adjustDialog();
      }
    });
    EventHandler.on(this._element, EVENT_MOUSEDOWN_DISMISS, (event) => {
      EventHandler.one(this._element, EVENT_CLICK_DISMISS, (event2) => {
        if (this._element !== event.target || this._element !== event2.target) {
          return;
        }
        if (this._config.backdrop === "static") {
          this._triggerBackdropTransition();
          return;
        }
        if (this._config.backdrop) {
          this.hide();
        }
      });
    });
  }
  _hideModal() {
    this._element.style.display = "none";
    this._element.setAttribute("aria-hidden", true);
    this._element.removeAttribute("aria-modal");
    this._element.removeAttribute("role");
    this._isTransitioning = false;
    this._backdrop.hide(() => {
      document.body.classList.remove(CLASS_NAME_OPEN);
      this._resetAdjustments();
      this._scrollBar.reset();
      EventHandler.trigger(this._element, EVENT_HIDDEN$4);
    });
  }
  _isAnimated() {
    return this._element.classList.contains(CLASS_NAME_FADE$3);
  }
  _triggerBackdropTransition() {
    const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE_PREVENTED$1);
    if (hideEvent.defaultPrevented) {
      return;
    }
    const isModalOverflowing = this._element.scrollHeight > document.documentElement.clientHeight;
    const initialOverflowY = this._element.style.overflowY;
    if (initialOverflowY === "hidden" || this._element.classList.contains(CLASS_NAME_STATIC)) {
      return;
    }
    if (!isModalOverflowing) {
      this._element.style.overflowY = "hidden";
    }
    this._element.classList.add(CLASS_NAME_STATIC);
    this._queueCallback(() => {
      this._element.classList.remove(CLASS_NAME_STATIC);
      this._queueCallback(() => {
        this._element.style.overflowY = initialOverflowY;
      }, this._dialog);
    }, this._dialog);
    this._element.focus();
  }
  _adjustDialog() {
    const isModalOverflowing = this._element.scrollHeight > document.documentElement.clientHeight;
    const scrollbarWidth = this._scrollBar.getWidth();
    const isBodyOverflowing = scrollbarWidth > 0;
    if (isBodyOverflowing && !isModalOverflowing) {
      const property = isRTL() ? "paddingLeft" : "paddingRight";
      this._element.style[property] = `${scrollbarWidth}px`;
    }
    if (!isBodyOverflowing && isModalOverflowing) {
      const property = isRTL() ? "paddingRight" : "paddingLeft";
      this._element.style[property] = `${scrollbarWidth}px`;
    }
  }
  _resetAdjustments() {
    this._element.style.paddingLeft = "";
    this._element.style.paddingRight = "";
  }
  static jQueryInterface(config, relatedTarget) {
    return this.each(function() {
      const data = Modal.getOrCreateInstance(this, config);
      if (typeof config !== "string") {
        return;
      }
      if (typeof data[config] === "undefined") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config](relatedTarget);
    });
  }
};
EventHandler.on(document, EVENT_CLICK_DATA_API$2, SELECTOR_DATA_TOGGLE$2, function(event) {
  const target = getElementFromSelector(this);
  if (["A", "AREA"].includes(this.tagName)) {
    event.preventDefault();
  }
  EventHandler.one(target, EVENT_SHOW$4, (showEvent) => {
    if (showEvent.defaultPrevented) {
      return;
    }
    EventHandler.one(target, EVENT_HIDDEN$4, () => {
      if (isVisible(this)) {
        this.focus();
      }
    });
  });
  const alreadyOpen = SelectorEngine.findOne(OPEN_SELECTOR$1);
  if (alreadyOpen) {
    Modal.getInstance(alreadyOpen).hide();
  }
  const data = Modal.getOrCreateInstance(target);
  data.toggle(this);
});
enableDismissTrigger(Modal);
defineJQueryPlugin(Modal);
var NAME$6 = "offcanvas";
var DATA_KEY$3 = "bs.offcanvas";
var EVENT_KEY$3 = `.${DATA_KEY$3}`;
var DATA_API_KEY$1 = ".data-api";
var EVENT_LOAD_DATA_API$2 = `load${EVENT_KEY$3}${DATA_API_KEY$1}`;
var ESCAPE_KEY = "Escape";
var CLASS_NAME_SHOW$3 = "show";
var CLASS_NAME_SHOWING$1 = "showing";
var CLASS_NAME_HIDING = "hiding";
var CLASS_NAME_BACKDROP = "offcanvas-backdrop";
var OPEN_SELECTOR = ".offcanvas.show";
var EVENT_SHOW$3 = `show${EVENT_KEY$3}`;
var EVENT_SHOWN$3 = `shown${EVENT_KEY$3}`;
var EVENT_HIDE$3 = `hide${EVENT_KEY$3}`;
var EVENT_HIDE_PREVENTED = `hidePrevented${EVENT_KEY$3}`;
var EVENT_HIDDEN$3 = `hidden${EVENT_KEY$3}`;
var EVENT_RESIZE = `resize${EVENT_KEY$3}`;
var EVENT_CLICK_DATA_API$1 = `click${EVENT_KEY$3}${DATA_API_KEY$1}`;
var EVENT_KEYDOWN_DISMISS = `keydown.dismiss${EVENT_KEY$3}`;
var SELECTOR_DATA_TOGGLE$1 = '[data-bs-toggle="offcanvas"]';
var Default$5 = {
  backdrop: true,
  keyboard: true,
  scroll: false
};
var DefaultType$5 = {
  backdrop: "(boolean|string)",
  keyboard: "boolean",
  scroll: "boolean"
};
var Offcanvas = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._isShown = false;
    this._backdrop = this._initializeBackDrop();
    this._focustrap = this._initializeFocusTrap();
    this._addEventListeners();
  }
  static get Default() {
    return Default$5;
  }
  static get DefaultType() {
    return DefaultType$5;
  }
  static get NAME() {
    return NAME$6;
  }
  toggle(relatedTarget) {
    return this._isShown ? this.hide() : this.show(relatedTarget);
  }
  show(relatedTarget) {
    if (this._isShown) {
      return;
    }
    const showEvent = EventHandler.trigger(this._element, EVENT_SHOW$3, {
      relatedTarget
    });
    if (showEvent.defaultPrevented) {
      return;
    }
    this._isShown = true;
    this._backdrop.show();
    if (!this._config.scroll) {
      new ScrollBarHelper().hide();
    }
    this._element.setAttribute("aria-modal", true);
    this._element.setAttribute("role", "dialog");
    this._element.classList.add(CLASS_NAME_SHOWING$1);
    const completeCallBack = () => {
      if (!this._config.scroll || this._config.backdrop) {
        this._focustrap.activate();
      }
      this._element.classList.add(CLASS_NAME_SHOW$3);
      this._element.classList.remove(CLASS_NAME_SHOWING$1);
      EventHandler.trigger(this._element, EVENT_SHOWN$3, {
        relatedTarget
      });
    };
    this._queueCallback(completeCallBack, this._element, true);
  }
  hide() {
    if (!this._isShown) {
      return;
    }
    const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE$3);
    if (hideEvent.defaultPrevented) {
      return;
    }
    this._focustrap.deactivate();
    this._element.blur();
    this._isShown = false;
    this._element.classList.add(CLASS_NAME_HIDING);
    this._backdrop.hide();
    const completeCallback = () => {
      this._element.classList.remove(CLASS_NAME_SHOW$3, CLASS_NAME_HIDING);
      this._element.removeAttribute("aria-modal");
      this._element.removeAttribute("role");
      if (!this._config.scroll) {
        new ScrollBarHelper().reset();
      }
      EventHandler.trigger(this._element, EVENT_HIDDEN$3);
    };
    this._queueCallback(completeCallback, this._element, true);
  }
  dispose() {
    this._backdrop.dispose();
    this._focustrap.deactivate();
    super.dispose();
  }
  _initializeBackDrop() {
    const clickCallback = () => {
      if (this._config.backdrop === "static") {
        EventHandler.trigger(this._element, EVENT_HIDE_PREVENTED);
        return;
      }
      this.hide();
    };
    const isVisible2 = Boolean(this._config.backdrop);
    return new Backdrop({
      className: CLASS_NAME_BACKDROP,
      isVisible: isVisible2,
      isAnimated: true,
      rootElement: this._element.parentNode,
      clickCallback: isVisible2 ? clickCallback : null
    });
  }
  _initializeFocusTrap() {
    return new FocusTrap({
      trapElement: this._element
    });
  }
  _addEventListeners() {
    EventHandler.on(this._element, EVENT_KEYDOWN_DISMISS, (event) => {
      if (event.key !== ESCAPE_KEY) {
        return;
      }
      if (!this._config.keyboard) {
        EventHandler.trigger(this._element, EVENT_HIDE_PREVENTED);
        return;
      }
      this.hide();
    });
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Offcanvas.getOrCreateInstance(this, config);
      if (typeof config !== "string") {
        return;
      }
      if (data[config] === void 0 || config.startsWith("_") || config === "constructor") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config](this);
    });
  }
};
EventHandler.on(document, EVENT_CLICK_DATA_API$1, SELECTOR_DATA_TOGGLE$1, function(event) {
  const target = getElementFromSelector(this);
  if (["A", "AREA"].includes(this.tagName)) {
    event.preventDefault();
  }
  if (isDisabled(this)) {
    return;
  }
  EventHandler.one(target, EVENT_HIDDEN$3, () => {
    if (isVisible(this)) {
      this.focus();
    }
  });
  const alreadyOpen = SelectorEngine.findOne(OPEN_SELECTOR);
  if (alreadyOpen && alreadyOpen !== target) {
    Offcanvas.getInstance(alreadyOpen).hide();
  }
  const data = Offcanvas.getOrCreateInstance(target);
  data.toggle(this);
});
EventHandler.on(window, EVENT_LOAD_DATA_API$2, () => {
  for (const selector of SelectorEngine.find(OPEN_SELECTOR)) {
    Offcanvas.getOrCreateInstance(selector).show();
  }
});
EventHandler.on(window, EVENT_RESIZE, () => {
  for (const element of SelectorEngine.find("[aria-modal][class*=show][class*=offcanvas-]")) {
    if (getComputedStyle(element).position !== "fixed") {
      Offcanvas.getOrCreateInstance(element).hide();
    }
  }
});
enableDismissTrigger(Offcanvas);
defineJQueryPlugin(Offcanvas);
var uriAttributes = /* @__PURE__ */ new Set(["background", "cite", "href", "itemtype", "longdesc", "poster", "src", "xlink:href"]);
var ARIA_ATTRIBUTE_PATTERN = /^aria-[\w-]*$/i;
var SAFE_URL_PATTERN = /^(?:(?:https?|mailto|ftp|tel|file|sms):|[^#&/:?]*(?:[#/?]|$))/i;
var DATA_URL_PATTERN = /^data:(?:image\/(?:bmp|gif|jpeg|jpg|png|tiff|webp)|video\/(?:mpeg|mp4|ogg|webm)|audio\/(?:mp3|oga|ogg|opus));base64,[\d+/a-z]+=*$/i;
var allowedAttribute = (attribute, allowedAttributeList) => {
  const attributeName = attribute.nodeName.toLowerCase();
  if (allowedAttributeList.includes(attributeName)) {
    if (uriAttributes.has(attributeName)) {
      return Boolean(SAFE_URL_PATTERN.test(attribute.nodeValue) || DATA_URL_PATTERN.test(attribute.nodeValue));
    }
    return true;
  }
  return allowedAttributeList.filter((attributeRegex) => attributeRegex instanceof RegExp).some((regex) => regex.test(attributeName));
};
var DefaultAllowlist = {
  "*": ["class", "dir", "id", "lang", "role", ARIA_ATTRIBUTE_PATTERN],
  a: ["target", "href", "title", "rel"],
  area: [],
  b: [],
  br: [],
  col: [],
  code: [],
  div: [],
  em: [],
  hr: [],
  h1: [],
  h2: [],
  h3: [],
  h4: [],
  h5: [],
  h6: [],
  i: [],
  img: ["src", "srcset", "alt", "title", "width", "height"],
  li: [],
  ol: [],
  p: [],
  pre: [],
  s: [],
  small: [],
  span: [],
  sub: [],
  sup: [],
  strong: [],
  u: [],
  ul: []
};
function sanitizeHtml(unsafeHtml, allowList, sanitizeFunction) {
  if (!unsafeHtml.length) {
    return unsafeHtml;
  }
  if (sanitizeFunction && typeof sanitizeFunction === "function") {
    return sanitizeFunction(unsafeHtml);
  }
  const domParser = new window.DOMParser();
  const createdDocument = domParser.parseFromString(unsafeHtml, "text/html");
  const elements = [].concat(...createdDocument.body.querySelectorAll("*"));
  for (const element of elements) {
    const elementName = element.nodeName.toLowerCase();
    if (!Object.keys(allowList).includes(elementName)) {
      element.remove();
      continue;
    }
    const attributeList = [].concat(...element.attributes);
    const allowedAttributes = [].concat(allowList["*"] || [], allowList[elementName] || []);
    for (const attribute of attributeList) {
      if (!allowedAttribute(attribute, allowedAttributes)) {
        element.removeAttribute(attribute.nodeName);
      }
    }
  }
  return createdDocument.body.innerHTML;
}
var NAME$5 = "TemplateFactory";
var Default$4 = {
  allowList: DefaultAllowlist,
  content: {},
  extraClass: "",
  html: false,
  sanitize: true,
  sanitizeFn: null,
  template: "<div></div>"
};
var DefaultType$4 = {
  allowList: "object",
  content: "object",
  extraClass: "(string|function)",
  html: "boolean",
  sanitize: "boolean",
  sanitizeFn: "(null|function)",
  template: "string"
};
var DefaultContentType = {
  entry: "(string|element|function|null)",
  selector: "(string|element)"
};
var TemplateFactory = class extends Config {
  constructor(config) {
    super();
    this._config = this._getConfig(config);
  }
  static get Default() {
    return Default$4;
  }
  static get DefaultType() {
    return DefaultType$4;
  }
  static get NAME() {
    return NAME$5;
  }
  getContent() {
    return Object.values(this._config.content).map((config) => this._resolvePossibleFunction(config)).filter(Boolean);
  }
  hasContent() {
    return this.getContent().length > 0;
  }
  changeContent(content) {
    this._checkContent(content);
    this._config.content = {
      ...this._config.content,
      ...content
    };
    return this;
  }
  toHtml() {
    const templateWrapper = document.createElement("div");
    templateWrapper.innerHTML = this._maybeSanitize(this._config.template);
    for (const [selector, text] of Object.entries(this._config.content)) {
      this._setContent(templateWrapper, text, selector);
    }
    const template = templateWrapper.children[0];
    const extraClass = this._resolvePossibleFunction(this._config.extraClass);
    if (extraClass) {
      template.classList.add(...extraClass.split(" "));
    }
    return template;
  }
  _typeCheckConfig(config) {
    super._typeCheckConfig(config);
    this._checkContent(config.content);
  }
  _checkContent(arg) {
    for (const [selector, content] of Object.entries(arg)) {
      super._typeCheckConfig({
        selector,
        entry: content
      }, DefaultContentType);
    }
  }
  _setContent(template, content, selector) {
    const templateElement = SelectorEngine.findOne(selector, template);
    if (!templateElement) {
      return;
    }
    content = this._resolvePossibleFunction(content);
    if (!content) {
      templateElement.remove();
      return;
    }
    if (isElement2(content)) {
      this._putElementInTemplate(getElement(content), templateElement);
      return;
    }
    if (this._config.html) {
      templateElement.innerHTML = this._maybeSanitize(content);
      return;
    }
    templateElement.textContent = content;
  }
  _maybeSanitize(arg) {
    return this._config.sanitize ? sanitizeHtml(arg, this._config.allowList, this._config.sanitizeFn) : arg;
  }
  _resolvePossibleFunction(arg) {
    return typeof arg === "function" ? arg(this) : arg;
  }
  _putElementInTemplate(element, templateElement) {
    if (this._config.html) {
      templateElement.innerHTML = "";
      templateElement.append(element);
      return;
    }
    templateElement.textContent = element.textContent;
  }
};
var NAME$4 = "tooltip";
var DISALLOWED_ATTRIBUTES = /* @__PURE__ */ new Set(["sanitize", "allowList", "sanitizeFn"]);
var CLASS_NAME_FADE$2 = "fade";
var CLASS_NAME_MODAL = "modal";
var CLASS_NAME_SHOW$2 = "show";
var SELECTOR_TOOLTIP_INNER = ".tooltip-inner";
var SELECTOR_MODAL = `.${CLASS_NAME_MODAL}`;
var EVENT_MODAL_HIDE = "hide.bs.modal";
var TRIGGER_HOVER = "hover";
var TRIGGER_FOCUS = "focus";
var TRIGGER_CLICK = "click";
var TRIGGER_MANUAL = "manual";
var EVENT_HIDE$2 = "hide";
var EVENT_HIDDEN$2 = "hidden";
var EVENT_SHOW$2 = "show";
var EVENT_SHOWN$2 = "shown";
var EVENT_INSERTED = "inserted";
var EVENT_CLICK$1 = "click";
var EVENT_FOCUSIN$1 = "focusin";
var EVENT_FOCUSOUT$1 = "focusout";
var EVENT_MOUSEENTER = "mouseenter";
var EVENT_MOUSELEAVE = "mouseleave";
var AttachmentMap = {
  AUTO: "auto",
  TOP: "top",
  RIGHT: isRTL() ? "left" : "right",
  BOTTOM: "bottom",
  LEFT: isRTL() ? "right" : "left"
};
var Default$3 = {
  allowList: DefaultAllowlist,
  animation: true,
  boundary: "clippingParents",
  container: false,
  customClass: "",
  delay: 0,
  fallbackPlacements: ["top", "right", "bottom", "left"],
  html: false,
  offset: [0, 0],
  placement: "top",
  popperConfig: null,
  sanitize: true,
  sanitizeFn: null,
  selector: false,
  template: '<div class="tooltip" role="tooltip"><div class="tooltip-arrow"></div><div class="tooltip-inner"></div></div>',
  title: "",
  trigger: "hover focus"
};
var DefaultType$3 = {
  allowList: "object",
  animation: "boolean",
  boundary: "(string|element)",
  container: "(string|element|boolean)",
  customClass: "(string|function)",
  delay: "(number|object)",
  fallbackPlacements: "array",
  html: "boolean",
  offset: "(array|string|function)",
  placement: "(string|function)",
  popperConfig: "(null|object|function)",
  sanitize: "boolean",
  sanitizeFn: "(null|function)",
  selector: "(string|boolean)",
  template: "string",
  title: "(string|element|function)",
  trigger: "string"
};
var Tooltip = class extends BaseComponent {
  constructor(element, config) {
    if (typeof lib_exports === "undefined") {
      throw new TypeError("Bootstrap's tooltips require Popper (https://popper.js.org)");
    }
    super(element, config);
    this._isEnabled = true;
    this._timeout = 0;
    this._isHovered = null;
    this._activeTrigger = {};
    this._popper = null;
    this._templateFactory = null;
    this._newContent = null;
    this.tip = null;
    this._setListeners();
    if (!this._config.selector) {
      this._fixTitle();
    }
  }
  static get Default() {
    return Default$3;
  }
  static get DefaultType() {
    return DefaultType$3;
  }
  static get NAME() {
    return NAME$4;
  }
  enable() {
    this._isEnabled = true;
  }
  disable() {
    this._isEnabled = false;
  }
  toggleEnabled() {
    this._isEnabled = !this._isEnabled;
  }
  toggle() {
    if (!this._isEnabled) {
      return;
    }
    this._activeTrigger.click = !this._activeTrigger.click;
    if (this._isShown()) {
      this._leave();
      return;
    }
    this._enter();
  }
  dispose() {
    clearTimeout(this._timeout);
    EventHandler.off(this._element.closest(SELECTOR_MODAL), EVENT_MODAL_HIDE, this._hideModalHandler);
    if (this._element.getAttribute("data-bs-original-title")) {
      this._element.setAttribute("title", this._element.getAttribute("data-bs-original-title"));
    }
    this._disposePopper();
    super.dispose();
  }
  show() {
    if (this._element.style.display === "none") {
      throw new Error("Please use show on visible elements");
    }
    if (!(this._isWithContent() && this._isEnabled)) {
      return;
    }
    const showEvent = EventHandler.trigger(this._element, this.constructor.eventName(EVENT_SHOW$2));
    const shadowRoot = findShadowRoot(this._element);
    const isInTheDom = (shadowRoot || this._element.ownerDocument.documentElement).contains(this._element);
    if (showEvent.defaultPrevented || !isInTheDom) {
      return;
    }
    this._disposePopper();
    const tip = this._getTipElement();
    this._element.setAttribute("aria-describedby", tip.getAttribute("id"));
    const {
      container
    } = this._config;
    if (!this._element.ownerDocument.documentElement.contains(this.tip)) {
      container.append(tip);
      EventHandler.trigger(this._element, this.constructor.eventName(EVENT_INSERTED));
    }
    this._popper = this._createPopper(tip);
    tip.classList.add(CLASS_NAME_SHOW$2);
    if ("ontouchstart" in document.documentElement) {
      for (const element of [].concat(...document.body.children)) {
        EventHandler.on(element, "mouseover", noop);
      }
    }
    const complete = () => {
      EventHandler.trigger(this._element, this.constructor.eventName(EVENT_SHOWN$2));
      if (this._isHovered === false) {
        this._leave();
      }
      this._isHovered = false;
    };
    this._queueCallback(complete, this.tip, this._isAnimated());
  }
  hide() {
    if (!this._isShown()) {
      return;
    }
    const hideEvent = EventHandler.trigger(this._element, this.constructor.eventName(EVENT_HIDE$2));
    if (hideEvent.defaultPrevented) {
      return;
    }
    const tip = this._getTipElement();
    tip.classList.remove(CLASS_NAME_SHOW$2);
    if ("ontouchstart" in document.documentElement) {
      for (const element of [].concat(...document.body.children)) {
        EventHandler.off(element, "mouseover", noop);
      }
    }
    this._activeTrigger[TRIGGER_CLICK] = false;
    this._activeTrigger[TRIGGER_FOCUS] = false;
    this._activeTrigger[TRIGGER_HOVER] = false;
    this._isHovered = null;
    const complete = () => {
      if (this._isWithActiveTrigger()) {
        return;
      }
      if (!this._isHovered) {
        this._disposePopper();
      }
      this._element.removeAttribute("aria-describedby");
      EventHandler.trigger(this._element, this.constructor.eventName(EVENT_HIDDEN$2));
    };
    this._queueCallback(complete, this.tip, this._isAnimated());
  }
  update() {
    if (this._popper) {
      this._popper.update();
    }
  }
  _isWithContent() {
    return Boolean(this._getTitle());
  }
  _getTipElement() {
    if (!this.tip) {
      this.tip = this._createTipElement(this._newContent || this._getContentForTemplate());
    }
    return this.tip;
  }
  _createTipElement(content) {
    const tip = this._getTemplateFactory(content).toHtml();
    if (!tip) {
      return null;
    }
    tip.classList.remove(CLASS_NAME_FADE$2, CLASS_NAME_SHOW$2);
    tip.classList.add(`bs-${this.constructor.NAME}-auto`);
    const tipId = getUID(this.constructor.NAME).toString();
    tip.setAttribute("id", tipId);
    if (this._isAnimated()) {
      tip.classList.add(CLASS_NAME_FADE$2);
    }
    return tip;
  }
  setContent(content) {
    this._newContent = content;
    if (this._isShown()) {
      this._disposePopper();
      this.show();
    }
  }
  _getTemplateFactory(content) {
    if (this._templateFactory) {
      this._templateFactory.changeContent(content);
    } else {
      this._templateFactory = new TemplateFactory({
        ...this._config,
        content,
        extraClass: this._resolvePossibleFunction(this._config.customClass)
      });
    }
    return this._templateFactory;
  }
  _getContentForTemplate() {
    return {
      [SELECTOR_TOOLTIP_INNER]: this._getTitle()
    };
  }
  _getTitle() {
    return this._resolvePossibleFunction(this._config.title) || this._element.getAttribute("data-bs-original-title");
  }
  _initializeOnDelegatedTarget(event) {
    return this.constructor.getOrCreateInstance(event.delegateTarget, this._getDelegateConfig());
  }
  _isAnimated() {
    return this._config.animation || this.tip && this.tip.classList.contains(CLASS_NAME_FADE$2);
  }
  _isShown() {
    return this.tip && this.tip.classList.contains(CLASS_NAME_SHOW$2);
  }
  _createPopper(tip) {
    const placement = typeof this._config.placement === "function" ? this._config.placement.call(this, tip, this._element) : this._config.placement;
    const attachment = AttachmentMap[placement.toUpperCase()];
    return createPopper3(this._element, tip, this._getPopperConfig(attachment));
  }
  _getOffset() {
    const {
      offset: offset2
    } = this._config;
    if (typeof offset2 === "string") {
      return offset2.split(",").map((value) => Number.parseInt(value, 10));
    }
    if (typeof offset2 === "function") {
      return (popperData) => offset2(popperData, this._element);
    }
    return offset2;
  }
  _resolvePossibleFunction(arg) {
    return typeof arg === "function" ? arg.call(this._element) : arg;
  }
  _getPopperConfig(attachment) {
    const defaultBsPopperConfig = {
      placement: attachment,
      modifiers: [{
        name: "flip",
        options: {
          fallbackPlacements: this._config.fallbackPlacements
        }
      }, {
        name: "offset",
        options: {
          offset: this._getOffset()
        }
      }, {
        name: "preventOverflow",
        options: {
          boundary: this._config.boundary
        }
      }, {
        name: "arrow",
        options: {
          element: `.${this.constructor.NAME}-arrow`
        }
      }, {
        name: "preSetPlacement",
        enabled: true,
        phase: "beforeMain",
        fn: (data) => {
          this._getTipElement().setAttribute("data-popper-placement", data.state.placement);
        }
      }]
    };
    return {
      ...defaultBsPopperConfig,
      ...typeof this._config.popperConfig === "function" ? this._config.popperConfig(defaultBsPopperConfig) : this._config.popperConfig
    };
  }
  _setListeners() {
    const triggers = this._config.trigger.split(" ");
    for (const trigger of triggers) {
      if (trigger === "click") {
        EventHandler.on(this._element, this.constructor.eventName(EVENT_CLICK$1), this._config.selector, (event) => {
          const context = this._initializeOnDelegatedTarget(event);
          context.toggle();
        });
      } else if (trigger !== TRIGGER_MANUAL) {
        const eventIn = trigger === TRIGGER_HOVER ? this.constructor.eventName(EVENT_MOUSEENTER) : this.constructor.eventName(EVENT_FOCUSIN$1);
        const eventOut = trigger === TRIGGER_HOVER ? this.constructor.eventName(EVENT_MOUSELEAVE) : this.constructor.eventName(EVENT_FOCUSOUT$1);
        EventHandler.on(this._element, eventIn, this._config.selector, (event) => {
          const context = this._initializeOnDelegatedTarget(event);
          context._activeTrigger[event.type === "focusin" ? TRIGGER_FOCUS : TRIGGER_HOVER] = true;
          context._enter();
        });
        EventHandler.on(this._element, eventOut, this._config.selector, (event) => {
          const context = this._initializeOnDelegatedTarget(event);
          context._activeTrigger[event.type === "focusout" ? TRIGGER_FOCUS : TRIGGER_HOVER] = context._element.contains(event.relatedTarget);
          context._leave();
        });
      }
    }
    this._hideModalHandler = () => {
      if (this._element) {
        this.hide();
      }
    };
    EventHandler.on(this._element.closest(SELECTOR_MODAL), EVENT_MODAL_HIDE, this._hideModalHandler);
  }
  _fixTitle() {
    const title = this._element.getAttribute("title");
    if (!title) {
      return;
    }
    if (!this._element.getAttribute("aria-label") && !this._element.textContent.trim()) {
      this._element.setAttribute("aria-label", title);
    }
    this._element.setAttribute("data-bs-original-title", title);
    this._element.removeAttribute("title");
  }
  _enter() {
    if (this._isShown() || this._isHovered) {
      this._isHovered = true;
      return;
    }
    this._isHovered = true;
    this._setTimeout(() => {
      if (this._isHovered) {
        this.show();
      }
    }, this._config.delay.show);
  }
  _leave() {
    if (this._isWithActiveTrigger()) {
      return;
    }
    this._isHovered = false;
    this._setTimeout(() => {
      if (!this._isHovered) {
        this.hide();
      }
    }, this._config.delay.hide);
  }
  _setTimeout(handler, timeout) {
    clearTimeout(this._timeout);
    this._timeout = setTimeout(handler, timeout);
  }
  _isWithActiveTrigger() {
    return Object.values(this._activeTrigger).includes(true);
  }
  _getConfig(config) {
    const dataAttributes = Manipulator.getDataAttributes(this._element);
    for (const dataAttribute of Object.keys(dataAttributes)) {
      if (DISALLOWED_ATTRIBUTES.has(dataAttribute)) {
        delete dataAttributes[dataAttribute];
      }
    }
    config = {
      ...dataAttributes,
      ...typeof config === "object" && config ? config : {}
    };
    config = this._mergeConfigObj(config);
    config = this._configAfterMerge(config);
    this._typeCheckConfig(config);
    return config;
  }
  _configAfterMerge(config) {
    config.container = config.container === false ? document.body : getElement(config.container);
    if (typeof config.delay === "number") {
      config.delay = {
        show: config.delay,
        hide: config.delay
      };
    }
    if (typeof config.title === "number") {
      config.title = config.title.toString();
    }
    if (typeof config.content === "number") {
      config.content = config.content.toString();
    }
    return config;
  }
  _getDelegateConfig() {
    const config = {};
    for (const key in this._config) {
      if (this.constructor.Default[key] !== this._config[key]) {
        config[key] = this._config[key];
      }
    }
    config.selector = false;
    config.trigger = "manual";
    return config;
  }
  _disposePopper() {
    if (this._popper) {
      this._popper.destroy();
      this._popper = null;
    }
    if (this.tip) {
      this.tip.remove();
      this.tip = null;
    }
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Tooltip.getOrCreateInstance(this, config);
      if (typeof config !== "string") {
        return;
      }
      if (typeof data[config] === "undefined") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config]();
    });
  }
};
defineJQueryPlugin(Tooltip);
var NAME$3 = "popover";
var SELECTOR_TITLE = ".popover-header";
var SELECTOR_CONTENT = ".popover-body";
var Default$2 = {
  ...Tooltip.Default,
  content: "",
  offset: [0, 8],
  placement: "right",
  template: '<div class="popover" role="tooltip"><div class="popover-arrow"></div><h3 class="popover-header"></h3><div class="popover-body"></div></div>',
  trigger: "click"
};
var DefaultType$2 = {
  ...Tooltip.DefaultType,
  content: "(null|string|element|function)"
};
var Popover = class extends Tooltip {
  static get Default() {
    return Default$2;
  }
  static get DefaultType() {
    return DefaultType$2;
  }
  static get NAME() {
    return NAME$3;
  }
  _isWithContent() {
    return this._getTitle() || this._getContent();
  }
  _getContentForTemplate() {
    return {
      [SELECTOR_TITLE]: this._getTitle(),
      [SELECTOR_CONTENT]: this._getContent()
    };
  }
  _getContent() {
    return this._resolvePossibleFunction(this._config.content);
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Popover.getOrCreateInstance(this, config);
      if (typeof config !== "string") {
        return;
      }
      if (typeof data[config] === "undefined") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config]();
    });
  }
};
defineJQueryPlugin(Popover);
var NAME$2 = "scrollspy";
var DATA_KEY$2 = "bs.scrollspy";
var EVENT_KEY$2 = `.${DATA_KEY$2}`;
var DATA_API_KEY = ".data-api";
var EVENT_ACTIVATE = `activate${EVENT_KEY$2}`;
var EVENT_CLICK = `click${EVENT_KEY$2}`;
var EVENT_LOAD_DATA_API$1 = `load${EVENT_KEY$2}${DATA_API_KEY}`;
var CLASS_NAME_DROPDOWN_ITEM = "dropdown-item";
var CLASS_NAME_ACTIVE$1 = "active";
var SELECTOR_DATA_SPY = '[data-bs-spy="scroll"]';
var SELECTOR_TARGET_LINKS = "[href]";
var SELECTOR_NAV_LIST_GROUP = ".nav, .list-group";
var SELECTOR_NAV_LINKS = ".nav-link";
var SELECTOR_NAV_ITEMS = ".nav-item";
var SELECTOR_LIST_ITEMS = ".list-group-item";
var SELECTOR_LINK_ITEMS = `${SELECTOR_NAV_LINKS}, ${SELECTOR_NAV_ITEMS} > ${SELECTOR_NAV_LINKS}, ${SELECTOR_LIST_ITEMS}`;
var SELECTOR_DROPDOWN = ".dropdown";
var SELECTOR_DROPDOWN_TOGGLE$1 = ".dropdown-toggle";
var Default$1 = {
  offset: null,
  rootMargin: "0px 0px -25%",
  smoothScroll: false,
  target: null,
  threshold: [0.1, 0.5, 1]
};
var DefaultType$1 = {
  offset: "(number|null)",
  rootMargin: "string",
  smoothScroll: "boolean",
  target: "element",
  threshold: "array"
};
var ScrollSpy = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._targetLinks = /* @__PURE__ */ new Map();
    this._observableSections = /* @__PURE__ */ new Map();
    this._rootElement = getComputedStyle(this._element).overflowY === "visible" ? null : this._element;
    this._activeTarget = null;
    this._observer = null;
    this._previousScrollData = {
      visibleEntryTop: 0,
      parentScrollTop: 0
    };
    this.refresh();
  }
  static get Default() {
    return Default$1;
  }
  static get DefaultType() {
    return DefaultType$1;
  }
  static get NAME() {
    return NAME$2;
  }
  refresh() {
    this._initializeTargetsAndObservables();
    this._maybeEnableSmoothScroll();
    if (this._observer) {
      this._observer.disconnect();
    } else {
      this._observer = this._getNewObserver();
    }
    for (const section of this._observableSections.values()) {
      this._observer.observe(section);
    }
  }
  dispose() {
    this._observer.disconnect();
    super.dispose();
  }
  _configAfterMerge(config) {
    config.target = getElement(config.target) || document.body;
    config.rootMargin = config.offset ? `${config.offset}px 0px -30%` : config.rootMargin;
    if (typeof config.threshold === "string") {
      config.threshold = config.threshold.split(",").map((value) => Number.parseFloat(value));
    }
    return config;
  }
  _maybeEnableSmoothScroll() {
    if (!this._config.smoothScroll) {
      return;
    }
    EventHandler.off(this._config.target, EVENT_CLICK);
    EventHandler.on(this._config.target, EVENT_CLICK, SELECTOR_TARGET_LINKS, (event) => {
      const observableSection = this._observableSections.get(event.target.hash);
      if (observableSection) {
        event.preventDefault();
        const root = this._rootElement || window;
        const height = observableSection.offsetTop - this._element.offsetTop;
        if (root.scrollTo) {
          root.scrollTo({
            top: height,
            behavior: "smooth"
          });
          return;
        }
        root.scrollTop = height;
      }
    });
  }
  _getNewObserver() {
    const options = {
      root: this._rootElement,
      threshold: this._config.threshold,
      rootMargin: this._config.rootMargin
    };
    return new IntersectionObserver((entries) => this._observerCallback(entries), options);
  }
  _observerCallback(entries) {
    const targetElement = (entry) => this._targetLinks.get(`#${entry.target.id}`);
    const activate = (entry) => {
      this._previousScrollData.visibleEntryTop = entry.target.offsetTop;
      this._process(targetElement(entry));
    };
    const parentScrollTop = (this._rootElement || document.documentElement).scrollTop;
    const userScrollsDown = parentScrollTop >= this._previousScrollData.parentScrollTop;
    this._previousScrollData.parentScrollTop = parentScrollTop;
    for (const entry of entries) {
      if (!entry.isIntersecting) {
        this._activeTarget = null;
        this._clearActiveClass(targetElement(entry));
        continue;
      }
      const entryIsLowerThanPrevious = entry.target.offsetTop >= this._previousScrollData.visibleEntryTop;
      if (userScrollsDown && entryIsLowerThanPrevious) {
        activate(entry);
        if (!parentScrollTop) {
          return;
        }
        continue;
      }
      if (!userScrollsDown && !entryIsLowerThanPrevious) {
        activate(entry);
      }
    }
  }
  _initializeTargetsAndObservables() {
    this._targetLinks = /* @__PURE__ */ new Map();
    this._observableSections = /* @__PURE__ */ new Map();
    const targetLinks = SelectorEngine.find(SELECTOR_TARGET_LINKS, this._config.target);
    for (const anchor of targetLinks) {
      if (!anchor.hash || isDisabled(anchor)) {
        continue;
      }
      const observableSection = SelectorEngine.findOne(anchor.hash, this._element);
      if (isVisible(observableSection)) {
        this._targetLinks.set(anchor.hash, anchor);
        this._observableSections.set(anchor.hash, observableSection);
      }
    }
  }
  _process(target) {
    if (this._activeTarget === target) {
      return;
    }
    this._clearActiveClass(this._config.target);
    this._activeTarget = target;
    target.classList.add(CLASS_NAME_ACTIVE$1);
    this._activateParents(target);
    EventHandler.trigger(this._element, EVENT_ACTIVATE, {
      relatedTarget: target
    });
  }
  _activateParents(target) {
    if (target.classList.contains(CLASS_NAME_DROPDOWN_ITEM)) {
      SelectorEngine.findOne(SELECTOR_DROPDOWN_TOGGLE$1, target.closest(SELECTOR_DROPDOWN)).classList.add(CLASS_NAME_ACTIVE$1);
      return;
    }
    for (const listGroup of SelectorEngine.parents(target, SELECTOR_NAV_LIST_GROUP)) {
      for (const item of SelectorEngine.prev(listGroup, SELECTOR_LINK_ITEMS)) {
        item.classList.add(CLASS_NAME_ACTIVE$1);
      }
    }
  }
  _clearActiveClass(parent) {
    parent.classList.remove(CLASS_NAME_ACTIVE$1);
    const activeNodes = SelectorEngine.find(`${SELECTOR_TARGET_LINKS}.${CLASS_NAME_ACTIVE$1}`, parent);
    for (const node of activeNodes) {
      node.classList.remove(CLASS_NAME_ACTIVE$1);
    }
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = ScrollSpy.getOrCreateInstance(this, config);
      if (typeof config !== "string") {
        return;
      }
      if (data[config] === void 0 || config.startsWith("_") || config === "constructor") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config]();
    });
  }
};
EventHandler.on(window, EVENT_LOAD_DATA_API$1, () => {
  for (const spy of SelectorEngine.find(SELECTOR_DATA_SPY)) {
    ScrollSpy.getOrCreateInstance(spy);
  }
});
defineJQueryPlugin(ScrollSpy);
var NAME$1 = "tab";
var DATA_KEY$1 = "bs.tab";
var EVENT_KEY$1 = `.${DATA_KEY$1}`;
var EVENT_HIDE$1 = `hide${EVENT_KEY$1}`;
var EVENT_HIDDEN$1 = `hidden${EVENT_KEY$1}`;
var EVENT_SHOW$1 = `show${EVENT_KEY$1}`;
var EVENT_SHOWN$1 = `shown${EVENT_KEY$1}`;
var EVENT_CLICK_DATA_API = `click${EVENT_KEY$1}`;
var EVENT_KEYDOWN = `keydown${EVENT_KEY$1}`;
var EVENT_LOAD_DATA_API = `load${EVENT_KEY$1}`;
var ARROW_LEFT_KEY = "ArrowLeft";
var ARROW_RIGHT_KEY = "ArrowRight";
var ARROW_UP_KEY = "ArrowUp";
var ARROW_DOWN_KEY = "ArrowDown";
var CLASS_NAME_ACTIVE = "active";
var CLASS_NAME_FADE$1 = "fade";
var CLASS_NAME_SHOW$1 = "show";
var CLASS_DROPDOWN = "dropdown";
var SELECTOR_DROPDOWN_TOGGLE = ".dropdown-toggle";
var SELECTOR_DROPDOWN_MENU = ".dropdown-menu";
var NOT_SELECTOR_DROPDOWN_TOGGLE = ":not(.dropdown-toggle)";
var SELECTOR_TAB_PANEL = '.list-group, .nav, [role="tablist"]';
var SELECTOR_OUTER = ".nav-item, .list-group-item";
var SELECTOR_INNER = `.nav-link${NOT_SELECTOR_DROPDOWN_TOGGLE}, .list-group-item${NOT_SELECTOR_DROPDOWN_TOGGLE}, [role="tab"]${NOT_SELECTOR_DROPDOWN_TOGGLE}`;
var SELECTOR_DATA_TOGGLE = '[data-bs-toggle="tab"], [data-bs-toggle="pill"], [data-bs-toggle="list"]';
var SELECTOR_INNER_ELEM = `${SELECTOR_INNER}, ${SELECTOR_DATA_TOGGLE}`;
var SELECTOR_DATA_TOGGLE_ACTIVE = `.${CLASS_NAME_ACTIVE}[data-bs-toggle="tab"], .${CLASS_NAME_ACTIVE}[data-bs-toggle="pill"], .${CLASS_NAME_ACTIVE}[data-bs-toggle="list"]`;
var Tab = class extends BaseComponent {
  constructor(element) {
    super(element);
    this._parent = this._element.closest(SELECTOR_TAB_PANEL);
    if (!this._parent) {
      return;
    }
    this._setInitialAttributes(this._parent, this._getChildren());
    EventHandler.on(this._element, EVENT_KEYDOWN, (event) => this._keydown(event));
  }
  static get NAME() {
    return NAME$1;
  }
  show() {
    const innerElem = this._element;
    if (this._elemIsActive(innerElem)) {
      return;
    }
    const active = this._getActiveElem();
    const hideEvent = active ? EventHandler.trigger(active, EVENT_HIDE$1, {
      relatedTarget: innerElem
    }) : null;
    const showEvent = EventHandler.trigger(innerElem, EVENT_SHOW$1, {
      relatedTarget: active
    });
    if (showEvent.defaultPrevented || hideEvent && hideEvent.defaultPrevented) {
      return;
    }
    this._deactivate(active, innerElem);
    this._activate(innerElem, active);
  }
  _activate(element, relatedElem) {
    if (!element) {
      return;
    }
    element.classList.add(CLASS_NAME_ACTIVE);
    this._activate(getElementFromSelector(element));
    const complete = () => {
      if (element.getAttribute("role") !== "tab") {
        element.classList.add(CLASS_NAME_SHOW$1);
        return;
      }
      element.removeAttribute("tabindex");
      element.setAttribute("aria-selected", true);
      this._toggleDropDown(element, true);
      EventHandler.trigger(element, EVENT_SHOWN$1, {
        relatedTarget: relatedElem
      });
    };
    this._queueCallback(complete, element, element.classList.contains(CLASS_NAME_FADE$1));
  }
  _deactivate(element, relatedElem) {
    if (!element) {
      return;
    }
    element.classList.remove(CLASS_NAME_ACTIVE);
    element.blur();
    this._deactivate(getElementFromSelector(element));
    const complete = () => {
      if (element.getAttribute("role") !== "tab") {
        element.classList.remove(CLASS_NAME_SHOW$1);
        return;
      }
      element.setAttribute("aria-selected", false);
      element.setAttribute("tabindex", "-1");
      this._toggleDropDown(element, false);
      EventHandler.trigger(element, EVENT_HIDDEN$1, {
        relatedTarget: relatedElem
      });
    };
    this._queueCallback(complete, element, element.classList.contains(CLASS_NAME_FADE$1));
  }
  _keydown(event) {
    if (![ARROW_LEFT_KEY, ARROW_RIGHT_KEY, ARROW_UP_KEY, ARROW_DOWN_KEY].includes(event.key)) {
      return;
    }
    event.stopPropagation();
    event.preventDefault();
    const isNext = [ARROW_RIGHT_KEY, ARROW_DOWN_KEY].includes(event.key);
    const nextActiveElement = getNextActiveElement(this._getChildren().filter((element) => !isDisabled(element)), event.target, isNext, true);
    if (nextActiveElement) {
      nextActiveElement.focus({
        preventScroll: true
      });
      Tab.getOrCreateInstance(nextActiveElement).show();
    }
  }
  _getChildren() {
    return SelectorEngine.find(SELECTOR_INNER_ELEM, this._parent);
  }
  _getActiveElem() {
    return this._getChildren().find((child) => this._elemIsActive(child)) || null;
  }
  _setInitialAttributes(parent, children) {
    this._setAttributeIfNotExists(parent, "role", "tablist");
    for (const child of children) {
      this._setInitialAttributesOnChild(child);
    }
  }
  _setInitialAttributesOnChild(child) {
    child = this._getInnerElement(child);
    const isActive = this._elemIsActive(child);
    const outerElem = this._getOuterElement(child);
    child.setAttribute("aria-selected", isActive);
    if (outerElem !== child) {
      this._setAttributeIfNotExists(outerElem, "role", "presentation");
    }
    if (!isActive) {
      child.setAttribute("tabindex", "-1");
    }
    this._setAttributeIfNotExists(child, "role", "tab");
    this._setInitialAttributesOnTargetPanel(child);
  }
  _setInitialAttributesOnTargetPanel(child) {
    const target = getElementFromSelector(child);
    if (!target) {
      return;
    }
    this._setAttributeIfNotExists(target, "role", "tabpanel");
    if (child.id) {
      this._setAttributeIfNotExists(target, "aria-labelledby", `#${child.id}`);
    }
  }
  _toggleDropDown(element, open) {
    const outerElem = this._getOuterElement(element);
    if (!outerElem.classList.contains(CLASS_DROPDOWN)) {
      return;
    }
    const toggle = (selector, className) => {
      const element2 = SelectorEngine.findOne(selector, outerElem);
      if (element2) {
        element2.classList.toggle(className, open);
      }
    };
    toggle(SELECTOR_DROPDOWN_TOGGLE, CLASS_NAME_ACTIVE);
    toggle(SELECTOR_DROPDOWN_MENU, CLASS_NAME_SHOW$1);
    outerElem.setAttribute("aria-expanded", open);
  }
  _setAttributeIfNotExists(element, attribute, value) {
    if (!element.hasAttribute(attribute)) {
      element.setAttribute(attribute, value);
    }
  }
  _elemIsActive(elem) {
    return elem.classList.contains(CLASS_NAME_ACTIVE);
  }
  _getInnerElement(elem) {
    return elem.matches(SELECTOR_INNER_ELEM) ? elem : SelectorEngine.findOne(SELECTOR_INNER_ELEM, elem);
  }
  _getOuterElement(elem) {
    return elem.closest(SELECTOR_OUTER) || elem;
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Tab.getOrCreateInstance(this);
      if (typeof config !== "string") {
        return;
      }
      if (data[config] === void 0 || config.startsWith("_") || config === "constructor") {
        throw new TypeError(`No method named "${config}"`);
      }
      data[config]();
    });
  }
};
EventHandler.on(document, EVENT_CLICK_DATA_API, SELECTOR_DATA_TOGGLE, function(event) {
  if (["A", "AREA"].includes(this.tagName)) {
    event.preventDefault();
  }
  if (isDisabled(this)) {
    return;
  }
  Tab.getOrCreateInstance(this).show();
});
EventHandler.on(window, EVENT_LOAD_DATA_API, () => {
  for (const element of SelectorEngine.find(SELECTOR_DATA_TOGGLE_ACTIVE)) {
    Tab.getOrCreateInstance(element);
  }
});
defineJQueryPlugin(Tab);
var NAME = "toast";
var DATA_KEY = "bs.toast";
var EVENT_KEY = `.${DATA_KEY}`;
var EVENT_MOUSEOVER = `mouseover${EVENT_KEY}`;
var EVENT_MOUSEOUT = `mouseout${EVENT_KEY}`;
var EVENT_FOCUSIN = `focusin${EVENT_KEY}`;
var EVENT_FOCUSOUT = `focusout${EVENT_KEY}`;
var EVENT_HIDE = `hide${EVENT_KEY}`;
var EVENT_HIDDEN = `hidden${EVENT_KEY}`;
var EVENT_SHOW = `show${EVENT_KEY}`;
var EVENT_SHOWN = `shown${EVENT_KEY}`;
var CLASS_NAME_FADE = "fade";
var CLASS_NAME_HIDE = "hide";
var CLASS_NAME_SHOW = "show";
var CLASS_NAME_SHOWING = "showing";
var DefaultType = {
  animation: "boolean",
  autohide: "boolean",
  delay: "number"
};
var Default = {
  animation: true,
  autohide: true,
  delay: 5e3
};
var Toast = class extends BaseComponent {
  constructor(element, config) {
    super(element, config);
    this._timeout = null;
    this._hasMouseInteraction = false;
    this._hasKeyboardInteraction = false;
    this._setListeners();
  }
  static get Default() {
    return Default;
  }
  static get DefaultType() {
    return DefaultType;
  }
  static get NAME() {
    return NAME;
  }
  show() {
    const showEvent = EventHandler.trigger(this._element, EVENT_SHOW);
    if (showEvent.defaultPrevented) {
      return;
    }
    this._clearTimeout();
    if (this._config.animation) {
      this._element.classList.add(CLASS_NAME_FADE);
    }
    const complete = () => {
      this._element.classList.remove(CLASS_NAME_SHOWING);
      EventHandler.trigger(this._element, EVENT_SHOWN);
      this._maybeScheduleHide();
    };
    this._element.classList.remove(CLASS_NAME_HIDE);
    reflow(this._element);
    this._element.classList.add(CLASS_NAME_SHOW, CLASS_NAME_SHOWING);
    this._queueCallback(complete, this._element, this._config.animation);
  }
  hide() {
    if (!this.isShown()) {
      return;
    }
    const hideEvent = EventHandler.trigger(this._element, EVENT_HIDE);
    if (hideEvent.defaultPrevented) {
      return;
    }
    const complete = () => {
      this._element.classList.add(CLASS_NAME_HIDE);
      this._element.classList.remove(CLASS_NAME_SHOWING, CLASS_NAME_SHOW);
      EventHandler.trigger(this._element, EVENT_HIDDEN);
    };
    this._element.classList.add(CLASS_NAME_SHOWING);
    this._queueCallback(complete, this._element, this._config.animation);
  }
  dispose() {
    this._clearTimeout();
    if (this.isShown()) {
      this._element.classList.remove(CLASS_NAME_SHOW);
    }
    super.dispose();
  }
  isShown() {
    return this._element.classList.contains(CLASS_NAME_SHOW);
  }
  _maybeScheduleHide() {
    if (!this._config.autohide) {
      return;
    }
    if (this._hasMouseInteraction || this._hasKeyboardInteraction) {
      return;
    }
    this._timeout = setTimeout(() => {
      this.hide();
    }, this._config.delay);
  }
  _onInteraction(event, isInteracting) {
    switch (event.type) {
      case "mouseover":
      case "mouseout": {
        this._hasMouseInteraction = isInteracting;
        break;
      }
      case "focusin":
      case "focusout": {
        this._hasKeyboardInteraction = isInteracting;
        break;
      }
    }
    if (isInteracting) {
      this._clearTimeout();
      return;
    }
    const nextElement = event.relatedTarget;
    if (this._element === nextElement || this._element.contains(nextElement)) {
      return;
    }
    this._maybeScheduleHide();
  }
  _setListeners() {
    EventHandler.on(this._element, EVENT_MOUSEOVER, (event) => this._onInteraction(event, true));
    EventHandler.on(this._element, EVENT_MOUSEOUT, (event) => this._onInteraction(event, false));
    EventHandler.on(this._element, EVENT_FOCUSIN, (event) => this._onInteraction(event, true));
    EventHandler.on(this._element, EVENT_FOCUSOUT, (event) => this._onInteraction(event, false));
  }
  _clearTimeout() {
    clearTimeout(this._timeout);
    this._timeout = null;
  }
  static jQueryInterface(config) {
    return this.each(function() {
      const data = Toast.getOrCreateInstance(this, config);
      if (typeof config === "string") {
        if (typeof data[config] === "undefined") {
          throw new TypeError(`No method named "${config}"`);
        }
        data[config](this);
      }
    });
  }
};
enableDismissTrigger(Toast);
defineJQueryPlugin(Toast);

// node_modules/bootstrap-vue-3/dist/bootstrap-vue-3.es.js
var Dl = Object.defineProperty;
var Hl = (e, t, a) => t in e ? Dl(e, t, { enumerable: true, configurable: true, writable: true, value: a }) : e[t] = a;
var me = (e, t, a) => (Hl(e, typeof t != "symbol" ? t + "" : t, a), a);
var sn = (e, t) => {
  var l;
  const a = shallowRef();
  return watchEffect(
    () => {
      a.value = e();
    },
    {
      ...t,
      flush: (l = t == null ? void 0 : t.flush) != null ? l : "sync"
    }
  ), readonly(a);
};
var ot = (e) => computed(() => e.value ? `justify-content-${e.value}` : "");
var ht = (e, t) => Object.keys(e).filter((a) => !t.includes(a)).reduce((a, l) => ({ ...a, [l]: e[l] }), {});
var je = () => ({ enumerable: true, configurable: false, writable: false });
var mt = (e) => Array.isArray(e) ? e.map((t) => mt(t)) : e instanceof Date ? new Date(e.getTime()) : e && typeof e == "object" ? Object.getOwnPropertyNames(e).reduce((t, a) => {
  var l;
  return Object.defineProperty(t, a, (l = Object.getOwnPropertyDescriptor(e, a)) != null ? l : {}), t[a] = mt(e[a]), t;
}, Object.create(Object.getPrototypeOf(e))) : e;
var Ot = (e) => new Promise((t) => t(mt(e)));
var yt = class {
  constructor(t, a = {}) {
    me(this, "cancelable", true);
    me(this, "componentId", null);
    me(this, "defaultPrevented", false);
    me(this, "nativeEvent", null);
    me(this, "preventDefault");
    me(this, "relatedTarget", null);
    me(this, "target", null);
    me(this, "eventType", "");
    me(this, "vueTarget", null);
    if (!t)
      throw new TypeError(
        `Failed to construct '${this.constructor.name}'. 1 argument required, ${arguments.length} given.`
      );
    Object.assign(this, yt.Defaults, this.constructor.Defaults, a, { eventType: t }), Object.defineProperties(this, {
      type: je(),
      cancelable: je(),
      nativeEvent: je(),
      target: je(),
      relatedTarget: je(),
      vueTarget: je(),
      componentId: je()
    });
    let l = false;
    this.preventDefault = function() {
      this.cancelable && (l = true);
    }, Object.defineProperty(this, "defaultPrevented", {
      enumerable: true,
      get() {
        return l;
      }
    });
  }
  static get Defaults() {
    return {
      eventType: "",
      cancelable: true,
      nativeEvent: null,
      target: null,
      relatedTarget: null,
      vueTarget: null,
      componentId: null
    };
  }
};
var Lt = (e) => e !== null && typeof e == "object";
var Oa = (e) => /^[0-9]*\.?[0-9]+$/.test(String(e));
var rn = (e) => Object.prototype.toString.call(e) === "[object Object]";
var Ne = (e) => e === null;
var La = /_/g;
var za = /([a-z])([A-Z])/g;
var un = /(\s|^)(\w)/g;
var dn = /(\s|^)(\w)/;
var dt = /\s+/;
var cn = /^#/;
var fn2 = /^#[A-Za-z]+[\w\-:.]*$/;
var vn = /-u-.+/;
var bt = (e, t = 2) => typeof e == "string" ? e : e == null ? "" : Array.isArray(e) || rn(e) && e.toString === Object.prototype.toString ? JSON.stringify(e, null, t) : String(e);
var aa = (e) => e.replace(La, " ").replace(za, (t, a, l) => `${a} ${l}`).replace(dn, (t, a, l) => a + l.toUpperCase());
var la = (e) => e.replace(La, " ").replace(za, (t, a, l) => `${a} ${l}`).replace(un, (t, a, l) => a + l.toUpperCase());
var mn = (e) => {
  const t = e.trim();
  return t.charAt(0).toUpperCase() + t.slice(1);
};
var xt = (e) => `\\${e}`;
var bn = (e) => {
  const t = bt(e), { length: a } = t, l = t.charCodeAt(0);
  return t.split("").reduce((n, u, f) => {
    const c = t.charCodeAt(f);
    return c === 0 ? `${n}\uFFFD` : c === 127 || c >= 1 && c <= 31 || f === 0 && c >= 48 && c <= 57 || f === 1 && c >= 48 && c <= 57 && l === 45 ? n + xt(`${c.toString(16)} `) : f === 0 && c === 45 && a === 1 ? n + xt(u) : c >= 128 || c === 45 || c === 95 || c >= 48 && c <= 57 || c >= 65 && c <= 90 || c >= 97 && c <= 122 ? n + u : n + xt(u);
  }, "");
};
var Kt = typeof window < "u";
var gn = typeof document < "u";
var pn = typeof navigator < "u";
var Na = Kt && gn && pn;
var na = Kt ? window : {};
var hn = (() => {
  let e = false;
  if (Na)
    try {
      const t = {
        get passive() {
          e = true;
        }
      };
      na.addEventListener("test", t, t), na.removeEventListener("test", t, t);
    } catch {
      e = false;
    }
  return e;
})();
var Ea = typeof window < "u";
var Da = typeof document < "u";
var yn = typeof Element < "u";
var Ha = typeof navigator < "u";
var Bt = Ea && Da && Ha;
var Ge = Ea ? window : {};
var $t = Da ? document : {};
var Ra = Ha ? navigator : {};
var qa = (Ra.userAgent || "").toLowerCase();
qa.indexOf("jsdom") > 0;
/msie|trident/.test(qa);
(() => {
  let e = false;
  if (Bt)
    try {
      const t = {
        get passive() {
          return e = true, e;
        }
      };
      Ge.addEventListener("test", t, t), Ge.removeEventListener("test", t, t);
    } catch {
      e = false;
    }
  return e;
})();
Bt && ("ontouchstart" in $t.documentElement || Ra.maxTouchPoints > 0);
Bt && Boolean(Ge.PointerEvent || Ge.MSPointerEvent);
Bt && "IntersectionObserver" in Ge && "IntersectionObserverEntry" in Ge && "intersectionRatio" in Ge.IntersectionObserverEntry.prototype;
var xe = yn ? Element.prototype : void 0;
var Bn = (xe == null ? void 0 : xe.matches) || (xe == null ? void 0 : xe.msMatchesSelector) || (xe == null ? void 0 : xe.webkitMatchesSelector);
var ze = (e) => !!(e && e.nodeType === Node.ELEMENT_NODE);
var $n = (e) => ze(e) ? e.getBoundingClientRect() : null;
var kn = (e = []) => {
  const { activeElement: t } = document;
  return t && !e.some((a) => a === t) ? t : null;
};
var Cn = (e) => ze(e) && e === kn();
var Sn = (e, t = {}) => {
  try {
    e.focus(t);
  } catch (a) {
    console.error(a);
  }
  return Cn(e);
};
var wn = (e, t) => t && ze(e) && e.getAttribute(t) || null;
var Tn = (e) => {
  if (wn(e, "display") === "none")
    return false;
  const t = $n(e);
  return !!(t && t.height > 0 && t.width > 0);
};
var Ve = (e, t) => !e || e(t).filter((a) => a.type !== Comment).length < 1;
var Ma = (e, t) => (ze(t) ? t : $t).querySelector(e) || null;
var _n = (e, t) => Array.from([(ze(t) ? t : $t).querySelectorAll(e)]);
var Xt = (e, t) => t && ze(e) ? e.getAttribute(t) : null;
var Vn = (e) => $t.getElementById(/^#/.test(e) ? e.slice(1) : e) || null;
var An = (e, t, a) => {
  t && ze(e) && e.setAttribute(t, a);
};
var xn = (e, t) => {
  t && ze(e) && e.removeAttribute(t);
};
var In = (e, t) => bt(e).toLowerCase() === bt(t).toLowerCase();
var it = Kt ? window.requestAnimationFrame || window.webkitRequestAnimationFrame || window.mozRequestAnimationFrame || window.msRequestAnimationFrame || window.oRequestAnimationFrame || ((e) => setTimeout(e, 16)) : (e) => setTimeout(e, 0);
var ja = (e, t) => ze(e) ? Bn.call(e, t) : false;
var Fn = (xe == null ? void 0 : xe.closest) || function(e) {
  let t = this;
  if (!t)
    return null;
  do {
    if (ja(t, e))
      return t;
    t = t.parentElement || t.parentNode;
  } while (t !== null && t.nodeType === Node.ELEMENT_NODE);
  return null;
};
var oa = (e, t, a = false) => {
  if (!ze(t))
    return null;
  const l = Fn.call(t, e);
  return a ? l : l === t ? null : l;
};
var kt = (e, t, a) => t.concat(["sm", "md", "lg", "xl", "xxl"]).reduce((l, n) => (l[e ? `${e}${n.charAt(0).toUpperCase() + n.slice(1)}` : n] = a, l), /* @__PURE__ */ Object.create(null));
var Ga = (e, t, a, l = a) => Object.keys(t).reduce((n, u) => (e[u] && n.push(
  [l, u.replace(a, ""), e[u]].filter((f) => f && typeof f != "boolean").join("-").toLowerCase()
), n), []);
var Ee = (e = "") => `__BVID__${Math.random().toString().slice(2, 8)}___BV_${e}__`;
var Ct = (e, t) => e === true || e === "true" || e === "" ? "true" : e === "grammar" || e === "spelling" ? e : t === false ? "true" : e === false || e === "false" ? "false" : void 0;
var It = (e) => !!e && typeof e == "object" && e.constructor === Object;
var zt = (e, t, a = true) => {
  const l = e instanceof Date && typeof e.getMonth == "function" ? new Date(e.getTime()) : Object.assign({}, e);
  return It(e) && It(t) && Object.keys(t).forEach((n) => {
    It(t[n]) ? n in e ? l[n] = zt(e[n], t[n], a) : Object.assign(l, { [n]: t[n] }) : Array.isArray(t[n]) && Array.isArray(e[n]) ? Object.assign(l, {
      [n]: a ? e[n].concat(
        t[n].filter((u) => !e[n].includes(u))
      ) : t[n]
    }) : Object.assign(l, { [n]: t[n] });
  }), l;
};
var Fe = (e, t = {}, a = {}) => {
  const l = [e];
  let n;
  for (let u = 0; u < l.length && !n; u++) {
    const f = l[u];
    n = a[f];
  }
  return n && typeof n == "function" ? n(t) : n;
};
var Le = (e, t = NaN) => Number.isInteger(e) ? e : t;
var Qe = (e, t = NaN) => {
  const a = Number.parseInt(e, 10);
  return Number.isNaN(a) ? t : a;
};
var tt = (e, t = NaN) => {
  const a = Number.parseFloat(e.toString());
  return Number.isNaN(a) ? t : a;
};
var sa = (e, t) => t + (e ? mn(e) : "");
var Jt = (e, t) => (Array.isArray(t) ? t.slice() : Object.keys(t)).reduce(
  (a, l) => (a[l] = e[l], a),
  {}
);
var Pn = (e) => typeof e == "boolean" ? e : e === "" ? true : e === "true";
var at = (e) => !!(e.href || e.to);
function r(e) {
  return sn(() => e.value === void 0 ? void 0 : Pn(e.value));
}
var Ua = Symbol();
var Wa = {
  items: reactive([]),
  reset() {
    this.items = reactive([]);
  }
};
var On = (e) => {
  e.provide(Ua, Wa);
};
var Ln = () => {
  const e = inject(Ua);
  return e || Wa;
};
var ye = (e, t, a) => {
  onMounted(() => {
    var l;
    (l = e == null ? void 0 : e.value) == null || l.addEventListener(t, a);
  }), onBeforeUnmount(() => {
    var l;
    (l = e == null ? void 0 : e.value) == null || l.removeEventListener(t, a);
  });
};
var Ka = (e) => computed(() => ({
  "form-check": e.plain === false && e.button === false,
  "form-check-inline": e.inline === true,
  "form-switch": e.switch === true,
  [`form-control-${e.size}`]: e.size !== void 0 && e.size !== "md"
}));
var Xa = (e) => computed(() => ({
  "form-check-input": e.plain === false && e.button === false,
  "is-valid": e.state === true,
  "is-invalid": e.state === false,
  "btn-check": e.button === true
}));
var Ja = (e) => computed(() => ({
  "form-check-label": e.plain === false && e.button === false,
  btn: e.button === true,
  [`btn-${e.buttonVariant}`]: e.button === true && e.buttonVariant !== void 0,
  [`btn-${e.size}`]: e.button && e.size && e.size !== "md"
}));
var Ya = (e) => computed(() => ({
  "aria-invalid": Ct(e.ariaInvalid, e.state),
  "aria-required": e.required === true ? true : void 0
}));
var Za = (e) => computed(() => ({
  "was-validated": e.validated === true,
  "btn-group": e.buttons === true && e.stacked === false,
  "btn-group-vertical": e.stacked === true,
  [`btn-group-${e.size}`]: e.size !== void 0
}));
var gt = (e, t, a) => e.reduce(
  (l, n) => n.type.toString() === "Symbol(Fragment)" ? l.concat(n.children) : l.concat([n]),
  []
).filter((l) => (l.type.__name || l.type.name) === t).map((l) => {
  const n = (l.children.default ? l.children.default() : []).find(
    (u) => u.type.toString() === "Symbol(Text)"
  );
  return {
    props: {
      disabled: a,
      ...l.props
    },
    text: n ? n.children : ""
  };
});
var Qa = (e, t) => typeof e == "string" ? {
  props: {
    value: e,
    disabled: t.disabled
  },
  text: e
} : {
  props: {
    value: e[t.valueField],
    disabled: t.disabled || e[t.disabledField],
    ...e.props
  },
  text: e[t.textField],
  html: e[t.htmlField]
};
var el = (e, t, a, l, n) => ({
  ...e,
  props: {
    "button-variant": a.buttonVariant,
    form: a.form,
    name: l.value,
    id: `${n.value}_option_${t}`,
    button: a.buttons,
    state: a.state,
    plain: a.plain,
    size: a.size,
    inline: !a.stacked,
    required: a.required,
    ...e.props
  }
});
var $e = (e, t) => computed(() => (e == null ? void 0 : e.value) || Ee(t));
var tl = {
  ariaInvalid: {
    type: [Boolean, String],
    default: void 0
  },
  autocomplete: { type: String, required: false },
  autofocus: { type: Boolean, default: false },
  disabled: { type: Boolean, default: false },
  form: { type: String, required: false },
  formatter: { type: Function, required: false },
  id: { type: String, required: false },
  lazy: { type: Boolean, default: false },
  lazyFormatter: { type: Boolean, default: false },
  list: { type: String, required: false },
  modelValue: { type: [String, Number], default: "" },
  name: { type: String, required: false },
  number: { type: Boolean, default: false },
  placeholder: { type: String, required: false },
  plaintext: { type: Boolean, default: false },
  readonly: { type: Boolean, default: false },
  required: { type: Boolean, default: false },
  size: { type: String, required: false },
  state: { type: Boolean, default: null },
  trim: { type: Boolean, default: false }
};
var al = (e, t) => {
  const a = ref();
  let l = null, n = true;
  const u = $e(toRef(e, "id"), "input"), f = (b, k, y = false) => (b = String(b), typeof e.formatter == "function" && (!e.lazyFormatter || y) ? (n = false, e.formatter(b, k)) : b), c = (b) => e.trim ? b.trim() : e.number ? Number.parseFloat(b) : b, v = () => {
    nextTick(() => {
      var b;
      e.autofocus && ((b = a.value) == null || b.focus());
    });
  };
  onMounted(v), onMounted(() => {
    a.value && (a.value.value = e.modelValue);
  }), onActivated(v);
  const p = computed(
    () => {
      var b;
      return Ct(e.ariaInvalid, (b = e.state) != null ? b : void 0);
    }
  ), $ = (b) => {
    const { value: k } = b.target, y = f(k, b);
    if (y === false || b.defaultPrevented) {
      b.preventDefault();
      return;
    }
    if (e.lazy)
      return;
    const C = c(y);
    e.modelValue !== C && (l = k, t("update:modelValue", C)), t("input", y);
  }, m = (b) => {
    const { value: k } = b.target, y = f(k, b);
    if (y === false || b.defaultPrevented) {
      b.preventDefault();
      return;
    }
    if (!e.lazy)
      return;
    l = k, t("update:modelValue", y);
    const C = c(y);
    e.modelValue !== C && t("change", y);
  }, B = (b) => {
    if (t("blur", b), !e.lazy && !e.lazyFormatter)
      return;
    const { value: k } = b.target, y = f(k, b, true);
    l = k, t("update:modelValue", y);
  }, V = () => {
    var b;
    e.disabled || (b = a.value) == null || b.focus();
  }, T = () => {
    var b;
    e.disabled || (b = a.value) == null || b.blur();
  };
  return watch(
    () => e.modelValue,
    (b) => {
      !a.value || (a.value.value = l && n ? l : b, l = null, n = true);
    }
  ), {
    input: a,
    computedId: u,
    computedAriaInvalid: p,
    onInput: $,
    onChange: m,
    onBlur: B,
    focus: V,
    blur: T
  };
};
var Xe = (e, t) => {
  if (!e)
    return e;
  if (t in e)
    return e[t];
  const a = t.split(".");
  return Xe(e[a[0]], a.splice(1).join("."));
};
var Ft = (e, t = null, a, l) => {
  if (Object.prototype.toString.call(e) === "[object Object]") {
    const n = Xe(e, l.valueField), u = Xe(e, l.textField), f = Xe(e, l.htmlField), c = Xe(e, l.disabledField), v = e[l.optionsField] || null;
    return v !== null ? {
      label: String(Xe(e, l.labelField) || u),
      options: Yt(v, a, l)
    } : {
      value: typeof n > "u" ? t || u : n,
      text: String(typeof u > "u" ? t : u),
      html: f,
      disabled: Boolean(c)
    };
  }
  return {
    value: t || e,
    text: String(e),
    disabled: false
  };
};
var Yt = (e, t, a) => Array.isArray(e) ? e.map((l) => Ft(l, null, t, a)) : Object.prototype.toString.call(e) === "[object Object]" ? (console.warn(
  `[BootstrapVue warn]: ${t} - Setting prop "options" to an object is deprecated. Use the array format instead.`
), Object.keys(e).map((l) => {
  const n = e[l];
  switch (typeof n) {
    case "object":
      return Ft(n.text, String(n.value), t, a);
    default:
      return Ft(n, String(l), t, a);
  }
})) : [];
var zn = ["id"];
var ll = Symbol();
var Nn = defineComponent({
  __name: "BAccordion",
  props: {
    flush: { default: false },
    free: { default: false },
    id: null
  },
  setup(e) {
    const t = e, a = $e(toRef(t, "id"), "accordion"), l = r(toRef(t, "flush")), n = r(toRef(t, "free")), u = computed(() => ({
      "accordion-flush": l.value
    }));
    return n.value || provide(ll, a.value), (f, c) => (openBlock(), createElementBlock("div", {
      id: unref(a),
      class: normalizeClass(["accordion", unref(u)])
    }, [
      renderSlot(f.$slots, "default")
    ], 10, zn));
  }
});
var nl = defineComponent({
  __name: "BCollapse",
  props: {
    accordion: null,
    id: { default: Ee() },
    modelValue: { default: false },
    tag: { default: "div" },
    toggle: { default: false },
    visible: { default: false },
    isNav: { default: false }
  },
  emits: ["update:modelValue", "show", "shown", "hide", "hidden"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "modelValue")), n = r(toRef(a, "toggle")), u = r(toRef(a, "visible")), f = r(toRef(a, "isNav")), c = ref(), v = ref(), p = computed(() => ({
      show: l.value,
      "navbar-collapse": f.value
    })), $ = () => t("update:modelValue", false);
    return watch(
      () => l.value,
      (m) => {
        var B, V;
        m ? (B = v.value) == null || B.show() : (V = v.value) == null || V.hide();
      }
    ), watch(
      () => u.value,
      (m) => {
        var B, V;
        m ? (t("update:modelValue", !!m), (B = v.value) == null || B.show()) : (t("update:modelValue", !!m), (V = v.value) == null || V.hide());
      }
    ), ye(c, "show.bs.collapse", () => {
      t("show"), t("update:modelValue", true);
    }), ye(c, "hide.bs.collapse", () => {
      t("hide"), t("update:modelValue", false);
    }), ye(c, "shown.bs.collapse", () => t("shown")), ye(c, "hidden.bs.collapse", () => t("hidden")), onMounted(() => {
      var m;
      v.value = new Collapse(c.value, {
        parent: a.accordion ? `#${a.accordion}` : void 0,
        toggle: n.value
      }), (u.value || l.value) && (t("update:modelValue", true), (m = v.value) == null || m.show());
    }), (m, B) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: e.id,
      ref_key: "element",
      ref: c,
      class: normalizeClass(["collapse", unref(p)]),
      "data-bs-parent": e.accordion || null,
      "is-nav": unref(f)
    }, {
      default: withCtx(() => [
        renderSlot(m.$slots, "default", {
          visible: unref(l),
          close: $
        })
      ]),
      _: 3
    }, 8, ["id", "class", "data-bs-parent", "is-nav"]));
  }
});
var En = {
  mounted(e, t) {
    let a = t.value;
    Object.keys(t.modifiers).length > 0 && ([a] = Object.keys(t.modifiers)), e.setAttribute("data-bs-toggle", "modal"), e.setAttribute("data-bs-target", `#${a}`);
  }
};
var Dn = {
  mounted(e, t) {
    let a = "right";
    const l = [];
    t.modifiers.left ? a = "left" : t.modifiers.right ? a = "right" : t.modifiers.bottom ? a = "bottom" : t.modifiers.top && (a = "top"), t.modifiers.manual ? l.push("manual") : (t.modifiers.click && l.push("click"), t.modifiers.hover && l.push("hover"), t.modifiers.focus && l.push("focus")), e.setAttribute("data-bs-toggle", "popover"), new Popover(e, {
      trigger: l.length === 0 ? "click" : l.join(" "),
      placement: a,
      content: t.value,
      html: t.modifiers.html
    });
  },
  unmounted(e) {
    const t = Popover.getInstance(e);
    t == null || t.dispose();
  }
};
var Hn = (e) => {
  if (e.classList.contains("offcanvas"))
    return "offcanvas";
  if (e.classList.contains("collapse"))
    return "collapse";
  throw Error("Couldn't resolve toggle type");
};
var Rn = (e, t) => {
  const { modifiers: a, arg: l, value: n } = e, u = Object.keys(a || {}), f = typeof n == "string" ? n.split(dt) : n;
  if (In(t.tagName, "a")) {
    const c = Xt(t, "href") || "";
    fn2.test(c) && u.push(c.replace(cn, ""));
  }
  return Array.prototype.concat.apply([], [l, f]).forEach((c) => typeof c == "string" && u.push(c)), u.filter((c, v, p) => c && p.indexOf(c) === v);
};
var Zt = {
  mounted(e, t) {
    const a = Rn(t, e), l = [];
    let n = "data-bs-target";
    e.tagName === "a" && (n = "href");
    for (let u = 0; u < a.length; u++) {
      const f = a[u], c = document.getElementById(f);
      c && (e.setAttribute("data-bs-toggle", Hn(c)), l.push(`#${f}`));
    }
    l.length > 0 && e.setAttribute(n, l.join(","));
  }
};
var qn = (e, t) => {
  if (t != null && t.trigger)
    return t.trigger;
  if (e.manual)
    return "manual";
  const a = [];
  return e.click && a.push("click"), e.hover && a.push("hover"), e.focus && a.push("focus"), a.length > 0 ? a.join(" ") : "hover focus";
};
var Mn = (e, t) => t != null && t.placement ? t.placement : e.left ? "left" : e.right ? "right" : e.bottom ? "bottom" : "top";
var jn = (e) => e != null && e.delay ? e.delay : 0;
var ia = (e) => typeof e > "u" ? (console.warn("Review tooltip directive usage. Some uses are not defining a title in root component or a value like `v-b-tooltip='{title: \"my title\"}'` nor `v-b-tooltip=\"'my title'\"` to define a title"), "") : typeof e == "object" ? e == null ? void 0 : e.title : e;
var Gn = {
  beforeMount(e, t) {
    e.setAttribute("data-bs-toggle", "tooltip"), e.getAttribute("title") || e.setAttribute("title", ia(t.value).toString());
    const a = /<("[^"]*"|'[^']*'|[^'">])*>/.test(e.title), l = qn(t.modifiers, t.value), n = Mn(t.modifiers, t.value), u = jn(t.value), f = e.getAttribute("title");
    new Tooltip(e, {
      trigger: l,
      placement: n,
      delay: u,
      html: a
    }), f && e.setAttribute("data-bs-original-title", f);
  },
  updated(e, t) {
    e.getAttribute("title") || e.setAttribute("title", ia(t.value).toString());
    const a = e.getAttribute("title"), l = e.getAttribute("data-bs-original-title"), n = Tooltip.getInstance(e);
    e.removeAttribute("title"), a && a !== l && (n == null || n.setContent({ ".tooltip-inner": a }), e.setAttribute("data-bs-original-title", a));
  },
  unmounted(e) {
    const t = Tooltip.getInstance(e);
    t == null || t.dispose();
  }
};
var ct = /* @__PURE__ */ new Map();
var ol = (e) => {
  if (ct.has(e)) {
    const t = ct.get(e);
    t && t.stop && t.stop(), ct.delete(e);
  }
};
var ra = (e, t) => {
  const a = {
    margin: "0px",
    once: false,
    callback: t.value
  };
  Object.keys(t.modifiers).forEach((n) => {
    Number.isInteger(n) ? a.margin = `${n}px` : n.toLowerCase() === "once" && (a.once = true);
  }), ol(e);
  const l = new Wn(
    e,
    a.margin,
    a.once,
    a.callback,
    t.instance
  );
  ct.set(e, l);
};
var Un = {
  beforeMount(e, t) {
    ra(e, t);
  },
  updated(e, t) {
    ra(e, t);
  },
  unmounted(e) {
    ol(e);
  }
};
var Wn = class {
  constructor(t, a, l, n, u) {
    me(this, "element");
    me(this, "margin");
    me(this, "once");
    me(this, "callback");
    me(this, "instance");
    me(this, "observer");
    me(this, "doneOnce");
    me(this, "visible");
    this.element = t, this.margin = a, this.once = l, this.callback = n, this.instance = u, this.createObserver();
  }
  createObserver() {
    if (this.observer && this.stop(), !(this.doneOnce || typeof this.callback != "function")) {
      try {
        this.observer = new IntersectionObserver(this.handler.bind(this), {
          root: null,
          rootMargin: this.margin,
          threshold: 0
        });
      } catch {
        console.error("Intersection Observer not supported"), this.doneOnce = true, this.observer = void 0, this.callback(null);
        return;
      }
      this.instance.$nextTick(() => {
        this.observer && this.observer.observe(this.element);
      });
    }
  }
  handler(t) {
    const [a] = t, l = Boolean(a.isIntersecting || a.intersectionRatio > 0);
    l !== this.visible && (this.visible = l, this.callback(l), this.once && this.visible && (this.doneOnce = true, this.stop()));
  }
  stop() {
    this.observer && this.observer.disconnect(), this.observer = null;
  }
};
var Kn = {
  mounted(e, t) {
    t.value !== false && e.focus();
  }
};
var Xn = {
  BModal: En,
  BPopover: Dn,
  BToggle: Zt,
  BTooltip: Gn,
  BVisible: Un,
  focus: Kn
};
var Jn = { class: "accordion-item" };
var Yn = ["id"];
var Zn = ["aria-expanded", "aria-controls"];
var Qn = { class: "accordion-body" };
var eo = defineComponent({
  __name: "BAccordionItem",
  props: {
    id: null,
    title: null,
    visible: { default: false }
  },
  setup(e) {
    const t = e, a = inject(ll, ""), l = $e(toRef(t, "id"), "accordion_item"), n = r(toRef(t, "visible"));
    return (u, f) => (openBlock(), createElementBlock("div", Jn, [
      createBaseVNode("h2", {
        id: `${unref(l)}heading`,
        class: "accordion-header"
      }, [
        withDirectives((openBlock(), createElementBlock("button", {
          class: normalizeClass(["accordion-button", { collapsed: !unref(n) }]),
          type: "button",
          "aria-expanded": unref(n) ? "true" : "false",
          "aria-controls": unref(l)
        }, [
          renderSlot(u.$slots, "title", {}, () => [
            createTextVNode(toDisplayString(e.title), 1)
          ])
        ], 10, Zn)), [
          [unref(Zt), void 0, unref(l)]
        ])
      ], 8, Yn),
      createVNode(nl, {
        id: unref(l),
        class: "accordion-collapse",
        visible: e.visible,
        accordion: unref(a),
        "aria-labelledby": `heading${unref(l)}`
      }, {
        default: withCtx(() => [
          createBaseVNode("div", Qn, [
            renderSlot(u.$slots, "default")
          ])
        ]),
        _: 3
      }, 8, ["id", "visible", "accordion", "aria-labelledby"])
    ]));
  }
});
var to = ["type", "disabled", "aria-label"];
var Ze = defineComponent({
  __name: "BCloseButton",
  props: {
    ariaLabel: { default: "Close" },
    disabled: { default: false },
    white: { default: false },
    type: { default: "button" }
  },
  emits: ["click"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "disabled")), n = r(toRef(a, "white")), u = computed(() => ({
      "btn-close-white": n.value
    }));
    return (f, c) => (openBlock(), createElementBlock("button", {
      type: e.type,
      class: normalizeClass(["btn-close", unref(u)]),
      disabled: unref(l),
      "aria-label": e.ariaLabel,
      onClick: c[0] || (c[0] = (v) => t("click", v))
    }, null, 10, to));
  }
});
var ao = defineComponent({
  __name: "BAlert",
  props: {
    dismissLabel: { default: "Close" },
    dismissible: { default: false },
    fade: { default: false },
    modelValue: { type: [Boolean, Number], default: false },
    show: { default: false },
    variant: { default: "info" }
  },
  emits: ["closed", "close-count-down", "update:modelValue"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "dismissible"));
    r(toRef(a, "fade"));
    const n = r(toRef(a, "show")), u = useSlots();
    let f;
    const c = ref(null), v = ref(), p = computed(() => !Ve(u.close)), $ = computed(() => !!a.modelValue || n.value), m = computed(() => [
      [`alert-${a.variant}`],
      {
        show: !!a.modelValue,
        "alert-dismissible": l.value,
        fade: !!a.modelValue
      }
    ]), B = (C) => {
      if (typeof C == "boolean")
        return 0;
      const q = Le(C, 0);
      return q > 0 ? q : 0;
    }, V = ref(0), T = computed(() => a.modelValue === true ? true : a.modelValue === false || Le(a.modelValue, 0) < 1 ? false : !!a.modelValue), b = () => {
      f !== void 0 && (clearTimeout(f), f = void 0);
    }, k = () => {
      V.value = B(a.modelValue), (T.value || n.value) && !v.value && (v.value = new Alert(c.value));
    }, y = () => {
      typeof a.modelValue == "boolean" ? t("update:modelValue", false) : t("update:modelValue", 0), t("closed");
    };
    return watch(() => a.modelValue, k), watch(() => n.value, k), watch(V, (C) => {
      b(), typeof a.modelValue != "boolean" && (t("close-count-down", C), C === 0 && a.modelValue > 0 && t("closed"), a.modelValue !== C && t("update:modelValue", C), C > 0 && (f = setTimeout(() => {
        V.value--;
      }, 1e3)));
    }), V.value = B(a.modelValue), onBeforeUnmount(() => {
      var C;
      b(), (C = v.value) == null || C.dispose(), v.value = void 0;
    }), (C, q) => unref($) ? (openBlock(), createElementBlock("div", {
      key: 0,
      ref_key: "element",
      ref: c,
      class: normalizeClass(["alert", unref(m)]),
      role: "alert"
    }, [
      renderSlot(C.$slots, "default"),
      unref(l) ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        unref(p) ? (openBlock(), createElementBlock("button", {
          key: 0,
          type: "button",
          "data-bs-dismiss": "alert",
          onClick: y
        }, [
          renderSlot(C.$slots, "close")
        ])) : (openBlock(), createBlock(Ze, {
          key: 1,
          "aria-label": e.dismissLabel,
          "data-bs-dismiss": "alert",
          onClick: y
        }, null, 8, ["aria-label"]))
      ], 64)) : createCommentVNode("", true)
    ], 2)) : createCommentVNode("", true);
  }
});
var sl = Symbol();
var lo = defineComponent({
  __name: "BAvatarGroup",
  props: {
    overlap: { default: 0.3 },
    rounded: { type: [Boolean, String], default: false },
    size: null,
    square: { default: false },
    tag: { default: "div" },
    variant: null
  },
  setup(e) {
    const t = e, a = r(toRef(t, "square")), l = computed(() => Nt(t.size)), n = computed(
      () => Math.min(Math.max(f(t.overlap), 0), 1) / 2
    ), u = computed(() => {
      const c = l.value ? `calc(${l.value} * ${n.value})` : null;
      return c ? { paddingLeft: c, paddingRight: c } : {};
    }), f = (c) => typeof c == "string" && Oa(c) ? tt(c, 0) : c || 0;
    return provide(sl, {
      overlapScale: n,
      size: t.size,
      square: a.value,
      rounded: t.rounded,
      variant: t.variant
    }), (c, v) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: "b-avatar-group",
      role: "group"
    }, {
      default: withCtx(() => [
        createBaseVNode("div", {
          class: "b-avatar-group-inner",
          style: normalizeStyle(unref(u))
        }, [
          renderSlot(c.$slots, "default")
        ], 4)
      ]),
      _: 3
    }));
  }
});
var no = {
  key: 0,
  class: "b-avatar-custom"
};
var oo = {
  key: 1,
  class: "b-avatar-img"
};
var so = ["src", "alt"];
var Nt = (e) => {
  const t = typeof e == "string" && Oa(e) ? tt(e, 0) : e;
  return typeof t == "number" ? `${t}px` : t || null;
};
var io = defineComponent({
  __name: "BAvatar",
  props: {
    alt: { default: "avatar" },
    ariaLabel: null,
    badge: { type: [Boolean, String], default: false },
    badgeLeft: { default: false },
    badgeOffset: null,
    badgeTop: { default: false },
    badgeVariant: { default: "primary" },
    button: { default: false },
    buttonType: { default: "button" },
    disabled: { default: false },
    icon: null,
    rounded: { type: [Boolean, String], default: "circle" },
    size: null,
    square: { default: false },
    src: null,
    text: null,
    textVariant: null,
    variant: { default: "secondary" }
  },
  emits: ["click", "img-error"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = inject(sl, null), u = ["sm", null, "lg"], f = 0.4, c = f * 0.7, v = r(toRef(a, "badgeLeft")), p = r(toRef(a, "badgeTop")), $ = r(toRef(a, "button")), m = r(toRef(a, "disabled")), B = r(toRef(a, "square")), V = computed(() => !Ve(l.default)), T = computed(() => !Ve(l.badge)), b = computed(() => !!a.badge || a.badge === "" || T.value), k = computed(
      () => n != null && n.size ? n.size : Nt(a.size)
    ), y = computed(
      () => n != null && n.variant ? n.variant : a.variant
    ), C = computed(
      () => n != null && n.rounded ? n.rounded : a.rounded
    ), q = computed(() => ({
      type: $.value ? a.buttonType : void 0,
      "aria-label": a.ariaLabel || null,
      disabled: m.value || null
    })), E = computed(() => [`bg-${a.badgeVariant}`]), I = computed(() => a.badge === true ? "" : a.badge), _ = computed(() => [[`text-${se(a.badgeVariant)}`]]), z = computed(() => ({
      [`b-avatar-${a.size}`]: !!a.size && u.indexOf(Nt(a.size)) !== -1,
      [`bg-${y.value}`]: !!y.value,
      badge: !$.value && y.value && V.value,
      rounded: C.value === "" || C.value === true,
      ["rounded-circle"]: !B.value && C.value === "circle",
      ["rounded-0"]: B.value || C.value === "0",
      ["rounded-1"]: !B.value && C.value === "sm",
      ["rounded-3"]: !B.value && C.value === "lg",
      ["rounded-top"]: !B.value && C.value === "top",
      ["rounded-bottom"]: !B.value && C.value === "bottom",
      ["rounded-start"]: !B.value && C.value === "left",
      ["rounded-end"]: !B.value && C.value === "right",
      btn: $.value,
      [`btn-${y.value}`]: $.value ? !!y.value : false
    })), x = computed(() => [
      [`text-${a.textVariant || se(y.value)}`]
    ]), w = computed(() => {
      const te = a.badgeOffset || "0px";
      return {
        fontSize: (u.indexOf(k.value || null) === -1 ? `calc(${k.value} * ${c})` : "") || "",
        top: p.value ? te : "",
        bottom: p.value ? "" : te,
        left: v.value ? te : "",
        right: v.value ? "" : te
      };
    }), P = computed(() => {
      const te = u.indexOf(k.value || null) === -1 ? `calc(${k.value} * ${f})` : null;
      return te ? { fontSize: te } : {};
    }), L = computed(() => {
      var ge;
      const te = ((ge = n == null ? void 0 : n.overlapScale) == null ? void 0 : ge.value) || 0, be = k.value && te ? `calc(${k.value} * -${te})` : null;
      return be ? { marginLeft: be, marginRight: be } : {};
    }), ee = computed(() => $.value ? "button" : "span"), Z = computed(() => ({
      ...L.value,
      width: k.value,
      height: k.value
    })), se = (te) => te === "light" || te === "warning" ? "dark" : "light", K = (te) => {
      !m.value && $.value && t("click", te);
    }, ve = (te) => t("img-error", te);
    return (te, be) => (openBlock(), createBlock(resolveDynamicComponent(unref(ee)), mergeProps({
      class: ["b-avatar", unref(z)],
      style: unref(Z)
    }, unref(q), { onClick: K }), {
      default: withCtx(() => [
        unref(V) ? (openBlock(), createElementBlock("span", no, [
          renderSlot(te.$slots, "default")
        ])) : e.src ? (openBlock(), createElementBlock("span", oo, [
          createBaseVNode("img", {
            src: e.src,
            alt: e.alt,
            onError: ve
          }, null, 40, so)
        ])) : e.text ? (openBlock(), createElementBlock("span", {
          key: 2,
          class: normalizeClass(["b-avatar-text", unref(x)]),
          style: normalizeStyle(unref(P))
        }, toDisplayString(e.text), 7)) : createCommentVNode("", true),
        unref(b) ? (openBlock(), createElementBlock("span", {
          key: 3,
          class: normalizeClass(["b-avatar-badge", unref(E)]),
          style: normalizeStyle(unref(w))
        }, [
          unref(T) ? renderSlot(te.$slots, "badge", { key: 0 }) : (openBlock(), createElementBlock("span", {
            key: 1,
            class: normalizeClass(unref(_))
          }, toDisplayString(unref(I)), 3))
        ], 6)) : createCommentVNode("", true)
      ]),
      _: 3
    }, 16, ["class", "style"]));
  }
});
var Ue = {
  active: { type: [Boolean, String], default: false },
  activeClass: { type: String, default: "router-link-active" },
  append: { type: [Boolean, String], default: false },
  disabled: { type: [Boolean, String], default: false },
  event: { type: [String, Array], default: "click" },
  exact: { type: [Boolean, String], default: false },
  exactActiveClass: { type: String, default: "router-link-exact-active" },
  href: { type: String },
  rel: { type: String, default: null },
  replace: { type: [Boolean, String], default: false },
  routerComponentName: { type: String, default: "router-link" },
  routerTag: { type: String, default: "a" },
  target: { type: String, default: "_self" },
  to: { type: [String, Object], default: null }
};
var ro = defineComponent({
  props: Ue,
  emits: ["click"],
  setup(e, { emit: t, attrs: a }) {
    const l = r(toRef(e, "active")), n = r(toRef(e, "append")), u = r(toRef(e, "disabled")), f = r(toRef(e, "exact")), c = r(toRef(e, "replace")), v = getCurrentInstance(), p = ref(null), $ = computed(() => {
      const b = e.routerComponentName.split("-").map((y) => y.charAt(0).toUpperCase() + y.slice(1)).join("");
      return !((v == null ? void 0 : v.appContext.app.component(b)) !== void 0) || u.value || !e.to ? "a" : e.routerComponentName;
    }), m = computed(() => {
      const b = "#";
      if (e.href)
        return e.href;
      if (typeof e.to == "string")
        return e.to || b;
      const k = e.to;
      if (Object.prototype.toString.call(k) === "[object Object]" && (k.path || k.query || k.hash)) {
        const y = k.path || "", C = k.query ? `?${Object.keys(k.query).map((E) => `${E}=${k.query[E]}`).join("=")}` : "", q = !k.hash || k.hash.charAt(0) === "#" ? k.hash || "" : `#${k.hash}`;
        return `${y}${C}${q}` || b;
      }
      return b;
    }), B = computed(() => ({
      to: e.to,
      href: m.value,
      target: e.target,
      rel: e.target === "_blank" && e.rel === null ? "noopener" : e.rel || null,
      tabindex: u.value ? "-1" : typeof a.tabindex > "u" ? null : a.tabindex,
      "aria-disabled": u.value ? "true" : null
    }));
    return {
      computedLinkClasses: computed(() => ({
        active: l.value,
        disabled: u.value
      })),
      tag: $,
      routerAttr: B,
      link: p,
      clicked: (b) => {
        if (u.value) {
          b.preventDefault(), b.stopImmediatePropagation();
          return;
        }
        t("click", b);
      },
      activeBoolean: l,
      appendBoolean: n,
      disabledBoolean: u,
      replaceBoolean: c,
      exactBoolean: f
    };
  }
});
var Te = (e, t) => {
  const a = e.__vccOpts || e;
  for (const [l, n] of t)
    a[l] = n;
  return a;
};
function uo(e, t, a, l, n, u) {
  return e.tag === "router-link" ? (openBlock(), createBlock(resolveDynamicComponent(e.tag), mergeProps({ key: 0 }, e.routerAttr, { custom: "" }), {
    default: withCtx(({ href: f, navigate: c, isActive: v, isExactActive: p }) => [
      (openBlock(), createBlock(resolveDynamicComponent(e.routerTag), mergeProps({
        ref: "link",
        href: f,
        class: [
          (v || e.activeBoolean) && e.activeClass,
          (p || e.exactBoolean) && e.exactActiveClass
        ]
      }, e.$attrs, { onClick: c }), {
        default: withCtx(() => [
          renderSlot(e.$slots, "default")
        ]),
        _: 2
      }, 1040, ["href", "class", "onClick"]))
    ]),
    _: 3
  }, 16)) : (openBlock(), createBlock(resolveDynamicComponent(e.tag), mergeProps({
    key: 1,
    ref: "link",
    class: e.computedLinkClasses
  }, e.routerAttr, { onClick: e.clicked }), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16, ["class", "onClick"]));
}
var Ae = Te(ro, [["render", uo]]);
var ua = ht(Ue, ["event", "routerTag"]);
var co = defineComponent({
  components: { BLink: Ae },
  props: {
    pill: { type: [Boolean, String], default: false },
    tag: { type: String, default: "span" },
    variant: { type: String, default: "secondary" },
    textIndicator: { type: [Boolean, String], default: false },
    dotIndicator: { type: [Boolean, String], default: false },
    ...ua
  },
  setup(e) {
    const t = r(toRef(e, "pill")), a = r(toRef(e, "textIndicator")), l = r(toRef(e, "dotIndicator")), n = r(toRef(e, "active")), u = r(toRef(e, "disabled")), f = computed(() => at(e)), c = computed(
      () => f.value ? Ae : e.tag
    ), v = computed(() => [
      [`bg-${e.variant}`],
      {
        active: n.value,
        disabled: u.value,
        "text-dark": ["warning", "info", "light"].includes(e.variant),
        "rounded-pill": t.value,
        "position-absolute top-0 start-100 translate-middle": a.value || l.value,
        "p-2 border border-light rounded-circle": l.value,
        "text-decoration-none": f.value
      }
    ]), p = computed(
      () => f.value ? Jt(e, ua) : {}
    );
    return {
      computedClasses: v,
      computedLinkProps: p,
      computedTag: c
    };
  }
});
function fo(e, t, a, l, n, u) {
  return openBlock(), createBlock(resolveDynamicComponent(e.computedTag), mergeProps({
    class: ["badge", e.computedClasses]
  }, e.computedLinkProps), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16, ["class"]);
}
var vo = Te(co, [["render", fo]]);
var da = ht(Ue, ["event", "routerTag"]);
var mo = defineComponent({
  components: { BLink: Ae },
  props: {
    ...da,
    active: { type: [Boolean, String], default: false },
    ariaCurrent: { type: String, default: "location" },
    disabled: { type: [Boolean, String], default: false },
    text: { type: String, required: false }
  },
  emits: ["click"],
  setup(e, { emit: t }) {
    const a = r(toRef(e, "active")), l = r(toRef(e, "disabled")), n = computed(() => ({
      active: a.value
    })), u = computed(
      () => a.value ? "span" : Ae
    ), f = computed(
      () => a.value ? e.ariaCurrent : void 0
    );
    return {
      computedLinkProps: computed(
        () => u.value !== "span" ? Jt(e, da) : {}
      ),
      computedClasses: n,
      computedTag: u,
      computedAriaCurrent: f,
      clicked: (p) => {
        if (l.value || a.value) {
          p.preventDefault(), p.stopImmediatePropagation();
          return;
        }
        l.value || t("click", p);
      }
    };
  }
});
function bo(e, t, a, l, n, u) {
  return openBlock(), createElementBlock("li", {
    class: normalizeClass(["breadcrumb-item", e.computedClasses])
  }, [
    (openBlock(), createBlock(resolveDynamicComponent(e.computedTag), mergeProps({ "aria-current": e.computedAriaCurrent }, e.computedLinkProps, { onClick: e.clicked }), {
      default: withCtx(() => [
        renderSlot(e.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 16, ["aria-current", "onClick"]))
  ], 2);
}
var il = Te(mo, [["render", bo]]);
var go = { "aria-label": "breadcrumb" };
var po = { class: "breadcrumb" };
var ho = defineComponent({
  __name: "BBreadcrumb",
  props: {
    items: null
  },
  setup(e) {
    const t = e, a = Ln(), l = computed(() => {
      const n = t.items || (a == null ? void 0 : a.items) || [];
      let u = false;
      return n.map((c, v) => (typeof c == "string" && (c = { text: c }, v < n.length - 1 && (c.href = "#")), c.active && (u = true), !c.active && !u && (c.active = v + 1 === n.length), c));
    });
    return (n, u) => (openBlock(), createElementBlock("nav", go, [
      createBaseVNode("ol", po, [
        renderSlot(n.$slots, "prepend"),
        (openBlock(true), createElementBlock(Fragment, null, renderList(unref(l), (f, c) => (openBlock(), createBlock(il, mergeProps({ key: c }, f), {
          default: withCtx(() => [
            createTextVNode(toDisplayString(f.text), 1)
          ]),
          _: 2
        }, 1040))), 128)),
        renderSlot(n.$slots, "default"),
        renderSlot(n.$slots, "append")
      ])
    ]));
  }
});
var yo = {
  key: 0,
  class: "visually-hidden"
};
var St = defineComponent({
  __name: "BSpinner",
  props: {
    label: null,
    role: { default: "status" },
    small: { default: false },
    tag: { default: "span" },
    type: { default: "border" },
    variant: null
  },
  setup(e) {
    const t = e, a = useSlots(), l = r(toRef(t, "small")), n = computed(() => ({
      "spinner-border": t.type === "border",
      "spinner-border-sm": t.type === "border" && l.value,
      "spinner-grow": t.type === "grow",
      "spinner-grow-sm": t.type === "grow" && l.value,
      [`text-${t.variant}`]: t.variant !== void 0
    })), u = computed(() => !Ve(a.label));
    return (f, c) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(unref(n)),
      role: e.label || unref(u) ? e.role : null,
      "aria-hidden": e.label || unref(u) ? null : true
    }, {
      default: withCtx(() => [
        e.label || unref(u) ? (openBlock(), createElementBlock("span", yo, [
          renderSlot(f.$slots, "label", {}, () => [
            createTextVNode(toDisplayString(e.label), 1)
          ])
        ])) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["class", "role", "aria-hidden"]));
  }
});
var Bo = defineComponent({
  components: { BLink: Ae, BSpinner: St },
  props: {
    ...Ue,
    active: { type: [Boolean, String], default: false },
    disabled: { type: [Boolean, String], default: false },
    href: { type: String, required: false },
    pill: { type: [Boolean, String], default: false },
    pressed: { type: [Boolean, String], default: false },
    rel: { type: String, default: void 0 },
    size: { type: String, default: "md" },
    squared: { type: [Boolean, String], default: false },
    tag: { type: String, default: "button" },
    target: { type: String, default: "_self" },
    type: { type: String, default: "button" },
    variant: { type: String, default: "secondary" },
    loading: { type: [Boolean, String], default: false },
    loadingMode: { type: String, default: "inline" }
  },
  emits: ["click", "update:pressed"],
  setup(e, { emit: t }) {
    const a = r(toRef(e, "active")), l = r(toRef(e, "disabled")), n = r(toRef(e, "pill")), u = r(toRef(e, "pressed")), f = r(toRef(e, "squared")), c = r(toRef(e, "loading")), v = computed(() => u.value === true), p = computed(
      () => e.tag === "button" && e.href === void 0 && e.to === null
    ), $ = computed(() => at(e)), m = computed(() => e.to !== null), B = computed(
      () => e.href !== void 0 ? false : !p.value
    ), V = computed(() => [
      [`btn-${e.variant}`],
      [`btn-${e.size}`],
      {
        active: a.value || u.value,
        "rounded-pill": n.value,
        "rounded-0": f.value,
        disabled: l.value
      }
    ]), T = computed(() => ({
      "aria-disabled": B.value ? l.value : null,
      "aria-pressed": v.value ? u.value : null,
      autocomplete: v.value ? "off" : null,
      disabled: p.value ? l.value : null,
      href: e.href,
      rel: $.value ? e.rel : null,
      role: B.value || $.value ? "button" : null,
      target: $.value ? e.target : null,
      type: p.value ? e.type : null,
      to: p.value ? null : e.to,
      append: $.value ? e.append : null,
      activeClass: m.value ? e.activeClass : null,
      event: m.value ? e.event : null,
      exact: m.value ? e.exact : null,
      exactActiveClass: m.value ? e.exactActiveClass : null,
      replace: m.value ? e.replace : null,
      routerComponentName: m.value ? e.routerComponentName : null,
      routerTag: m.value ? e.routerTag : null
    })), b = computed(
      () => m.value ? Ae : e.href ? "a" : e.tag
    );
    return {
      computedClasses: V,
      computedAttrs: T,
      computedTag: b,
      clicked: (y) => {
        if (l.value) {
          y.preventDefault(), y.stopPropagation();
          return;
        }
        t("click", y), v.value && t("update:pressed", !u.value);
      },
      loadingBoolean: c
    };
  }
});
function $o(e, t, a, l, n, u) {
  const f = resolveComponent("b-spinner");
  return openBlock(), createBlock(resolveDynamicComponent(e.computedTag), mergeProps({
    class: ["btn", e.computedClasses]
  }, e.computedAttrs, { onClick: e.clicked }), {
    default: withCtx(() => [
      e.loadingBoolean ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["btn-loading", { "mode-fill": e.loadingMode === "fill", "mode-inline": e.loadingMode === "inline" }])
      }, [
        renderSlot(e.$slots, "loading", {}, () => [
          createVNode(f, {
            class: "btn-spinner",
            small: e.size !== "lg"
          }, null, 8, ["small"])
        ])
      ], 2)) : createCommentVNode("", true),
      createBaseVNode("div", {
        class: normalizeClass(["btn-content", { "btn-loading-fill": e.loadingBoolean && e.loadingMode === "fill" }])
      }, [
        renderSlot(e.$slots, "default")
      ], 2)
    ]),
    _: 3
  }, 16, ["class", "onClick"]);
}
var lt = Te(Bo, [["render", $o]]);
var ko = defineComponent({
  __name: "BButtonGroup",
  props: {
    ariaLabel: { default: "Group" },
    size: null,
    tag: { default: "div" },
    vertical: { default: false }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "vertical")), l = computed(() => ({
      "btn-group": !a.value,
      [`btn-group-${t.size}`]: t.size !== void 0,
      "btn-group-vertical": a.value
    }));
    return (n, u) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(unref(l)),
      role: "group",
      "aria-label": e.ariaLabel
    }, {
      default: withCtx(() => [
        renderSlot(n.$slots, "default")
      ]),
      _: 3
    }, 8, ["class", "aria-label"]));
  }
});
var Co = ["role", "aria-label"];
var So = defineComponent({
  __name: "BButtonToolbar",
  props: {
    ariaLabel: { default: "Group" },
    justify: { default: false },
    role: { default: "toolbar" }
  },
  setup(e) {
    const a = r(toRef(e, "justify")), l = computed(() => ({
      "justify-content-between": a.value
    }));
    return (n, u) => (openBlock(), createElementBlock("div", {
      class: normalizeClass([unref(l), "btn-toolbar"]),
      role: e.role,
      "aria-label": e.ariaLabel
    }, [
      renderSlot(n.$slots, "default")
    ], 10, Co));
  }
});
var Qt = defineComponent({
  __name: "BImg",
  props: {
    alt: null,
    blank: { default: false },
    blankColor: { default: "transparent" },
    block: { default: false },
    center: { default: false },
    fluid: { default: false },
    lazy: { default: false },
    fluidGrow: { default: false },
    height: null,
    left: { default: false },
    start: { default: false },
    right: { default: false },
    end: { default: false },
    rounded: { type: [Boolean, String], default: false },
    sizes: null,
    src: null,
    srcset: null,
    thumbnail: { default: false },
    width: null
  },
  emits: ["load"],
  setup(e, { emit: t }) {
    const a = e, l = '<svg width="%{w}" height="%{h}" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 %{w} %{h}" preserveAspectRatio="none"><rect width="100%" height="100%" style="fill:%{f};"></rect></svg>', n = r(toRef(a, "lazy")), u = r(toRef(a, "blank")), f = r(toRef(a, "block")), c = r(toRef(a, "center")), v = r(toRef(a, "fluid")), p = r(toRef(a, "fluidGrow")), $ = r(toRef(a, "left")), m = r(toRef(a, "start")), B = r(toRef(a, "right")), V = r(toRef(a, "end")), T = r(toRef(a, "thumbnail")), b = computed(
      () => typeof a.srcset == "string" ? a.srcset.split(",").filter((z) => z).join(",") : Array.isArray(a.srcset) ? a.srcset.filter((z) => z).join(",") : void 0
    ), k = computed(
      () => typeof a.sizes == "string" ? a.sizes.split(",").filter((z) => z).join(",") : Array.isArray(a.sizes) ? a.sizes.filter((z) => z).join(",") : void 0
    ), y = computed(() => {
      const z = (P) => P === void 0 ? void 0 : typeof P == "number" ? P : Number.parseInt(P, 10) || void 0, x = z(a.width), w = z(a.height);
      if (u.value) {
        if (x !== void 0 && w === void 0)
          return { height: x, width: x };
        if (x === void 0 && w !== void 0)
          return { height: w, width: w };
        if (x === void 0 && w === void 0)
          return { height: 1, width: 1 };
      }
      return {
        width: x,
        height: w
      };
    }), C = computed(
      () => _(y.value.width, y.value.height, a.blankColor)
    ), q = computed(() => ({
      src: u.value ? C.value : a.src,
      alt: a.alt,
      width: y.value.width || void 0,
      height: y.value.height || void 0,
      srcset: u.value ? void 0 : b.value,
      sizes: u.value ? void 0 : k.value,
      loading: n.value ? "lazy" : "eager"
    })), E = computed(
      () => $.value || m.value ? "float-start" : B.value || V.value ? "float-end" : c.value ? "mx-auto" : void 0
    ), I = computed(() => ({
      "img-thumbnail": T.value,
      "img-fluid": v.value || p.value,
      "w-100": p.value,
      rounded: a.rounded === "" || a.rounded === true,
      [`rounded-${a.rounded}`]: typeof a.rounded == "string" && a.rounded !== "",
      [`${E.value}`]: E.value !== void 0,
      "d-block": f.value || c.value
    })), _ = (z, x, w) => `data:image/svg+xml;charset=UTF-8,${encodeURIComponent(
      l.replace("%{w}", String(z)).replace("%{h}", String(x)).replace("%{f}", w)
    )}`;
    return (z, x) => (openBlock(), createElementBlock("img", mergeProps({ class: unref(I) }, unref(q), {
      onLoad: x[0] || (x[0] = (w) => t("load", w))
    }), null, 16));
  }
});
var pt = defineComponent({
  __name: "BCardImg",
  props: {
    alt: null,
    blank: { default: false },
    blankColor: null,
    bottom: { default: false },
    lazy: { default: false },
    height: null,
    left: { default: false },
    start: { default: false },
    right: { default: false },
    end: { default: false },
    sizes: null,
    src: null,
    srcset: null,
    top: { default: false },
    width: null
  },
  emits: ["load"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "bottom")), n = r(toRef(a, "end")), u = r(toRef(a, "left")), f = r(toRef(a, "right")), c = r(toRef(a, "start")), v = r(toRef(a, "top")), p = computed(
      () => v.value ? "card-img-top" : f.value || n.value ? "card-img-right" : l.value ? "card-img-bottom" : u.value || c.value ? "card-img-left" : "card-img"
    ), $ = computed(() => ({
      alt: a.alt,
      height: a.height,
      src: a.src,
      lazy: a.lazy,
      width: a.width,
      blank: a.blank,
      blankColor: a.blankColor,
      sizes: a.sizes,
      srcset: a.srcset
    }));
    return (m, B) => (openBlock(), createBlock(Qt, mergeProps({ class: unref(p) }, unref($), {
      onLoad: B[0] || (B[0] = (V) => t("load", V))
    }), null, 16, ["class"]));
  }
});
var wo = ["innerHTML"];
var rl = defineComponent({
  __name: "BCardHeadFoot",
  props: {
    text: null,
    bgVariant: null,
    borderVariant: null,
    html: null,
    tag: { default: "div" },
    textVariant: null
  },
  setup(e) {
    const t = e, a = computed(() => ({
      [`text-${t.textVariant}`]: t.textVariant !== void 0,
      [`bg-${t.bgVariant}`]: t.bgVariant !== void 0,
      [`border-${t.borderVariant}`]: t.borderVariant !== void 0
    }));
    return (l, n) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(unref(a))
    }, {
      default: withCtx(() => [
        e.html ? (openBlock(), createElementBlock("div", {
          key: 0,
          innerHTML: e.html
        }, null, 8, wo)) : renderSlot(l.$slots, "default", { key: 1 }, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var ul = defineComponent({
  __name: "BCardHeader",
  props: {
    text: null,
    bgVariant: null,
    borderVariant: null,
    html: null,
    tag: { default: "div" },
    textVariant: null
  },
  setup(e) {
    const t = e;
    return (a, l) => (openBlock(), createBlock(rl, mergeProps({ class: "card-header" }, t), {
      default: withCtx(() => [
        renderSlot(a.$slots, "default")
      ]),
      _: 3
    }, 16));
  }
});
var dl = defineComponent({
  __name: "BCardTitle",
  props: {
    text: null,
    tag: { default: "h4" }
  },
  setup(e) {
    return (t, a) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), { class: "card-title" }, {
      default: withCtx(() => [
        renderSlot(t.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }));
  }
});
var cl = defineComponent({
  __name: "BCardSubtitle",
  props: {
    text: null,
    tag: { default: "h6" },
    textVariant: { default: "muted" }
  },
  setup(e) {
    const t = e, a = computed(() => [`text-${t.textVariant}`]);
    return (l, n) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(["card-subtitle mb-2", unref(a)])
    }, {
      default: withCtx(() => [
        renderSlot(l.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var fl = defineComponent({
  __name: "BCardBody",
  props: {
    bodyBgVariant: null,
    bodyTag: { default: "div" },
    bodyTextVariant: null,
    overlay: { default: false },
    subtitle: null,
    subtitleTag: { default: "h4" },
    subtitleTextVariant: null,
    title: null,
    titleTag: { default: "h4" },
    text: null
  },
  setup(e) {
    const t = e, a = useSlots(), l = r(toRef(t, "overlay")), n = computed(() => !Ve(a.title)), u = computed(() => !Ve(a.subtitle)), f = computed(() => ({
      "card-img-overlay": l.value,
      [`text-${t.bodyTextVariant}`]: t.bodyTextVariant !== void 0,
      [`bg-${t.bodyBgVariant}`]: t.bodyBgVariant !== void 0
    }));
    return (c, v) => (openBlock(), createBlock(resolveDynamicComponent(e.bodyTag), {
      class: normalizeClass(["card-body", unref(f)])
    }, {
      default: withCtx(() => [
        !!e.title || unref(n) ? (openBlock(), createBlock(dl, {
          key: 0,
          tag: e.titleTag
        }, {
          default: withCtx(() => [
            renderSlot(c.$slots, "title", {}, () => [
              createTextVNode(toDisplayString(e.title), 1)
            ])
          ]),
          _: 3
        }, 8, ["tag"])) : createCommentVNode("", true),
        !!e.subtitle || unref(u) ? (openBlock(), createBlock(cl, {
          key: 1,
          tag: e.subtitleTag,
          "text-variant": e.subtitleTextVariant
        }, {
          default: withCtx(() => [
            renderSlot(c.$slots, "subtitle", {}, () => [
              createTextVNode(toDisplayString(e.subtitle), 1)
            ])
          ]),
          _: 3
        }, 8, ["tag", "text-variant"])) : createCommentVNode("", true),
        renderSlot(c.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var vl = defineComponent({
  __name: "BCardFooter",
  props: {
    text: null,
    bgVariant: null,
    borderVariant: null,
    html: null,
    tag: { default: "div" },
    textVariant: null
  },
  setup(e) {
    const t = e;
    return (a, l) => (openBlock(), createBlock(rl, mergeProps({ class: "card-footer" }, t), {
      default: withCtx(() => [
        renderSlot(a.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 16));
  }
});
var ml = defineComponent({
  __name: "BCard",
  props: {
    align: null,
    bgVariant: null,
    bodyBgVariant: null,
    bodyClass: null,
    bodyTag: { default: "div" },
    bodyTextVariant: null,
    borderVariant: null,
    footer: null,
    footerBgVariant: null,
    footerBorderVariant: null,
    footerClass: null,
    footerHtml: { default: "" },
    footerTag: { default: "div" },
    footerTextVariant: null,
    header: null,
    headerBgVariant: null,
    headerBorderVariant: null,
    headerClass: null,
    headerHtml: { default: "" },
    headerTag: { default: "div" },
    headerTextVariant: null,
    imgAlt: null,
    imgBottom: { default: false },
    imgEnd: { default: false },
    imgHeight: null,
    imgLeft: { default: false },
    imgRight: { default: false },
    imgSrc: null,
    imgStart: { default: false },
    imgTop: { default: false },
    imgWidth: null,
    noBody: { default: false },
    overlay: { default: false },
    subtitle: null,
    subtitleTag: { default: "h6" },
    subtitleTextVariant: { default: "muted" },
    tag: { default: "div" },
    textVariant: null,
    title: null,
    titleTag: { default: "h4" },
    bodyText: { default: "" }
  },
  setup(e) {
    const t = e, a = useSlots(), l = r(toRef(t, "imgBottom")), n = r(toRef(t, "imgEnd")), u = r(toRef(t, "imgLeft")), f = r(toRef(t, "imgRight")), c = r(toRef(t, "imgStart")), v = r(toRef(t, "noBody")), p = computed(() => !Ve(a.header)), $ = computed(() => !Ve(a.footer)), m = computed(() => ({
      [`text-${t.align}`]: t.align !== void 0,
      [`text-${t.textVariant}`]: t.textVariant !== void 0,
      [`bg-${t.bgVariant}`]: t.bgVariant !== void 0,
      [`border-${t.borderVariant}`]: t.borderVariant !== void 0,
      "flex-row": u.value || c.value,
      "flex-row-reverse": n.value || f.value
    })), B = computed(() => ({
      bgVariant: t.headerBgVariant,
      borderVariant: t.headerBorderVariant,
      html: t.headerHtml,
      tag: t.headerTag,
      textVariant: t.headerTextVariant
    })), V = computed(() => ({
      overlay: t.overlay,
      bodyBgVariant: t.bodyBgVariant,
      bodyTag: t.bodyTag,
      bodyTextVariant: t.bodyTextVariant,
      subtitle: t.subtitle,
      subtitleTag: t.subtitleTag,
      subtitleTextVariant: t.subtitleTextVariant,
      title: t.title,
      titleTag: t.titleTag
    })), T = computed(() => ({
      bgVariant: t.footerBgVariant,
      borderVariant: t.footerBorderVariant,
      html: t.footerHtml,
      tag: t.footerTag,
      textVariant: t.footerTextVariant
    })), b = computed(() => ({
      src: t.imgSrc,
      alt: t.imgAlt,
      height: t.imgHeight,
      width: t.imgWidth,
      bottom: t.imgBottom,
      end: t.imgEnd,
      left: t.imgLeft,
      right: t.imgRight,
      start: t.imgStart,
      top: t.imgTop
    }));
    return (k, y) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(["card", unref(m)])
    }, {
      default: withCtx(() => [
        unref(l) ? createCommentVNode("", true) : renderSlot(k.$slots, "img", { key: 0 }, () => [
          e.imgSrc ? (openBlock(), createBlock(pt, normalizeProps(mergeProps({ key: 0 }, unref(b))), null, 16)) : createCommentVNode("", true)
        ]),
        e.header || unref(p) || e.headerHtml ? (openBlock(), createBlock(ul, mergeProps({ key: 1 }, unref(B), { class: e.headerClass }), {
          default: withCtx(() => [
            renderSlot(k.$slots, "header", {}, () => [
              createTextVNode(toDisplayString(e.header), 1)
            ])
          ]),
          _: 3
        }, 16, ["class"])) : createCommentVNode("", true),
        unref(v) ? renderSlot(k.$slots, "default", { key: 3 }, () => [
          createTextVNode(toDisplayString(e.bodyText), 1)
        ]) : (openBlock(), createBlock(fl, mergeProps({ key: 2 }, unref(V), { class: e.bodyClass }), {
          default: withCtx(() => [
            renderSlot(k.$slots, "default", {}, () => [
              createTextVNode(toDisplayString(e.bodyText), 1)
            ])
          ]),
          _: 3
        }, 16, ["class"])),
        e.footer || unref($) || e.footerHtml ? (openBlock(), createBlock(vl, mergeProps({ key: 4 }, unref(T), { class: e.footerClass }), {
          default: withCtx(() => [
            renderSlot(k.$slots, "footer", {}, () => [
              createTextVNode(toDisplayString(e.footer), 1)
            ])
          ]),
          _: 3
        }, 16, ["class"])) : createCommentVNode("", true),
        unref(l) ? renderSlot(k.$slots, "img", { key: 5 }, () => [
          e.imgSrc ? (openBlock(), createBlock(pt, normalizeProps(mergeProps({ key: 0 }, unref(b))), null, 16)) : createCommentVNode("", true)
        ]) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var To = defineComponent({
  __name: "BCardGroup",
  props: {
    columns: { default: false },
    deck: { default: false },
    tag: { default: "div" }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "columns")), l = r(toRef(t, "deck")), n = computed(
      () => l.value ? "card-deck" : a.value ? "card-columns" : "card-group"
    ), u = computed(() => [n.value]);
    return (f, c) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(unref(u))
    }, {
      default: withCtx(() => [
        renderSlot(f.$slots, "default")
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var _o = defineComponent({
  __name: "BCardText",
  props: {
    text: null,
    tag: { default: "p" }
  },
  setup(e) {
    return (t, a) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), { class: "card-text" }, {
      default: withCtx(() => [
        renderSlot(t.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }));
  }
});
var Vo = ["id"];
var Ao = {
  key: 0,
  class: "carousel-indicators"
};
var xo = ["data-bs-target", "data-bs-slide-to", "aria-label"];
var Io = { class: "carousel-inner" };
var Fo = ["data-bs-target"];
var Po = createBaseVNode("span", {
  class: "carousel-control-prev-icon",
  "aria-hidden": "true"
}, null, -1);
var Oo = { class: "visually-hidden" };
var Lo = ["data-bs-target"];
var zo = createBaseVNode("span", {
  class: "carousel-control-next-icon",
  "aria-hidden": "true"
}, null, -1);
var No = { class: "visually-hidden" };
var bl = Symbol();
var Eo = defineComponent({
  __name: "BCarousel",
  props: {
    startingSlide: { default: 0 },
    id: null,
    imgHeight: null,
    imgWidth: null,
    background: null,
    modelValue: { default: 0 },
    controls: { default: false },
    indicators: { default: false },
    interval: { default: 5e3 },
    noTouch: { default: false },
    noWrap: { default: false },
    controlsPrevText: { default: "Previous" },
    controlsNextText: { default: "Next" },
    indicatorsButtonLabel: { default: "Slide" }
  },
  emits: ["sliding-start", "sliding-end"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = $e(toRef(a, "id"), "carousel"), u = r(toRef(a, "controls")), f = r(toRef(a, "indicators")), c = r(toRef(a, "noTouch"));
    r(toRef(a, "noWrap"));
    const v = ref(), p = ref(), $ = ref([]);
    return ye(v, "slide.bs.carousel", (m) => t("sliding-start", m)), ye(v, "slid.bs.carousel", (m) => t("sliding-end", m)), onMounted(() => {
      p.value = new Carousel(v.value, {
        wrap: !c.value,
        interval: a.interval,
        touch: !c.value
      }), l.default && ($.value = l.default().filter((m) => {
        var B;
        return ((B = m.type) == null ? void 0 : B.__name) === "BCarouselSlide";
      }));
    }), provide(bl, {
      background: a.background,
      width: a.imgWidth,
      height: a.imgHeight
    }), (m, B) => (openBlock(), createElementBlock("div", {
      id: unref(n),
      ref_key: "element",
      ref: v,
      class: "carousel slide",
      "data-bs-ride": "carousel"
    }, [
      unref(f) ? (openBlock(), createElementBlock("div", Ao, [
        (openBlock(true), createElementBlock(Fragment, null, renderList($.value, (V, T) => (openBlock(), createElementBlock("button", {
          key: T,
          type: "button",
          "data-bs-target": `#${unref(n)}`,
          "data-bs-slide-to": T,
          class: normalizeClass(T === e.startingSlide ? "active" : ""),
          "aria-current": "true",
          "aria-label": `${e.indicatorsButtonLabel} ${T}`
        }, null, 10, xo))), 128))
      ])) : createCommentVNode("", true),
      createBaseVNode("div", Io, [
        renderSlot(m.$slots, "default")
      ]),
      unref(u) ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        createBaseVNode("button", {
          class: "carousel-control-prev",
          type: "button",
          "data-bs-target": `#${unref(n)}`,
          "data-bs-slide": "prev"
        }, [
          Po,
          createBaseVNode("span", Oo, toDisplayString(e.controlsPrevText), 1)
        ], 8, Fo),
        createBaseVNode("button", {
          class: "carousel-control-next",
          type: "button",
          "data-bs-target": `#${unref(n)}`,
          "data-bs-slide": "next"
        }, [
          zo,
          createBaseVNode("span", No, toDisplayString(e.controlsNextText), 1)
        ], 8, Lo)
      ], 64)) : createCommentVNode("", true)
    ], 8, Vo));
  }
});
var Do = ["data-bs-interval"];
var Ho = ["innerHTML"];
var Ro = { key: 1 };
var qo = ["innerHTML"];
var Mo = { key: 1 };
var jo = defineComponent({
  __name: "BCarouselSlide",
  props: {
    imgSrc: null,
    imgHeight: null,
    imgWidth: null,
    interval: null,
    active: { default: false },
    background: null,
    caption: null,
    captionHtml: null,
    captionTag: { default: "h3" },
    contentTag: { default: "div" },
    contentVisibleUp: null,
    id: null,
    imgAlt: null,
    imgBlank: { default: false },
    imgBlankColor: { default: "transparent" },
    text: null,
    textHtml: null,
    textTag: { default: "p" }
  },
  setup(e) {
    const t = e, a = useSlots(), l = inject(bl, {}), n = r(toRef(t, "active")), u = computed(() => !Ve(a.default)), f = computed(() => ({
      background: `${t.background || l.background || "rgb(171, 171, 171)"} none repeat scroll 0% 0%`
    })), c = computed(() => ({
      "d-none": t.contentVisibleUp !== void 0,
      [`d-${t.contentVisibleUp}-block`]: t.contentVisibleUp !== void 0
    })), v = computed(() => l.width), p = computed(() => l.height);
    return ($, m) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["carousel-item", { active: unref(n) }]),
      "data-bs-interval": e.interval,
      style: normalizeStyle(unref(f))
    }, [
      renderSlot($.$slots, "img", {}, () => [
        createVNode(Qt, {
          class: "d-block w-100",
          alt: e.imgAlt,
          src: e.imgSrc,
          width: e.imgWidth || unref(v),
          height: e.imgHeight || unref(p),
          blank: e.imgBlank,
          "blank-color": e.imgBlankColor
        }, null, 8, ["alt", "src", "width", "height", "blank", "blank-color"])
      ]),
      e.caption || e.captionHtml || e.text || e.textHtml || unref(u) ? (openBlock(), createBlock(resolveDynamicComponent(e.contentTag), {
        key: 0,
        class: normalizeClass(["carousel-caption", unref(c)])
      }, {
        default: withCtx(() => [
          e.caption || e.captionHtml ? (openBlock(), createBlock(resolveDynamicComponent(e.captionTag), { key: 0 }, {
            default: withCtx(() => [
              e.captionHtml ? (openBlock(), createElementBlock("span", {
                key: 0,
                innerHTML: e.captionHtml
              }, null, 8, Ho)) : (openBlock(), createElementBlock("span", Ro, toDisplayString(e.caption), 1))
            ]),
            _: 1
          })) : createCommentVNode("", true),
          e.text || e.textHtml ? (openBlock(), createBlock(resolveDynamicComponent(e.textTag), { key: 1 }, {
            default: withCtx(() => [
              e.textHtml ? (openBlock(), createElementBlock("span", {
                key: 0,
                innerHTML: e.textHtml
              }, null, 8, qo)) : (openBlock(), createElementBlock("span", Mo, toDisplayString(e.text), 1))
            ]),
            _: 1
          })) : createCommentVNode("", true),
          renderSlot($.$slots, "default")
        ]),
        _: 3
      }, 8, ["class"])) : createCommentVNode("", true)
    ], 14, Do));
  }
});
var ca = kt("", [], { type: [Boolean, String, Number], default: false });
var fa = kt("offset", [""], { type: [String, Number], default: null });
var va = kt("order", [""], { type: [String, Number], default: null });
var Go = defineComponent({
  name: "BCol",
  props: {
    col: { type: [Boolean, String], default: false },
    cols: { type: [String, Number], default: null },
    ...ca,
    offset: { type: [String, Number], default: null },
    ...fa,
    order: { type: [String, Number], default: null },
    ...va,
    alignSelf: { type: String, default: null },
    tag: { type: String, default: "div" }
  },
  setup(e) {
    const t = [
      { content: ca, propPrefix: "cols", classPrefix: "col" },
      { content: fa, propPrefix: "offset" },
      { content: va, propPrefix: "order" }
    ], a = r(toRef(e, "col")), l = computed(
      () => t.flatMap((u) => Ga(e, u.content, u.propPrefix, u.classPrefix))
    );
    return {
      computedClasses: computed(() => [
        l.value,
        {
          col: a.value || !l.value.some((u) => /^col-/.test(u)) && !e.cols,
          [`col-${e.cols}`]: !!e.cols,
          [`offset-${e.offset}`]: !!e.offset,
          [`order-${e.order}`]: !!e.order,
          [`align-self-${e.alignSelf}`]: !!e.alignSelf
        }
      ])
    };
  }
});
function Uo(e, t, a, l, n, u) {
  return openBlock(), createBlock(resolveDynamicComponent(e.tag), {
    class: normalizeClass(e.computedClasses)
  }, {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 8, ["class"]);
}
var et = Te(Go, [["render", Uo]]);
var Ke = { autoHide: true, delay: 5e3, noCloseButton: false, pos: "top-right", value: true };
var ma = class {
  constructor(t) {
    me(this, "vm");
    me(this, "containerPositions");
    isReactive(t) ? this.vm = t : this.vm = reactive(t), this.containerPositions = computed(() => {
      const a = /* @__PURE__ */ new Set([]);
      return this.vm.toasts.map((l) => {
        l.options.pos && a.add(l.options.pos);
      }), a;
    });
  }
  toasts(t) {
    return t ? computed(
      () => this.vm.toasts.filter((a) => {
        if (a.options.pos === t && a.options.value)
          return a;
      })
    ) : computed(() => this.vm.toasts);
  }
  remove(...t) {
    this.vm.toasts = this.vm.toasts.filter((a) => {
      if (a.options.id && !t.includes(a.options.id))
        return a;
    });
  }
  isRoot() {
    var t;
    return (t = this.vm.root) != null ? t : false;
  }
  show(t, a = Ke) {
    const l = { id: Ee(), ...Ke, ...a }, n = {
      options: reactive(l),
      content: t
    };
    return this.vm.toasts.push(n), n;
  }
  info(t, a = Ke) {
    return this.show(t, { variant: "info", ...a });
  }
  danger(t, a = Ke) {
    return this.show(t, { variant: "danger", ...a });
  }
  warning(t, a = Ke) {
    return this.show(t, { variant: "warning", ...a });
  }
  success(t, a = Ke) {
    return this.show(t, { variant: "success", ...a });
  }
  hide() {
  }
};
var Wo = class {
  constructor() {
    me(this, "vms");
    me(this, "rootInstance");
    me(this, "useToast", pl);
    this.vms = {};
  }
  getOrCreateViewModel(t) {
    if (!t) {
      if (this.rootInstance)
        return this.vms[this.rootInstance];
      const a = { root: true, toasts: [], container: void 0, id: Symbol("toast") };
      return this.rootInstance = a.id, this.vms[a.id] = a, a;
    }
    if (t.root) {
      if (this.rootInstance)
        return this.vms[this.rootInstance];
      this.rootInstance = t.id;
    }
    return this.vms[t.id] = t, t;
  }
  getVM(t) {
    if (!t && this.rootInstance)
      return this.vms[this.rootInstance];
    if (t)
      return this.vms[t];
  }
};
var Et = Symbol();
var gl = Symbol();
var Ko = {
  container: void 0,
  toasts: [],
  root: false
};
function Xo() {
  return inject(gl);
}
function pl(e, t = Et) {
  const a = inject(Xo());
  if (!e)
    return new ma(a.getOrCreateViewModel());
  const l = { id: Symbol("toastInstance") }, n = { ...Ko, ...l, ...e }, u = a.getOrCreateViewModel(n);
  return new ma(u);
}
var Jo = {
  install: (e, t = {}) => {
    var a, l, n, u;
    e.provide(gl, (l = (a = t == null ? void 0 : t.BToast) == null ? void 0 : a.injectkey) != null ? l : Et), e.provide((u = (n = t == null ? void 0 : t.BToast) == null ? void 0 : n.injectkey) != null ? u : Et, new Wo());
  }
};
var wt = defineComponent({
  __name: "BTransition",
  props: {
    appear: { default: false },
    mode: null,
    noFade: { default: false },
    transProps: null
  },
  setup(e) {
    const t = e, a = r(toRef(t, "appear")), l = r(toRef(t, "noFade")), n = computed(() => {
      const c = {
        name: "",
        enterActiveClass: "",
        enterToClass: "",
        leaveActiveClass: "",
        leaveToClass: "showing",
        enterFromClass: "showing",
        leaveFromClass: ""
      }, v = {
        ...c,
        enterActiveClass: "fade showing",
        leaveActiveClass: "fade showing"
      };
      return l.value ? c : v;
    }), u = computed(() => ({ mode: t.mode, css: true, ...n.value })), f = computed(
      () => t.transProps !== void 0 ? {
        ...u.value,
        ...t.transProps
      } : a.value ? {
        ...u.value,
        appear: true,
        appearActiveClass: n.value.enterActiveClass,
        appearToClass: n.value.enterToClass
      } : u.value
    );
    return (c, v) => (openBlock(), createBlock(Transition, normalizeProps(guardReactiveProps(unref(f))), {
      default: withCtx(() => [
        renderSlot(c.$slots, "default")
      ]),
      _: 3
    }, 16));
  }
});
var Yo = "toast-title";
var ba = 1e3;
var hl = defineComponent({
  components: { BLink: Ae },
  props: {
    ...Ue,
    delay: { type: Number, default: 5e3 },
    bodyClass: { type: String },
    body: { type: [Object, String] },
    headerClass: { type: String },
    headerTag: { type: String, default: "div" },
    animation: { type: [Boolean, String], default: true },
    id: { type: String },
    isStatus: { type: [Boolean, String], default: false },
    autoHide: { type: [Boolean, String], default: true },
    noCloseButton: { type: [Boolean, String], default: false },
    noFade: { type: [Boolean, String], default: false },
    noHoverPause: { type: [Boolean, String], default: false },
    solid: { type: [Boolean, String], default: false },
    static: { type: [Boolean, String], default: false },
    title: { type: String },
    modelValue: { type: [Boolean, String], default: false },
    toastClass: { type: Array },
    variant: { type: String }
  },
  emits: ["destroyed", "update:modelValue"],
  setup(e, { emit: t, slots: a }) {
    r(toRef(e, "animation"));
    const l = r(toRef(e, "isStatus")), n = r(toRef(e, "autoHide")), u = r(toRef(e, "noCloseButton")), f = r(toRef(e, "noFade")), c = r(toRef(e, "noHoverPause"));
    r(toRef(e, "solid")), r(toRef(e, "static"));
    const v = r(toRef(e, "modelValue")), p = ref(false), $ = ref(false), m = ref(false), B = computed(() => ({
      [`b-toast-${e.variant}`]: e.variant !== void 0,
      show: m.value || p.value
    }));
    let V, T, b;
    const k = () => {
      typeof V > "u" || (clearTimeout(V), V = void 0);
    }, y = computed(
      () => Math.max(Le(e.delay, 0), ba)
    ), C = () => {
      v.value && (T = b = 0, k(), $.value = true, it(() => {
        m.value = false;
      }));
    }, q = () => {
      k(), t("update:modelValue", true), T = b = 0, $.value = false, nextTick(() => {
        it(() => {
          m.value = true;
        });
      });
    }, E = () => {
      if (!n.value || c.value || !V || b)
        return;
      const ee = Date.now() - T;
      ee > 0 && (k(), b = Math.max(y.value - ee, ba));
    }, I = () => {
      (!n.value || c.value || !b) && (b = T = 0), _();
    };
    watch(
      () => v.value,
      (ee) => {
        ee ? q() : C();
      }
    );
    const _ = () => {
      k(), n.value && (V = setTimeout(C, b || y.value), T = Date.now(), b = 0);
    }, z = () => {
      p.value = true, t("update:modelValue", true);
    }, x = () => {
      p.value = false, _();
    }, w = () => {
      p.value = true;
    }, P = () => {
      p.value = false, b = T = 0, t("update:modelValue", false);
    };
    onUnmounted(() => {
      k(), n.value && t("destroyed", e.id);
    }), onMounted(() => {
      nextTick(() => {
        v.value && it(() => {
          q();
        });
      });
    });
    const L = () => {
      nextTick(() => {
        it(() => {
          C();
        });
      });
    };
    return () => {
      const ee = () => {
        const Z = [], se = Fe(Yo, { hide: C }, a);
        se ? Z.push(h(se)) : e.title && Z.push(h("strong", { class: "me-auto" }, e.title)), !u.value && Z.length !== 0 && Z.push(
          h(Ze, {
            class: ["btn-close"],
            onClick: () => {
              C();
            }
          })
        );
        const K = [];
        if (Z.length > 0 && K.push(
          h(
            e.headerTag,
            {
              class: "toast-header"
            },
            { default: () => Z }
          )
        ), Fe("default", { hide: C }, a) || e.body) {
          const ve = h(
            at(e) ? "b-link" : "div",
            {
              class: ["toast-body", e.bodyClass],
              onClick: at(e) ? { click: L } : {}
            },
            Fe("default", { hide: C }, a) || e.body
          );
          K.push(ve);
        }
        return h(
          "div",
          {
            class: ["toast", e.toastClass, B.value],
            tabindex: "0"
          },
          K
        );
      };
      return h(
        "div",
        {
          class: ["b-toast"],
          id: e.id,
          role: $.value ? null : l.value ? "status" : "alert",
          "aria-live": $.value ? null : l.value ? "polite" : "assertive",
          "aria-atomic": $.value ? null : "true",
          onmouseenter: E,
          onmouseleave: I
        },
        [
          h(
            wt,
            {
              noFade: f.value,
              onAfterEnter: x,
              onBeforeEnter: z,
              onAfterLeave: P,
              onBeforeLeave: w
            },
            () => [m.value ? ee() : ""]
          )
        ]
      );
    };
  }
});
var Dt = defineComponent({
  __name: "BToaster",
  props: {
    position: { default: "top-right" },
    instance: null
  },
  setup(e) {
    const t = e, a = {
      "top-left": "top-0 start-0",
      "top-center": "top-0 start-50 translate-middle-x",
      "top-right": "top-0 end-0",
      "middle-left": "top-50 start-0 translate-middle-y",
      "middle-center": "top-50 start-50 translate-middle",
      "middle-right": "top-50 end-0 translate-middle-y",
      "bottom-left": "bottom-0 start-0",
      "bottom-center": "bottom-0 start-50 translate-middle-x",
      "bottom-right": "bottom-0 end-0"
    }, l = computed(() => a[t.position]), n = (u) => {
      var f;
      (f = t.instance) == null || f.remove(u);
    };
    return (u, f) => {
      var c;
      return openBlock(), createElementBlock("div", {
        class: normalizeClass([[unref(l)], "b-toaster position-fixed p-3"]),
        style: { "z-index": "11" }
      }, [
        (openBlock(true), createElementBlock(Fragment, null, renderList((c = e.instance) == null ? void 0 : c.toasts(e.position).value, (v) => (openBlock(), createBlock(hl, {
          id: v.options.id,
          key: v.options.id,
          modelValue: v.options.value,
          "onUpdate:modelValue": (p) => v.options.value = p,
          "auto-hide": v.options.autoHide,
          delay: v.options.delay,
          "no-close-button": v.options.noCloseButton,
          title: v.content.title,
          body: v.content.body,
          component: v.content.body,
          variant: v.options.variant,
          onDestroyed: n
        }, null, 8, ["id", "modelValue", "onUpdate:modelValue", "auto-hide", "delay", "no-close-button", "title", "body", "component", "variant"]))), 128))
      ], 2);
    };
  }
});
var Zo = defineComponent({
  props: {
    gutterX: { type: String, default: null },
    gutterY: { type: String, default: null },
    fluid: { type: [Boolean, String], default: false },
    toast: { type: Object },
    position: { type: String, required: false }
  },
  setup(e, { slots: t, expose: a }) {
    const l = ref();
    let n;
    const u = computed(() => ({
      container: !e.fluid,
      ["container-fluid"]: typeof e.fluid == "boolean" && e.fluid,
      [`container-${e.fluid}`]: typeof e.fluid == "string",
      [`gx-${e.gutterX}`]: e.gutterX !== null,
      [`gy-${e.gutterY}`]: e.gutterY !== null
    }));
    return onMounted(() => {
      e.toast;
    }), e.toast && (n = pl({ container: l, root: e.toast.root }), a({})), () => {
      var c;
      const f = [];
      return n == null || n.containerPositions.value.forEach((v) => {
        f.push(h(Dt, { key: v, instance: n, position: v }));
      }), h("div", { class: [u.value, e.position], ref: l }, [
        ...f,
        (c = t.default) == null ? void 0 : c.call(t)
      ]);
    };
  },
  methods: {}
});
var Qo = { class: "visually-hidden" };
var es = ["aria-labelledby", "role"];
var yl = defineComponent({
  __name: "BDropdown",
  props: {
    id: null,
    menuClass: null,
    size: null,
    splitClass: null,
    splitVariant: null,
    text: null,
    toggleClass: null,
    autoClose: { type: [Boolean, String], default: true },
    block: { default: false },
    boundary: { default: "clippingParents" },
    dark: { default: false },
    disabled: { default: false },
    isNav: { default: false },
    dropup: { default: false },
    dropright: { default: false },
    dropleft: { default: false },
    noFlip: { default: false },
    offset: { default: 0 },
    popperOpts: { default: () => ({}) },
    right: { default: false },
    role: { default: "menu" },
    split: { default: false },
    splitButtonType: { default: "button" },
    splitHref: { default: void 0 },
    noCaret: { default: false },
    toggleText: { default: "Toggle dropdown" },
    variant: { default: "secondary" }
  },
  emits: ["show", "shown", "hide", "hidden", "click", "toggle"],
  setup(e, { expose: t, emit: a }) {
    const l = e, n = $e(toRef(l, "id"), "dropdown"), u = r(toRef(l, "block")), f = r(toRef(l, "dark")), c = r(toRef(l, "dropup")), v = r(toRef(l, "dropright")), p = r(toRef(l, "isNav")), $ = r(toRef(l, "dropleft")), m = r(toRef(l, "right")), B = r(toRef(l, "split")), V = r(toRef(l, "noCaret")), T = ref(), b = ref(), k = ref(), y = computed(() => ({
      "d-grid": u.value,
      "d-flex": u.value && B.value
    })), C = computed(() => [
      B.value ? l.splitClass : l.toggleClass,
      {
        "nav-link": p.value,
        "dropdown-toggle": !B.value,
        "dropdown-toggle-no-caret": V.value && !B.value,
        "w-100": B.value && u.value
      }
    ]), q = computed(() => [
      l.menuClass,
      {
        "dropdown-menu-dark": f.value,
        "dropdown-menu-end": m.value
      }
    ]), E = computed(() => ({
      "data-bs-toggle": B.value ? void 0 : "dropdown",
      "aria-expanded": B.value ? void 0 : false,
      ref: B.value ? void 0 : b,
      href: B.value ? l.splitHref : void 0
    })), I = computed(() => ({
      ref: B.value ? b : void 0
    })), _ = () => {
      var x;
      (x = k.value) == null || x.hide();
    }, z = (x) => {
      B.value && a("click", x);
    };
    return ye(T, "show.bs.dropdown", () => a("show")), ye(T, "shown.bs.dropdown", () => a("shown")), ye(T, "hide.bs.dropdown", () => a("hide")), ye(T, "hidden.bs.dropdown", () => a("hidden")), onMounted(() => {
      var x;
      k.value = new Dropdown((x = b.value) == null ? void 0 : x.$el, {
        autoClose: l.autoClose,
        boundary: l.boundary,
        offset: l.offset ? l.offset.toString() : "",
        reference: l.offset || B.value ? "parent" : "toggle",
        popperConfig: (w) => {
          const P = {
            placement: "bottom-start",
            modifiers: l.noFlip ? [
              {
                name: "flip",
                options: {
                  fallbackPlacements: []
                }
              }
            ] : []
          };
          return c.value ? P.placement = m.value ? "top-end" : "top-start" : v.value ? P.placement = "right-start" : $.value ? P.placement = "left-start" : m.value && (P.placement = "bottom-end"), zt(w, zt(P, l.popperOpts));
        }
      });
    }), t({
      hide: _
    }), (x, w) => (openBlock(), createElementBlock("div", {
      ref_key: "parent",
      ref: T,
      class: normalizeClass([unref(y), "btn-group"])
    }, [
      createVNode(lt, mergeProps({
        id: unref(n),
        variant: e.splitVariant || e.variant,
        size: e.size,
        class: unref(C),
        disabled: e.disabled,
        type: e.splitButtonType
      }, unref(E), { onClick: z }), {
        default: withCtx(() => [
          renderSlot(x.$slots, "button-content", {}, () => [
            createTextVNode(toDisplayString(e.text), 1)
          ])
        ]),
        _: 3
      }, 16, ["id", "variant", "size", "class", "disabled", "type"]),
      unref(B) ? (openBlock(), createBlock(lt, mergeProps({
        key: 0,
        variant: e.variant,
        size: e.size,
        disabled: e.disabled
      }, unref(I), {
        class: [e.toggleClass, "dropdown-toggle-split dropdown-toggle"],
        "data-bs-toggle": "dropdown",
        "aria-expanded": "false",
        onClick: w[0] || (w[0] = (P) => a("toggle"))
      }), {
        default: withCtx(() => [
          createBaseVNode("span", Qo, [
            renderSlot(x.$slots, "toggle-text", {}, () => [
              createTextVNode(toDisplayString(e.toggleText), 1)
            ])
          ])
        ]),
        _: 3
      }, 16, ["variant", "size", "disabled", "class"])) : createCommentVNode("", true),
      createBaseVNode("ul", {
        class: normalizeClass(["dropdown-menu", unref(q)]),
        "aria-labelledby": unref(n),
        role: e.role
      }, [
        renderSlot(x.$slots, "default")
      ], 10, es)
    ], 2));
  }
});
var ts = { role: "presentation" };
var as = defineComponent({
  __name: "BDropdownDivider",
  props: {
    tag: { default: "hr" }
  },
  setup(e) {
    return (t, a) => (openBlock(), createElementBlock("li", ts, [
      (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
        class: "dropdown-divider",
        role: "separator",
        "aria-orientation": "horizontal"
      }))
    ]));
  }
});
var ls = {};
var ns = { role: "presentation" };
var os = { class: "px-4 py-3" };
function ss(e, t) {
  return openBlock(), createElementBlock("li", ns, [
    createBaseVNode("form", os, [
      renderSlot(e.$slots, "default")
    ])
  ]);
}
var is = Te(ls, [["render", ss]]);
var rs = { role: "presentation" };
var us = ["id", "aria-describedby"];
var ds = {
  inheritAttrs: false
};
var cs = defineComponent({
  ...ds,
  __name: "BDropdownGroup",
  props: {
    id: null,
    ariaDescribedby: null,
    header: null,
    headerClass: null,
    headerTag: { default: "header" },
    headerVariant: null
  },
  setup(e) {
    const t = e, a = computed(
      () => t.id ? `${t.id}_group_dd_header` : void 0
    ), l = computed(
      () => t.headerTag === "header" ? void 0 : "heading"
    ), n = computed(() => [
      t.headerClass,
      {
        [`text-${t.headerVariant}`]: t.headerVariant !== void 0
      }
    ]);
    return (u, f) => (openBlock(), createElementBlock("li", rs, [
      (openBlock(), createBlock(resolveDynamicComponent(e.headerTag), {
        id: unref(a),
        class: normalizeClass(["dropdown-header", unref(n)]),
        role: unref(l)
      }, {
        default: withCtx(() => [
          renderSlot(u.$slots, "header", {}, () => [
            createTextVNode(toDisplayString(e.header), 1)
          ])
        ]),
        _: 3
      }, 8, ["id", "class", "role"])),
      createBaseVNode("ul", mergeProps({
        id: e.id,
        role: "group",
        class: "list-unstyled"
      }, u.$attrs, {
        "aria-describedby": e.ariaDescribedby || unref(a)
      }), [
        renderSlot(u.$slots, "default")
      ], 16, us)
    ]));
  }
});
var fs = {};
var vs = { class: "dropdown-header" };
function ms(e, t) {
  return openBlock(), createElementBlock("li", null, [
    createBaseVNode("h6", vs, [
      renderSlot(e.$slots, "default")
    ])
  ]);
}
var bs = Te(fs, [["render", ms]]);
var gs = {
  inheritAttrs: false
};
var ps = defineComponent({
  ...gs,
  __name: "BDropdownItem",
  props: {
    href: null,
    linkClass: null,
    active: { default: false },
    disabled: { default: false },
    rel: { default: void 0 },
    target: { default: "_self" },
    variant: null
  },
  emits: ["click"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "active")), n = r(toRef(a, "disabled")), u = useAttrs(), f = computed(() => [
      a.linkClass,
      {
        active: l.value,
        disabled: n.value,
        [`text-${a.variant}`]: a.variant !== void 0
      }
    ]), c = computed(
      () => a.href ? "a" : u.to ? Ae : "button"
    ), v = computed(() => ({
      disabled: n.value,
      "aria-current": l.value ? "true" : null,
      href: c.value === "a" ? a.href : null,
      rel: a.rel,
      type: c.value === "button" ? "button" : null,
      target: a.target,
      ...u.to ? { activeClass: "active", ...u } : {}
    })), p = ($) => t("click", $);
    return ($, m) => (openBlock(), createElementBlock("li", {
      role: "presentation",
      class: normalizeClass($.$attrs.class)
    }, [
      (openBlock(), createBlock(resolveDynamicComponent(unref(c)), mergeProps({
        class: ["dropdown-item", unref(f)]
      }, unref(v), { onClick: p }), {
        default: withCtx(() => [
          renderSlot($.$slots, "default")
        ]),
        _: 3
      }, 16, ["class"]))
    ], 2));
  }
});
var hs = ["disabled"];
var ys = {
  inheritAttrs: false
};
var Bs = defineComponent({
  ...ys,
  __name: "BDropdownItemButton",
  props: {
    buttonClass: null,
    active: { default: false },
    activeClass: { default: "active" },
    disabled: { default: false },
    variant: null
  },
  emits: ["click"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "active")), n = r(toRef(a, "disabled")), u = computed(() => [
      a.buttonClass,
      {
        [a.activeClass]: l.value,
        disabled: n.value,
        [`text-${a.variant}`]: a.variant !== void 0
      }
    ]), f = (c) => t("click", c);
    return (c, v) => (openBlock(), createElementBlock("li", {
      role: "presentation",
      class: normalizeClass(c.$attrs.class)
    }, [
      createBaseVNode("button", {
        role: "menu",
        type: "button",
        class: normalizeClass(["dropdown-item", unref(u)]),
        disabled: unref(n),
        onClick: f
      }, [
        renderSlot(c.$slots, "default")
      ], 10, hs)
    ], 2));
  }
});
var $s = { role: "presentation" };
var ks = { class: "px-4 py-1 mb-0 text-muted" };
var Cs = defineComponent({
  __name: "BDropdownText",
  props: {
    text: { default: "" }
  },
  setup(e) {
    return (t, a) => (openBlock(), createElementBlock("li", $s, [
      createBaseVNode("p", ks, [
        renderSlot(t.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ])
    ]));
  }
});
var Ss = ["id", "novalidate", "onSubmit"];
var Bl = defineComponent({
  __name: "BForm",
  props: {
    id: null,
    floating: { default: false },
    novalidate: { default: false },
    validated: { default: false }
  },
  emits: ["submit"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "floating")), n = r(toRef(a, "novalidate")), u = r(toRef(a, "validated")), f = computed(() => ({
      "form-floating": l.value,
      "was-validated": u.value
    })), c = (v) => t("submit", v);
    return (v, p) => (openBlock(), createElementBlock("form", {
      id: e.id,
      novalidate: unref(n),
      class: normalizeClass(unref(f)),
      onSubmit: withModifiers(c, ["prevent"])
    }, [
      renderSlot(v.$slots, "default")
    ], 42, Ss));
  }
});
var ws = { class: "form-floating" };
var Ts = ["for"];
var _s = defineComponent({
  __name: "BFormFloatingLabel",
  props: {
    labelFor: null,
    label: null,
    text: null
  },
  setup(e) {
    return (t, a) => (openBlock(), createElementBlock("div", ws, [
      renderSlot(t.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(e.text), 1)
      ]),
      createBaseVNode("label", { for: e.labelFor }, [
        renderSlot(t.$slots, "label", {}, () => [
          createTextVNode(toDisplayString(e.label), 1)
        ])
      ], 8, Ts)
    ]));
  }
});
var Ht = defineComponent({
  __name: "BFormInvalidFeedback",
  props: {
    ariaLive: null,
    forceShow: { default: false },
    id: null,
    text: null,
    role: null,
    state: { default: void 0 },
    tag: { default: "div" },
    tooltip: { default: false }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "forceShow")), l = r(toRef(t, "state")), n = r(toRef(t, "tooltip")), u = computed(
      () => a.value === true || l.value === false
    ), f = computed(() => ({
      "d-block": u.value,
      "invalid-feedback": !n.value,
      "invalid-tooltip": n.value
    })), c = computed(() => ({
      id: t.id,
      role: t.role,
      "aria-live": t.ariaLive,
      "aria-atomic": t.ariaLive ? "true" : void 0
    }));
    return (v, p) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), mergeProps({ class: unref(f) }, unref(c)), {
      default: withCtx(() => [
        renderSlot(v.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 16, ["class"]));
  }
});
var ft = defineComponent({
  __name: "BFormRow",
  props: {
    tag: { default: "div" }
  },
  setup(e) {
    return (t, a) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), { class: "row d-flex flex-wrap" }, {
      default: withCtx(() => [
        renderSlot(t.$slots, "default")
      ]),
      _: 3
    }));
  }
});
var Rt = defineComponent({
  __name: "BFormText",
  props: {
    id: null,
    inline: { default: false },
    tag: { default: "small" },
    text: null,
    textVariant: { default: "muted" }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "inline")), l = computed(() => [
      [`text-${t.textVariant}`],
      {
        "form-text": !a.value
      }
    ]);
    return (n, u) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: e.id,
      class: normalizeClass(unref(l))
    }, {
      default: withCtx(() => [
        renderSlot(n.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 8, ["id", "class"]));
  }
});
var qt = defineComponent({
  __name: "BFormValidFeedback",
  props: {
    ariaLive: null,
    forceShow: { default: false },
    id: null,
    role: null,
    text: null,
    state: { default: void 0 },
    tag: { default: "div" },
    tooltip: { default: false }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "forceShow")), l = r(toRef(t, "state")), n = r(toRef(t, "tooltip")), u = computed(
      () => a.value === true || l.value === true
    ), f = computed(() => ({
      "d-block": u.value,
      "valid-feedback": !n.value,
      "valid-tooltip": n.value
    })), c = computed(() => t.ariaLive ? "true" : void 0);
    return (v, p) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: e.id,
      role: e.role,
      "aria-live": e.ariaLive,
      "aria-atomic": unref(c),
      class: normalizeClass(unref(f))
    }, {
      default: withCtx(() => [
        renderSlot(v.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }, 8, ["id", "role", "aria-live", "aria-atomic", "class"]));
  }
});
var Vs = ["id", "disabled", "required", "name", "form", "aria-label", "aria-labelledby", "aria-required", "value", "indeterminate"];
var As = ["for"];
var xs = {
  inheritAttrs: false
};
var $l = defineComponent({
  ...xs,
  __name: "BFormCheckbox",
  props: {
    ariaLabel: null,
    ariaLabelledBy: null,
    form: null,
    indeterminate: null,
    name: null,
    id: { default: void 0 },
    autofocus: { default: false },
    plain: { default: false },
    button: { default: false },
    switch: { default: false },
    disabled: { default: false },
    buttonVariant: { default: "secondary" },
    inline: { default: false },
    required: { default: void 0 },
    size: { default: "md" },
    state: { default: void 0 },
    uncheckedValue: { type: [Array, Set, Boolean, String, Object, Number], default: false },
    value: { type: [Array, Set, Boolean, String, Object, Number], default: true },
    modelValue: { type: [Array, Set, Boolean, String, Object, Number], default: void 0 }
  },
  emits: ["update:modelValue", "input", "change"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = $e(toRef(a, "id"), "form-check"), u = r(toRef(a, "indeterminate")), f = r(toRef(a, "autofocus")), c = r(toRef(a, "plain")), v = r(toRef(a, "button")), p = r(toRef(a, "switch")), $ = r(toRef(a, "disabled")), m = r(toRef(a, "inline")), B = r(toRef(a, "required")), V = r(toRef(a, "state")), T = ref(null), b = ref(false), k = computed(() => !Ve(l.default)), y = computed({
      get: () => a.uncheckedValue ? Array.isArray(a.modelValue) ? a.modelValue.indexOf(a.value) > -1 : a.modelValue === a.value : a.modelValue,
      set: (z) => {
        let x = z;
        Array.isArray(a.modelValue) ? a.uncheckedValue && (x = a.modelValue, z ? (x.indexOf(a.uncheckedValue) > -1 && x.splice(x.indexOf(a.uncheckedValue), 1), x.push(a.value)) : (x.indexOf(a.value) > -1 && x.splice(x.indexOf(a.value), 1), x.push(a.uncheckedValue))) : x = z ? a.value : a.uncheckedValue, t("input", x), t("update:modelValue", x), t("change", x);
      }
    }), C = computed(() => Array.isArray(a.modelValue) ? a.modelValue.indexOf(a.value) > -1 : JSON.stringify(a.modelValue) === JSON.stringify(a.value)), q = reactive({
      plain: toRef(c, "value"),
      button: toRef(v, "value"),
      inline: toRef(m, "value"),
      switch: toRef(p, "value"),
      size: toRef(a, "size"),
      state: toRef(V, "value"),
      buttonVariant: toRef(a, "buttonVariant")
    }), E = Ka(q), I = Xa(q), _ = Ja(q);
    return onMounted(() => {
      f.value && T.value.focus();
    }), (z, x) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(unref(E))
    }, [
      withDirectives(createBaseVNode("input", mergeProps({ id: unref(n) }, z.$attrs, {
        ref_key: "input",
        ref: T,
        "onUpdate:modelValue": x[0] || (x[0] = (w) => isRef(y) ? y.value = w : null),
        class: unref(I),
        type: "checkbox",
        disabled: unref($),
        required: !!e.name && !!unref(B),
        name: e.name,
        form: e.form,
        "aria-label": e.ariaLabel,
        "aria-labelledby": e.ariaLabelledBy,
        "aria-required": e.name && unref(B) ? "true" : void 0,
        value: e.value,
        indeterminate: unref(u),
        onFocus: x[1] || (x[1] = (w) => b.value = true),
        onBlur: x[2] || (x[2] = (w) => b.value = false)
      }), null, 16, Vs), [
        [vModelCheckbox, unref(y)]
      ]),
      unref(k) || !unref(c) ? (openBlock(), createElementBlock("label", {
        key: 0,
        for: unref(n),
        class: normalizeClass([unref(_), { active: unref(C), focus: b.value }])
      }, [
        renderSlot(z.$slots, "default")
      ], 10, As)) : createCommentVNode("", true)
    ], 2));
  }
});
var Is = ["id"];
var Fs = ["innerHTML"];
var Ps = ["textContent"];
var Os = defineComponent({
  __name: "BFormCheckboxGroup",
  props: {
    id: null,
    form: null,
    modelValue: { default: () => [] },
    ariaInvalid: { default: void 0 },
    autofocus: { default: false },
    buttonVariant: { default: "secondary" },
    buttons: { default: false },
    disabled: { default: false },
    disabledField: { default: "disabled" },
    htmlField: { default: "html" },
    name: null,
    options: { default: () => [] },
    plain: { default: false },
    required: { default: false },
    size: null,
    stacked: { default: false },
    state: { default: void 0 },
    switches: { default: false },
    textField: { default: "text" },
    validated: { default: false },
    valueField: { default: "value" }
  },
  emits: ["input", "update:modelValue", "change"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = "BFormCheckbox", u = $e(toRef(a, "id"), "checkbox"), f = $e(toRef(a, "name"), "checkbox");
    r(toRef(a, "autofocus"));
    const c = r(toRef(a, "buttons")), v = r(toRef(a, "disabled"));
    r(toRef(a, "plain"));
    const p = r(toRef(a, "required")), $ = r(toRef(a, "stacked")), m = r(toRef(a, "state")), B = r(toRef(a, "switches")), V = r(toRef(a, "validated")), T = computed({
      get: () => a.modelValue,
      set: (q) => {
        if (JSON.stringify(q) === JSON.stringify(a.modelValue))
          return;
        const E = a.options.filter(
          (I) => q.map((_) => JSON.stringify(_)).includes(JSON.stringify(typeof I == "string" ? I : I[a.valueField]))
        ).map((I) => typeof I == "string" ? I : I[a.valueField]);
        t("input", E), t("update:modelValue", E), t("change", E);
      }
    }), b = computed(
      () => (l.first ? gt(l.first(), n, v.value) : []).concat(a.options.map((q) => Qa(q, a))).concat(l.default ? gt(l.default(), n, v.value) : []).map((q, E) => el(q, E, a, f, u)).map((q) => ({
        ...q,
        props: {
          switch: B.value,
          ...q.props
        }
      }))
    ), k = reactive({
      required: toRef(p, "value"),
      ariaInvalid: toRef(a, "ariaInvalid"),
      state: toRef(m, "value"),
      validated: toRef(V, "value"),
      buttons: toRef(c, "value"),
      stacked: toRef($, "value"),
      size: toRef(a, "size")
    }), y = Ya(k), C = Za(k);
    return (q, E) => (openBlock(), createElementBlock("div", mergeProps(unref(y), {
      id: unref(u),
      role: "group",
      class: [unref(C), "bv-no-focus-ring"],
      tabindex: "-1"
    }), [
      (openBlock(true), createElementBlock(Fragment, null, renderList(unref(b), (I, _) => (openBlock(), createBlock($l, mergeProps({
        key: _,
        modelValue: unref(T),
        "onUpdate:modelValue": E[0] || (E[0] = (z) => isRef(T) ? T.value = z : null)
      }, I.props), {
        default: withCtx(() => [
          I.html ? (openBlock(), createElementBlock("span", {
            key: 0,
            innerHTML: I.html
          }, null, 8, Fs)) : (openBlock(), createElementBlock("span", {
            key: 1,
            textContent: toDisplayString(I.text)
          }, null, 8, Ps))
        ]),
        _: 2
      }, 1040, ["modelValue"]))), 128))
    ], 16, Is));
  }
});
var kl = ["input", "select", "textarea"];
var Ls = kl.map((e) => `${e}:not([disabled])`).join();
var zs = [...kl, "a", "button", "label"];
var Ns = "label";
var Es = "invalid-feedback";
var Ds = "valid-feedback";
var Hs = "description";
var Rs = "default";
var qs = defineComponent({
  components: { BCol: et, BFormInvalidFeedback: Ht, BFormRow: ft, BFormText: Rt, BFormValidFeedback: qt },
  props: {
    contentCols: { type: [Boolean, String, Number], required: false },
    contentColsLg: { type: [Boolean, String, Number], required: false },
    contentColsMd: { type: [Boolean, String, Number], required: false },
    contentColsSm: { type: [Boolean, String, Number], required: false },
    contentColsXl: { type: [Boolean, String, Number], required: false },
    description: { type: [String], required: false },
    disabled: { type: [Boolean, String], default: false },
    feedbackAriaLive: { type: String, default: "assertive" },
    id: { type: String, required: false },
    invalidFeedback: { type: String, required: false },
    label: { type: String, required: false },
    labelAlign: { type: [Boolean, String, Number], required: false },
    labelAlignLg: { type: [Boolean, String, Number], required: false },
    labelAlignMd: { type: [Boolean, String, Number], required: false },
    labelAlignSm: { type: [Boolean, String, Number], required: false },
    labelAlignXl: { type: [Boolean, String, Number], required: false },
    labelClass: { type: [Array, Object, String], required: false },
    labelCols: { type: [Boolean, String, Number], required: false },
    labelColsLg: { type: [Boolean, String, Number], required: false },
    labelColsMd: { type: [Boolean, String, Number], required: false },
    labelColsSm: { type: [Boolean, String, Number], required: false },
    labelColsXl: { type: [Boolean, String, Number], required: false },
    labelFor: { type: String, required: false },
    labelSize: { type: String, required: false },
    labelSrOnly: { type: [Boolean, String], default: false },
    state: { type: [Boolean, String], default: null },
    tooltip: { type: [Boolean, String], default: false },
    validFeedback: { type: String, required: false },
    validated: { type: [Boolean, String], default: false },
    floating: { type: [Boolean, String], default: false }
  },
  setup(e, { attrs: t }) {
    const a = r(toRef(e, "disabled")), l = r(toRef(e, "labelSrOnly")), n = r(toRef(e, "state")), u = r(toRef(e, "tooltip")), f = r(toRef(e, "validated")), c = r(toRef(e, "floating")), v = null, p = ["xs", "sm", "md", "lg", "xl"], $ = (_, z) => p.reduce((x, w) => {
      const P = sa(w === "xs" ? "" : w, `${z}Align`), L = _[P] || null;
      return L && (w === "xs" ? x.push(`text-${L}`) : x.push(`text-${w}-${L}`)), x;
    }, []), m = (_, z) => p.reduce((x, w) => {
      const P = sa(w === "xs" ? "" : w, `${z}Cols`);
      let L = _[P];
      return L = L === "" ? true : L || false, typeof L != "boolean" && L !== "auto" && (L = Qe(L, 0), L = L > 0 ? L : false), L && (w === "xs" ? x.cols = L : x[w || (typeof L == "boolean" ? "col" : "cols")] = L), x;
    }, {}), B = ref(), V = (_, z = null) => {
      if (Na && e.labelFor) {
        const x = Ma(`#${bn(e.labelFor)}`, B);
        if (x) {
          const w = "aria-describedby", P = (_ || "").split(dt), L = (z || "").split(dt), ee = (Xt(x, w) || "").split(dt).filter((Z) => !L.includes(Z)).concat(P).filter((Z, se, K) => K.indexOf(Z) === se).filter((Z) => Z).join(" ").trim();
          ee ? An(x, w, ee) : xn(x, w);
        }
      }
    }, T = computed(() => m(e, "content")), b = computed(() => $(e, "label")), k = computed(() => m(e, "label")), y = computed(
      () => Object.keys(T.value).length > 0 || Object.keys(k.value).length > 0
    ), C = computed(
      () => typeof n.value == "boolean" ? n.value : null
    ), q = computed(() => {
      const _ = C.value;
      return _ === true ? "is-valid" : _ === false ? "is-invalid" : null;
    }), E = computed(
      () => Ct(t.ariaInvalid, n.value)
    );
    return watch(
      () => v,
      (_, z) => {
        _ !== z && V(_, z);
      }
    ), onMounted(() => {
      nextTick(() => {
        V(v);
      });
    }), {
      disabledBoolean: a,
      labelSrOnlyBoolean: l,
      stateBoolean: n,
      tooltipBoolean: u,
      validatedBoolean: f,
      floatingBoolean: c,
      ariaDescribedby: v,
      computedAriaInvalid: E,
      contentColProps: T,
      isHorizontal: y,
      labelAlignClasses: b,
      labelColProps: k,
      onLegendClick: (_) => {
        if (e.labelFor)
          return;
        const { target: z } = _, x = z ? z.tagName : "";
        if (zs.indexOf(x) !== -1)
          return;
        const w = _n(Ls, B).filter(Tn);
        w.length === 1 && Sn(w[0]);
      },
      stateClass: q
    };
  },
  render() {
    const e = this.$props, t = this.$slots, a = $e(), l = !e.labelFor;
    let n = null;
    const u = Fe(Ns, {}, t) || e.label, f = u ? Ee("_BV_label_") : null;
    if (u || this.isHorizontal) {
      const E = l ? "legend" : "label";
      if (this.labelSrOnlyBoolean)
        u && (n = h(
          E,
          {
            class: "visually-hidden",
            id: f,
            for: e.labelFor || null
          },
          u
        )), this.isHorizontal ? n = h(et, this.labelColProps, { default: () => n }) : n = h("div", {}, [n]);
      else {
        const I = {
          onClick: l ? this.onLegendClick : null,
          ...this.isHorizontal ? this.labelColProps : {},
          tag: this.isHorizontal ? E : null,
          id: f,
          for: e.labelFor || null,
          tabIndex: l ? "-1" : null,
          class: [
            this.isHorizontal ? "col-form-label" : "form-label",
            {
              "bv-no-focus-ring": l,
              "col-form-label": this.isHorizontal || l,
              "pt-0": !this.isHorizontal && l,
              "d-block": !this.isHorizontal && !l,
              [`col-form-label-${e.labelSize}`]: !!e.labelSize
            },
            this.labelAlignClasses,
            e.labelClass
          ]
        };
        this.isHorizontal ? n = h(et, I, { default: () => u }) : n = h(E, I, u);
      }
    }
    let c = null;
    const v = Fe(Es, {}, t) || this.invalidFeedback, p = v ? Ee("_BV_feedback_invalid_") : void 0;
    v && (c = h(
      Ht,
      {
        ariaLive: e.feedbackAriaLive,
        id: p,
        state: this.stateBoolean,
        tooltip: this.tooltipBoolean
      },
      { default: () => v }
    ));
    let $ = null;
    const m = Fe(Ds, {}, t) || this.validFeedback, B = m ? Ee("_BV_feedback_valid_") : void 0;
    m && ($ = h(
      qt,
      {
        ariaLive: e.feedbackAriaLive,
        id: B,
        state: this.stateBoolean,
        tooltip: this.tooltipBoolean
      },
      { default: () => m }
    ));
    let V = null;
    const T = Fe(Hs, {}, t) || this.description, b = T ? Ee("_BV_description_") : void 0;
    T && (V = h(
      Rt,
      {
        id: b
      },
      { default: () => T }
    ));
    const k = this.ariaDescribedby = [
      b,
      this.stateBoolean === false ? p : null,
      this.stateBoolean === true ? B : null
    ].filter((E) => E).join(" ") || null, y = [
      Fe(Rs, { ariaDescribedby: k, descriptionId: b, id: a, labelId: f }, t) || "",
      c,
      $,
      V
    ];
    !this.isHorizontal && this.floatingBoolean && y.push(n);
    let C = h(
      "div",
      {
        ref: "content",
        class: [
          {
            "form-floating": !this.isHorizontal && this.floatingBoolean
          }
        ]
      },
      y
    );
    this.isHorizontal && (C = h(et, { ref: "content", ...this.contentColProps }, { default: () => y }));
    const q = {
      class: [
        "mb-3",
        this.stateClass,
        {
          "was-validated": this.validatedBoolean
        }
      ],
      id: $e(toRef(e, "id")).value,
      disabled: l ? this.disabledBoolean : null,
      role: l ? null : "group",
      "aria-invalid": this.computedAriaInvalid,
      "aria-labelledby": l && this.isHorizontal ? f : null
    };
    return this.isHorizontal && !l ? h(ft, q, { default: () => [n, C] }) : h(
      l ? "fieldset" : "div",
      q,
      this.isHorizontal && l ? [h(ft, {}, { default: () => [n, C] })] : this.isHorizontal || !this.floatingBoolean ? [n, C] : [C]
    );
  }
});
var ga = [
  "text",
  "number",
  "email",
  "password",
  "search",
  "url",
  "tel",
  "date",
  "time",
  "range",
  "color"
];
var Ms = defineComponent({
  props: {
    ...tl,
    max: { type: [String, Number], required: false },
    min: { type: [String, Number], required: false },
    step: { type: [String, Number], required: false },
    type: {
      type: String,
      default: "text",
      validator: (e) => ga.includes(e)
    }
  },
  emits: ["update:modelValue", "change", "blur", "input"],
  setup(e, { emit: t }) {
    const { input: a, computedId: l, computedAriaInvalid: n, onInput: u, onChange: f, onBlur: c, focus: v, blur: p } = al(e, t), $ = ref(false), m = computed(() => {
      const T = e.type === "range", b = e.type === "color";
      return {
        "form-control-highlighted": $.value,
        "form-range": T,
        "form-control": b || !e.plaintext && !T,
        "form-control-color": b,
        "form-control-plaintext": e.plaintext && !T && !b,
        [`form-control-${e.size}`]: !!e.size,
        "is-valid": e.state === true,
        "is-invalid": e.state === false
      };
    }), B = computed(
      () => ga.includes(e.type) ? e.type : "text"
    );
    return {
      computedClasses: m,
      localType: B,
      input: a,
      computedId: l,
      computedAriaInvalid: n,
      onInput: u,
      onChange: f,
      onBlur: c,
      focus: v,
      blur: p,
      highlight: () => {
        $.value !== true && ($.value = true, setTimeout(() => {
          $.value = false;
        }, 2e3));
      }
    };
  }
});
var js = ["id", "name", "form", "type", "disabled", "placeholder", "required", "autocomplete", "readonly", "min", "max", "step", "list", "aria-required", "aria-invalid"];
function Gs(e, t, a, l, n, u) {
  return openBlock(), createElementBlock("input", mergeProps({
    id: e.computedId,
    ref: "input",
    class: e.computedClasses,
    name: e.name || void 0,
    form: e.form || void 0,
    type: e.localType,
    disabled: e.disabled,
    placeholder: e.placeholder,
    required: e.required,
    autocomplete: e.autocomplete || void 0,
    readonly: e.readonly || e.plaintext,
    min: e.min,
    max: e.max,
    step: e.step,
    list: e.type !== "password" ? e.list : void 0,
    "aria-required": e.required ? "true" : void 0,
    "aria-invalid": e.computedAriaInvalid
  }, e.$attrs, {
    onInput: t[0] || (t[0] = (f) => e.onInput(f)),
    onChange: t[1] || (t[1] = (f) => e.onChange(f)),
    onBlur: t[2] || (t[2] = (f) => e.onBlur(f))
  }), null, 16, js);
}
var Us = Te(Ms, [["render", Gs]]);
var Ws = ["id", "disabled", "required", "name", "form", "aria-label", "aria-labelledby", "value", "aria-required"];
var Ks = ["for"];
var Cl = defineComponent({
  __name: "BFormRadio",
  props: {
    ariaLabel: null,
    ariaLabelledby: null,
    form: null,
    id: null,
    name: null,
    size: null,
    autofocus: { default: false },
    modelValue: { type: [Boolean, String, Array, Object, Number], default: void 0 },
    plain: { default: false },
    button: { default: false },
    switch: { default: false },
    disabled: { default: false },
    buttonVariant: { default: "secondary" },
    inline: { default: false },
    required: { default: false },
    state: { default: void 0 },
    value: { type: [String, Boolean, Object, Number], default: true }
  },
  emits: ["input", "change", "update:modelValue"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = $e(toRef(a, "id"), "form-check"), u = r(toRef(a, "autofocus")), f = r(toRef(a, "plain")), c = r(toRef(a, "button")), v = r(toRef(a, "switch")), p = r(toRef(a, "disabled")), $ = r(toRef(a, "inline")), m = r(toRef(a, "required")), B = r(toRef(a, "state")), V = ref(null), T = ref(false), b = computed({
      get: () => Array.isArray(a.modelValue) ? a.modelValue[0] : a.modelValue,
      set: (_) => {
        const z = _ ? a.value : false, x = Array.isArray(a.modelValue) ? [z] : z;
        t("input", x), t("change", x), t("update:modelValue", x);
      }
    }), k = computed(() => Array.isArray(a.modelValue) ? (a.modelValue || []).find((_) => _ === a.value) : JSON.stringify(a.modelValue) === JSON.stringify(a.value)), y = computed(() => !Ve(l.default)), C = reactive({
      plain: toRef(f, "value"),
      button: toRef(c, "value"),
      inline: toRef($, "value"),
      switch: toRef(v, "value"),
      size: toRef(a, "size"),
      state: toRef(B, "value"),
      buttonVariant: toRef(a, "buttonVariant")
    }), q = Ka(C), E = Xa(C), I = Ja(C);
    return onMounted(() => {
      u.value && V.value !== null && V.value.focus();
    }), (_, z) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(unref(q))
    }, [
      withDirectives(createBaseVNode("input", mergeProps({ id: unref(n) }, _.$attrs, {
        ref_key: "input",
        ref: V,
        "onUpdate:modelValue": z[0] || (z[0] = (x) => isRef(b) ? b.value = x : null),
        class: unref(E),
        type: "radio",
        disabled: unref(p),
        required: !!e.name && unref(m),
        name: e.name,
        form: e.form,
        "aria-label": e.ariaLabel,
        "aria-labelledby": e.ariaLabelledby,
        value: e.value,
        "aria-required": !!e.name && unref(m) ? true : void 0,
        onFocus: z[1] || (z[1] = (x) => T.value = true),
        onBlur: z[2] || (z[2] = (x) => T.value = false)
      }), null, 16, Ws), [
        [vModelRadio, unref(b)]
      ]),
      unref(y) || unref(f) === false ? (openBlock(), createElementBlock("label", {
        key: 0,
        for: unref(n),
        class: normalizeClass([unref(I), { active: unref(k), focus: T.value }])
      }, [
        renderSlot(_.$slots, "default")
      ], 10, Ks)) : createCommentVNode("", true)
    ], 2));
  }
});
var Xs = ["id"];
var Js = ["innerHTML"];
var Ys = ["textContent"];
var Zs = defineComponent({
  __name: "BFormRadioGroup",
  props: {
    size: null,
    form: null,
    id: null,
    name: null,
    modelValue: { type: [String, Boolean, Array, Object, Number], default: "" },
    ariaInvalid: { default: void 0 },
    autofocus: { default: false },
    buttonVariant: { default: "secondary" },
    buttons: { default: false },
    disabled: { default: false },
    disabledField: { default: "disabled" },
    htmlField: { default: "html" },
    options: { default: () => [] },
    plain: { default: false },
    required: { default: false },
    stacked: { default: false },
    state: { default: void 0 },
    textField: { default: "text" },
    validated: { default: false },
    valueField: { default: "value" }
  },
  emits: ["input", "update:modelValue", "change"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = "BFormRadio", u = $e(toRef(a, "id"), "radio"), f = $e(toRef(a, "name"), "checkbox");
    r(toRef(a, "autofocus"));
    const c = r(toRef(a, "buttons")), v = r(toRef(a, "disabled"));
    r(toRef(a, "plain"));
    const p = r(toRef(a, "required")), $ = r(toRef(a, "stacked")), m = r(toRef(a, "state")), B = r(toRef(a, "validated")), V = computed({
      get: () => a.modelValue,
      set: (C) => {
        t("input", C), t("update:modelValue", C), t("change", C);
      }
    }), T = computed(
      () => (l.first ? gt(l.first(), n, v.value) : []).concat(a.options.map((C) => Qa(C, a))).concat(l.default ? gt(l.default(), n, v.value) : []).map((C, q) => el(C, q, a, f, u)).map((C) => ({
        ...C
      }))
    ), b = reactive({
      required: toRef(p, "value"),
      ariaInvalid: toRef(a, "ariaInvalid"),
      state: toRef(m, "value"),
      validated: toRef(B, "value"),
      buttons: toRef(c, "value"),
      stacked: toRef($, "value"),
      size: toRef(a, "size")
    }), k = Ya(b), y = Za(b);
    return (C, q) => (openBlock(), createElementBlock("div", mergeProps(unref(k), {
      id: unref(u),
      role: "radiogroup",
      class: [unref(y), "bv-no-focus-ring"],
      tabindex: "-1"
    }), [
      (openBlock(true), createElementBlock(Fragment, null, renderList(unref(T), (E, I) => (openBlock(), createBlock(Cl, mergeProps({
        key: I,
        modelValue: unref(V),
        "onUpdate:modelValue": q[0] || (q[0] = (_) => isRef(V) ? V.value = _ : null)
      }, E.props), {
        default: withCtx(() => [
          E.html ? (openBlock(), createElementBlock("span", {
            key: 0,
            innerHTML: E.html
          }, null, 8, Js)) : (openBlock(), createElementBlock("span", {
            key: 1,
            textContent: toDisplayString(E.text)
          }, null, 8, Ys))
        ]),
        _: 2
      }, 1040, ["modelValue"]))), 128))
    ], 16, Xs));
  }
});
var Qs = ["value", "disabled"];
var ea = defineComponent({
  __name: "BFormSelectOption",
  props: {
    value: null,
    disabled: { default: false }
  },
  setup(e) {
    const a = r(toRef(e, "disabled"));
    return (l, n) => (openBlock(), createElementBlock("option", {
      value: e.value,
      disabled: unref(a)
    }, [
      renderSlot(l.$slots, "default")
    ], 8, Qs));
  }
});
var ei = ["label"];
var Sl = defineComponent({
  __name: "BFormSelectOptionGroup",
  props: {
    label: null,
    disabledField: { default: "disabled" },
    htmlField: { default: "html" },
    options: { default: () => [] },
    textField: { default: "text" },
    valueField: { default: "value" }
  },
  setup(e) {
    const t = e, a = computed(
      () => Yt(t.options, "BFormSelectOptionGroup", t)
    );
    return (l, n) => (openBlock(), createElementBlock("optgroup", { label: e.label }, [
      renderSlot(l.$slots, "first"),
      (openBlock(true), createElementBlock(Fragment, null, renderList(unref(a), (u, f) => (openBlock(), createBlock(ea, mergeProps({
        key: f,
        value: u.value,
        disabled: u.disabled
      }, l.$attrs, {
        innerHTML: u.html || u.text
      }), null, 16, ["value", "disabled", "innerHTML"]))), 128)),
      renderSlot(l.$slots, "default")
    ], 8, ei));
  }
});
var ti = ["id", "name", "form", "multiple", "size", "disabled", "required", "aria-required", "aria-invalid"];
var ai = defineComponent({
  __name: "BFormSelect",
  props: {
    ariaInvalid: { default: void 0 },
    autofocus: { default: false },
    disabled: { default: false },
    disabledField: { default: "disabled" },
    form: null,
    htmlField: { default: "html" },
    id: null,
    labelField: { default: "label" },
    multiple: { default: false },
    name: null,
    options: { default: () => [] },
    optionsField: { default: "options" },
    plain: { default: false },
    required: { default: false },
    selectSize: { default: 0 },
    size: null,
    state: { default: void 0 },
    textField: { default: "text" },
    valueField: { default: "value" },
    modelValue: { default: "" }
  },
  emits: ["input", "update:modelValue", "change"],
  setup(e, { expose: t, emit: a }) {
    const l = e, n = $e(toRef(l, "id"), "input"), u = r(toRef(l, "autofocus")), f = r(toRef(l, "disabled")), c = r(toRef(l, "multiple")), v = r(toRef(l, "plain")), p = r(toRef(l, "required")), $ = r(toRef(l, "state")), m = ref(), B = computed(() => ({
      "form-control": v.value,
      [`form-control-${l.size}`]: l.size && v.value,
      "form-select": !v.value,
      [`form-select-${l.size}`]: l.size && !v.value,
      "is-valid": $.value === true,
      "is-invalid": $.value === false
    })), V = computed(() => {
      if (l.selectSize || v.value)
        return l.selectSize;
    }), T = computed(
      () => Ct(l.ariaInvalid, $.value)
    ), b = computed(
      () => Yt(l.options, "BFormSelect", l)
    ), k = computed({
      get() {
        return l.modelValue;
      },
      set(E) {
        a("change", E), a("update:modelValue", E), a("input", E);
      }
    }), y = () => {
      var E;
      f.value || (E = m.value) == null || E.focus();
    }, C = () => {
      var E;
      f.value || (E = m.value) == null || E.blur();
    }, q = () => {
      nextTick(() => {
        var E;
        u.value && ((E = m.value) == null || E.focus());
      });
    };
    return onMounted(q), onActivated(q), t({
      blur: C,
      focus: y
    }), (E, I) => withDirectives((openBlock(), createElementBlock("select", mergeProps({
      id: unref(n),
      ref_key: "input",
      ref: m
    }, E.$attrs, {
      "onUpdate:modelValue": I[0] || (I[0] = (_) => isRef(k) ? k.value = _ : null),
      class: unref(B),
      name: e.name,
      form: e.form || void 0,
      multiple: unref(c) || void 0,
      size: unref(V),
      disabled: unref(f),
      required: unref(p),
      "aria-required": unref(p) ? true : void 0,
      "aria-invalid": unref(T)
    }), [
      renderSlot(E.$slots, "first"),
      (openBlock(true), createElementBlock(Fragment, null, renderList(unref(b), (_, z) => (openBlock(), createElementBlock(Fragment, { key: z }, [
        Array.isArray(_.options) ? (openBlock(), createBlock(Sl, {
          key: 0,
          label: _.label,
          options: _.options
        }, null, 8, ["label", "options"])) : (openBlock(), createBlock(ea, {
          key: 1,
          value: _.value,
          disabled: _.disabled,
          innerHTML: _.html || _.text
        }, null, 8, ["value", "disabled", "innerHTML"]))
      ], 64))), 128)),
      renderSlot(E.$slots, "default")
    ], 16, ti)), [
      [vModelSelect, unref(k)]
    ]);
  }
});
var li = ["id"];
var wl = defineComponent({
  __name: "BFormTag",
  props: {
    id: null,
    title: null,
    disabled: { default: false },
    noRemove: { default: false },
    pill: { default: false },
    removeLabel: { default: "Remove tag" },
    tag: { default: "span" },
    variant: { default: "secondary" }
  },
  emits: ["remove"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = $e(toRef(a, "id")), u = r(toRef(a, "disabled")), f = r(toRef(a, "noRemove")), c = r(toRef(a, "pill")), v = computed(
      () => {
        var m, B, V;
        return (V = ((B = (m = l.default) == null ? void 0 : m.call(l)[0].children) != null ? B : "").toString() || a.title) != null ? V : "";
      }
    ), p = computed(() => `${n.value}taglabel__`), $ = computed(() => [
      `bg-${a.variant}`,
      {
        "text-dark": ["warning", "info", "light"].includes(a.variant),
        "rounded-pill": c.value,
        disabled: u.value
      }
    ]);
    return (m, B) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: unref(n),
      title: unref(v),
      class: normalizeClass(["badge b-form-tag d-inline-flex align-items-center mw-100", unref($)]),
      "aria-labelledby": unref(p)
    }, {
      default: withCtx(() => [
        createBaseVNode("span", {
          id: unref(p),
          class: "b-form-tag-content flex-grow-1 text-truncate"
        }, [
          renderSlot(m.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(unref(v)), 1)
          ])
        ], 8, li),
        !unref(u) && !unref(f) ? (openBlock(), createBlock(Ze, {
          key: 0,
          "aria-keyshortcuts": "Delete",
          "aria-label": e.removeLabel,
          class: "b-form-tag-remove",
          white: !["warning", "info", "light"].includes(e.variant),
          "aria-describedby": unref(p),
          "aria-controls": e.id,
          onClick: B[0] || (B[0] = (V) => t("remove", unref(v)))
        }, null, 8, ["aria-label", "white", "aria-describedby", "aria-controls"])) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["id", "title", "class", "aria-labelledby"]));
  }
});
var ni = ["id"];
var oi = ["id", "for", "aria-live"];
var si = ["id", "aria-live"];
var ii = ["id"];
var ri = ["aria-controls"];
var ui = {
  role: "group",
  class: "d-flex"
};
var di = ["id", "disabled", "value", "type", "placeholder", "form", "required"];
var ci = ["disabled"];
var fi = {
  "aria-live": "polite",
  "aria-atomic": "true"
};
var vi = {
  key: 0,
  class: "d-block invalid-feedback"
};
var mi = {
  key: 1,
  class: "form-text text-muted"
};
var bi = {
  key: 2,
  class: "form-text text-muted"
};
var gi = ["name", "value"];
var pi = defineComponent({
  __name: "BFormTags",
  props: {
    addButtonText: { default: "Add" },
    addButtonVariant: { default: "outline-secondary" },
    addOnChange: { default: false },
    autofocus: { default: false },
    disabled: { default: false },
    duplicateTagText: { default: "Duplicate tag(s)" },
    inputAttrs: null,
    inputClass: null,
    inputId: null,
    inputType: { default: "text" },
    invalidTagText: { default: "Invalid tag(s)" },
    form: null,
    limit: null,
    limitTagsText: { default: "Tag limit reached" },
    modelValue: { default: () => [] },
    name: null,
    noAddOnEnter: { default: false },
    noOuterFocus: { default: false },
    noTagRemove: { default: false },
    placeholder: { default: "Add tag..." },
    removeOnDelete: { default: false },
    required: { default: false },
    separator: null,
    state: { default: void 0 },
    size: null,
    tagClass: null,
    tagPills: { default: false },
    tagRemoveLabel: null,
    tagRemovedLabel: { default: "Tag removed" },
    tagValidator: { type: Function, default: () => true },
    tagVariant: { default: "secondary" }
  },
  emits: ["update:modelValue", "input", "tag-state", "focus", "focusin", "focusout", "blur"],
  setup(e, { emit: t }) {
    const a = e, l = $e(), n = r(toRef(a, "addOnChange")), u = r(toRef(a, "autofocus")), f = r(toRef(a, "disabled")), c = r(toRef(a, "noAddOnEnter")), v = r(toRef(a, "noOuterFocus")), p = r(toRef(a, "noTagRemove")), $ = r(toRef(a, "removeOnDelete")), m = r(toRef(a, "required")), B = r(toRef(a, "state")), V = r(toRef(a, "tagPills")), T = ref(null), b = computed(() => a.inputId || `${l.value}input__`), k = ref(a.modelValue), y = ref(""), C = ref(false), q = ref(false), E = ref(""), I = ref([]), _ = ref([]), z = ref([]), x = computed(() => ({
      [`form-control-${a.size}`]: a.size !== void 0,
      disabled: f.value,
      focus: q.value,
      "is-invalid": B.value === false,
      "is-valid": B.value === true
    })), w = computed(() => k.value.includes(y.value)), P = computed(
      () => y.value === "" ? false : !a.tagValidator(y.value)
    ), L = computed(() => k.value.length === a.limit), ee = computed(() => !P.value && !w.value), Z = computed(() => ({
      addButtonText: a.addButtonText,
      addButtonVariant: a.addButtonVariant,
      addTag: ie,
      disableAddButton: ee.value,
      disabled: f.value,
      duplicateTagText: a.duplicateTagText,
      duplicateTags: z.value,
      form: a.form,
      inputAttrs: {
        ...a.inputAttrs,
        disabled: f.value,
        form: a.form,
        id: b,
        value: y
      },
      inputHandlers: {
        input: be,
        keydown: Ce,
        change: ge
      },
      inputId: b,
      inputType: a.inputType,
      invalidTagText: a.invalidTagText,
      invalidTags: _.value,
      isDuplicate: w.value,
      isInvalid: P.value,
      isLimitReached: L.value,
      limitTagsText: a.limitTagsText,
      limit: a.limit,
      noTagRemove: p.value,
      placeholder: a.placeholder,
      removeTag: H,
      required: m.value,
      separator: a.separator,
      size: a.size,
      state: B.value,
      tagClass: a.tagClass,
      tagPills: V.value,
      tagRemoveLabel: a.tagRemoveLabel,
      tagVariant: a.tagVariant,
      tags: k.value
    }));
    watch(
      () => a.modelValue,
      (N) => {
        k.value = N;
      }
    );
    const se = () => {
      var N;
      u.value && ((N = T.value) == null || N.focus());
    }, K = (N) => {
      if (f.value) {
        N.target.blur();
        return;
      }
      t("focusin", N);
    }, ve = (N) => {
      f.value || v.value || (q.value = true, t("focus", N));
    }, te = (N) => {
      q.value = false, t("blur", N);
    }, be = (N) => {
      var le, ne;
      const X = typeof N == "string" ? N : N.target.value;
      if (C.value = false, ((le = a.separator) == null ? void 0 : le.includes(X.charAt(0))) && X.length > 0) {
        T.value && (T.value.value = "");
        return;
      }
      if (y.value = X, (ne = a.separator) != null && ne.includes(X.charAt(X.length - 1))) {
        ie(X.slice(0, X.length - 1));
        return;
      }
      I.value = a.tagValidator(X) && !w.value ? [X] : [], _.value = a.tagValidator(X) ? [] : [X], z.value = w.value ? [X] : [], t("tag-state", I.value, _.value, z.value);
    }, ge = (N) => {
      n.value && (be(N), w.value || ie(y.value));
    }, Ce = (N) => {
      if (N.key === "Enter" && !c.value) {
        ie(y.value);
        return;
      }
      (N.key === "Backspace" || N.key === "Delete") && $.value && y.value === "" && C.value && k.value.length > 0 ? H(k.value[k.value.length - 1]) : C.value = true;
    }, ie = (N) => {
      var le;
      if (N = (N || y.value).trim(), N === "" || w.value || !a.tagValidator(N) || a.limit && L.value)
        return;
      const X = [...a.modelValue, N];
      y.value = "", C.value = true, t("update:modelValue", X), t("input", X), (le = T.value) == null || le.focus();
    }, H = (N) => {
      var le;
      const X = k.value.indexOf((le = N == null ? void 0 : N.toString()) != null ? le : "");
      E.value = k.value.splice(X, 1).toString(), t("update:modelValue", k.value);
    };
    return onMounted(() => {
      se(), a.modelValue.length > 0 && (C.value = true);
    }), onActivated(() => se()), (N, X) => (openBlock(), createElementBlock("div", {
      id: unref(l),
      class: normalizeClass(["b-form-tags form-control h-auto", unref(x)]),
      role: "group",
      tabindex: "-1",
      onFocusin: K,
      onFocusout: X[1] || (X[1] = (le) => t("focusout", le))
    }, [
      createBaseVNode("output", {
        id: `${unref(l)}selected_tags__`,
        class: "visually-hidden",
        role: "status",
        for: unref(b),
        "aria-live": q.value ? "polite" : "off",
        "aria-atomic": "true",
        "aria-relevant": "additions text"
      }, toDisplayString(k.value.join(", ")), 9, oi),
      createBaseVNode("div", {
        id: `${unref(l)}removed_tags__`,
        role: "status",
        "aria-live": q.value ? "assertive" : "off",
        "aria-atomic": "true",
        class: "visually-hidden"
      }, " (" + toDisplayString(e.tagRemovedLabel) + ") " + toDisplayString(E.value), 9, si),
      renderSlot(N.$slots, "default", normalizeProps(guardReactiveProps(unref(Z))), () => [
        createBaseVNode("ul", {
          id: `${unref(l)}tag_list__`,
          class: "b-form-tags-list list-unstyled mb-0 d-flex flex-wrap align-items-center"
        }, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(k.value, (le, ne) => renderSlot(N.$slots, "tag", normalizeProps(mergeProps({ key: ne }, { tag: le, tagClass: e.tagClass, tagVariant: e.tagVariant, tagPills: unref(V), removeTag: H })), () => [
            createVNode(wl, {
              class: normalizeClass(e.tagClass),
              tag: "li",
              variant: e.tagVariant,
              pill: e.tagPills,
              onRemove: H
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(le), 1)
              ]),
              _: 2
            }, 1032, ["class", "variant", "pill"])
          ])), 128)),
          createBaseVNode("li", {
            role: "none",
            "aria-live": "off",
            class: "b-from-tags-field flex-grow-1",
            "aria-controls": `${unref(l)}tag_list__`
          }, [
            createBaseVNode("div", ui, [
              createBaseVNode("input", mergeProps({
                id: unref(b),
                ref_key: "input",
                ref: T,
                disabled: unref(f),
                value: y.value,
                type: e.inputType,
                placeholder: e.placeholder,
                class: "b-form-tags-input w-100 flex-grow-1 p-0 m-0 bg-transparent border-0",
                style: { outline: "currentcolor none 0px", "min-width": "5rem" }
              }, e.inputAttrs, {
                form: e.form,
                required: unref(m),
                onInput: be,
                onChange: ge,
                onKeydown: Ce,
                onFocus: ve,
                onBlur: te
              }), null, 16, di),
              unref(ee) ? (openBlock(), createElementBlock("button", {
                key: 0,
                type: "button",
                class: normalizeClass(["btn b-form-tags-button py-0", [
                  `btn-${e.addButtonVariant}`,
                  {
                    "disabled invisible": y.value.length === 0
                  },
                  e.inputClass
                ]]),
                style: { "font-size": "90%" },
                disabled: unref(f) || y.value.length === 0 || unref(L),
                onClick: X[0] || (X[0] = (le) => ie(y.value))
              }, [
                renderSlot(N.$slots, "add-button-text", {}, () => [
                  createTextVNode(toDisplayString(e.addButtonText), 1)
                ])
              ], 10, ci)) : createCommentVNode("", true)
            ])
          ], 8, ri)
        ], 8, ii),
        createBaseVNode("div", fi, [
          unref(P) ? (openBlock(), createElementBlock("div", vi, toDisplayString(e.invalidTagText) + ": " + toDisplayString(y.value), 1)) : createCommentVNode("", true),
          unref(w) ? (openBlock(), createElementBlock("small", mi, toDisplayString(e.duplicateTagText) + ": " + toDisplayString(y.value), 1)) : createCommentVNode("", true),
          k.value.length === e.limit ? (openBlock(), createElementBlock("small", bi, "Tag limit reached")) : createCommentVNode("", true)
        ])
      ]),
      e.name ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(k.value, (le, ne) => (openBlock(), createElementBlock("input", {
        key: ne,
        type: "hidden",
        name: e.name,
        value: le
      }, null, 8, gi))), 128)) : createCommentVNode("", true)
    ], 42, ni));
  }
});
var hi = defineComponent({
  props: {
    ...tl,
    noResize: { type: [Boolean, String], default: false },
    rows: { type: [String, Number], required: false, default: 2 },
    wrap: { type: String, default: "soft" }
  },
  emits: ["update:modelValue", "change", "blur", "input"],
  setup(e, { emit: t }) {
    const { input: a, computedId: l, computedAriaInvalid: n, onInput: u, onChange: f, onBlur: c, focus: v, blur: p } = al(e, t), $ = r(toRef(e, "noResize")), m = computed(() => ({
      "form-control": !e.plaintext,
      "form-control-plaintext": e.plaintext,
      [`form-control-${e.size}`]: !!e.size,
      "is-valid": e.state === true,
      "is-invalid": e.state === false
    })), B = computed(
      () => $.value ? { resize: "none" } : void 0
    );
    return {
      input: a,
      computedId: l,
      computedAriaInvalid: n,
      onInput: u,
      onChange: f,
      onBlur: c,
      focus: v,
      blur: p,
      computedClasses: m,
      computedStyles: B
    };
  }
});
var yi = ["id", "name", "form", "disabled", "placeholder", "required", "autocomplete", "readonly", "aria-required", "aria-invalid", "rows", "wrap"];
function Bi(e, t, a, l, n, u) {
  return openBlock(), createElementBlock("textarea", mergeProps({
    id: e.computedId,
    ref: "input",
    class: e.computedClasses,
    name: e.name || void 0,
    form: e.form || void 0,
    disabled: e.disabled,
    placeholder: e.placeholder,
    required: e.required,
    autocomplete: e.autocomplete || void 0,
    readonly: e.readonly || e.plaintext,
    "aria-required": e.required ? "true" : void 0,
    "aria-invalid": e.computedAriaInvalid,
    rows: e.rows,
    style: e.computedStyles,
    wrap: e.wrap || void 0
  }, e.$attrs, {
    onInput: t[0] || (t[0] = (f) => e.onInput(f)),
    onChange: t[1] || (t[1] = (f) => e.onChange(f)),
    onBlur: t[2] || (t[2] = (f) => e.onBlur(f))
  }), null, 16, yi);
}
var $i = Te(hi, [["render", Bi]]);
var ki = {
  key: 0,
  class: "input-group-text"
};
var Ci = ["innerHTML"];
var Si = { key: 1 };
var wi = {
  key: 0,
  class: "input-group-text"
};
var Ti = ["innerHTML"];
var _i = { key: 1 };
var Vi = defineComponent({
  __name: "BInputGroup",
  props: {
    append: null,
    appendHtml: null,
    id: null,
    prepend: null,
    prependHtml: null,
    size: null,
    tag: { default: "div" }
  },
  setup(e) {
    const t = e, a = computed(() => ({
      "input-group-sm": t.size === "sm",
      "input-group-lg": t.size === "lg"
    })), l = computed(() => !!t.append || !!t.appendHtml), n = computed(() => !!t.prepend || !!t.prependHtml);
    return (u, f) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: e.id,
      class: normalizeClass(["input-group", unref(a)]),
      role: "group"
    }, {
      default: withCtx(() => [
        renderSlot(u.$slots, "prepend", {}, () => [
          unref(n) ? (openBlock(), createElementBlock("span", ki, [
            e.prependHtml ? (openBlock(), createElementBlock("span", {
              key: 0,
              innerHTML: e.prependHtml
            }, null, 8, Ci)) : (openBlock(), createElementBlock("span", Si, toDisplayString(e.prepend), 1))
          ])) : createCommentVNode("", true)
        ]),
        renderSlot(u.$slots, "default"),
        renderSlot(u.$slots, "append", {}, () => [
          unref(l) ? (openBlock(), createElementBlock("span", wi, [
            e.appendHtml ? (openBlock(), createElementBlock("span", {
              key: 0,
              innerHTML: e.appendHtml
            }, null, 8, Ti)) : (openBlock(), createElementBlock("span", _i, toDisplayString(e.append), 1))
          ])) : createCommentVNode("", true)
        ])
      ]),
      _: 3
    }, 8, ["id", "class"]));
  }
});
var Tl = defineComponent({
  __name: "BInputGroupText",
  props: {
    tag: { default: "div" },
    text: null
  },
  setup(e) {
    return (t, a) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), { class: "input-group-text" }, {
      default: withCtx(() => [
        renderSlot(t.$slots, "default", {}, () => [
          createTextVNode(toDisplayString(e.text), 1)
        ])
      ]),
      _: 3
    }));
  }
});
var ta = defineComponent({
  __name: "BInputGroupAddon",
  props: {
    isText: { default: false }
  },
  setup(e) {
    const a = r(toRef(e, "isText"));
    return (l, n) => unref(a) ? (openBlock(), createBlock(Tl, { key: 0 }, {
      default: withCtx(() => [
        renderSlot(l.$slots, "default")
      ]),
      _: 3
    })) : renderSlot(l.$slots, "default", { key: 1 });
  }
});
var Ai = defineComponent({
  __name: "BInputGroupAppend",
  props: {
    isText: { default: false }
  },
  setup(e) {
    return (t, a) => (openBlock(), createBlock(ta, { "is-text": e.isText }, {
      default: withCtx(() => [
        renderSlot(t.$slots, "default")
      ]),
      _: 3
    }, 8, ["is-text"]));
  }
});
var xi = defineComponent({
  __name: "BInputGroupPrepend",
  props: {
    isText: { default: false }
  },
  setup(e) {
    return (t, a) => (openBlock(), createBlock(ta, { "is-text": e.isText }, {
      default: withCtx(() => [
        renderSlot(t.$slots, "default")
      ]),
      _: 3
    }, 8, ["is-text"]));
  }
});
var _l = Symbol();
var Ii = defineComponent({
  __name: "BListGroup",
  props: {
    flush: { default: false },
    horizontal: { type: [Boolean, String], default: false },
    numbered: { default: false },
    tag: { default: "div" }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "flush")), l = r(toRef(t, "numbered")), n = computed(() => {
      const f = a.value ? false : t.horizontal;
      return {
        "list-group-flush": a.value,
        "list-group-horizontal": f === true,
        [`list-group-horizontal-${f}`]: typeof f == "string",
        "list-group-numbered": l.value
      };
    }), u = computed(() => l.value === true ? "ol" : t.tag);
    return provide(_l, {
      numbered: l.value
    }), (f, c) => (openBlock(), createBlock(resolveDynamicComponent(unref(u)), {
      class: normalizeClass(["list-group", unref(n)])
    }, {
      default: withCtx(() => [
        renderSlot(f.$slots, "default")
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var Fi = defineComponent({
  __name: "BListGroupItem",
  props: {
    action: { default: false },
    active: { default: false },
    button: { default: false },
    disabled: { default: false },
    href: null,
    tag: { default: "div" },
    target: { default: "_self" },
    to: null,
    variant: null
  },
  setup(e) {
    const t = e, a = useAttrs(), l = inject(_l, null), n = r(toRef(t, "action")), u = r(toRef(t, "active")), f = r(toRef(t, "button")), c = r(toRef(t, "disabled")), v = computed(() => !f.value && (!!t.href || !!t.to)), p = computed(
      () => l != null && l.numbered ? "li" : f.value ? "button" : v.value ? Ae : t.tag
    ), $ = computed(
      () => n.value || v.value || f.value || ["a", "router-link", "button", "b-link"].includes(t.tag)
    ), m = computed(() => ({
      [`list-group-item-${t.variant}`]: t.variant !== void 0,
      "list-group-item-action": $.value,
      active: u.value,
      disabled: c.value
    })), B = computed(() => {
      const V = {};
      return f.value && ((!a || !a.type) && (V.type = "button"), c.value && (V.disabled = true)), V;
    });
    return (V, T) => (openBlock(), createBlock(resolveDynamicComponent(unref(p)), mergeProps({
      class: ["list-group-item", unref(m)],
      "aria-current": unref(u) ? true : void 0,
      "aria-disabled": unref(c) ? true : void 0,
      target: unref(v) ? e.target : void 0,
      href: unref(f) ? void 0 : e.href,
      to: unref(f) ? void 0 : e.to
    }, unref(B)), {
      default: withCtx(() => [
        renderSlot(V.$slots, "default")
      ]),
      _: 3
    }, 16, ["class", "aria-current", "aria-disabled", "target", "href", "to"]));
  }
});
var Pi = ["id", "aria-labelledby", "aria-describedby", "onKeyup"];
var Oi = ["id"];
var Li = {
  inheritAttrs: false
};
var zi = defineComponent({
  ...Li,
  __name: "BModal",
  props: {
    bodyBgVariant: null,
    bodyClass: null,
    bodyTextVariant: null,
    busy: { default: false },
    lazy: { default: false },
    buttonSize: { default: "md" },
    cancelDisabled: { default: false },
    cancelTitle: { default: "Cancel" },
    cancelVariant: { default: "secondary" },
    centered: { default: false },
    contentClass: null,
    dialogClass: null,
    footerBgVariant: null,
    footerBorderVariant: null,
    footerClass: null,
    footerTextVariant: null,
    fullscreen: { type: [Boolean, String], default: false },
    headerBgVariant: null,
    headerBorderVariant: null,
    headerClass: null,
    headerCloseLabel: { default: "Close" },
    headerCloseWhite: { default: false },
    headerTextVariant: null,
    hideBackdrop: { default: false },
    hideFooter: { default: false },
    hideHeader: { default: false },
    hideHeaderClose: { default: false },
    id: null,
    modalClass: null,
    modelValue: { default: false },
    noCloseOnBackdrop: { default: false },
    noCloseOnEsc: { default: false },
    noFade: { default: false },
    noFocus: { default: false },
    okDisabled: { default: false },
    okOnly: { default: false },
    okTitle: { default: "Ok" },
    okVariant: { default: "primary" },
    scrollable: { default: false },
    show: { default: false },
    size: null,
    title: null,
    titleClass: null,
    titleSrOnly: { default: false },
    titleTag: { default: "h5" },
    static: { default: false }
  },
  emits: ["update:modelValue", "show", "shown", "hide", "hidden", "hide-prevented", "ok", "cancel"],
  setup(e, { emit: t }) {
    const a = e, l = useSlots(), n = $e(toRef(a, "id"), "modal"), u = r(toRef(a, "busy")), f = r(toRef(a, "lazy")), c = r(toRef(a, "cancelDisabled")), v = r(toRef(a, "centered")), p = r(toRef(a, "hideBackdrop")), $ = r(toRef(a, "hideFooter")), m = r(toRef(a, "hideHeader")), B = r(toRef(a, "hideHeaderClose")), V = r(toRef(a, "modelValue")), T = r(toRef(a, "noCloseOnBackdrop")), b = r(toRef(a, "noCloseOnEsc")), k = r(toRef(a, "noFade")), y = r(toRef(a, "noFocus")), C = r(toRef(a, "okDisabled")), q = r(toRef(a, "okOnly")), E = r(toRef(a, "scrollable")), I = r(toRef(a, "titleSrOnly")), _ = r(toRef(a, "static")), z = ref(false), x = ref(null), w = ref(false), P = computed(() => [
      a.modalClass,
      {
        fade: !k.value,
        show: z.value
      }
    ]), L = computed(() => !Ve(l["header-close"])), ee = computed(() => [
      a.dialogClass,
      {
        "modal-fullscreen": a.fullscreen === true,
        [`modal-fullscreen-${a.fullscreen}-down`]: typeof a.fullscreen == "string",
        [`modal-${a.size}`]: a.size !== void 0,
        "modal-dialog-centered": v.value,
        "modal-dialog-scrollable": E.value
      }
    ]), Z = computed(() => [
      a.bodyClass,
      {
        [`bg-${a.bodyBgVariant}`]: a.bodyBgVariant !== void 0,
        [`text-${a.bodyTextVariant}`]: a.bodyTextVariant !== void 0
      }
    ]), se = computed(() => [
      a.headerClass,
      {
        [`bg-${a.headerBgVariant}`]: a.headerBgVariant !== void 0,
        [`border-${a.headerBorderVariant}`]: a.headerBorderVariant !== void 0,
        [`text-${a.headerTextVariant}`]: a.headerTextVariant !== void 0
      }
    ]), K = computed(() => [
      a.footerClass,
      {
        [`bg-${a.footerBgVariant}`]: a.footerBgVariant !== void 0,
        [`border-${a.footerBorderVariant}`]: a.footerBorderVariant !== void 0,
        [`text-${a.footerTextVariant}`]: a.footerTextVariant !== void 0
      }
    ]), ve = computed(() => [
      a.titleClass,
      {
        ["visually-hidden"]: I.value
      }
    ]), te = computed(() => c.value || u.value), be = computed(() => C.value || u.value), ge = () => t("update:modelValue", false), Ce = (ne) => {
      !b.value && V.value && ne.key === "Escape" && ge();
    }, ie = () => t("show"), H = () => {
      z.value = true, t("shown"), f.value === true && (w.value = true);
    }, N = () => t("hide"), X = () => {
      z.value = false;
    }, le = () => {
      t("hidden"), f.value === true && (w.value = false);
    };
    return watch(
      () => V.value,
      (ne) => {
        ne === true && !y.value && nextTick(() => {
          x.value !== null && x.value.focus();
        });
      }
    ), (ne, pe) => (openBlock(), createBlock(Teleport, {
      to: "body",
      disabled: unref(_)
    }, [
      createVNode(wt, {
        "no-fade": true,
        "trans-props": { enterToClass: "show" },
        onBeforeEnter: ie,
        onAfterEnter: H,
        onBeforeLeave: N,
        onLeave: X,
        onAfterLeave: le
      }, {
        default: withCtx(() => [
          withDirectives(createBaseVNode("div", mergeProps({
            id: unref(n),
            ref_key: "element",
            ref: x,
            class: ["modal", unref(P)],
            role: "dialog",
            "aria-labelledby": `${unref(n)}-label`,
            "aria-describedby": `${unref(n)}-body`,
            tabindex: "-1"
          }, ne.$attrs, {
            onKeyup: withKeys(Ce, ["esc"])
          }), [
            createBaseVNode("div", {
              class: normalizeClass(["modal-dialog", unref(ee)])
            }, [
              !unref(f) || unref(f) && w.value || unref(f) && unref(V) === true ? (openBlock(), createElementBlock("div", {
                key: 0,
                class: normalizeClass(["modal-content", e.contentClass])
              }, [
                unref(m) ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", {
                  key: 0,
                  class: normalizeClass(["modal-header", unref(se)])
                }, [
                  renderSlot(ne.$slots, "header", {}, () => [
                    (openBlock(), createBlock(resolveDynamicComponent(e.titleTag), {
                      id: `${unref(n)}-label`,
                      class: normalizeClass(["modal-title", unref(ve)])
                    }, {
                      default: withCtx(() => [
                        renderSlot(ne.$slots, "title", {}, () => [
                          createTextVNode(toDisplayString(e.title), 1)
                        ], true)
                      ]),
                      _: 3
                    }, 8, ["id", "class"])),
                    unref(B) ? createCommentVNode("", true) : (openBlock(), createElementBlock(Fragment, { key: 0 }, [
                      unref(L) ? (openBlock(), createElementBlock("button", {
                        key: 0,
                        type: "button",
                        "data-bs-dismiss": "modal",
                        onClick: pe[0] || (pe[0] = (_e) => ge())
                      }, [
                        renderSlot(ne.$slots, "header-close", {}, void 0, true)
                      ])) : (openBlock(), createBlock(Ze, {
                        key: 1,
                        "aria-label": e.headerCloseLabel,
                        "data-bs-dismiss": "modal",
                        white: e.headerCloseWhite,
                        onClick: pe[1] || (pe[1] = (_e) => ge())
                      }, null, 8, ["aria-label", "white"]))
                    ], 64))
                  ], true)
                ], 2)),
                createBaseVNode("div", {
                  id: `${unref(n)}-body`,
                  class: normalizeClass(["modal-body", unref(Z)])
                }, [
                  renderSlot(ne.$slots, "default", {}, void 0, true)
                ], 10, Oi),
                unref($) ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", {
                  key: 1,
                  class: normalizeClass(["modal-footer", unref(K)])
                }, [
                  renderSlot(ne.$slots, "footer", {}, () => [
                    renderSlot(ne.$slots, "cancel", {}, () => [
                      unref(q) ? createCommentVNode("", true) : (openBlock(), createBlock(lt, {
                        key: 0,
                        type: "button",
                        class: "btn",
                        disabled: unref(te),
                        size: e.buttonSize,
                        variant: e.cancelVariant,
                        onClick: pe[2] || (pe[2] = (_e) => (ge(), t("cancel")))
                      }, {
                        default: withCtx(() => [
                          createTextVNode(toDisplayString(e.cancelTitle), 1)
                        ]),
                        _: 1
                      }, 8, ["disabled", "size", "variant"]))
                    ], true),
                    renderSlot(ne.$slots, "ok", {}, () => [
                      createVNode(lt, {
                        type: "button",
                        class: "btn",
                        disabled: unref(be),
                        size: e.buttonSize,
                        variant: e.okVariant,
                        onClick: pe[3] || (pe[3] = (_e) => (ge(), t("ok")))
                      }, {
                        default: withCtx(() => [
                          createTextVNode(toDisplayString(e.okTitle), 1)
                        ]),
                        _: 1
                      }, 8, ["disabled", "size", "variant"])
                    ], true)
                  ], true)
                ], 2))
              ], 2)) : createCommentVNode("", true)
            ], 2),
            unref(p) ? createCommentVNode("", true) : renderSlot(ne.$slots, "backdrop", { key: 0 }, () => [
              createBaseVNode("div", {
                class: "modal-backdrop fade show",
                onClick: pe[4] || (pe[4] = (_e) => !unref(T) && ge())
              })
            ], true)
          ], 16, Pi), [
            [vShow, unref(V)]
          ])
        ]),
        _: 3
      })
    ], 8, ["disabled"]));
  }
});
var Ni = Te(zi, [["__scopeId", "data-v-9670f554"]]);
var Ei = defineComponent({
  __name: "BNav",
  props: {
    align: null,
    cardHeader: { default: false },
    fill: { default: false },
    justified: { default: false },
    pills: { default: false },
    small: { default: false },
    tabs: { default: false },
    tag: { default: "ul" },
    vertical: { default: false }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "cardHeader")), l = r(toRef(t, "fill")), n = r(toRef(t, "justified")), u = r(toRef(t, "pills")), f = r(toRef(t, "small")), c = r(toRef(t, "tabs")), v = r(toRef(t, "vertical")), p = ot(toRef(t, "align")), $ = computed(() => ({
      "nav-tabs": c.value,
      "nav-pills": u.value && !c.value,
      "card-header-tabs": !v.value && a.value && c.value,
      "card-header-pills": !v.value && a.value && u.value && !c.value,
      "flex-column": v.value,
      "nav-fill": !v.value && l.value,
      "nav-justified": !v.value && n.value,
      [p.value]: !v.value && t.align !== void 0,
      small: f.value
    }));
    return (m, B) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(["nav", unref($)])
    }, {
      default: withCtx(() => [
        renderSlot(m.$slots, "default")
      ]),
      _: 3
    }, 8, ["class"]));
  }
});
var Di = defineComponent({
  __name: "BNavForm",
  props: {
    role: null,
    id: null,
    floating: { default: false },
    novalidate: { default: false },
    validated: { default: false }
  },
  emits: ["submit"],
  setup(e, { emit: t }) {
    const a = e, l = computed(() => ({
      floating: a.floating,
      role: a.role,
      id: a.id,
      novalidate: a.novalidate,
      validated: a.validated
    })), n = (u) => t("submit", u);
    return (u, f) => (openBlock(), createBlock(Bl, mergeProps(unref(l), {
      class: "d-flex",
      onSubmit: withModifiers(n, ["prevent"])
    }), {
      default: withCtx(() => [
        renderSlot(u.$slots, "default")
      ]),
      _: 3
    }, 16, ["onSubmit"]));
  }
});
var Hi = defineComponent({
  components: { BLink: Ae },
  props: {
    ...ht(Ue, ["event", "routerTag"])
  },
  setup(e) {
    return { disabledBoolean: r(toRef(e, "disabled")) };
  }
});
var Ri = { class: "nav-item" };
function qi(e, t, a, l, n, u) {
  const f = resolveComponent("b-link");
  return openBlock(), createElementBlock("li", Ri, [
    createVNode(f, mergeProps({ class: "nav-link" }, e.$props, {
      "active-class": "active",
      tabindex: e.disabledBoolean ? -1 : void 0,
      "aria-disabled": e.disabledBoolean ? true : void 0
    }), {
      default: withCtx(() => [
        renderSlot(e.$slots, "default")
      ]),
      _: 3
    }, 16, ["tabindex", "aria-disabled"])
  ]);
}
var Mi = Te(Hi, [["render", qi]]);
var ji = { class: "nav-item dropdown" };
var Gi = defineComponent({
  __name: "BNavItemDropdown",
  props: {
    id: null,
    text: null,
    toggleClass: null,
    size: null,
    offset: null,
    autoClose: { type: [Boolean, String], default: true },
    dark: { type: Boolean, default: false },
    dropleft: { type: Boolean, default: false },
    dropright: { type: Boolean, default: false },
    dropup: { type: Boolean, default: false },
    right: { type: Boolean, default: false },
    left: { type: [Boolean, String], default: false },
    split: { type: Boolean, default: false },
    splitVariant: null,
    noCaret: { type: Boolean, default: false },
    variant: { default: "link" }
  },
  setup(e) {
    const t = e;
    return (a, l) => (openBlock(), createElementBlock("li", ji, [
      createVNode(yl, mergeProps(t, { "is-nav": "" }), createSlots({ _: 2 }, [
        renderList(a.$slots, (n, u, f) => ({
          name: u,
          fn: withCtx((c) => [
            renderSlot(a.$slots, u, normalizeProps(guardReactiveProps(c || {})))
          ])
        }))
      ]), 1040)
    ]));
  }
});
var Ui = { class: "navbar-text" };
var Wi = defineComponent({
  __name: "BNavText",
  props: {
    text: null
  },
  setup(e) {
    return (t, a) => (openBlock(), createElementBlock("li", Ui, [
      renderSlot(t.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(e.text), 1)
      ])
    ]));
  }
});
var Ki = defineComponent({
  __name: "BNavbar",
  props: {
    fixed: null,
    print: { default: false },
    sticky: null,
    tag: { default: "nav" },
    toggleable: { type: [Boolean, String], default: false },
    dark: { default: false },
    variant: null,
    container: { type: [String, Boolean], default: "fluid" }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "print")), l = r(toRef(t, "dark")), n = computed(
      () => t.tag === "nav" ? void 0 : "navigation"
    ), u = computed(
      () => typeof t.toggleable == "string" ? `navbar-expand-${t.toggleable}` : t.toggleable === false ? "navbar-expand" : void 0
    ), f = computed(
      () => t.container === true ? "container" : "container-fluid"
    ), c = computed(() => ({
      "d-print": a.value,
      [`sticky-${t.sticky}`]: t.sticky !== void 0,
      "navbar-dark": l.value,
      [`bg-${t.variant}`]: t.variant !== void 0,
      [`fixed-${t.fixed}`]: t.fixed !== void 0,
      [`${u.value}`]: u.value !== void 0
    }));
    return (v, p) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(["navbar", unref(c)]),
      role: unref(n)
    }, {
      default: withCtx(() => [
        e.container !== false ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass(unref(f))
        }, [
          renderSlot(v.$slots, "default")
        ], 2)) : renderSlot(v.$slots, "default", { key: 1 })
      ]),
      _: 3
    }, 8, ["class", "role"]));
  }
});
var pa = ht(Ue, ["event", "routerTag"]);
var Xi = defineComponent({
  components: {
    BLink: Ae
  },
  props: {
    tag: { type: String, default: "div" },
    ...pa
  },
  setup(e) {
    const t = computed(() => at(e)), a = computed(
      () => t.value ? Ae : e.tag
    );
    return {
      computedLinkProps: computed(
        () => t.value ? Jt(e, pa) : {}
      ),
      computedTag: a
    };
  }
});
function Ji(e, t, a, l, n, u) {
  return openBlock(), createBlock(resolveDynamicComponent(e.computedTag), mergeProps({ class: "navbar-brand" }, e.computedLinkProps), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16);
}
var Yi = Te(Xi, [["render", Ji]]);
var Zi = defineComponent({
  __name: "BNavbarNav",
  props: {
    align: null,
    fill: { default: false },
    justified: { default: false },
    small: { default: false },
    tag: { default: "ul" }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "fill")), l = r(toRef(t, "justified")), n = r(toRef(t, "small")), u = ot(toRef(t, "align")), f = computed(() => ({
      "nav-fill": a.value,
      "nav-justified": l.value,
      [u.value]: t.align !== void 0,
      small: n.value
    }));
    return (c, v) => (openBlock(), createElementBlock("ul", {
      class: normalizeClass(["navbar-nav", unref(f)])
    }, [
      renderSlot(c.$slots, "default")
    ], 2));
  }
});
var Qi = createBaseVNode("span", { class: "navbar-toggler-icon" }, null, -1);
var er = defineComponent({
  __name: "BNavbarToggle",
  props: {
    disabled: { default: false },
    label: { default: "Toggle navigation" },
    target: null
  },
  emits: ["click"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "disabled")), n = computed(() => ({
      disabled: l.value,
      "aria-label": a.label
    })), u = computed(() => ({
      disabled: l.value
    })), f = (c) => {
      l.value || t("click", c);
    };
    return (c, v) => withDirectives((openBlock(), createElementBlock("button", mergeProps({
      class: ["navbar-toggler", unref(u)],
      type: "button"
    }, unref(n), { onClick: f }), [
      renderSlot(c.$slots, "default", {}, () => [
        Qi
      ])
    ], 16)), [
      [unref(Zt), unref(l) ? void 0 : e.target]
    ]);
  }
});
var tr = ["data-bs-backdrop", "data-bs-scroll"];
var ar = {
  key: 0,
  class: "offcanvas-header"
};
var lr = {
  id: "offcanvasLabel",
  class: "offcanvas-title"
};
var nr = { class: "offcanvas-body" };
var or = { key: 1 };
var sr = defineComponent({
  __name: "BOffcanvas",
  props: {
    dismissLabel: { default: "Close" },
    modelValue: { default: false },
    bodyScrolling: { default: false },
    backdrop: { default: true },
    placement: { default: "start" },
    title: null,
    noHeaderClose: { default: false },
    noHeader: { default: false }
  },
  emits: ["update:modelValue", "show", "shown", "hide", "hidden"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "modelValue")), n = r(toRef(a, "bodyScrolling")), u = r(toRef(a, "backdrop")), f = r(toRef(a, "noHeaderClose")), c = r(toRef(a, "noHeader")), v = useSlots(), p = ref(), $ = ref(), m = computed(() => !Ve(v.footer)), B = computed(() => [`offcanvas-${a.placement}`]), V = () => {
      t("show"), t("update:modelValue", true);
    }, T = () => {
      t("hide"), t("update:modelValue", false);
    };
    return watch(
      () => l.value,
      (b) => {
        var k, y;
        b ? (k = $.value) == null || k.show(p.value) : (y = $.value) == null || y.hide();
      }
    ), ye(p, "shown.bs.offcanvas", () => t("shown")), ye(p, "hidden.bs.offcanvas", () => t("hidden")), ye(p, "show.bs.offcanvas", () => {
      V();
    }), ye(p, "hide.bs.offcanvas", () => {
      T();
    }), onMounted(() => {
      var b;
      $.value = new Offcanvas(p.value), l.value && ((b = $.value) == null || b.show(p.value));
    }), (b, k) => (openBlock(), createElementBlock("div", {
      ref_key: "element",
      ref: p,
      class: normalizeClass(["offcanvas", unref(B)]),
      tabindex: "-1",
      "aria-labelledby": "offcanvasLabel",
      "data-bs-backdrop": unref(u),
      "data-bs-scroll": unref(n)
    }, [
      unref(c) ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", ar, [
        renderSlot(b.$slots, "header", normalizeProps(guardReactiveProps({ visible: unref(l), placement: e.placement, hide: T })), () => [
          createBaseVNode("h5", lr, [
            renderSlot(b.$slots, "title", {}, () => [
              createTextVNode(toDisplayString(e.title), 1)
            ])
          ]),
          unref(f) ? createCommentVNode("", true) : (openBlock(), createBlock(Ze, {
            key: 0,
            class: "text-reset",
            "data-bs-dismiss": "offcanvas",
            "aria-label": e.dismissLabel
          }, null, 8, ["aria-label"]))
        ])
      ])),
      createBaseVNode("div", nr, [
        renderSlot(b.$slots, "default")
      ]),
      unref(m) ? (openBlock(), createElementBlock("div", or, [
        renderSlot(b.$slots, "footer", normalizeProps(guardReactiveProps({ visible: unref(l), placement: e.placement, hide: T })))
      ])) : createCommentVNode("", true)
    ], 10, tr));
  }
});
var ir = defineComponent({
  __name: "BOverlay",
  props: {
    bgColor: null,
    blur: { default: "2px" },
    fixed: { default: false },
    noCenter: { default: false },
    noFade: { default: false },
    noWrap: { default: false },
    opacity: { default: 0.85 },
    overlayTag: { default: "div" },
    rounded: { type: [Boolean, String], default: false },
    show: { default: false },
    spinnerSmall: { default: false },
    spinnerType: { default: "border" },
    spinnerVariant: null,
    variant: { default: "light" },
    wrapTag: { default: "div" },
    zIndex: { default: 10 }
  },
  emits: ["click", "hidden", "shown"],
  setup(e, { emit: t }) {
    const a = e, l = { top: 0, left: 0, bottom: 0, right: 0 }, n = r(toRef(a, "fixed")), u = r(toRef(a, "noCenter")), f = r(toRef(a, "noWrap")), c = r(toRef(a, "show")), v = r(toRef(a, "spinnerSmall")), p = computed(
      () => a.rounded === true || a.rounded === "" ? "rounded" : a.rounded === false ? "" : `rounded-${a.rounded}`
    ), $ = computed(
      () => a.variant && !a.bgColor ? `bg-${a.variant}` : ""
    ), m = computed(() => c.value ? "true" : null), B = computed(() => ({
      type: a.spinnerType || void 0,
      variant: a.spinnerVariant || void 0,
      small: v.value
    })), V = computed(() => ({
      ...l,
      zIndex: a.zIndex || 10
    })), T = computed(() => [
      "b-overlay",
      {
        "position-absolute": !f.value || !n.value,
        "position-fixed": f.value && n.value
      }
    ]), b = computed(() => [$.value, p.value]), k = computed(() => ({
      ...l,
      opacity: a.opacity,
      backgroundColor: a.bgColor || void 0,
      backdropFilter: blur ? `blur(${blur})` : void 0
    })), y = computed(
      () => u.value ? l : {
        top: "50%",
        left: "50%",
        transform: "translateX(-50%) translateY(-50%)"
      }
    );
    return (C, q) => (openBlock(), createBlock(resolveDynamicComponent(e.wrapTag), {
      class: "b-overlay-wrap position-relative",
      "aria-busy": unref(m)
    }, {
      default: withCtx(() => [
        renderSlot(C.$slots, "default"),
        createVNode(wt, {
          "no-fade": e.noFade,
          "trans-props": { enterToClass: "show" },
          name: "fade",
          onOnAfterEnter: q[1] || (q[1] = (E) => t("shown")),
          onOnAfterLeave: q[2] || (q[2] = (E) => t("hidden"))
        }, {
          default: withCtx(() => [
            unref(c) ? (openBlock(), createBlock(resolveDynamicComponent(e.overlayTag), {
              key: 0,
              class: normalizeClass(unref(T)),
              style: normalizeStyle(unref(V)),
              onClick: q[0] || (q[0] = (E) => t("click", E))
            }, {
              default: withCtx(() => [
                createBaseVNode("div", {
                  class: normalizeClass(["position-absolute", unref(b)]),
                  style: normalizeStyle(unref(k))
                }, null, 6),
                createBaseVNode("div", {
                  class: "position-absolute",
                  style: normalizeStyle(unref(y))
                }, [
                  renderSlot(C.$slots, "overlay", normalizeProps(guardReactiveProps(unref(B))), () => [
                    createVNode(St, normalizeProps(guardReactiveProps(unref(B))), null, 16)
                  ])
                ], 4)
              ]),
              _: 3
            }, 8, ["class", "style"])) : createCommentVNode("", true)
          ]),
          _: 3
        }, 8, ["no-fade"])
      ]),
      _: 3
    }, 8, ["aria-busy"]));
  }
});
var rr = 5;
var Vl = 20;
var Al = 0;
var Oe = 3;
var ur = "ellipsis-text";
var dr = "first-text";
var cr = "last-text";
var fr = "next-text";
var vr = "page";
var mr = "prev-text";
var ha = (e) => Math.max(Le(e) || Vl, 1);
var ya = (e) => Math.max(Le(e) || Al, 0);
var br = (e, t) => {
  const a = Le(e) || 1;
  return a > t ? t : a < 1 ? 1 : a;
};
var gr = defineComponent({
  name: "BPagination",
  props: {
    align: { type: String, default: "start" },
    ariaControls: { type: String, required: false },
    ariaLabel: { type: String, default: "Pagination" },
    disabled: { type: [Boolean, String], default: false },
    ellipsisClass: { type: [Array, String], default: () => [] },
    ellipsisText: { type: String, default: "\u2026" },
    firstClass: { type: [Array, String], default: () => [] },
    firstNumber: { type: [Boolean, String], default: false },
    firstText: { type: String, default: "\xAB" },
    hideEllipsis: { type: [Boolean, String], default: false },
    hideGotoEndButtons: { type: [Boolean, String], default: false },
    labelFirstPage: { type: String, default: "Go to first page" },
    labelLastPage: { type: String, default: "Go to last page" },
    labelNextPage: { type: String, default: "Go to next page" },
    labelPage: { type: String, default: "Go to page" },
    labelPrevPage: { type: String, default: "Go to previous page" },
    lastClass: { type: [Array, String], default: () => [] },
    lastNumber: { type: [Boolean, String], default: false },
    lastText: { type: String, default: "\xBB" },
    limit: { type: Number, default: rr },
    modelValue: { type: Number, default: 1 },
    nextClass: { type: [Array, String], default: () => [] },
    nextText: { type: String, default: "\u203A" },
    pageClass: { type: [Array, String], default: () => [] },
    perPage: { type: Number, default: Vl },
    pills: { type: [Boolean, String], default: false },
    prevClass: { type: [Array, String], default: () => [] },
    prevText: { type: String, default: "\u2039" },
    size: { type: String, required: false },
    totalRows: { type: Number, default: Al }
  },
  emits: ["update:modelValue", "page-click"],
  setup(e, { emit: t, slots: a }) {
    const l = r(toRef(e, "disabled")), n = r(toRef(e, "firstNumber")), u = r(toRef(e, "hideEllipsis")), f = r(toRef(e, "hideGotoEndButtons")), c = r(toRef(e, "lastNumber")), v = r(toRef(e, "pills")), p = computed(
      () => e.align === "fill" ? "start" : e.align
    ), $ = ot(toRef(p, "value")), m = computed(
      () => Math.ceil(ya(e.totalRows) / ha(e.perPage))
    ), B = computed(() => {
      let I;
      return m.value - e.modelValue + 2 < e.limit && e.limit > Oe ? I = m.value - T.value + 1 : I = e.modelValue - Math.floor(T.value / 2), I < 1 ? I = 1 : I > m.value - T.value && (I = m.value - T.value + 1), e.limit <= Oe && c.value && m.value === I + T.value - 1 && (I = Math.max(I - 1, 1)), I;
    }), V = computed(() => {
      const I = m.value - e.modelValue;
      let _ = false;
      return I + 2 < e.limit && e.limit > Oe ? e.limit > Oe && (_ = true) : e.limit > Oe && (_ = !!(!u.value || n.value)), B.value <= 1 && (_ = false), _ && n.value && B.value < 4 && (_ = false), _;
    }), T = computed(() => {
      let I = e.limit;
      return m.value <= e.limit ? I = m.value : e.modelValue < e.limit - 1 && e.limit > Oe ? ((!u.value || c.value) && (I = e.limit - (n.value ? 0 : 1)), I = Math.min(I, e.limit)) : m.value - e.modelValue + 2 < e.limit && e.limit > Oe ? (!u.value || n.value) && (I = e.limit - (c.value ? 0 : 1)) : e.limit > Oe && (I = e.limit - (u.value ? 0 : 2)), I;
    }), b = computed(() => {
      const I = m.value - T.value;
      let _ = false;
      e.modelValue < e.limit - 1 && e.limit > Oe ? (!u.value || c.value) && (_ = true) : e.limit > Oe && (_ = !!(!u.value || c.value)), B.value > I && (_ = false);
      const z = B.value + T.value - 1;
      return _ && c.value && z > m.value - 3 && (_ = false), _;
    }), k = reactive({
      pageSize: ha(e.perPage),
      totalRows: ya(e.totalRows),
      numberOfPages: m.value
    }), y = (I, _) => {
      if (_ === e.modelValue)
        return;
      const { target: z } = I, x = new yt("page-click", {
        cancelable: true,
        vueTarget: this,
        target: z
      });
      t("page-click", x, _), !x.defaultPrevented && t("update:modelValue", _);
    }, C = computed(() => e.size ? `pagination-${e.size}` : ""), q = computed(() => v.value ? "b-pagination-pills" : "");
    watch(
      () => e.modelValue,
      (I) => {
        const _ = br(I, m.value);
        _ !== e.modelValue && t("update:modelValue", _);
      }
    ), watch(k, (I, _) => {
      I != null && (_.pageSize !== I.pageSize && _.totalRows === I.totalRows || _.numberOfPages !== I.numberOfPages && e.modelValue > _.numberOfPages) && t("update:modelValue", 1);
    });
    const E = computed(() => {
      const I = [];
      for (let _ = 0; _ < T.value; _++)
        I.push({ number: B.value + _, classes: null });
      return I;
    });
    return () => {
      const I = [], _ = E.value.map((K) => K.number), z = (K) => K === e.modelValue, x = e.modelValue < 1, w = e.align === "fill", P = (K, ve, te, be, ge, Ce) => {
        const ie = l.value || z(Ce) || x || K < 1 || K > m.value, H = K < 1 ? 1 : K > m.value ? m.value : K, N = { disabled: ie, page: H, index: H - 1 }, X = Fe(te, N, a) || be || "";
        return h(
          "li",
          {
            class: [
              "page-item",
              {
                disabled: ie,
                "flex-fill": w,
                "d-flex": w && !ie
              },
              ge
            ]
          },
          h(
            ie ? "span" : "button",
            {
              class: ["page-link", { "flex-grow-1": !ie && w }],
              "aria-label": ve,
              "aria-controls": e.ariaControls || null,
              "aria-disabled": ie ? "true" : null,
              role: "menuitem",
              type: ie ? null : "button",
              tabindex: ie ? null : "-1",
              onClick: (le) => {
                ie || y(le, H);
              }
            },
            X
          )
        );
      }, L = (K) => h(
        "li",
        {
          class: [
            "page-item",
            "disabled",
            "bv-d-xs-down-none",
            w ? "flex-fill" : "",
            e.ellipsisClass
          ],
          role: "separator",
          key: `ellipsis-${K ? "last" : "first"}`
        },
        [
          h(
            "span",
            { class: ["page-link"] },
            Fe(ur, {}, a) || e.ellipsisText || "..."
          )
        ]
      ), ee = (K, ve) => {
        const te = z(K.number) && !x, be = l.value ? null : te || x && ve === 0 ? "0" : "-1", ge = {
          active: te,
          disabled: l.value,
          page: K.number,
          index: K.number - 1,
          content: K.number
        }, Ce = Fe(vr, ge, a) || K.number, ie = h(
          l.value ? "span" : "button",
          {
            class: ["page-link", { "flex-grow-1": !l.value && w }],
            "aria-controls": e.ariaControls || null,
            "aria-disabled": l.value ? "true" : null,
            "aria-label": e.labelPage ? `${e.labelPage} ${K.number}` : null,
            role: "menuitemradio",
            type: l.value ? null : "button",
            tabindex: be,
            onClick: (H) => {
              l.value || y(H, K.number);
            }
          },
          Ce
        );
        return h(
          "li",
          {
            class: [
              "page-item",
              {
                disabled: l.value,
                active: te,
                "flex-fill": w,
                "d-flex": w && !l.value
              },
              e.pageClass
            ],
            role: "presentation",
            key: `page-${K.number}`
          },
          ie
        );
      };
      if (!f.value && !n.value) {
        const K = P(
          1,
          e.labelFirstPage,
          dr,
          e.firstText,
          e.firstClass,
          1
        );
        I.push(K);
      }
      const Z = P(
        e.modelValue - 1,
        e.labelFirstPage,
        mr,
        e.prevText,
        e.prevClass,
        1
      );
      I.push(Z), n.value && _[0] !== 1 && I.push(ee({ number: 1 }, 0)), V.value && I.push(L(false)), E.value.forEach((K, ve) => {
        const te = V.value && n.value && _[0] !== 1 ? 1 : 0;
        I.push(ee(K, ve + te));
      }), b.value && I.push(L(true)), c.value && _[_.length - 1] !== m.value && I.push(ee({ number: m.value }, -1));
      const se = P(
        e.modelValue + 1,
        e.labelNextPage,
        fr,
        e.nextText,
        e.nextClass,
        m.value
      );
      if (I.push(se), !c.value && !f.value) {
        const K = P(
          m.value,
          e.labelLastPage,
          cr,
          e.lastText,
          e.lastClass,
          m.value
        );
        I.push(K);
      }
      return h(
        "ul",
        {
          class: ["pagination", C.value, $.value, q.value],
          role: "menubar",
          "aria-disabled": l.value,
          "aria-label": e.ariaLabel || null
        },
        I
      );
    };
  }
});
var Ie = defineComponent({
  __name: "BPlaceholder",
  props: {
    tag: { default: "span" },
    width: null,
    cols: null,
    variant: null,
    size: null,
    animation: null
  },
  setup(e) {
    const t = e, a = computed(
      () => t.width === void 0 ? void 0 : typeof t.width == "number" ? t.width.toString() : t.width.includes("%") ? t.width.replaceAll("%", "") : t.width
    ), l = computed(
      () => t.cols === void 0 ? void 0 : typeof t.cols == "number" ? t.cols.toString() : t.cols
    ), n = computed(() => ({
      [`col-${l.value}`]: l.value !== void 0 && a.value === void 0,
      [`bg-${t.variant}`]: t.variant !== void 0,
      [`placeholder-${t.size}`]: t.size !== void 0,
      [`placeholder-${t.animation}`]: t.animation !== void 0
    })), u = computed(
      () => a.value === void 0 ? void 0 : `width: ${a.value}%;`
    );
    return (f, c) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      class: normalizeClass(["placeholder", unref(n)]),
      style: normalizeStyle(unref(u))
    }, null, 8, ["class", "style"]));
  }
});
var xl = defineComponent({
  __name: "BPlaceholderButton",
  props: {
    tag: { default: "div" },
    width: null,
    cols: null,
    variant: { default: "primary" },
    animation: null
  },
  setup(e) {
    const t = e, a = computed(() => ["btn", `btn-${t.variant}`, "disabled"]), l = computed(() => ({
      animation: t.animation,
      width: t.width,
      cols: t.cols,
      tag: t.tag
    }));
    return (n, u) => (openBlock(), createBlock(Ie, mergeProps({ class: unref(a) }, unref(l)), null, 16, ["class"]));
  }
});
var pr = defineComponent({
  __name: "BPlaceholderCard",
  props: {
    noHeader: { default: false },
    headerWidth: { default: 100 },
    headerVariant: null,
    headerAnimation: null,
    headerSize: null,
    noFooter: { default: false },
    footerWidth: { default: 100 },
    footerVariant: null,
    footerAnimation: null,
    footerSize: null,
    animation: null,
    size: null,
    variant: null,
    noButton: { default: false },
    imgBottom: { default: false },
    imgSrc: null,
    imgBlankColor: { default: "#868e96" },
    imgHeight: { default: 100 },
    noImg: { default: false }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "noButton")), l = r(toRef(t, "noHeader")), n = r(toRef(t, "noFooter")), u = r(toRef(t, "noImg")), f = computed(() => ({
      width: t.headerWidth,
      variant: t.headerVariant,
      animation: t.headerAnimation,
      size: t.headerSize
    })), c = computed(() => ({
      width: t.footerWidth,
      animation: t.footerAnimation,
      size: a.value ? t.footerSize : void 0,
      variant: t.footerVariant
    })), v = computed(() => ({
      blank: !t.imgSrc,
      blankColor: t.imgBlankColor,
      height: t.imgSrc ? void 0 : t.imgHeight,
      src: t.imgSrc,
      top: !t.imgBottom,
      bottom: t.imgBottom
    }));
    return (p, $) => (openBlock(), createBlock(ml, { "img-bottom": e.imgBottom }, createSlots({
      default: withCtx(() => [
        renderSlot(p.$slots, "default", {}, () => [
          createVNode(Ie, { cols: "7" }),
          createVNode(Ie, { cols: "4" }),
          createVNode(Ie, { cols: "4" }),
          createVNode(Ie, { cols: "6" }),
          createVNode(Ie, { cols: "8" })
        ])
      ]),
      _: 2
    }, [
      unref(u) ? void 0 : {
        name: "img",
        fn: withCtx(() => [
          renderSlot(p.$slots, "img", {}, () => [
            createVNode(pt, normalizeProps(guardReactiveProps(unref(v))), null, 16)
          ])
        ]),
        key: "0"
      },
      unref(l) ? void 0 : {
        name: "header",
        fn: withCtx(() => [
          renderSlot(p.$slots, "header", {}, () => [
            createVNode(Ie, normalizeProps(guardReactiveProps(unref(f))), null, 16)
          ])
        ]),
        key: "1"
      },
      unref(n) ? void 0 : {
        name: "footer",
        fn: withCtx(() => [
          renderSlot(p.$slots, "footer", {}, () => [
            unref(a) ? (openBlock(), createBlock(Ie, normalizeProps(mergeProps({ key: 1 }, unref(c))), null, 16)) : (openBlock(), createBlock(xl, normalizeProps(mergeProps({ key: 0 }, unref(c))), null, 16))
          ])
        ]),
        key: "2"
      }
    ]), 1032, ["img-bottom"]));
  }
});
var Tt = defineComponent({
  __name: "BTableSimple",
  props: {
    bordered: { default: false },
    borderless: { default: false },
    borderVariant: null,
    captionTop: { default: false },
    dark: { default: false },
    hover: { default: false },
    responsive: { type: [Boolean, String], default: false },
    stacked: { type: [Boolean, String], default: false },
    striped: { default: false },
    small: { default: false },
    tableClass: null,
    tableVariant: null,
    stickyHeader: { default: false }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "captionTop")), l = r(toRef(t, "borderless")), n = r(toRef(t, "bordered")), u = r(toRef(t, "dark")), f = r(toRef(t, "hover")), c = r(toRef(t, "small")), v = r(toRef(t, "striped")), p = r(toRef(t, "stickyHeader")), $ = computed(() => [
      "table",
      "b-table",
      {
        "table-bordered": n.value,
        "table-borderless": l.value,
        [`border-${t.borderVariant}`]: t.borderVariant !== void 0,
        "caption-top": a.value,
        "table-dark": u.value,
        "table-hover": f.value,
        "b-table-stacked": typeof t.stacked == "boolean" && t.stacked,
        [`b-table-stacked-${t.stacked}`]: typeof t.stacked == "string",
        "table-striped": v.value,
        "table-sm": c.value,
        [`table-${t.tableVariant}`]: t.tableVariant !== void 0
      },
      t.tableClass
    ]), m = computed(() => [
      {
        "table-responsive": t.responsive === true,
        [`table-responsive-${t.responsive}`]: typeof t.responsive == "string",
        "b-table-sticky-header": p.value
      }
    ]);
    return (B, V) => e.responsive ? (openBlock(), createElementBlock("div", {
      key: 1,
      class: normalizeClass(unref(m))
    }, [
      createBaseVNode("table", {
        role: "table",
        class: normalizeClass(unref($))
      }, [
        renderSlot(B.$slots, "default")
      ], 2)
    ], 2)) : (openBlock(), createElementBlock("table", {
      key: 0,
      role: "table",
      class: normalizeClass(unref($))
    }, [
      renderSlot(B.$slots, "default")
    ], 2));
  }
});
var hr = defineComponent({
  __name: "BPlaceholderTable",
  props: {
    rows: { default: 3 },
    columns: { default: 5 },
    cellWidth: { default: 100 },
    size: null,
    animation: null,
    variant: null,
    headerColumns: null,
    hideHeader: { default: false },
    headerCellWidth: { default: 100 },
    headerSize: null,
    headerAnimation: null,
    headerVariant: null,
    footerColumns: null,
    showFooter: { default: false },
    footerCellWidth: { default: 100 },
    footerSize: null,
    footerAnimation: null,
    footerVariant: null
  },
  setup(e) {
    const t = e, a = computed(
      () => typeof t.columns == "string" ? Qe(t.columns, 5) : t.columns
    ), l = computed(
      () => typeof t.rows == "string" ? Qe(t.rows, 3) : t.rows
    ), n = computed(
      () => t.headerColumns === void 0 ? a.value : typeof t.headerColumns == "string" ? Qe(t.headerColumns, a.value) : t.headerColumns
    ), u = computed(
      () => t.footerColumns === void 0 ? a.value : typeof t.footerColumns == "string" ? Qe(t.footerColumns, a.value) : t.footerColumns
    ), f = computed(() => ({
      size: t.size,
      variant: t.variant,
      animation: t.animation,
      width: t.cellWidth
    })), c = computed(() => ({
      size: t.headerSize,
      variant: t.headerVariant,
      animation: t.headerAnimation,
      width: t.headerCellWidth
    })), v = computed(() => ({
      size: t.footerSize,
      variant: t.footerVariant,
      animation: t.footerAnimation,
      width: t.footerCellWidth
    })), p = r(toRef(t, "hideHeader")), $ = r(toRef(t, "showFooter"));
    return (m, B) => (openBlock(), createBlock(Tt, null, {
      default: withCtx(() => [
        unref(p) ? createCommentVNode("", true) : renderSlot(m.$slots, "thead", { key: 0 }, () => [
          createBaseVNode("thead", null, [
            createBaseVNode("tr", null, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(unref(n), (V, T) => (openBlock(), createElementBlock("th", { key: T }, [
                createVNode(Ie, normalizeProps(guardReactiveProps(unref(c))), null, 16)
              ]))), 128))
            ])
          ])
        ]),
        renderSlot(m.$slots, "default", {}, () => [
          createBaseVNode("tbody", null, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(unref(l), (V, T) => (openBlock(), createElementBlock("tr", { key: T }, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(unref(a), (b, k) => (openBlock(), createElementBlock("td", { key: k }, [
                createVNode(Ie, normalizeProps(guardReactiveProps(unref(f))), null, 16)
              ]))), 128))
            ]))), 128))
          ])
        ]),
        unref($) ? renderSlot(m.$slots, "tfoot", { key: 1 }, () => [
          createBaseVNode("tfoot", null, [
            createBaseVNode("tr", null, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(unref(u), (V, T) => (openBlock(), createElementBlock("th", { key: T }, [
                createVNode(Ie, normalizeProps(guardReactiveProps(unref(v))), null, 16)
              ]))), 128))
            ])
          ])
        ]) : createCommentVNode("", true)
      ]),
      _: 3
    }));
  }
});
var yr = defineComponent({
  __name: "BPlaceholderWrapper",
  props: {
    loading: { default: false }
  },
  setup(e) {
    const a = r(toRef(e, "loading"));
    return (l, n) => unref(a) ? renderSlot(l.$slots, "loading", { key: 0 }) : renderSlot(l.$slots, "default", { key: 1 });
  }
});
var Br = defineComponent({
  props: {
    container: {
      type: [String, Object],
      default: "body"
    },
    content: { type: String },
    id: { type: String },
    customClass: { type: String, default: "" },
    noninteractive: { type: [Boolean, String], default: false },
    placement: { type: String, default: "right" },
    target: {
      type: [String, Object],
      default: void 0
    },
    title: { type: String },
    delay: { type: [Number, Object], default: 0 },
    triggers: { type: String, default: "click" },
    show: { type: [Boolean, String], default: false },
    variant: { type: String, default: void 0 },
    html: { type: [Boolean, String], default: true },
    sanitize: { type: [Boolean, String], default: false },
    offset: { type: String, default: "0" }
  },
  emits: ["show", "shown", "hide", "hidden", "inserted"],
  setup(e, { emit: t, slots: a }) {
    r(toRef(e, "noninteractive"));
    const l = r(toRef(e, "show")), n = r(toRef(e, "html")), u = r(toRef(e, "sanitize")), f = ref(), c = ref(), v = ref(), p = ref(), $ = ref(), m = computed(() => ({
      [`b-popover-${e.variant}`]: e.variant !== void 0
    })), B = (b) => {
      if (typeof b == "string")
        return b;
      if (b instanceof HTMLElement)
        return b;
      if (typeof b < "u")
        return b.$el;
    }, V = (b) => {
      if (!!b) {
        if (typeof b == "string") {
          const k = document.getElementById(b);
          return k || void 0;
        }
        return b;
      }
    }, T = (b) => {
      c.value = V(B(b)), c.value && (v.value = new Popover(c.value, {
        customClass: e.customClass,
        container: B(e.container),
        trigger: e.triggers,
        placement: e.placement,
        title: e.title || a.title ? p.value : "",
        content: $.value,
        html: n.value,
        delay: e.delay,
        sanitize: u.value,
        offset: e.offset
      }));
    };
    return watch(
      () => e.target,
      (b) => {
        var k;
        (k = v.value) == null || k.dispose(), T(b);
      }
    ), watch(
      () => l.value,
      (b, k) => {
        var y, C;
        b !== k && (b ? (y = v.value) == null || y.show() : (C = v.value) == null || C.hide());
      }
    ), ye(c, "show.bs.popover", () => t("show")), ye(c, "shown.bs.popover", () => t("shown")), ye(c, "hide.bs.popover", () => t("hide")), ye(c, "hidden.bs.popover", () => t("hidden")), ye(c, "inserted.bs.popover", () => t("inserted")), onMounted(() => {
      var b, k, y;
      nextTick(() => {
        T(e.target);
      }), (k = (b = f.value) == null ? void 0 : b.parentNode) == null || k.removeChild(f.value), l.value && ((y = v.value) == null || y.show());
    }), onBeforeUnmount(() => {
      var b;
      (b = v.value) == null || b.dispose();
    }), {
      element: f,
      titleRef: p,
      contentRef: $,
      computedClasses: m
    };
  }
});
var $r = ["id"];
var kr = { ref: "titleRef" };
var Cr = { ref: "contentRef" };
function Sr(e, t, a, l, n, u) {
  return openBlock(), createElementBlock("div", {
    id: e.id,
    ref: "element",
    class: normalizeClass(["popover b-popover", e.computedClasses]),
    role: "tooltip",
    tabindex: "-1"
  }, [
    createBaseVNode("div", kr, [
      renderSlot(e.$slots, "title", {}, () => [
        createTextVNode(toDisplayString(e.title), 1)
      ])
    ], 512),
    createBaseVNode("div", Cr, [
      renderSlot(e.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(e.content), 1)
      ])
    ], 512)
  ], 10, $r);
}
var wr = Te(Br, [["render", Sr]]);
var Tr = ["aria-valuenow", "aria-valuemax"];
var Il = defineComponent({
  __name: "BProgressBar",
  props: {
    animated: { default: false },
    label: null,
    labelHtml: null,
    max: null,
    precision: { default: 0 },
    showProgress: { default: false },
    showValue: { default: false },
    striped: { default: false },
    value: { default: 0 },
    variant: null
  },
  setup(e) {
    const t = e, a = inject(Fl), l = r(toRef(t, "animated")), n = r(toRef(t, "showProgress")), u = r(toRef(t, "showValue")), f = r(toRef(t, "striped")), c = computed(() => ({
      "progress-bar-animated": l.value || (a == null ? void 0 : a.animated),
      "progress-bar-striped": f.value || (a == null ? void 0 : a.striped) || l.value || (a == null ? void 0 : a.animated),
      [`bg-${t.variant}`]: t.variant !== void 0
    })), v = computed(
      () => typeof t.precision == "number" ? t.precision : Number.parseFloat(t.precision)
    ), p = computed(
      () => typeof t.value == "number" ? t.value : Number.parseFloat(t.value)
    ), $ = computed(
      () => typeof t.max == "number" ? t.max : t.max === void 0 ? void 0 : Number.parseFloat(t.max)
    ), m = computed(
      () => t.labelHtml !== void 0 ? t.labelHtml : u.value || (a == null ? void 0 : a.showValue) ? p.value.toFixed(v.value) : n.value || (a == null ? void 0 : a.showProgress) ? (p.value * 100 / ($.value || 100)).toFixed(v.value) : t.label !== void 0 ? t.label : ""
    ), B = computed(
      () => a != null && a.max ? `${p.value * 100 / (typeof a.max == "number" ? a.max : Number.parseInt(a.max))}%` : t.max ? `${p.value * 100 / (typeof t.max == "number" ? t.max : Number.parseInt(t.max))}%` : typeof t.value == "string" ? t.value : `${t.value}%`
    );
    return (V, T) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["progress-bar", unref(c)]),
      role: "progressbar",
      "aria-valuenow": e.value,
      "aria-valuemin": "0",
      "aria-valuemax": e.max,
      style: normalizeStyle({ width: unref(B) })
    }, [
      renderSlot(V.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(unref(m)), 1)
      ])
    ], 14, Tr));
  }
});
var Fl = Symbol();
var _r = defineComponent({
  __name: "BProgress",
  props: {
    variant: null,
    max: null,
    height: null,
    animated: { default: false },
    precision: { default: 0 },
    showProgress: { default: false },
    showValue: { default: false },
    striped: { default: false },
    value: { default: 0 }
  },
  setup(e) {
    const t = e, a = r(toRef(t, "animated")), l = r(toRef(t, "showProgress")), n = r(toRef(t, "showValue")), u = r(toRef(t, "striped")), f = computed(() => ({
      animated: t.animated,
      max: t.max,
      precision: t.precision,
      showProgress: t.showProgress,
      showValue: t.showValue,
      striped: t.striped,
      value: t.value,
      variant: t.variant
    }));
    return provide(Fl, {
      animated: a.value,
      max: t.max,
      showProgress: l.value,
      showValue: n.value,
      striped: u.value
    }), (c, v) => (openBlock(), createElementBlock("div", {
      class: "progress",
      style: normalizeStyle({ height: e.height })
    }, [
      renderSlot(c.$slots, "default", {}, () => [
        createVNode(Il, normalizeProps(guardReactiveProps(unref(f))), null, 16)
      ])
    ], 4));
  }
});
var Ba = kt("cols", [""], { type: [String, Number], default: null });
var Vr = defineComponent({
  name: "BRow",
  props: {
    tag: { type: String, default: "div" },
    gutterX: { type: String, default: null },
    gutterY: { type: String, default: null },
    noGutters: { type: [Boolean, String], default: false },
    alignV: { type: String, default: null },
    alignH: { type: String, default: null },
    alignContent: { type: String, default: null },
    ...Ba
  },
  setup(e) {
    const t = r(toRef(e, "noGutters")), a = ot(toRef(e, "alignH")), l = computed(() => Ga(e, Ba, "cols", "row-cols"));
    return {
      computedClasses: computed(() => [
        l.value,
        {
          [`gx-${e.gutterX}`]: e.gutterX !== null,
          [`gy-${e.gutterY}`]: e.gutterY !== null,
          "g-0": t.value,
          [`align-items-${e.alignV}`]: e.alignV !== null,
          [a.value]: e.alignH !== null,
          [`align-content-${e.alignContent}`]: e.alignContent !== null
        }
      ])
    };
  }
});
function Ar(e, t, a, l, n, u) {
  return openBlock(), createBlock(resolveDynamicComponent(e.tag), {
    class: normalizeClass(["row", e.computedClasses])
  }, {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 8, ["class"]);
}
var xr = Te(Vr, [["render", Ar]]);
var vt = defineComponent({
  __name: "BSkeleton",
  props: {
    height: null,
    width: null,
    size: null,
    animation: { default: "wave" },
    type: { default: "text" },
    variant: null
  },
  setup(e) {
    const t = e, a = computed(() => [
      `b-skeleton-${t.type}`,
      {
        [`b-skeleton-animate-${t.animation}`]: typeof t.animation == "boolean" ? false : t.animation,
        [`bg-${t.variant}`]: t.variant !== void 0
      }
    ]), l = computed(() => ({
      width: t.size || t.width,
      height: t.size || t.height
    }));
    return (n, u) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["b-skeleton", unref(a)]),
      style: normalizeStyle(unref(l))
    }, null, 6));
  }
});
var Ir = defineComponent({
  __name: "BSkeletonIcon",
  props: {
    animation: { default: "wave" }
  },
  setup(e) {
    const t = e, a = computed(() => [`b-skeleton-animate-${t.animation}`]);
    return (l, n) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["b-skeleton-icon-wrapper position-relative d-inline-block overflow-hidden", unref(a)])
    }, [
      renderSlot(l.$slots, "default")
    ], 2));
  }
});
var Fr = { key: 0 };
var Pr = { key: 1 };
var Or = defineComponent({
  __name: "BSkeletonTable",
  props: {
    animation: { default: "wave" },
    columns: { default: 5 },
    hideHeader: { default: false },
    rows: { default: 3 },
    showFooter: { default: false },
    tableProps: null
  },
  setup(e) {
    const t = e, a = r(toRef(t, "hideHeader")), l = r(toRef(t, "showFooter"));
    return (n, u) => (openBlock(), createBlock(Tt, normalizeProps(guardReactiveProps(e.tableProps)), {
      default: withCtx(() => [
        unref(a) ? createCommentVNode("", true) : (openBlock(), createElementBlock("thead", Fr, [
          createBaseVNode("tr", null, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(e.columns, (f, c) => (openBlock(), createElementBlock("th", { key: c }, [
              createVNode(vt)
            ]))), 128))
          ])
        ])),
        createBaseVNode("tbody", null, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(e.rows, (f, c) => (openBlock(), createElementBlock("tr", { key: c }, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(e.columns, (v, p) => (openBlock(), createElementBlock("td", { key: p }, [
              createVNode(vt, { width: "75%" })
            ]))), 128))
          ]))), 128))
        ]),
        unref(l) ? (openBlock(), createElementBlock("tfoot", Pr, [
          createBaseVNode("tr", null, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(e.columns, (f, c) => (openBlock(), createElementBlock("th", { key: c }, [
              createVNode(vt)
            ]))), 128))
          ])
        ])) : createCommentVNode("", true)
      ]),
      _: 1
    }, 16));
  }
});
var Lr = defineComponent({
  __name: "BSkeletonWrapper",
  props: {
    loading: { default: false }
  },
  setup(e) {
    const a = r(toRef(e, "loading"));
    return (l, n) => unref(a) ? renderSlot(l.$slots, "loading", { key: 0 }) : renderSlot(l.$slots, "default", { key: 1 });
  }
});
var $a = [
  "ar",
  "az",
  "ckb",
  "fa",
  "he",
  "ks",
  "lrc",
  "mzn",
  "ps",
  "sd",
  "te",
  "ug",
  "ur",
  "yi"
].map((e) => e.toLowerCase());
var zr = (e) => {
  const t = bt(e).toLowerCase().replace(vn, "").split("-"), a = t.slice(0, 2).join("-"), l = t[0];
  return $a.includes(a) || $a.includes(l);
};
var Nr = (e) => hn ? Lt(e) ? e : { capture: !!e || false } : !!(Lt(e) ? e.capture : e);
var Er = (e, t, a, l) => {
  e && e.addEventListener && e.addEventListener(t, a, Nr(l));
};
var Dr = (e, t, a, l) => {
  e && e.removeEventListener && e.removeEventListener(t, a, l);
};
var ka = (e, t) => {
  (e ? Er : Dr)(...t);
};
var rt = (e, { preventDefault: t = true, propagation: a = true, immediatePropagation: l = false } = {}) => {
  t && e.preventDefault(), a && e.stopPropagation(), l && e.stopImmediatePropagation();
};
var Mt = "ArrowDown";
var Pl = "End";
var Ol = "Home";
var Ll = "PageDown";
var zl = "PageUp";
var jt = "ArrowUp";
var Ca = 1;
var Sa = 100;
var wa = 1;
var Ta = 500;
var _a = 100;
var Va = 10;
var Aa = 4;
var xa = [jt, Mt, Ol, Pl, zl, Ll];
var Hr = defineComponent({
  props: {
    ariaControls: { type: String, required: false },
    ariaLabel: { type: String, required: false },
    labelIncrement: { type: String, default: "Increment" },
    labelDecrement: { type: String, default: "Decrement" },
    modelValue: { type: Number, default: null },
    name: { type: String, default: "BFormSpinbutton" },
    disabled: { type: [Boolean, String], default: false },
    placeholder: { type: String, required: false },
    locale: { type: String, default: "locale" },
    form: { type: String, required: false },
    inline: { type: Boolean, default: false },
    size: { type: String, required: false },
    formatterFn: {
      type: Function
    },
    readonly: { type: Boolean, default: false },
    vertical: { type: Boolean, default: false },
    repeatDelay: {
      type: [String, Number],
      default: Ta
    },
    repeatInterval: {
      type: [String, Number],
      default: _a
    },
    repeatStepMultiplier: {
      type: [String, Number],
      default: Aa
    },
    repeatThreshold: {
      type: [String, Number],
      default: Va
    },
    required: { type: [Boolean, String], default: false },
    step: { type: [String, Number], default: wa },
    min: { type: [String, Number], default: Ca },
    max: { type: [String, Number], default: Sa },
    wrap: { type: Boolean, default: false },
    state: { type: [Boolean, String], default: null }
  },
  emits: ["update:modelValue", "change"],
  setup(e, { emit: t }) {
    const a = ref(false), l = computed(() => 1), n = () => {
      t("change", f.value);
    }, u = ref(null), f = computed({
      get() {
        return Ne(e.modelValue) ? u.value : e.modelValue;
      },
      set(H) {
        Ne(e.modelValue) ? u.value = H : t("update:modelValue", H);
      }
    });
    let c, v, p = false;
    const $ = computed(() => tt(e.step, wa)), m = computed(() => tt(e.min, Ca)), B = computed(() => {
      const H = tt(e.max, Sa), N = $.value, X = m.value;
      return Math.floor((H - X) / N) * N + X;
    }), V = computed(() => {
      const H = Le(e.repeatDelay, 0);
      return H > 0 ? H : Ta;
    }), T = computed(() => {
      const H = Le(e.repeatInterval, 0);
      return H > 0 ? H : _a;
    }), b = computed(
      () => Math.max(Le(e.repeatThreshold, Va), 1)
    ), k = computed(
      () => Math.max(Le(e.repeatStepMultiplier, Aa), 1)
    ), y = computed(() => {
      const H = $.value;
      return Math.floor(H) === H ? 0 : (H.toString().split(".")[1] || "").length;
    }), C = computed(() => Math.pow(10, y.value || 0)), q = computed(() => {
      const { value: H } = f;
      return H === null ? "" : H.toFixed(y.value);
    }), E = computed(() => {
      const H = [e.locale];
      return new Intl.NumberFormat(H).resolvedOptions().locale;
    }), I = computed(
      () => zr(E.value)
    ), _ = () => {
      const H = y.value;
      return new Intl.NumberFormat(E.value, {
        style: "decimal",
        useGrouping: false,
        minimumIntegerDigits: 1,
        minimumFractionDigits: H,
        maximumFractionDigits: H,
        notation: "standard"
      }).format;
    }, z = computed(
      () => e.formatterFn ? e.formatterFn : _()
    ), x = computed(() => ({
      role: "group",
      lang: E.value,
      tabindex: e.disabled ? null : "-1",
      title: e.ariaLabel
    })), w = computed(() => !Ne(e.modelValue) || !Ne(u.value)), P = computed(() => ({
      dir: I.value,
      spinId: l.value,
      tabindex: e.disabled ? null : "0",
      role: "spinbutton",
      "aria-live": "off",
      "aria-label": e.ariaLabel || null,
      "aria-controls": e.ariaControls || null,
      "aria-invalid": e.state === false || !w.value && e.required ? "true" : null,
      "aria-required": e.required ? "true" : null,
      "aria-valuemin": m.value,
      "aria-valuemax": B.value,
      "aria-valuenow": Ne(f.value) ? null : f.value,
      "aria-valuetext": Ne(f.value) ? null : z.value(f.value)
    })), L = (H) => {
      let { value: N } = f;
      if (!e.disabled && !Ne(N)) {
        const X = $.value * H, le = m.value, ne = B.value, pe = C.value, { wrap: _e } = e;
        N = Math.round((N - le) / X) * X + le + X, N = Math.round(N * pe) / pe, f.value = N > ne ? _e ? le : ne : N < le ? _e ? ne : le : N;
      }
    }, ee = (H = 1) => {
      Ne(f.value) ? f.value = m.value : L(1 * H);
    }, Z = (H = 1) => {
      Ne(f.value) ? f.value = e.wrap ? B.value : m.value : L(-1 * H);
    }, se = (H) => {
      const { code: N, altKey: X, ctrlKey: le, metaKey: ne } = H;
      if (!(e.disabled || e.readonly || X || le || ne) && xa.includes(N)) {
        if (rt(H, { propagation: false }), p)
          return;
        Ce(), [jt, Mt].includes(N) ? (p = true, N === jt ? ve(H, ee) : N === Mt && ve(H, Z)) : N === zl ? ee(k.value) : N === Ll ? Z(k.value) : N === Ol ? f.value = m.value : N === Pl && (f.value = B.value);
      }
    }, K = (H) => {
      const { code: N, altKey: X, ctrlKey: le, metaKey: ne } = H;
      e.disabled || e.readonly || X || le || ne || xa.includes(N) && (rt(H, { propagation: false }), Ce(), p = false, n());
    }, ve = (H, N) => {
      const { type: X } = H || {};
      if (!e.disabled && !e.readonly) {
        if (te(H) && X === "mousedown" && H.button)
          return;
        Ce(), N(1);
        const le = b.value, ne = k.value, pe = V.value, _e = T.value;
        c = setTimeout(() => {
          let We = 0;
          v = setInterval(() => {
            N(We < le ? 1 : ne), We++;
          }, _e);
        }, pe);
      }
    };
    function te(H) {
      return H.type === "mouseup" || H.type === "mousedown";
    }
    const be = (H) => {
      te(H) && H.type === "mouseup" && H.button || (rt(H, { propagation: false }), Ce(), ge(false), n());
    }, ge = (H) => {
      try {
        ka(H, [document.body, "mouseup", be, false]), ka(H, [document.body, "touchend", be, false]);
      } catch {
        return 0;
      }
    }, Ce = () => {
      clearTimeout(c), clearInterval(v), c = void 0, v = void 0;
    }, ie = (H, N, X, le, ne, pe, _e) => {
      const We = h(X, {
        props: { scale: a.value ? 1.5 : 1.25 },
        attrs: { "aria-hidden": "true" }
      }), _t = { hasFocus: a.value }, st = (Re) => {
        !e.disabled && !e.readonly && (rt(Re, { propagation: false }), ge(true), ve(Re, H));
      };
      return h(
        "button",
        {
          class: [{ "py-0": !e.vertical }, "btn", "btn-sm", "border-0", "rounded-0"],
          tabindex: "-1",
          type: "button",
          disabled: e.disabled || e.readonly || pe,
          "aria-disabled": e.disabled || e.readonly || pe ? "true" : null,
          "aria-controls": l.value,
          "aria-label": N || null,
          "aria-keyshortcuts": ne || null,
          onmousedown: st,
          ontouchstart: st
        },
        [Fe(_e, _t) || We]
      );
    };
    return () => {
      const H = ie(
        ee,
        e.labelIncrement,
        h(
          "svg",
          {
            xmlns: "http://www.w3.org/2000/svg",
            width: "16",
            height: "16",
            fill: "currentColor",
            class: "bi bi-plus",
            viewBox: "0 0 16 16"
          },
          h("path", {
            d: "M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z"
          })
        ),
        "inc",
        "ArrowUp",
        false,
        "increment"
      ), N = ie(
        Z,
        e.labelDecrement,
        h(
          "svg",
          {
            xmlns: "http://www.w3.org/2000/svg",
            width: "16",
            height: "16",
            fill: "currentColor",
            class: "bi bi-dash",
            viewBox: "0 0 16 16"
          },
          h("path", { d: "M4 8a.5.5 0 0 1 .5-.5h7a.5.5 0 0 1 0 1h-7A.5.5 0 0 1 4 8z" })
        ),
        "dec",
        "ArrowDown",
        false,
        "decrement"
      ), X = [];
      e.name && !e.disabled && X.push(
        h("input", {
          type: "hidden",
          name: e.name,
          form: e.form || null,
          value: q.value,
          key: "hidden"
        })
      );
      const le = h(
        "output",
        {
          class: [
            { "d-flex": e.vertical },
            { "align-self-center": !e.vertical },
            { "align-items-center": e.vertical },
            { "border-top": e.vertical },
            { "border-bottom": e.vertical },
            { "border-start": !e.vertical },
            { "border-end": !e.vertical },
            "flex-grow-1"
          ],
          ...P.value,
          key: "output"
        },
        [
          h(
            "bdi",
            w.value ? z.value(f.value) : e.placeholder || ""
          )
        ]
      );
      return h(
        "div",
        {
          class: [
            "b-form-spinbutton form-control",
            { disabled: e.disabled },
            { readonly: e.readonly },
            { focus: a },
            { "d-inline-flex": e.inline || e.vertical },
            { "d-flex": !e.inline && !e.vertical },
            { "align-items-stretch": !e.vertical },
            { "flex-column": e.vertical },
            e.size ? `form-control-${e.size}` : null
          ],
          ...x.value,
          onkeydown: se,
          onkeyup: K
        },
        e.vertical ? [H, X, le, N] : [N, X, le, H]
      );
    };
  }
});
var Rr = ["TD", "TH", "TR"];
var qr = [
  "a",
  "a *",
  "button",
  "button *",
  "input:not(.disabled):not([disabled])",
  "select:not(.disabled):not([disabled])",
  "textarea:not(.disabled):not([disabled])",
  '[role="link"]',
  '[role="link"] *',
  '[role="button"]',
  '[role="button"] *',
  "[tabindex]:not(.disabled):not([disabled])"
].join(",");
var ut = (e) => {
  if (!e || !e.target)
    return false;
  const t = e.target;
  if ("disabled" in t && t.disabled || Rr.indexOf(t.tagName) !== -1)
    return false;
  if (oa(".dropdown-menu", t))
    return true;
  const a = t.tagName === "LABEL" ? t : oa("label", t);
  if (a) {
    const l = Xt(a, "for"), n = l ? Vn(l) : Ma("input, select, textarea", a);
    if (n && !n.disabled)
      return true;
  }
  return ja(t, qr);
};
var Mr = () => {
  const e = (v, p) => {
    const $ = [];
    return !(v != null && v.length) && (p == null ? void 0 : p.length) ? (Object.keys(p[0]).forEach((m) => $.push({ key: m, label: aa(m) })), $) : (Array.isArray(v) && v.forEach((m) => {
      typeof m == "string" ? $.push({ key: m, label: aa(m) }) : Lt(m) && m.key && typeof m.key == "string" && $.push({ ...m });
    }), $);
  }, t = ref([]), a = (v, p, $, m) => (t.value = mt(p), "isFilterableTable" in m && m.isFilterableTable.value === true && $.filter && (t.value = u(t.value, $.filter, $.filterable)), "isSortable" in m && m.isSortable.value === true && (t.value = n(
    v,
    t.value,
    {
      key: $.sortBy,
      desc: m.sortDescBoolean.value
    },
    $.sortCompare
  )), t.value), l = ref(void 0), n = (v, p, $, m) => {
    if (!$ || !$.key)
      return p;
    const B = $.key;
    return p.sort((V, T) => {
      if (m !== void 0)
        return m(V, T, $.key, $.desc);
      const b = (C) => typeof C == "object" ? JSON.stringify(C) : C;
      return b(V[B]) > b(T[B]) ? $.desc ? -1 : 1 : b(T[B]) > b(V[B]) ? $.desc ? 1 : -1 : 0;
    });
  }, u = (v, p, $) => v.filter(
    (m) => Object.entries(m).filter((B) => {
      const [V, T] = B;
      return !T || V[0] === "_" || $.length > 0 && !$.includes(V) ? false : (typeof T == "object" ? JSON.stringify(Object.values(T)) : typeof T == "string" ? T : T.toString()).toLowerCase().includes(p.toLowerCase());
    }).length > 0
  );
  return {
    normaliseFields: e,
    mapItems: a,
    internalItems: t,
    updateInternalItems: async (v) => {
      try {
        return t.value = await Ot(v), t.value;
      } catch {
        return;
      }
    },
    filterEvent: l,
    notifyFilteredItems: () => {
      l.value && l.value(t.value);
    }
  };
};
var jr = ["title", "abbr", "onClick"];
var Gr = { class: "d-inline-flex flex-nowrap align-items-center gap-1" };
var Ur = { key: 1 };
var Wr = ["onClick", "onDblclick", "onMouseenter", "onMouseleave"];
var Kr = {
  key: 0,
  class: "b-table-stacked-label"
};
var Xr = ["colspan"];
var Jr = ["colspan"];
var Yr = { class: "d-flex align-items-center justify-content-center gap-2" };
var Zr = createBaseVNode("strong", null, "Loading...", -1);
var Qr = {
  key: 1,
  class: "b-table-empty-slot"
};
var eu = ["colspan"];
var tu = { key: 0 };
var au = ["title", "abbr", "onClick"];
var lu = { key: 1 };
var nu = { key: 2 };
var ou = { key: 3 };
var su = defineComponent({
  __name: "BTable",
  props: {
    align: null,
    caption: null,
    captionTop: { default: false },
    borderless: { default: false },
    bordered: { default: false },
    borderVariant: null,
    dark: { default: false },
    fields: { default: () => [] },
    footClone: { default: false },
    hover: { default: false },
    items: { default: () => [] },
    provider: null,
    sortCompare: null,
    noProvider: null,
    noProviderPaging: null,
    noProviderSorting: null,
    noProviderFiltering: null,
    responsive: { type: [Boolean, String], default: false },
    small: { default: false },
    striped: { default: false },
    stacked: { type: [Boolean, String], default: false },
    labelStacked: { type: Boolean, default: false },
    variant: null,
    sortBy: null,
    sortDesc: { default: false },
    sortInternal: { default: true },
    selectable: { default: false },
    stickySelect: { default: false },
    selectHead: { type: [Boolean, String], default: true },
    selectMode: { default: "single" },
    selectionVariant: { default: "primary" },
    stickyHeader: { default: false },
    busy: { default: false },
    showEmpty: { default: false },
    perPage: null,
    currentPage: { default: 1 },
    filter: null,
    filterable: null,
    emptyText: { default: "There are no records to show" },
    emptyFilteredText: { default: "There are no records matching your request" }
  },
  emits: ["headClicked", "rowClicked", "rowDblClicked", "rowHovered", "rowUnhovered", "rowSelected", "rowUnselected", "selection", "update:busy", "update:sortBy", "update:sortDesc", "sorted", "filtered"],
  setup(e, { expose: t, emit: a }) {
    const l = e, n = useSlots(), u = Mr(), f = r(toRef(l, "footClone")), c = r(toRef(l, "sortDesc")), v = r(toRef(l, "sortInternal")), p = r(toRef(l, "selectable")), $ = r(toRef(l, "stickySelect")), m = r(toRef(l, "labelStacked")), B = r(toRef(l, "busy")), V = r(toRef(l, "showEmpty")), T = r(toRef(l, "noProviderPaging")), b = r(toRef(l, "noProviderSorting")), k = r(toRef(l, "noProviderFiltering")), y = ref(B.value);
    u.filterEvent.value = async (S) => {
      if (w.value) {
        await N();
        return;
      }
      const j = await Ot(S);
      a("filtered", j);
    };
    const C = ref(/* @__PURE__ */ new Set([])), q = computed(() => C.value.size > 0), E = computed(() => ({
      [`align-${l.align}`]: l.align !== void 0,
      "b-table-selectable": p.value,
      [`b-table-select-${l.selectMode}`]: p.value,
      "b-table-selecting user-select-none": p.value && q.value,
      "b-table-busy": y.value,
      "b-table-sortable": L.value,
      "b-table-sort-desc": L.value && c.value === true,
      "b-table-sort-asc": L.value && c.value === false
    })), I = computed(() => ({
      bordered: l.bordered,
      borderless: l.borderless,
      borderVariant: l.borderVariant,
      captionTop: l.captionTop,
      dark: l.dark,
      hover: l.hover,
      responsive: l.responsive,
      striped: l.striped,
      stacked: l.stacked,
      small: l.small,
      tableClass: E.value,
      tableVariant: l.variant,
      stickyHeader: l.stickyHeader
    })), _ = computed(() => u.normaliseFields(l.fields, l.items)), z = computed(
      () => _.value.length + (p.value ? 1 : 0)
    ), x = computed(() => l.filter !== void 0 && l.filter !== ""), w = computed(() => l.provider !== void 0), P = computed(
      () => p.value && (!!l.selectHead || n.selectHead !== void 0)
    ), L = computed(
      () => l.fields.filter((S) => typeof S == "string" ? false : S.sortable).length > 0
    ), ee = computed(() => L.value && v.value === true), Z = computed(() => {
      const S = w.value ? u.internalItems.value : ee.value ? u.mapItems(l.fields, l.items, l, {
        isSortable: L,
        isFilterableTable: x,
        sortDescBoolean: c
      }) : l.items;
      if (l.perPage !== void 0) {
        const j = (l.currentPage - 1) * l.perPage;
        return S.splice(j, l.perPage);
      }
      return S;
    }), se = (S) => typeof S == "string" ? la(S) : S.label !== void 0 ? S.label : typeof S.key == "string" ? la(S.key) : S.key, K = (S, j, re = false) => {
      const R = typeof S == "string" ? S : S.key;
      a("headClicked", R, S, j, re), Ce(S);
    }, ve = (S, j, re) => {
      a("rowClicked", S, j, re), H(S, j, re.shiftKey);
    }, te = (S, j, re) => a("rowDblClicked", S, j, re), be = (S, j, re) => a("rowHovered", S, j, re), ge = (S, j, re) => a("rowUnhovered", S, j, re), Ce = (S) => {
      if (!L.value)
        return;
      const j = typeof S == "string" ? S : S.key, re = typeof S == "string" ? false : S.sortable;
      if (L.value === true && re === true) {
        const R = !c.value;
        j !== l.sortBy && a("update:sortBy", j), a("update:sortDesc", R), a("sorted", j, R);
      }
    }, ie = () => {
      !p.value || a("selection", Array.from(C.value));
    }, H = (S, j, re = false) => {
      if (!!p.value) {
        if (C.value.has(S))
          C.value.delete(S), a("rowUnselected", S);
        else if (l.selectMode === "single" && C.value.size > 0 && (C.value.forEach((R) => a("rowUnselected", R)), C.value.clear()), l.selectMode === "range" && C.value.size > 0 && re) {
          const R = Array.from(C.value).pop(), Se = Z.value.findIndex((Me) => Me === R), ue = Math.min(Se, j), Vt = Math.max(Se, j);
          Z.value.slice(ue, Vt + 1).forEach((Me) => {
            C.value.has(Me) || (C.value.add(Me), a("rowSelected", Me));
          });
        } else
          C.value.add(S), a("rowSelected", S);
        ie();
      }
    }, N = async () => {
      if (!w.value || !l.provider || y.value)
        return;
      y.value = true;
      const S = new Proxy(
        {
          currentPage: l.currentPage,
          filter: l.filter,
          sortBy: l.sortBy,
          sortDesc: l.sortDesc,
          perPage: l.perPage
        },
        {
          get(re, R) {
            return R in re ? re[R] : void 0;
          },
          set() {
            return console.error("BTable provider context is a read-only object."), true;
          }
        }
      ), j = l.provider(S, u.updateInternalItems);
      if (j !== void 0) {
        if (j instanceof Promise)
          try {
            const re = await j;
            return Array.isArray(re) ? await u.updateInternalItems(re) : void 0;
          } finally {
            y.value && (y.value = false);
          }
        try {
          return await u.updateInternalItems(j);
        } finally {
          y.value && (y.value = false);
        }
      }
    }, X = (S) => {
      S._showDetails = !S._showDetails;
    }, le = (S) => [
      S.class,
      S.thClass,
      S.variant ? `table-${S.variant}` : void 0,
      {
        "b-table-sortable-column": L.value && S.sortable,
        "b-table-sticky-column": S.stickyColumn
      }
    ], ne = (S, j) => [
      S.class,
      S.tdClass,
      S.variant ? `table-${S.variant}` : void 0,
      (j == null ? void 0 : j._cellVariants) && (j == null ? void 0 : j._cellVariants[S.key]) ? `table-${j == null ? void 0 : j._cellVariants[S.key]}` : void 0,
      {
        "b-table-sticky-column": S.stickyColumn
      }
    ], pe = (S) => [
      S._rowVariant ? `table-${S._rowVariant}` : null,
      S._rowVariant ? `table-${S._rowVariant}` : null,
      p.value && C.value.has(S) ? `selected table-${l.selectionVariant}` : null
    ], _e = () => {
      if (!p.value)
        return;
      const S = C.value.size > 0 ? Array.from(C.value) : [];
      C.value = /* @__PURE__ */ new Set([...Z.value]), C.value.forEach((j) => {
        S.includes(j) || a("rowSelected", j);
      }), ie();
    }, We = () => {
      !p.value || (C.value.forEach((S) => {
        a("rowUnselected", S);
      }), C.value = /* @__PURE__ */ new Set([]), ie());
    }, _t = (S) => {
      if (!p.value)
        return;
      const j = Z.value[S];
      !j || C.value.has(j) || (C.value.add(j), a("rowSelected", j), ie());
    }, st = (S) => {
      if (!p.value)
        return;
      const j = Z.value[S];
      !j || !C.value.has(j) || (C.value.delete(j), a("rowUnselected", j), ie());
    }, Re = async (S, j, re) => {
      if (j === re)
        return;
      const R = (El) => l.noProvider && l.noProvider.includes(El), Se = !["currentPage", "perPage"].includes(S), ue = ["currentPage", "perPage"].includes(S) && (R("paging") || T.value === true), Vt = ["filter"].includes(S) && (R("filtering") || k.value === true), Me = ["sortBy", "sortDesc"].includes(S) && (R("sorting") || b.value === true);
      ue || Vt || Me || (await N(), Se && u.notifyFilteredItems());
    };
    return watch(
      () => l.filter,
      (S, j) => {
        S === j || w.value || S || Ot(l.items).then((re) => a("filtered", re));
      }
    ), watch(
      () => y.value,
      () => y.value !== B.value && a("update:busy", y.value)
    ), watch(
      () => B.value,
      () => y.value !== B.value && (y.value = B.value)
    ), watch(
      () => l.filter,
      (S, j) => Re("filter", S, j)
    ), watch(
      () => l.currentPage,
      (S, j) => Re("currentPage", S, j)
    ), watch(
      () => l.perPage,
      (S, j) => Re("perPage", S, j)
    ), watch(
      () => l.sortBy,
      (S, j) => Re("sortBy", S, j)
    ), watch(
      () => l.sortDesc,
      (S, j) => Re("sortDesc", S, j)
    ), onMounted(() => {
      w.value && N();
    }), t({
      selectAllRows: _e,
      clearSelected: We,
      selectRow: _t,
      unselectRow: st
    }), (S, j) => (openBlock(), createBlock(Tt, normalizeProps(guardReactiveProps(unref(I))), {
      default: withCtx(() => {
        var re;
        return [
          createBaseVNode("thead", null, [
            S.$slots["thead-top"] ? renderSlot(S.$slots, "thead-top", { key: 0 }) : createCommentVNode("", true),
            createBaseVNode("tr", null, [
              unref(P) ? (openBlock(), createElementBlock("th", {
                key: 0,
                class: normalizeClass(["b-table-selection-column", {
                  "b-table-sticky-column": unref($)
                }])
              }, [
                renderSlot(S.$slots, "select-head", {}, () => [
                  createTextVNode(toDisplayString(typeof e.selectHead == "boolean" ? "Selected" : e.selectHead), 1)
                ])
              ], 2)) : createCommentVNode("", true),
              (openBlock(true), createElementBlock(Fragment, null, renderList(unref(_), (R) => (openBlock(), createElementBlock("th", mergeProps({
                key: R.key,
                scope: "col",
                class: le(R),
                title: R.headerTitle,
                abbr: R.headerAbbr,
                style: R.thStyle
              }, R.thAttr, {
                onClick: (Se) => K(R, Se)
              }), [
                createBaseVNode("div", Gr, [
                  renderSlot(S.$slots, "sort-icon", {
                    field: R,
                    sortBy: e.sortBy,
                    selected: R.key === e.sortBy,
                    isDesc: unref(c),
                    direction: unref(c) ? "desc" : "asc"
                  }, () => [
                    unref(L) && R.sortable ? (openBlock(), createElementBlock("span", {
                      key: 0,
                      class: normalizeClass(["b-table-sort-icon", {
                        sorted: R.key === e.sortBy,
                        [`sorted-${unref(c) ? "desc" : "asc"}`]: R.key === e.sortBy
                      }])
                    }, null, 2)) : createCommentVNode("", true)
                  ]),
                  createBaseVNode("div", null, [
                    S.$slots["head(" + R.key + ")"] || S.$slots["head()"] ? renderSlot(S.$slots, S.$slots["head(" + R.key + ")"] ? "head(" + R.key + ")" : "head()", {
                      key: 0,
                      label: R.label
                    }) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
                      createTextVNode(toDisplayString(se(R)), 1)
                    ], 64))
                  ])
                ])
              ], 16, jr))), 128))
            ]),
            S.$slots["thead-sub"] ? (openBlock(), createElementBlock("tr", Ur, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(unref(_), (R) => (openBlock(), createElementBlock("td", {
                key: R.key,
                scope: "col",
                class: normalizeClass([R.class, R.thClass, R.variant ? `table-${R.variant}` : ""])
              }, [
                S.$slots["thead-sub"] ? renderSlot(S.$slots, "thead-sub", mergeProps({
                  key: 0,
                  items: unref(_)
                }, R)) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
                  createTextVNode(toDisplayString(R.label), 1)
                ], 64))
              ], 2))), 128))
            ])) : createCommentVNode("", true)
          ]),
          createBaseVNode("tbody", null, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(unref(Z), (R, Se) => (openBlock(), createElementBlock(Fragment, { key: Se }, [
              createBaseVNode("tr", {
                class: normalizeClass(pe(R)),
                onClick: (ue) => !unref(ut)(ue) && ve(R, Se, ue),
                onDblclick: (ue) => !unref(ut)(ue) && te(R, Se, ue),
                onMouseenter: (ue) => !unref(ut)(ue) && be(R, Se, ue),
                onMouseleave: (ue) => !unref(ut)(ue) && ge(R, Se, ue)
              }, [
                unref(P) ? (openBlock(), createElementBlock("td", {
                  key: 0,
                  class: normalizeClass(["b-table-selection-column", {
                    "b-table-sticky-column": unref($)
                  }])
                }, [
                  renderSlot(S.$slots, "select-cell", {}, () => [
                    createBaseVNode("span", {
                      class: normalizeClass(C.value.has(R) ? "text-primary" : "")
                    }, "\u{1F5F9}", 2)
                  ])
                ], 2)) : createCommentVNode("", true),
                (openBlock(true), createElementBlock(Fragment, null, renderList(unref(_), (ue) => (openBlock(), createElementBlock("td", mergeProps({
                  key: ue.key
                }, ue.tdAttr, {
                  class: ne(ue, R)
                }), [
                  e.stacked && unref(m) ? (openBlock(), createElementBlock("label", Kr, toDisplayString(se(ue)), 1)) : createCommentVNode("", true),
                  S.$slots["cell(" + ue.key + ")"] || S.$slots["cell()"] ? renderSlot(S.$slots, S.$slots["cell(" + ue.key + ")"] ? "cell(" + ue.key + ")" : "cell()", {
                    key: 1,
                    value: R[ue.key],
                    index: Se,
                    item: R,
                    field: ue,
                    items: e.items,
                    toggleDetails: () => X(R),
                    detailsShowing: R._showDetails
                  }) : (openBlock(), createElementBlock(Fragment, { key: 2 }, [
                    createTextVNode(toDisplayString(R[ue.key]), 1)
                  ], 64))
                ], 16))), 128))
              ], 42, Wr),
              R._showDetails === true && S.$slots["row-details"] ? (openBlock(), createElementBlock("tr", {
                key: 0,
                class: normalizeClass(pe(R))
              }, [
                createBaseVNode("td", { colspan: unref(z) }, [
                  renderSlot(S.$slots, "row-details", {
                    item: R,
                    toggleDetails: () => X(R)
                  })
                ], 8, Xr)
              ], 2)) : createCommentVNode("", true)
            ], 64))), 128)),
            y.value ? (openBlock(), createElementBlock("tr", {
              key: 0,
              class: normalizeClass(["b-table-busy-slot", { "b-table-static-busy": unref(Z).length == 0 }])
            }, [
              createBaseVNode("td", { colspan: unref(z) }, [
                renderSlot(S.$slots, "table-busy", {}, () => [
                  createBaseVNode("div", Yr, [
                    createVNode(St, { class: "align-middle" }),
                    Zr
                  ])
                ])
              ], 8, Jr)
            ], 2)) : createCommentVNode("", true),
            unref(V) && unref(Z).length === 0 ? (openBlock(), createElementBlock("tr", Qr, [
              createBaseVNode("td", { colspan: unref(z) }, [
                renderSlot(S.$slots, "empty", {
                  items: unref(Z),
                  filtered: unref(x)
                }, () => [
                  createTextVNode(toDisplayString(unref(x) ? e.emptyFilteredText : e.emptyText), 1)
                ])
              ], 8, eu)
            ])) : createCommentVNode("", true)
          ]),
          unref(f) ? (openBlock(), createElementBlock("tfoot", tu, [
            createBaseVNode("tr", null, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(unref(_), (R) => (openBlock(), createElementBlock("th", mergeProps({
                key: R.key
              }, R.thAttr, {
                scope: "col",
                class: [R.class, R.thClass, R.variant ? `table-${R.variant}` : ""],
                title: R.headerTitle,
                abbr: R.headerAbbr,
                style: R.thStyle,
                onClick: (Se) => K(R, Se, true)
              }), toDisplayString(R.label), 17, au))), 128))
            ])
          ])) : S.$slots["custom-foot"] ? (openBlock(), createElementBlock("tfoot", lu, [
            renderSlot(S.$slots, "custom-foot", {
              fields: unref(_),
              items: e.items,
              columns: (re = unref(_)) == null ? void 0 : re.length
            })
          ])) : createCommentVNode("", true),
          S.$slots["table-caption"] ? (openBlock(), createElementBlock("caption", nu, [
            renderSlot(S.$slots, "table-caption")
          ])) : e.caption ? (openBlock(), createElementBlock("caption", ou, toDisplayString(e.caption), 1)) : createCommentVNode("", true)
        ];
      }),
      _: 3
    }, 16));
  }
});
var iu = defineComponent({
  __name: "BTbody",
  props: {
    variant: null
  },
  setup(e) {
    const t = e, a = computed(() => ({
      [`thead-${t.variant}`]: t.variant !== void 0
    }));
    return (l, n) => (openBlock(), createElementBlock("tbody", {
      role: "rowgroup",
      class: normalizeClass(unref(a))
    }, [
      renderSlot(l.$slots, "default")
    ], 2));
  }
});
var ru = ["scope", "colspan", "rowspan", "data-label"];
var uu = { key: 0 };
var du = defineComponent({
  __name: "BTd",
  props: {
    colspan: null,
    rowspan: null,
    stackedHeading: null,
    stickyColumn: { default: false },
    variant: null
  },
  setup(e) {
    const t = e, a = r(toRef(t, "stickyColumn")), l = computed(() => ({
      [`table-${t.variant}`]: t.variant !== void 0,
      "b-table-sticky-column": a.value,
      "table-b-table-default": a.value && t.variant === void 0
    })), n = computed(() => t.colspan ? "colspan" : t.rowspan ? "rowspan" : "col");
    return (u, f) => (openBlock(), createElementBlock("td", {
      role: "cell",
      scope: unref(n),
      class: normalizeClass(unref(l)),
      colspan: e.colspan,
      rowspan: e.rowspan,
      "data-label": e.stackedHeading
    }, [
      e.stackedHeading ? (openBlock(), createElementBlock("div", uu, [
        renderSlot(u.$slots, "default")
      ])) : renderSlot(u.$slots, "default", { key: 1 })
    ], 10, ru));
  }
});
var cu = defineComponent({
  __name: "BTfoot",
  props: {
    variant: null
  },
  setup(e) {
    const t = e, a = computed(() => ({
      [`table-${t.variant}`]: t.variant !== void 0
    }));
    return (l, n) => (openBlock(), createElementBlock("tfoot", {
      role: "rowgroup",
      class: normalizeClass(unref(a))
    }, [
      renderSlot(l.$slots, "default")
    ], 2));
  }
});
var fu = ["scope", "colspan", "rowspan", "data-label"];
var vu = { key: 0 };
var mu = defineComponent({
  __name: "BTh",
  props: {
    colspan: null,
    rowspan: null,
    stackedHeading: null,
    stickyColumn: { default: false },
    variant: null
  },
  setup(e) {
    const t = e, a = r(toRef(t, "stickyColumn")), l = computed(() => ({
      [`table-${t.variant}`]: t.variant !== void 0,
      "b-table-sticky-column": a.value,
      "table-b-table-default": a.value && t.variant === void 0
    })), n = computed(() => t.colspan ? "colspan" : t.rowspan ? "rowspan" : "col");
    return (u, f) => (openBlock(), createElementBlock("th", {
      role: "columnheader",
      scope: unref(n),
      class: normalizeClass(unref(l)),
      colspan: e.colspan,
      rowspan: e.rowspan,
      "data-label": e.stackedHeading
    }, [
      e.stackedHeading !== void 0 ? (openBlock(), createElementBlock("div", vu, [
        renderSlot(u.$slots, "default")
      ])) : renderSlot(u.$slots, "default", { key: 1 })
    ], 10, fu));
  }
});
var bu = defineComponent({
  __name: "BThead",
  props: {
    variant: null
  },
  setup(e) {
    const t = e, a = computed(() => ({
      [`table-${t.variant}`]: t.variant !== void 0
    }));
    return (l, n) => (openBlock(), createElementBlock("thead", {
      role: "rowgroup",
      class: normalizeClass(unref(a))
    }, [
      renderSlot(l.$slots, "default")
    ], 2));
  }
});
var gu = defineComponent({
  __name: "BTr",
  props: {
    variant: null
  },
  setup(e) {
    const t = e, a = computed(() => ({
      [`table-${t.variant}`]: t.variant !== void 0
    }));
    return (l, n) => (openBlock(), createElementBlock("tr", {
      role: "row",
      class: normalizeClass(unref(a))
    }, [
      renderSlot(l.$slots, "default")
    ], 2));
  }
});
var pu = ["id", "data-bs-target", "aria-controls", "aria-selected", "onClick"];
var Nl = Symbol();
var hu = defineComponent({
  __name: "BTabs",
  props: {
    activeNavItemClass: null,
    activeTabClass: null,
    align: null,
    card: { default: false },
    contentClass: null,
    end: { default: false },
    fill: { default: false },
    id: null,
    justified: { default: false },
    lazy: { default: false },
    navClass: null,
    navWrapperClass: null,
    noFade: { default: false },
    noNavStyle: { default: false },
    pills: { default: false },
    small: { default: false },
    tag: { default: "div" },
    vertical: { default: false },
    modelValue: { default: -1 }
  },
  emits: ["update:modelValue", "activate-tab", "click"],
  setup(e, { emit: t }) {
    const a = e, l = r(toRef(a, "card")), n = r(toRef(a, "end")), u = r(toRef(a, "fill")), f = r(toRef(a, "justified")), c = r(toRef(a, "lazy")), v = r(toRef(a, "noFade")), p = r(toRef(a, "noNavStyle")), $ = r(toRef(a, "pills")), m = r(toRef(a, "small")), B = r(toRef(a, "vertical")), V = useSlots(), T = ref(a.modelValue), b = ref(""), k = computed({
      get: () => T.value,
      set: (w) => {
        T.value = w, y.value.length > 0 && w >= 0 && w < y.value.length ? b.value = y.value[w].buttonId : b.value = "", t("update:modelValue", w);
      }
    }), y = computed(() => {
      let w = [];
      return V.default && (w = x(V).map((P, L) => {
        P.props || (P.props = {});
        const ee = P.props["button-id"] || Ee("tab"), Z = P.props.id || Ee(), se = k.value > -1 ? L === k.value : P.props.active === "", K = P.props["title-item-class"], ve = P.props["title-link-attributes"];
        return {
          buttonId: ee,
          contentId: Z,
          active: se,
          disabled: P.props.disabled === "" || P.props.disabled === true,
          navItemClasses: [
            {
              active: se,
              disabled: P.props.disabled === "" || P.props.disabled === true
            },
            se && a.activeNavItemClass ? a.activeNavItemClass : null,
            P.props["title-link-class"]
          ],
          tabClasses: [
            {
              fade: !v.value
            },
            se && a.activeTabClass ? a.activeTabClass : null
          ],
          target: `#${Z}`,
          title: P.props.title,
          titleItemClass: K,
          titleLinkAttributes: ve,
          onClick: P.props.onClick,
          tab: P,
          tabComponent: () => x(V)[L]
        };
      })), w;
    }), C = computed(() => !((y == null ? void 0 : y.value) && y.value.length > 0)), q = computed(() => ({
      "d-flex": B.value,
      "align-items-start": B.value
    })), E = ot(toRef(a, "align")), I = computed(() => ({
      "nav-pills": $.value,
      "flex-column me-3": B.value,
      [E.value]: a.align !== void 0,
      "nav-fill": u.value,
      "card-header-tabs": l.value,
      "nav-justified": f.value,
      "nav-tabs": !p.value && !$.value,
      small: m.value
    })), _ = (w) => {
      let P = false;
      if (w !== void 0 && w > -1 && w < y.value.length && !y.value[w].disabled && (k.value < 0 || y.value[w].buttonId !== b.value)) {
        const L = new yt("activate-tab", { cancelable: true, vueTarget: this });
        t("activate-tab", w, k.value, L), L.defaultPrevented || (k.value = w, P = true);
      }
      return !P && a.modelValue !== k.value && t("update:modelValue", k.value), P;
    }, z = (w, P) => {
      var L;
      _(P), P >= 0 && !y.value[P].disabled && ((L = y.value[P]) == null ? void 0 : L.onClick) && typeof y.value[P].onClick == "function" && y.value[P].onClick(w);
    }, x = (w) => !w || !w.default ? [] : w.default().reduce((P, L) => (typeof L.type == "symbol" ? P = P.concat(L.children) : P.push(L), P), []).filter((P) => {
      var L;
      return ((L = P.type) == null ? void 0 : L.__name) === "BTab";
    });
    return _(T.value), watch(
      () => a.modelValue,
      (w, P) => {
        if (w === P)
          return;
        if (w = Math.max(w, -1), P = Math.max(P, -1), y.value.length <= 0) {
          k.value = -1;
          return;
        }
        const L = w > P;
        let ee = w;
        const Z = y.value.length - 1;
        for (; ee >= 0 && ee <= Z && y.value[ee].disabled; )
          ee += L ? 1 : -1;
        if (ee < 0) {
          _(0);
          return;
        }
        if (ee >= y.value.length) {
          _(y.value.length - 1);
          return;
        }
        _(ee);
      }
    ), watch(
      () => y.value,
      () => {
        let w = y.value.map((P) => P.active && !P.disabled).lastIndexOf(true);
        w < 0 && (k.value >= y.value.length ? w = y.value.map((P) => !P.disabled).lastIndexOf(true) : y.value[k.value] && !y.value[k.value].disabled && (w = k.value)), w < 0 && (w = y.value.map((P) => !P.disabled).indexOf(true)), y.value.forEach((P, L) => P.active = L === w), _(w);
      }
    ), onMounted(() => {
      if (k.value < 0 && y.value.length > 0 && !y.value.some((w) => w.active)) {
        const w = y.value.map((P) => !P.disabled).indexOf(true);
        _(w >= 0 ? w : -1);
      }
    }), provide(Nl, {
      lazy: c.value,
      card: l.value
    }), (w, P) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: e.id,
      class: normalizeClass(["tabs", unref(q)])
    }, {
      default: withCtx(() => [
        unref(n) ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass(["tab-content", e.contentClass])
        }, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(unref(y), ({ tabComponent: L, contentId: ee, tabClasses: Z, active: se }, K) => (openBlock(), createBlock(resolveDynamicComponent(L()), {
            id: ee,
            key: K,
            class: normalizeClass(Z),
            active: se
          }, null, 8, ["id", "class", "active"]))), 128)),
          unref(C) ? (openBlock(), createElementBlock("div", {
            key: "bv-empty-tab",
            class: normalizeClass(["tab-pane active", { "card-body": unref(l) }])
          }, [
            renderSlot(w.$slots, "empty")
          ], 2)) : createCommentVNode("", true)
        ], 2)) : createCommentVNode("", true),
        createBaseVNode("div", {
          class: normalizeClass([e.navWrapperClass, { "card-header": unref(l), "ms-auto": e.vertical && unref(n) }])
        }, [
          createBaseVNode("ul", {
            class: normalizeClass(["nav", [unref(I), e.navClass]]),
            role: "tablist"
          }, [
            renderSlot(w.$slots, "tabs-start"),
            (openBlock(true), createElementBlock(Fragment, null, renderList(unref(y), ({ tab: L, buttonId: ee, contentId: Z, navItemClasses: se, active: K, target: ve }, te) => (openBlock(), createElementBlock("li", {
              key: te,
              class: normalizeClass(["nav-item", L.props["title-item-class"]])
            }, [
              createBaseVNode("button", mergeProps({
                id: ee,
                class: ["nav-link", se],
                "data-bs-toggle": "tab",
                "data-bs-target": ve,
                role: "tab",
                "aria-controls": Z,
                "aria-selected": K
              }, L.props["title-link-attributes"], {
                onClick: withModifiers((be) => z(be, te), ["stop", "prevent"])
              }), [
                L.children && L.children.title ? (openBlock(), createBlock(resolveDynamicComponent(L.children.title), { key: 0 })) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
                  createTextVNode(toDisplayString(L.props.title), 1)
                ], 64))
              ], 16, pu)
            ], 2))), 128)),
            renderSlot(w.$slots, "tabs-end")
          ], 2)
        ], 2),
        unref(n) ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", {
          key: 1,
          class: normalizeClass(["tab-content", e.contentClass])
        }, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(unref(y), ({ tabComponent: L, contentId: ee, tabClasses: Z, active: se }, K) => (openBlock(), createBlock(resolveDynamicComponent(L()), {
            id: ee,
            key: K,
            class: normalizeClass(Z),
            active: se
          }, null, 8, ["id", "class", "active"]))), 128)),
          unref(C) ? (openBlock(), createElementBlock("div", {
            key: "bv-empty-tab",
            class: normalizeClass(["tab-pane active", { "card-body": unref(l) }])
          }, [
            renderSlot(w.$slots, "empty")
          ], 2)) : createCommentVNode("", true)
        ], 2))
      ]),
      _: 3
    }, 8, ["id", "class"]));
  }
});
var yu = defineComponent({
  __name: "BTab",
  props: {
    id: null,
    title: null,
    active: { default: false },
    buttonId: { default: void 0 },
    disabled: { default: false },
    lazy: { default: void 0 },
    lazyOnce: { default: void 0 },
    noBody: { type: [Boolean, String], default: false },
    tag: { default: "div" },
    titleItemClass: null,
    titleLinkAttributes: { default: void 0 },
    titleLinkClass: null
  },
  setup(e) {
    const t = e, a = inject(Nl, null), l = r(toRef(t, "active")), n = r(toRef(t, "disabled")), u = r(toRef(t, t.lazyOnce !== void 0 ? "lazyOnce" : "lazy")), f = ref(false), c = computed(() => !!((a == null ? void 0 : a.lazy) || u.value)), v = computed(() => t.lazyOnce !== void 0), p = computed(() => l.value && !n.value), $ = computed(() => {
      const B = c.value && v.value && f.value;
      return p.value || !c.value || B;
    }), m = computed(() => ({
      active: l.value,
      show: l.value,
      "card-body": (a == null ? void 0 : a.card) && t.noBody === false
    }));
    return watch(
      () => $.value,
      (B) => {
        B && !f.value && (f.value = true);
      }
    ), (B, V) => (openBlock(), createBlock(resolveDynamicComponent(e.tag), {
      id: e.id,
      class: normalizeClass(["tab-pane", unref(m)]),
      role: "tabpanel",
      "aria-labelledby": "profile-tab"
    }, {
      default: withCtx(() => [
        unref($) ? renderSlot(B.$slots, "default", { key: 0 }) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["id", "class"]));
  }
});
var Bu = {
  BAccordion: Nn,
  BAccordionItem: eo,
  BNavText: Wi,
  BAlert: ao,
  BAvatar: io,
  BAvatarGroup: lo,
  BNavForm: Di,
  BBadge: vo,
  BBreadcrumb: ho,
  BBreadcrumbItem: il,
  BButton: lt,
  BButtonGroup: ko,
  BButtonToolbar: So,
  BCard: ml,
  BCardBody: fl,
  BCardFooter: vl,
  BCardGroup: To,
  BCardHeader: ul,
  BCardImg: pt,
  BCardSubtitle: cl,
  BCardText: _o,
  BCardTitle: dl,
  BCarousel: Eo,
  BCarouselSlide: jo,
  BCloseButton: Ze,
  BCol: et,
  BCollapse: nl,
  BContainer: Zo,
  BDropdown: yl,
  BDropdownDivider: as,
  BDropdownForm: is,
  BDropdownGroup: cs,
  BDropdownHeader: bs,
  BDropdownItem: ps,
  BDropdownItemButton: Bs,
  BDropdownText: Cs,
  BForm: Bl,
  BFormCheckbox: $l,
  BFormCheckboxGroup: Os,
  BFormFloatingLabel: _s,
  BFormGroup: qs,
  BFormInput: Us,
  BFormInvalidFeedback: Ht,
  BFormRadio: Cl,
  BFormRadioGroup: Zs,
  BFormRow: ft,
  BFormSelect: ai,
  BFormSelectOption: ea,
  BFormSelectOptionGroup: Sl,
  BFormText: Rt,
  BFormTextarea: $i,
  BFormTag: wl,
  BFormTags: pi,
  BFormValidFeedback: qt,
  BImg: Qt,
  BInputGroup: Vi,
  BInputGroupAddon: ta,
  BInputGroupAppend: Ai,
  BInputGroupPrepend: xi,
  BInputGroupText: Tl,
  BLink: Ae,
  BListGroup: Ii,
  BListGroupItem: Fi,
  BModal: Ni,
  BNav: Ei,
  BNavbar: Ki,
  BNavbarBrand: Yi,
  BNavbarNav: Zi,
  BNavbarToggle: er,
  BNavItem: Mi,
  BNavItemDropdown: Gi,
  BOffcanvas: sr,
  BOverlay: ir,
  BPagination: gr,
  BPlaceholder: Ie,
  BPlaceholderButton: xl,
  BPlaceholderCard: pr,
  BPlaceholderTable: hr,
  BPlaceholderWrapper: yr,
  BPopover: wr,
  BProgress: _r,
  BProgressBar: Il,
  BRow: xr,
  BSkeleton: vt,
  BSkeletonIcon: Ir,
  BSkeletonTable: Or,
  BSkeletonWrapper: Lr,
  BSpinner: St,
  BFormSpinButton: Hr,
  BTab: yu,
  BTable: su,
  BTableSimple: Tt,
  BTbody: iu,
  BTd: du,
  BTfoot: cu,
  BTh: mu,
  BThead: bu,
  BTr: gu,
  BToast: hl,
  BToaster: Dt,
  BToastContainer: Dt,
  BTabs: hu,
  BTransition: wt,
  BToastPlugin: Jo
};
var Su = {
  install(e, t = {}) {
    Object.entries(Bu).forEach(([a, l]) => {
      e.component(a, l);
    }), Object.entries(Xn).forEach(([a, l]) => {
      e.directive(a, l);
    }), On(e);
  }
};

// dep:bootstrap-vue-3
var bootstrap_vue_3_default = Su;
export {
  Nn as BAccordion,
  eo as BAccordionItem,
  ao as BAlert,
  io as BAvatar,
  lo as BAvatarGroup,
  vo as BBadge,
  ho as BBreadcrumb,
  il as BBreadcrumbItem,
  lt as BButton,
  ko as BButtonGroup,
  So as BButtonToolbar,
  ml as BCard,
  fl as BCardBody,
  vl as BCardFooter,
  To as BCardGroup,
  ul as BCardHeader,
  pt as BCardImg,
  cl as BCardSubtitle,
  _o as BCardText,
  dl as BCardTitle,
  Eo as BCarousel,
  jo as BCarouselSlide,
  Ze as BCloseButton,
  et as BCol,
  nl as BCollapse,
  Zo as BContainer,
  yl as BDropdown,
  as as BDropdownDivider,
  is as BDropdownForm,
  cs as BDropdownGroup,
  bs as BDropdownHeader,
  ps as BDropdownItem,
  Bs as BDropdownItemButton,
  Cs as BDropdownText,
  Bl as BForm,
  $l as BFormCheckbox,
  Os as BFormCheckboxGroup,
  _s as BFormFloatingLabel,
  qs as BFormGroup,
  Us as BFormInput,
  Ht as BFormInvalidFeedback,
  Cl as BFormRadio,
  Zs as BFormRadioGroup,
  ft as BFormRow,
  ai as BFormSelect,
  ea as BFormSelectOption,
  Sl as BFormSelectOptionGroup,
  Hr as BFormSpinButton,
  wl as BFormTag,
  pi as BFormTags,
  Rt as BFormText,
  $i as BFormTextarea,
  qt as BFormValidFeedback,
  Qt as BImg,
  Vi as BInputGroup,
  ta as BInputGroupAddon,
  Ai as BInputGroupAppend,
  xi as BInputGroupPrepend,
  Tl as BInputGroupText,
  Ae as BLink,
  Ii as BListGroup,
  Fi as BListGroupItem,
  Ni as BModal,
  Ei as BNav,
  Di as BNavForm,
  Mi as BNavItem,
  Gi as BNavItemDropdown,
  Wi as BNavText,
  Ki as BNavbar,
  Yi as BNavbarBrand,
  Zi as BNavbarNav,
  er as BNavbarToggle,
  sr as BOffcanvas,
  ir as BOverlay,
  gr as BPagination,
  Ie as BPlaceholder,
  xl as BPlaceholderButton,
  pr as BPlaceholderCard,
  hr as BPlaceholderTable,
  yr as BPlaceholderWrapper,
  wr as BPopover,
  _r as BProgress,
  Il as BProgressBar,
  xr as BRow,
  vt as BSkeleton,
  Ir as BSkeletonIcon,
  Or as BSkeletonTable,
  Lr as BSkeletonWrapper,
  St as BSpinner,
  yu as BTab,
  su as BTable,
  Tt as BTableSimple,
  hu as BTabs,
  iu as BTbody,
  du as BTd,
  cu as BTfoot,
  mu as BTh,
  bu as BThead,
  hl as BToast,
  Dt as BToastContainer,
  Jo as BToastPlugin,
  Dt as BToaster,
  gu as BTr,
  wt as BTransition,
  Su as BootstrapVue3,
  yt as BvEvent,
  bootstrap_vue_3_default as default,
  Ln as useBreadcrumb,
  pl as useToast,
  Dn as vBPopover,
  Zt as vBToggle,
  Gn as vBTooltip,
  Un as vBVisible
};
/*!
  * Bootstrap v5.2.3 (https://getbootstrap.com/)
  * Copyright 2011-2022 The Bootstrap Authors (https://github.com/twbs/bootstrap/graphs/contributors)
  * Licensed under MIT (https://github.com/twbs/bootstrap/blob/main/LICENSE)
  */
//# sourceMappingURL=bootstrap-vue-3.js.map
